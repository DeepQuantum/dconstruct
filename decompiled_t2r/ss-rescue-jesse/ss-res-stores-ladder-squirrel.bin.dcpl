// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-rescue-jesse\ss-res-stores-ladder-squirrel.bin         #
// #   filesize: 10900 bytes                                                                          #
// #   total entries: 1                                                                               #
// #   functions: 19                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bool activate-immediately = nullptr
        bool activated-manually? = nullptr
        f32 player-distance = 0.00
        f32 seconds-to-check-dist = 0.10
        bool activated-with-weapon? = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(rescue-jesse, res-stores-pre-bank-explore)) {
                        go(already-complete, 1);
                    }
                }
                lambda {
                    if (task-active?(rescue-jesse, res-stores-pre-bank-explore)) {
                        go(wait-player-activates, 1);
                    }
                }
            }
        }
        block event task-complete {
            track main {
                lambda {
                    if (task-active?(rescue-jesse, res-stores-pre-bank-explore)) {
                        go(wait-player-activates, 1);
                    }
                }
            }
        }
    }
    state wait-player-activates {
        block start {
            track anim {
                lambda {
                    u64? var_1 = #%alloc-array(single-frame, 16, 3, 1);
                    *(i32*)*(*var_1 + (0 * 8)) = 10;
                    *(u64*)(*(*var_1 + (0 * 8)) + 8) = 0;
                    *(u64*)(*(*var_1 + (0 * 8)) + 8) = get-spawner-locator(ap-res-stores-squirrel-1, 1);
                    *(i32*)*(*var_1 + (1 * 8)) = 30;
                    *(u64*)(*(*var_1 + (1 * 8)) + 8) = 0;
                    *(f32*)(*(*var_1 + (1 * 8)) + 8) = 0.30;
                    *(i32*)*(*var_1 + (2 * 8)) = 31;
                    *(u64*)(*(*var_1 + (2 * 8)) + 8) = 0;
                    *(u8*)(*(*var_1 + (2 * 8)) + 8) = 1;
                    animate_(get-self-id(), res-neighborhood-ladder-squirrel-idle--squirrel, var_1);
                }
            }
            track update-checks {
                lambda {
                    if (get-boolean(activate-immediately, self)) {
                        go(flee, 1);
                    }
                }
                lambda {
                    if (tag-boolean(check-distance, self)) {
                        while (!get-boolean(activated-manually?, self)) {
                            set-float(player-distance, get-object-distance(get-self-id(), player), self);
                            if (get-float(player-distance, self) > (f32)(4 * (i32)tag-float(activate-immediately-radius, self))) {
                                set-float(seconds-to-check-dist, 1.00, self);
                            } else if (get-float(player-distance, self) > (f32)(3 * (i32)tag-float(activate-immediately-radius, self))) {
                                set-float(seconds-to-check-dist, 0.50, self);
                            } else {
                                set-float(seconds-to-check-dist, 0.10, self);
                            }
                            if (get-float(player-distance, self) < tag-float(activate-immediately-radius, self) && is-on-screen?(get-object-position(get-self-id()), 0.00) && get-float(player-distance, self) < tag-float(activate-if-looking-radius, self)) {
                                go(flee, 1);
                            }
                            wait(get-float(seconds-to-check-dist, self));
                        }
                    }
                }
            }
        }
        block event i-am-late-join {
            track main {
                lambda {
                    if (!is-final-build?()) {
                        display(dc:format("MOTHAFUCKIN FLEE BITCHES"), 19);
                    }
                }
                lambda {
                    kill-entity(get-self-id());
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
        block event player-fired-weapon {
            track main {
                lambda {
                    if (!get-boolean(activated-manually?, self) && get-float(player-distance, self) < tag-float(activate-when-firing-radius, self)) {
                        set-boolean(activated-with-weapon?, 1, self);
                        go(flee, 1);
                    }
                }
            }
        }
        block event exploded {
            track main {
                lambda {
                    if (!get-boolean(activated-manually?, self) && get-locator-distance(get-object-locator(get-self-id()), event-arg-bound-frame(0)) < (tag-float(activate-if-bullet-hits-within-radius, self) * 2.00)) {
                        set-boolean(activated-with-weapon?, 1, self);
                    }
                }
            }
        }
        block event grenade-exploded {
            track main {
                lambda {
                    if (!get-boolean(activated-manually?, self) && get-locator-distance(get-object-locator(get-self-id()), event-arg-bound-frame(0)) < (tag-float(activate-if-bullet-hits-within-radius, self) * 2.00)) {
                        set-boolean(activated-with-weapon?, 1, self);
                        go(flee, 1);
                    }
                }
            }
        }
        block event bullet-hit-position {
            track main {
                lambda {
                    if (!get-boolean(activated-manually?, self) && get-locator-distance(get-object-locator(get-self-id()), event-arg-bound-frame(0)) < tag-float(activate-if-bullet-hits-within-radius, self)) {
                        set-boolean(activated-with-weapon?, 1, self);
                        go(flee, 1);
                    }
                }
            }
        }
        block event force-flee {
            track main {
                lambda {
                    go(flee, 1);
                }
            }
        }
    }
    state flee {
        block start {
            track main {
                lambda {
                    if (tag-specified?(sfx-flee, self)) {
                        play-sound-at-joint(tag-symbol(sfx-flee, self), get-self-id(), 0, 0, 0);
                    }
                }
                lambda {
                    u64? var_1 = #%alloc-array(single-frame, 16, 3, 1);
                    *(i32*)*(*var_1 + (0 * 8)) = 10;
                    *(u64*)(*(*var_1 + (0 * 8)) + 8) = 0;
                    *(u64*)(*(*var_1 + (0 * 8)) + 8) = get-spawner-locator(ap-res-stores-squirrel-1, 1);
                    *(i32*)*(*var_1 + (1 * 8)) = 28;
                    *(u64*)(*(*var_1 + (1 * 8)) + 8) = 0;
                    *(f32*)(*(*var_1 + (1 * 8)) + 8) = 0.50;
                    *(i32*)*(*var_1 + (2 * 8)) = 30;
                    *(u64*)(*(*var_1 + (2 * 8)) + 8) = 0;
                    *(f32*)(*(*var_1 + (2 * 8)) + 8) = 0.00;
                    wait-animate_(get-self-id(), res-neighborhood-ladder-squirrel-escape--squirrel, var_1);
                }
                lambda {
                    kill-entity(get-self-id());
                }
            }
        }
    }
    state already-complete {
        block start {
            track main {
                lambda {
                    kill-entity(get-self-id());
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
    }
}
