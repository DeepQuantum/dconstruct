// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-medicine\ss-med-hospital-assembly-line-packer.bin      #
// #   filesize: 16758 bytes                                                                          #
// #   total entries: 3                                                                               #
// #   functions: 39                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

u64? update-shipping-info(u64? arg_0, u64? arg_1) {
    return set-symbol(parcel-id, arg_1, arg_0);
}

u16 signal-packer-ready() {
    u16 var_1;
    if (!(get-symbol(waiting-npc-1, self) == 0)) {
        set-symbol(shipping-npc, get-symbol(waiting-npc-1, self), self);
        set-symbol(waiting-npc-1, 0, self);
        var_1 = send-event(packer-ready, get-symbol(shipping-npc, self));
    } else if (!(get-symbol(waiting-npc-2, self) == 0)) {
        set-symbol(shipping-npc, get-symbol(waiting-npc-2, self), self);
        set-symbol(waiting-npc-2, 0, self);
        var_1 = send-event(packer-ready, get-symbol(shipping-npc, self));
    } else {
        var_1 = 0;
    }
    return var_1;
}

statescript {
    options {
        player
        med-hospital-entry-crowd
    }
    declarations {
        bool is-busy = nullptr
        symbol shipping-npc = nullptr
        symbol waiting-npc-1 = nullptr
        symbol waiting-npc-2 = nullptr
        bool player-blocking = nullptr
        symbol new-parcel = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(medicine, med-hospital-explore)) {
                        go(complete, 1);
                    } else if (task-range-active?(medicine, med-hospital-approach-swim, medicine, med-hospital-explore)) {
                        go(init, 1);
                    }
                }
                lambda {
                    animate_(get-self-id(), bindpose, #%alloc-array(single-frame, 16, 0, 1));
                }
            }
        }
        block event task-complete {
            track main {
                lambda {
                    if (task-active?(medicine, med-hospital-approach-swim)) {
                        if (object-anim-playing?(get-self-id(), bindpose, 0)) {
                            stop-animating(get-self-id(), 2, 0.00, bindpose, 0);
                        }
                        go(init, 1);
                    }
                }
            }
        }
    }
    state init {
        block start {
            track main {
                lambda {
                    if (tag-symbol(wait-point-1, self) == 0) {
                        u64? var_2 = dc:format("wait-point-1 not set for %s", get-self-name());
                        display(var_2, 8);
                        display-error(var_2);
                    }
                }
                lambda {
                    if (tag-symbol(wait-point-2, self) == 0) {
                        u64? var_2 = dc:format("wait-point-2 not set for %s", get-self-name());
                        display(var_2, 8);
                        display-error(var_2);
                    }
                }
                lambda {
                    if (tag-symbol(pass-off-anim, self) == 0) {
                        u64? var_2 = dc:format("pass-off-anim not set for %s", get-self-name());
                        display(var_2, 8);
                        display-error(var_2);
                    }
                }
                lambda {
                    if (tag-symbol(box-anim, self) == 0) {
                        u64? var_2 = dc:format("box-anim not set for %s", get-self-name());
                        display(var_2, 8);
                        display-error(var_2);
                    }
                }
                lambda {
                    if (tag-symbol(idle-anim, self) == 0) {
                        u64? var_2 = dc:format("idle-anim not set for %s", get-self-name());
                        display(var_2, 8);
                        display-error(var_2);
                    }
                }
                lambda {
                    if (tag-symbol(packing-ap, self) == 0) {
                        u64? var_2 = dc:format("packing-ap not set for %s", get-self-name());
                        display(var_2, 8);
                        display-error(var_2);
                    }
                }
                lambda {
                    if (tag-symbol(prop-name, self) == 0) {
                        u64? var_2 = dc:format("prop-name not set for %s", get-self-name());
                        display(var_2, 8);
                        display-error(var_2);
                    }
                }
                lambda {
                    if (!(tag-specified?(detach-frame, self))) {
                        u64? var_3 = dc:format("detach-frame not set for %s", get-self-name());
                        display(var_3, 8);
                        display-error(var_3);
                    }
                }
                lambda {
                    go(idle-unblocked, 1);
                }
            }
        }
    }
    state idle-unblocked {
        block start {
            track main {
                lambda {
                    u64? var_1 = #%alloc-array(single-frame, 16, 3, 1);
                    *(i32*)*(*var_1 + (0 * 8)) = 10;
                    *(u64*)(*(*var_1 + (0 * 8)) + 8) = 0;
                    *(u64*)(*(*var_1 + (0 * 8)) + 8) = get-spawner-locator(tag-symbol(packing-ap, self), 1);
                    *(i32*)*(*var_1 + (1 * 8)) = 28;
                    *(u64*)(*(*var_1 + (1 * 8)) + 8) = 0;
                    *(f32*)(*(*var_1 + (1 * 8)) + 8) = 0.50;
                    *(i32*)*(*var_1 + (2 * 8)) = 31;
                    *(u64*)(*(*var_1 + (2 * 8)) + 8) = 0;
                    *(u8*)(*(*var_1 + (2 * 8)) + 8) = 1;
                    animate_(self, tag-symbol(idle-anim, self), var_1);
                }
                lambda {
                    signal-packer-ready();
                }
            }
        }
        block event delivery-person-approaching {
            track main {
                lambda {
                    go(wait-distribute-parcel, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-boolean(player-blocking, self)) {
                        go(idle-blocked, 1);
                    }
                }
            }
        }
    }
    state idle-blocked {
        block start {
            track main {
                lambda {
                    wait-boolean(player-blocking, 0, self);
                }
                lambda {
                    go(idle-unblocked, 1);
                }
            }
        }
    }
    state wait-distribute-parcel {
        block start {
            track main {
                lambda {
                    set-boolean(is-busy, 1, self);
                }
            }
        }
        block event delivery-person-arrived {
            track main {
                lambda {
                    set-symbol(shipping-npc, event-arg-symbol(0), self);
                }
                lambda {
                    go(distribute-box, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (4.00 > distance-between-locators(get-spawner-locator(tag-symbol(packing-ap, self), 1), get-object-locator(player)) && joypad-stick-pressed-towards?(move, get-spawner-locator(tag-symbol(packing-ap, self), 1), 0, 45.00, 0.00)) {
                        joypad-disable-commands(sprint-press, sprint-hold);
                    }
                }
            }
        }
    }
    state distribute-box {
        block start {
            track packer {
                lambda {
                    set-boolean(is-busy, 1, self);
                }
                lambda {
                    set-symbol(new-parcel, spawn-attachable("med-hosp-supply-box-a", self, "rightHand", 0, 0), self);
                }
                lambda {
                    wait-one-frame();
                }
                lambda {
                    while (!!(get-symbol(new-parcel, self) == 0)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    update-shipping-info(get-symbol(shipping-npc, self), get-symbol(new-parcel, self));
                }
                lambda {
                    signal(animation-started, 0);
                }
                lambda {
                    u64? var_1 = #%alloc-array(single-frame, 16, 2, 1);
                    *(i32*)*(*var_1 + (0 * 8)) = 10;
                    *(u64*)(*(*var_1 + (0 * 8)) + 8) = 0;
                    *(u64*)(*(*var_1 + (0 * 8)) + 8) = get-spawner-locator(tag-symbol(packing-ap, self), 1);
                    *(i32*)*(*var_1 + (1 * 8)) = 28;
                    *(u64*)(*(*var_1 + (1 * 8)) + 8) = 0;
                    *(f32*)(*(*var_1 + (1 * 8)) + 8) = 0.50;
                    wait-animate_(self, tag-symbol(pass-off-anim, self), var_1);
                }
                lambda {
                    set-symbol(shipping-npc, 0, self);
                }
                lambda {
                    set-boolean(is-busy, 0, self);
                }
                lambda {
                    go(idle-unblocked, 1);
                }
            }
            track box {
                lambda {
                    wait-for(animation-started);
                }
                lambda {
                    wait-until-anim-frame(self, tag-symbol(pass-off-anim, self), tag-float(anim-sync-frame, self), 2);
                }
                lambda {
                    send-event(pass-off-started, get-symbol(shipping-npc, self));
                }
                lambda {
                    wait-until-anim-frame(self, tag-symbol(pass-off-anim, self), tag-float(detach-frame, self), 2);
                }
                lambda {
                    send-event(attach-new-parcel, get-symbol(shipping-npc, self));
                }
            }
        }
    }
}
