// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-medicine\hospital-annex\ss-med-ratking-bloater-accessibility-nav-goals.bin#
// #   filesize: 10790 bytes                                                                          #
// #   total entries: 4                                                                               #
// #   functions: 12                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

u16 med-bloater-get-safe-pos(u64? arg_0) {
    u16 var_2;
    foreach (u64? var_4 : arg_0) {
        u64? var_6 = get-locator-distance(get-spawner-locator(var_4, 1), get-object-locator(npc-ratking-bloater-1));
        u64? var_13 = absf(get-angle-between-vectors(vector-between-locators(get-object-locator(player), get-spawner-locator(var_4, 1)), vector-between-objects(player, npc-ratking-bloater-1), alloc-vector(0.00, 1.00, 0.00, 1.00, 0)));
        u16 var_18;
        if (var_6 > 0.00 && var_13 > get-float(min-angle-allowed, self) && !(point-is-in-infected-cloud?(get-spawner-position(var_4, 1)))) {
            var_18 = var_4;
            var_19 = var_6;
            var_20 = set-float(next-best-angle, var_13, self);
        }
        var_2 = var_18;
    }
    return var_2;
}

u64? med-ratking-player-using-blind-settings?() {
    return is-navigation-assistance-accessibility?() || is-auto-traversal-accessibility?() || is-traversal-audio-cues?() || is-combat-audio-cues?() && is-text-to-speech-enabled?();
}

statescript {
    options {
        player
        npc-ratking-bloater-1
    }
    declarations {
        symbol current-goal = ap-nav-safety-pos-9
        f32 current-angle = 0.00
        f32 next-best-angle = 0.00
        f32 min-angle-allowed = 35.00
        bool disable-proximity-kill? = nullptr
        timer proximity-disable-cooldown = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(medicine, med-hospital-basement-ratking-split)) {
                        go(complete, 1);
                    }
                }
                lambda {
                    if (task-range-active?(medicine, med-hospital-basement-ratking-basement-fight, medicine, med-hospital-basement-ratking-split)) {
                        go(find-target, 1);
                    }
                }
            }
        }
        block event task-complete {
            track main {
                lambda {
                    if (task-active?(medicine, med-hospital-basement-ratking-basement-fight)) {
                        go(find-target, 1);
                    }
                }
            }
        }
    }
    state find-target {
        block start {
            track set-goal {
                lambda {
                    while (1) {
                        set-float(current-angle, absf(get-angle-between-vectors(vector-between-locators(get-object-locator(player), get-spawner-locator(get-symbol(current-goal, self), 1)), vector-between-objects(player, npc-ratking-bloater-1), alloc-vector(0.00, 1.00, 0.00, 1.00, 0))), self);
                        u64? var_8 = med-bloater-get-safe-pos(ratking-nav-safety-goals);
                        if (!(var_8 == 0) || closer-by-distance-to-target?(get-symbol(current-goal, self), var_8, npc-ratking-bloater-1, 2.00) && get-float(current-angle, self) < get-float(min-angle-allowed, self)) {
                            var_13 = set-symbol(current-goal, var_8, self);
                        } else {
                            var_13 = set-float(current-angle, get-float(next-best-angle, self), self);
                        }
                        set-accessibility-pathfind-goal(get-symbol(current-goal, self));
                        set-boolean(goal-is-flee, 1, ss-accessibility-pathfind);
                        wait(0.50);
                    }
                }
            }
            track goal-invincibility {
                lambda {
                    if (med-ratking-player-using-blind-settings?()) {
                        spawn-entity(obj-nav-assist-blocker-1, 0, 0);
                        spawn-entity(obj-nav-assist-blocker-2, 0, 0);
                        spawn-entity(obj-nav-assist-blocker-3, 0, 0);
                        spawn-entity(obj-nav-assist-blocker-4, 0, 0);
                        spawn-entity(obj-nav-assist-blocker-5, 0, 0);
                    }
                }
                lambda {
                    while (med-ratking-player-using-blind-settings?()) {
                        if (joypad-command-active?(evade-start, 6) && is-evasive-dodge-enabled?()) {
                            set-boolean(disable-proximity-kill?, 1, self);
                            f32 var_4;
                            if (get-difficulty-setting-enemies() == 0) {
                                var_4 = 5.00;
                            } else if (get-difficulty-setting-enemies() == 1) {
                                var_4 = 4.00;
                            } else if (get-difficulty-setting-enemies() == 2) {
                                var_4 = 3.20;
                            } else if (get-difficulty-setting-enemies() == 3) {
                                var_4 = 2.50;
                            } else {
                                var_4 = 2.20;
                            }
                            start-timer(proximity-disable-cooldown, var_4, self, 1);
                        } else if (joypad-command-active?(look-at, 6)) {
                            set-boolean(disable-proximity-kill?, 1, self);
                            f32 var_10;
                            if (get-difficulty-setting-enemies() == 0) {
                                var_10 = 6.00;
                            } else if (get-difficulty-setting-enemies() == 1) {
                                var_10 = 5.00;
                            } else if (get-difficulty-setting-enemies() == 2) {
                                var_10 = 3.80;
                            } else if (get-difficulty-setting-enemies() == 3) {
                                var_10 = 2.80;
                            } else {
                                var_10 = 2.20;
                            }
                            start-timer(proximity-disable-cooldown, var_10, self, 1);
                        } else if (get-boolean(disable-proximity-kill?, self) || (get-locator-distance(get-object-locator(npc-ratking-bloater-1), get-object-locator(player)) - get-locator-distance(get-spawner-locator(get-symbol(current-goal, self), 1), get-object-locator(player))) > 2.00 && get-locator-distance(get-spawner-locator(get-symbol(current-goal, self), 1), get-object-locator(player)) < 2.00) {
                            set-boolean(disable-proximity-kill?, 0, self);
                        }
                        melee-fact-set-bool(npc-ratking-bloater-1, proximity-kill-disabled, get-boolean(disable-proximity-kill?, self));
                        wait-one-frame();
                    }
                }
            }
        }
        block event proximity-disable-cooldown {
            track main {
                lambda {
                    set-boolean(disable-proximity-kill?, 0, self);
                }
            }
        }
        block event player-fired-weapon {
            track main {
                lambda {
                    set-boolean(disable-proximity-kill?, 0, self);
                }
            }
        }
        block event npc-attacked {
            track main {
                lambda {
                    event-arg-symbol(1);
                    if (*(u64*)(event::get-attack-info(get-event()) + 8) == player && event-attack-type?(1)) {
                        var_3 = set-boolean(disable-proximity-kill?, 0, self);
                    }
                }
            }
        }
        block event task-complete {
            track main {
                lambda {
                    if (task-complete?(medicine, med-hospital-basement-ratking-split)) {
                        set-boolean(goal-is-flee, 0, ss-accessibility-pathfind);
                        kill-entity(obj-nav-assist-blocker-1);
                        kill-entity(obj-nav-assist-blocker-2);
                        kill-entity(obj-nav-assist-blocker-3);
                        kill-entity(obj-nav-assist-blocker-4);
                        kill-entity(obj-nav-assist-blocker-5);
                        go(complete, 1);
                    }
                }
            }
        }
    }
}
