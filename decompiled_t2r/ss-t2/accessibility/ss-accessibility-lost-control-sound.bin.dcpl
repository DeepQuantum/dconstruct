// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-t2\accessibility\ss-accessibility-lost-control-sound.bin#
// #   filesize: 3969 bytes                                                                           #
// #   total entries: 3                                                                               #
// #   functions: 8                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

bool is-scripted-state?(sid arg_0) {
    return arg_0 == Script;
}

statescript {
    options {
        player
        sp-all
    }
    declarations {
        timer play-sound-timer = nullptr
        bool suppress-next = nullptr
    }
    state accessibility-sound {
        block event player-changed-state {
            track main {
                lambda {
                    if (is-traversal-audio-cues?() && get-black-fade() < 1.00) {
                        darray-append(player-state-stack, boxed_symbol(event-arg-symbol(1)), 1);
                        darray-append(player-state-stack, boxed_symbol(event-arg-symbol(0)), 1);
                        start-timer(play-sound-timer, 0.10, self, 1);
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (player-is-door-scripted-move?() && player-is-pickup-scripted-move?()) {
                        darray-clear(player-state-stack);
                        set-boolean(suppress-next, 1, self);
                        start-timer(play-sound-timer, 1.00, self, 1);
                    }
                }
            }
        }
        block event play-sound-timer {
            track main {
                lambda {
                    u64? var_0 = group-get-first-member(player-state-stack);
                    u64? var_1 = group-get-last-member(player-state-stack);
                    if (get-boolean(suppress-next, self)) {
                        var_3 = set-boolean(suppress-next, 0, self);
                    } else if (!is-traversal-audio-cues?()) {}
 else if (is-scripted-state?(var_0) && !is-scripted-state?(var_1)) {
                        var_3 = play-accessibility-control-regained-control-sound();
                    } else if (!is-scripted-state?(var_0) && is-scripted-state?(var_1)) {
                        var_3 = play-accessibility-control-lost-control-sound();
                    }
                }
                lambda {
                    darray-clear(player-state-stack);
                }
            }
        }
        block event suppress-control-sounds {
            track main {
                lambda {
                    start-timer(play-sound-timer, 1.00, self, 1);
                }
                lambda {
                    darray-clear(player-state-stack);
                }
                lambda {
                    if (player-in-state?(Script) || player-in-state?(ScriptMove) && 0) {
                        set-boolean(suppress-next, 1, self);
                    }
                }
            }
        }
    }
}
