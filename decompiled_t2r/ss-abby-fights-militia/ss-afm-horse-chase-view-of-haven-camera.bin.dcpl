// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-abby-fights-militia\ss-afm-horse-chase-view-of-haven-camera.bin#
// #   filesize: 6492 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 7                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        f32 camera-constraint-hor-start = 90.00
        f32 camera-constraint-vert-max-start = 90.00
        f32 camera-constraint-vert-min-start = 90.00
        f32 camera-constraint-hor-end = 0.00
        f32 camera-constraint-vert-max-end = 20.00
        f32 camera-constraint-vert-min-end = 20.00
        f32 camera-constraint-time = 2.00
        timer camera-constraint-timer = nullptr
        bool reverse-constrain? = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(abby-fights-militia, afm-horse-chase-crossfire)) {
                        go(complete, 1);
                    }
                }
                lambda {
                    if (task-active?(abby-fights-militia, afm-horse-chase-crossfire)) {
                        go(wait-for-region-enter, 1);
                    }
                }
            }
        }
        block event task-complete {
            track main {
                lambda {
                    if (task-active?(abby-fights-militia, afm-horse-chase-crossfire)) {
                        go(wait-for-region-enter, 1);
                    }
                }
            }
        }
    }
    state wait-for-region-enter {
        block event enter {
            track main {
                lambda {
                    if (is-centered-on-screen?(get-spawner-position(pos-afm-horse-chase-view-of-haven-poi-1, 1), 400.00)) {
                        go(complete, 1);
                    } else {
                        go(constrain-camera, 1);
                    }
                }
            }
        }
    }
    state constrain-camera {
        block start {
            track main {
                lambda {
                    start-timer-if-not-running(camera-constraint-timer, get-float(camera-constraint-time, self), self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    u64? var_1;
                    if (get-boolean(reverse-constrain?, self)) {
                        var_1 = absf(1.00 - ((get-float(camera-constraint-time, self) - get-timer-value(camera-constraint-timer, self)) / get-float(camera-constraint-time, self)));
                    } else {
                        var_1 = (get-float(camera-constraint-time, self) - get-timer-value(camera-constraint-timer, self)) / get-float(camera-constraint-time, self);
                    }
                    u64? var_10 = make-curve(var_1, 3);
                    if (!get-boolean(reverse-constrain?, self)) {
                        joypad-disable-commands(aim);
                    }
                    if (is-centered-on-screen?(get-spawner-position(pos-afm-horse-chase-view-of-haven-poi-1, 1), 200.00)) {
                        var_23 = go(complete, 1);
                    } else if (!get-boolean(reverse-constrain?, self) && get-timer-value(camera-constraint-timer, self) > 0.05) {
                        set-follow-camera-constraints(
                            lerp(get-float(camera-constraint-hor-start, self), get-float(camera-constraint-hor-end, self), var_10),
                            lerp(get-float(camera-constraint-vert-max-start, self), get-float(camera-constraint-vert-max-end, self), var_10),
                            lerp(get-float(camera-constraint-vert-min-start, self), get-float(camera-constraint-vert-min-end, self), var_10),
                            flatten-locator(rotate-locator-lookat(get-object-locator(get-player-horse-id(1, 1)), get-spawner-locator(pos-afm-horse-chase-view-of-haven-poi-1, 1), 1.00, 0))
                        );
                        print-float(var_10, self);
                        var_23 = debug-draw-sphere(get-spawner-position(pos-afm-horse-chase-view-of-haven-poi-1, 1), 1.00, 6, 10.00, 0, 0);
                    } else {
                        var_23 = go(complete, 1);
                    }
                }
            }
        }
        block event camera-constraint-timer {
            track main {
                lambda {
                    if (!get-boolean(reverse-constrain?, self)) {
                        set-boolean(reverse-constrain?, 1, self);
                        start-timer(camera-constraint-timer, get-float(camera-constraint-time, self), self, 1);
                    } else {
                        go(complete, 1);
                    }
                }
            }
        }
    }
}
