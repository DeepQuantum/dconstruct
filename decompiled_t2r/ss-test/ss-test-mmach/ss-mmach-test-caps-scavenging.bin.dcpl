// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-mmach\ss-mmach-test-caps-scavenging.bin   #
// #   filesize: 24014 bytes                                                                          #
// #   total entries: 6                                                                               #
// #   functions: 28                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

u16 select-activity(u64? arg_0) {
    u16 var_1;
    if (debug-joypad-command-active?(jump, 0)) {
        if (locator-in-region?(arg_0, rgn-cap-1, player)) {
            set-symbol(pivot-start, pivot-start-1, self);
            set-symbol(pivot-end, pivot-end-1, self);
            set-symbol(position-start, position-start-1, self);
            set-symbol(position-end, position-end-1, self);
            set-symbol(activity, cap-1, self);
            debug-draw-text-box-2D(
                "Pistol wall check",
                60,
                340.00,
                60.00,
                1.00,
                1,
                0,
                1,
                1.00
            );
            var_1 = cap-1;
        } else if (locator-in-region?(arg_0, rgn-cap-2, player)) {
            set-symbol(pivot-start, pivot-start-2, self);
            set-symbol(pivot-end, pivot-end-2, self);
            set-symbol(position-start, position-start-2, self);
            set-symbol(position-end, position-end-2, self);
            set-symbol(activity, cap-2, self);
            debug-draw-text-box-2D(
                "Long gun wall check",
                60,
                340.00,
                60.00,
                1.00,
                1,
                0,
                1,
                1.00
            );
            var_1 = cap-2;
        } else if (locator-in-region?(arg_0, rgn-cap-debris-1, player)) {
            set-symbol(pivot-start, pivot-start-debris-1, self);
            set-symbol(pivot-end, pivot-end-debris-1, self);
            set-symbol(position-start, position-start-debris-1, self);
            set-symbol(position-end, position-end-debris-1, self);
            set-symbol(activity, cap-debris-1, self);
            debug-draw-text-box-2D(
                "Debris check",
                60,
                340.00,
                60.00,
                1.00,
                1,
                0,
                1,
                1.00
            );
            var_1 = cap-debris-1;
        } else if (locator-in-region?(arg_0, rgn-npc-1, player)) {
            debug-spawn-test-npc(npc-hammer-1);
            var_1 = 0;
        } else if (locator-in-region?(arg_0, rgn-npc-pistol-1, player)) {
            debug-spawn-test-npc(npc-pistol-1);
            var_1 = 0;
        } else if (locator-in-region?(arg_0, rgn-npc-rifle-1, player)) {
            debug-spawn-test-npc(npc-rifle-1);
            var_1 = 0;
        } else if (locator-in-region?(arg_0, rgn-npc-shotgun-1, player)) {
            debug-spawn-test-npc(npc-shotgun-1);
            var_1 = 0;
        } else if (locator-in-region?(arg_0, rgn-npc-bow-1, player)) {
            debug-spawn-test-npc(npc-bow-1);
            var_1 = 0;
        } else if (locator-in-region?(arg_0, rgn-npc-sledge-1, player)) {
            debug-spawn-test-npc(npc-sledge-1);
            var_1 = 0;
        } else {
            var_1 = 0;
        }
    } else {
        var_1 = 0;
    }
    return var_1;
}

u64? draw-help() {
    u64? var_1;
    if (get-boolean(draw-help?, self)) {
        debug-draw-string-2d(
            alloc-point(1000.00, get-float(console-y-start, self) + 50.00, -4.00, 0),
            "Activity selection: R2(hold) + X",
            7,
            0.00,
            0.50,
            0,
            0,
            0,
            1.00
        );
        debug-draw-string-2d(
            alloc-point(1000.00, get-float(console-y-start, self) + 60.00, -4.00, 0),
            "Perform Corner Check: X",
            7,
            0.00,
            0.50,
            0,
            0,
            0,
            1.00
        );
        debug-draw-string-2d(
            alloc-point(1000.00, get-float(console-y-start, self) + 70.00, -4.00, 0),
            "-------------------------------------",
            7,
            0.00,
            0.50,
            0,
            0,
            0,
            1.00
        );
        debug-draw-string-2d(
            alloc-point(1000.00, get-float(console-y-start, self) + 80.00, -4.00, 0),
            "Change Enter Angle: L2(hold) + Dpad L/R",
            7,
            0.00,
            0.50,
            0,
            0,
            0,
            1.00
        );
        debug-draw-string-2d(
            alloc-point(1000.00, get-float(console-y-start, self) + 90.00, -4.00, 0),
            "Change Exit Angle: R2(hold) + Dpad L/R",
            7,
            0.00,
            0.50,
            0,
            0,
            0,
            1.00
        );
        debug-draw-string-2d(
            alloc-point(1000.00, get-float(console-y-start, self) + 100.00, -4.00, 0),
            "Speed Change: L2(hold) + R2(hold) + Dpad Up/Down",
            7,
            0.00,
            0.50,
            0,
            0,
            0,
            1.00
        );
        debug-draw-string-2d(
            alloc-point(1000.00, get-float(console-y-start, self) + 110.00, -4.00, 0),
            "Demeanor Change: L2(hold) + R2(hold) + Circle",
            7,
            0.00,
            0.50,
            0,
            0,
            0,
            1.00
        );
        var_1 = debug-draw-string-2d(
            alloc-point(1000.00, get-float(console-y-start, self) + 120.00, -4.00, 0),
            "Hide Help: Square",
            7,
            0.00,
            0.50,
            0,
            0,
            0,
            1.00
        );
    } else {
        var_1 = debug-draw-string-2d(
            alloc-point(1000.00, get-float(console-y-start, self) + 50.00, -4.00, 0),
            "Display Help: Square",
            7,
            0.00,
            0.50,
            0,
            0,
            0,
            1.00
        );
    }
    return var_1;
}

u64? update-rig() {
    u64? var_1 = get-spawner-position(get-symbol(activity, self), 1);
    return set-object-position(get-symbol(rig-origin-obj, self), alloc-point(*(f32*)var_1, 0.00, *(f32*)(var_1 + 8), 0));
}

statescript {
    options {
        player
    }
    declarations {
        i32 angle-start-pointer = 0
        i32 angle-end-pointer = 0
        i32 angle-start = 0
        i32 angle-end = 0
        i32 demeanor-pointer = 0
        i32 speed-pointer = 0
        i32 speed-enum = 0
        f32 console-y-start = 50.00
        f32 console-x-start = 100.00
        bool draw-help? = nullptr
        symbol rig-origin-obj = obj-cap-rig-1
        symbol pivot-start = pivot-start-1
        symbol pivot-end = pivot-end-1
        symbol position-start = position-start-1
        symbol position-end = position-end-1
        symbol activity = cap-drawer-70-1
        symbol active-npc = #0000000000000000
        bound-frame active-region-locator = nullptr
        symbol active-npc-selection = select-npc-to-spawn
    }
    state initial {
        block start {
            track main {
                lambda {
                    player-remove-weapons();
                }
                lambda {
                    fatigue-disable();
                }
                lambda {
                    update-rig();
                }
            }
        }
        block update {
            track main {
                lambda {
                    set-symbol(active-npc-selection, hud-debug-group-menu(
                        character-group,
                        get-symbol(active-npc-selection, self),
                        alloc-point(get-float(console-x-start, self) + 150.00, get-float(console-y-start, self), 0.00, 0),
                        alloc-vector(-14.00, 6.00, 0.00, 1.00, 0),
                        npc-group-menu-confirmed,
                        hud-debug-group-menu-updated,
                        0.60,
                        "triangle",
                        8.00
                    ), self);
                }
                lambda {
                    set-symbol(activity, hud-debug-group-menu(
                        caps-group,
                        get-symbol(activity, self),
                        alloc-point(get-float(console-x-start, self), get-float(console-y-start, self), 0.00, 0),
                        alloc-vector(-14.00, 6.00, 0.00, 1.00, 0),
                        cap-group-menu-confirmed,
                        hud-debug-group-menu-updated,
                        0.60,
                        "square",
                        8.00
                    ), self);
                }
                lambda {
                    draw-movement-dir-caption(get-symbol(position-start, self), dir-start, get-object-locator(get-symbol(rig-origin-obj, self)));
                }
                lambda {
                    draw-movement-dir-caption(get-symbol(position-end, self), dir-end, get-object-locator(get-symbol(rig-origin-obj, self)));
                }
                lambda {
                    draw-crosshair();
                }
                lambda {
                    if (debug-joypad-command-active?(fire-hold, 0)) {
                        send-event(selection-active, self);
                    }
                }
                lambda {
                    if (debug-joypad-command-active?(jump, 0) && !debug-joypad-command-active?(fire-hold, 0)) {
                        send-event(start-moving, self);
                    }
                }
                lambda {
                    if (debug-joypad-command-active?(fire-hold, 0)) {
                        if (debug-joypad-command-active?(menu-left, 0)) {
                            set-int32(angle-end, get-int32(angle-end, self) - 10, self);
                            set-object-locator(get-symbol(pivot-end, self), rotate-locator-angle(get-object-locator-default-rotation(get-symbol(pivot-end, self)), 1, (f32)get-int32(angle-end, self)));
                        }
                        if (debug-joypad-command-active?(menu-right, 0)) {
                            set-int32(angle-end, get-int32(angle-end, self) + 10, self);
                            set-object-locator(get-symbol(pivot-end, self), rotate-locator-angle(get-object-locator-default-rotation(get-symbol(pivot-end, self)), 1, (f32)get-int32(angle-end, self)));
                        }
                    }
                }
                lambda {
                    if (debug-joypad-command-active?(aim, 0)) {
                        if (debug-joypad-command-active?(menu-left, 0)) {
                            set-int32(angle-start, get-int32(angle-start, self) - 10, self);
                            set-object-locator(get-symbol(pivot-start, self), rotate-locator-angle(get-object-locator-default-rotation(get-symbol(pivot-start, self)), 1, (f32)get-int32(angle-start, self)));
                        }
                        if (debug-joypad-command-active?(menu-right, 0)) {
                            set-int32(angle-start, get-int32(angle-start, self) + 10, self);
                            set-object-locator(get-symbol(pivot-start, self), rotate-locator-angle(get-object-locator-default-rotation(get-symbol(pivot-start, self)), 1, (f32)get-int32(angle-start, self)));
                        }
                    }
                }
                lambda {
                    if (debug-joypad-command-active?(aim, 0) && debug-joypad-command-active?(fire-hold, 0)) {
                        if (debug-joypad-command-active?(crouch-press, 0)) {
                            send-event(change-demeanor, self, boxed_i32(0));
                        }
                    }
                }
                lambda {
                    if (debug-joypad-command-active?(aim, 0) && debug-joypad-command-active?(fire-hold, 0)) {
                        if (debug-joypad-command-active?(menu-up, 0)) {
                            send-event(change-speed, self, boxed_i32(1));
                        }
                        if (debug-joypad-command-active?(menu-down, 0)) {
                            send-event(change-speed, self, boxed_i32(0));
                        }
                    }
                }
                lambda {
                    if (debug-joypad-command-active?(melee-attack, 0)) {
                        send-event(help-toggle, self);
                    }
                }
            }
        }
        block event start-moving {
            track main {
                lambda {
                    if (spawner-exists?(get-symbol(activity, self))) {
                        kill-entity(get-symbol(activity, self));
                        spawn-entity(get-symbol(activity, self), 0, 0);
                    }
                }
                lambda {
                    if (entity-exists?(get-symbol(active-npc, self)) && spawner-exists?(get-symbol(activity, self))) {
                        issue-mm-teleport(get-symbol(active-npc, self), get-object-locator-remove-binding(get-symbol(position-start, self)));
                        wait-npc-use-cinematic-action-pack(get-symbol(active-npc, self), get-symbol(activity, self), 0, 0, 0);
                        issue-mm-move-speed(get-symbol(active-npc, self), 0, get-object-locator-remove-binding(get-symbol(position-end, self)), 1, 0, 0, 0);
                    }
                }
            }
        }
        block event help-toggle {
            track main {
                lambda {
                    if (get-boolean(draw-help?, self)) {
                        set-boolean(draw-help?, 0, self);
                    } else {
                        set-boolean(draw-help?, 1, self);
                    }
                }
            }
        }
        block event change-demeanor {
            track main {
                lambda {
                    if (entity-exists?(get-symbol(active-npc, self))) {
                        if (event-arg-int32(0) == 0) {
                            ++(demeanor-pointer, self);
                        } else {
                            --(demeanor-pointer, self);
                        }
                        if (get-int32(demeanor-pointer, self) > 6) {
                            set-int32(demeanor-pointer, 0, self);
                        } else if (get-int32(demeanor-pointer, self) < 0) {
                            set-int32(demeanor-pointer, 6, self);
                        }
                        update-npc-demeanor(get-symbol(active-npc, self), 0);
                    } else {
                        debug-draw-text-box-2D(
                            "Spawn npc first!",
                            60,
                            340.00,
                            60.00,
                            1.00,
                            1,
                            0,
                            1,
                            1.00
                        );
                    }
                }
            }
        }
        block event change-speed {
            track main {
                lambda {
                    if (event-arg-int32(0) == 1) {
                        ++(speed-pointer, self);
                    } else {
                        --(speed-pointer, self);
                    }
                }
                lambda {
                    if (get-int32(speed-pointer, self) > 3) {
                        set-int32(speed-pointer, 0, self);
                    } else if (get-int32(speed-pointer, self) < 0) {
                        set-int32(speed-pointer, 3, self);
                    }
                }
                lambda {
                    if (get-int32(speed-pointer, self) == 0) {
                        set-int32(speed-enum, 0, self);
                    } else if (get-int32(speed-pointer, self) == 1) {
                        set-int32(speed-enum, 1, self);
                    } else if (get-int32(speed-pointer, self) == 2) {
                        set-int32(speed-enum, 2, self);
                    } else if (get-int32(speed-pointer, self) == 3) {
                        set-int32(speed-enum, 3, self);
                    }
                }
            }
        }
        block event selection-active {
            track main {
                lambda {
                    u64? var_4 = kick-raycast(camera-locator(1), offset-locator(camera-locator(1), alloc-vector(0.00, 0.00, 35.00, 1.00, 0)), 0, 4097);
                    wait-one-frame();
                    if (wait-raycast-hit-nothing?(var_4)) {}
 else {
                        set-bound-frame(active-region-locator, wait-raycast(var_4), self);
                        debug-draw-sphere(get-locator-position(get-bound-frame(active-region-locator, self)), 0.25, 3, 0.00, 0, 0);
                        u64? var_11 = select-activity(get-bound-frame(active-region-locator, self));
                        if (!(var_11 == 0)) {
                            var_12 = set-symbol(activity, var_11, self);
                        }
                    }
                }
            }
        }
        block event npc-group-menu-confirmed {
            track main {
                lambda {
                    if (!(get-symbol(active-npc-selection, self) == select-npc-to-spawn)) {
                        set-symbol(active-npc, debug-spawn-test-npc(get-symbol(active-npc-selection, self)), self);
                    }
                }
            }
        }
        block event cap-group-menu-confirmed {
            track main {
                lambda {
                    update-rig();
                }
            }
        }
    }
}
