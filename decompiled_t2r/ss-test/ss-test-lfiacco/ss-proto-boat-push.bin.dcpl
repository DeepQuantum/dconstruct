// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-lfiacco\ss-proto-boat-push.bin            #
// #   filesize: 9921 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 28                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        f32 var1 = 0.00
        bound-frame boat-attatch = nullptr
        vector boat-offset = nullptr
        i32 boat-raycast = nullptr
        bound-frame water-surface = nullptr
    }
    state initial {
        block start {
            track main {
                lambda {
                    wait(0.50);
                }
                lambda {
                    go(ready, 1);
                }
            }
        }
    }
    state ready {
        block start {
            track main {
                lambda {
                    interactable-enable(tag-symbol(interact, self));
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    go(engaged, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (fact-symbol(player, player-interactable, 0, 0) != 0) {
                        if (!(interactable-disabled?(tag-symbol(interact, self)))) {
                            interactable-disable(tag-symbol(interact, self));
                        }
                    } else if (interactable-disabled?(tag-symbol(interact, self))) {
                        interactable-enable(tag-symbol(interact, self));
                    }
                }
            }
        }
    }
    state engaged {
        block start {
            track main {
                lambda {
                    fact-set(player, player-interactable, new-boxed-value(7, self));
                }
                lambda {
                    enable-collision-gamedriven(self, 0, 0.00);
                }
                lambda {
                    boat-motor-set-enabled(self, 0);
                }
                lambda {
                    boat-steering-set-enabled(self, 0);
                }
                lambda {
                    interactable-disable(tag-symbol(interact, self));
                }
                lambda {
                    set-bound-frame(boat-attatch, offset-locator(rotate-locator-angle(alloc-bound-frame(get-object-position(player), get-locator-rotation(camera-locator(0)), 0, 0, 0), 1, 0.00), alloc-vector(0.25, -0.25, 5.75, 1.00, 0)), self);
                }
                lambda {
                    alloc-vector(0.25, -0.25, 5.75, 1.00, 0);
                }
                lambda {
                    wait(1.00);
                }
                lambda {
                    while (1) {
                        u16 var_1;
                        if (joypad-command-active?(interact, 6)) {
                            var_1 = 1;
                        } else {
                            var_1 = 0;
                        }
                        if (var_1) {
                            go(exit, 1);
                        }
                        wait-one-frame();
                    }
                }
            }
            track raycast {
                lambda {
                    while (1) {
                        if (!player-swimming?()) {
                            set-int32(boat-raycast, kick-raycast(
                                offset-locator(get-spawner-locator(self, 1), alloc-vector(0.00, 1.00, 0.00, 1.00, 0)),
                                get-object-locator(self),
                                self,
                                65
                            ), self);
                            wait-one-frame();
                            set-bound-frame(water-surface, wait-raycast(get-int32(boat-raycast, self)), self);
                        }
                        wait-one-frame();
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    player-disable-melee();
                }
                lambda {
                    player-force-walk();
                }
                lambda {
                    if (player-is-crouched?()) {
                        go(exit, 1);
                    }
                }
                lambda {
                    if (player-diving-underwater?()) {
                        go(exit, 1);
                    }
                }
                lambda {
                    if (player-swimming?()) {
                        set-vector(boat-offset, alloc-vector(0.25, -0.30, 4.00, 1.00, 0), self);
                    }
                }
                lambda {
                    if (!player-swimming?()) {
                        set-vector(boat-offset, alloc-vector(0.25, 0.50, 4.00, 1.00, 0), self);
                    }
                }
                lambda {
                    set-object-locator(self, blend-locator(get-object-locator(self), offset-locator(get-object-locator(player), get-vector(boat-offset, self)), 10.00 * get-delta-time()));
                }
            }
        }
    }
    state exit {
        block start {
            track main {
                lambda {
                    enable-physics(self, 0, 0.00);
                }
                lambda {
                    boat-motor-set-enabled(self, 1);
                }
                lambda {
                    boat-steering-set-enabled(self, 1);
                }
                lambda {
                    wait(1.00);
                }
                lambda {
                    fact-set(player, player-interactable, new-boxed-value(7, 0));
                }
                lambda {
                    go(ready, 1);
                }
            }
        }
    }
}
