// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-aeinhorn\ss-perch.bin                     #
// #   filesize: 7244 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 14                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        sp-all
    }
    declarations {
        i32 raycast = -1
        f32 hide-score = 0.00
        f32 dist-to-npc = 0.00
        f32 dist-to-player = 0.00
        f32 y-dist-above-player = 0.00
        bool raycast-hit-player = nullptr
        bool blocked-by-player = nullptr
        f32 score = 0.00
    }
    state test {
        block start {
            track main {
                lambda {
                    while (!npc-alive?(tag-symbol(hiding-npc, self))) {
                        wait-one-frame();
                    }
                }
                lambda {
                    while (!is-script-running?(tag-symbol(hiding-npc, self))) {
                        wait-one-frame();
                    }
                }
                lambda {
                    send-event(register-hide-post, tag-symbol(hiding-npc, self), new-boxed-value(7, get-self-id()));
                }
            }
        }
        block update {
            track main {
                lambda {
                    u64? var_3 = get-spawner-locator(self, 1);
                    set-float(dist-to-player, get-locator-distance(get-object-locator(player), var_3), self);
                    set-float(dist-to-npc, get-locator-distance(get-object-locator(tag-symbol(hiding-npc, self)), var_3), self);
                    set-float(y-dist-above-player, *(f32*)(get-spawner-position(self, 1) + 4) - *(f32*)(get-object-position(player) + 4), self);
                }
                lambda {
                    set-boolean(blocked-by-player, get-float(dist-to-player, self) < get-float(dist-to-npc, self) && is-behind?(
                        rotate-locator-lookat(get-object-locator(player), get-object-locator(tag-symbol(hiding-npc, self)), 1.00, 0),
                        get-spawner-locator(self, 1),
                        2,
                        180.00
                    ), self);
                }
                lambda {
                    set-boolean(raycast-hit-player, get-int32(raycast, self) != -1 && wait-raycast-hit-nothing?(get-int32(raycast, self)), self);
                }
                lambda {
                    set-int32(raycast, -1, self);
                }
                lambda {
                    set-float(score, 1.00, self);
                }
                lambda {
                    if (get-boolean(blocked-by-player, self)) {
                        set-float(score, 0.00 * get-float(score, self), self);
                    }
                }
                lambda {
                    if (!get-boolean(raycast-hit-player, self)) {
                        set-float(score, 0.00 * get-float(score, self), self);
                    }
                }
                lambda {
                    set-float(score, curve-scale(0.00, 40.00, 0.00, 1.00, get-float(dist-to-player, self), 4) * get-float(score, self), self);
                }
                lambda {
                    set-float(score, curve-scale(80.00, 40.00, 0.00, 1.00, get-float(dist-to-player, self), 4) * get-float(score, self), self);
                }
                lambda {
                    set-float(score, curve-scale(0.00, 5.00, 0.50, 1.00, get-float(y-dist-above-player, self), 1) * get-float(score, self), self);
                }
                lambda {
                    set-int32(raycast, kick-raycast(
                        offset-locator(get-object-locator(player), alloc-vector(0.00, 1.00, 0.00, 1.00, 0)),
                        offset-locator(get-spawner-locator(self, 1), alloc-vector(0.00, 1.00, 0.00, 1.00, 0)),
                        player,
                        1
                    ), self);
                }
            }
        }
    }
}
