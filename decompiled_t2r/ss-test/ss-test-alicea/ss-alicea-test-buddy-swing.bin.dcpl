// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-alicea\ss-alicea-test-buddy-swing.bin     #
// #   filesize: 13269 bytes                                                                          #
// #   total entries: 1                                                                               #
// #   functions: 41                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bool rope-up-and-over = nullptr
        bool empty-once = nullptr
        bool jump-once = nullptr
        bool hi-once = nullptr
    }
    state startup {
        block start {
            track main {
                lambda {
                    wait-boolean(hi-once, 1, self);
                }
                lambda {
                    wait(1.00);
                }
                lambda {
                    script-caption(jesse, "How am I gonna get over to you?", -1.00, 0, 0);
                }
                lambda {
                    go(initial, 1);
                }
            }
        }
    }
    state initial {
        block update {
            track main {
                lambda {
                    if (get-player-carry-object-id() == tag-symbol(rope-to-check, self)) {
                        go(carrying, 1);
                    }
                }
            }
        }
    }
    state carrying {
        block start {
            track main {
                lambda {
                    set-boolean(rope-up-and-over, 0, self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-object-distance(tag-symbol(rope-to-check, self), player) > 2.00) {
                        go(thrown, 1);
                    }
                }
            }
        }
    }
    state thrown {
        block start {
            track main {
                lambda {
                    while (script-in-state?(tag-symbol(script-to-check, self), catch-rope) && !((get-current-time(0) - get-current-time(0)) > 5.00)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    if (get-boolean(rope-up-and-over, self) && script-in-state?(tag-symbol(script-to-check, self), catch-rope)) {
                        wait(1.00);
                        go(buddy-has-rope, 1);
                    } else if (!(get-boolean(empty-once, self)) && get-boolean(rope-up-and-over, self)) {
                        go(empty-hang, 1);
                    } else if (!(get-boolean(jump-once, self)) && script-in-state?(tag-symbol(script-to-check, self), catch-rope)) {
                        wait(1.00);
                        go(jump-rope, 1);
                    } else {
                        while (!(get-object-distance(tag-symbol(rope-to-check, self), player) < 2.00)) {
                            wait-one-frame();
                        }
                        go(carrying, 1);
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!(get-player-carry-object-id() == tag-symbol(rope-to-check, self))) {
                        go(initial, 1);
                    }
                }
            }
        }
    }
    state jump-rope {
        block start {
            track main {
                lambda {
                    set-boolean(jump-once, 1, self);
                }
                lambda {
                    wait-script-caption(jesse, "Cool.  Now what?", -1.00, 0, 0);
                }
                lambda {
                    wait-script-caption(player, "Uh, you can jump down and climb up the rope?", -1.00, 0, 0);
                }
                lambda {
                    wait-script-caption(jesse, "No way! That'll kill me.", -1.00, 0, 0);
                }
                lambda {
                    wait-script-caption(player, "Ugh...fine.  Let's figure something else out then.", -1.00, 0, 0);
                }
                lambda {
                    if (script-in-state?(tag-symbol(script-to-check, self), catch-rope)) {
                        go(thrown, 1);
                    }
                }
                lambda {
                    while (!(get-object-distance(tag-symbol(rope-to-check, self), player) < 2.00)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    go(carrying, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!(get-player-carry-object-id() == tag-symbol(rope-to-check, self))) {
                        go(initial, 1);
                    }
                }
            }
        }
    }
    state empty-hang {
        block start {
            track main {
                lambda {
                    set-boolean(empty-once, 1, self);
                }
                lambda {
                    wait-script-caption(jesse, "Uh, how will that help?", -1.00, 0, 0);
                }
                lambda {
                    wait-script-caption(player, "You jump to it and swing over.", -1.00, 0, 0);
                }
                lambda {
                    wait-script-caption(jesse, "If I could jump all the way over, I'd have done that already.", -1.00, 0, 0);
                }
                lambda {
                    wait-script-caption(player, "But there's a rope now?", -1.00, 0, 0);
                }
                lambda {
                    wait-script-caption(jesse, "Let's think of something else...", -1.00, 0, 0);
                }
                lambda {
                    if (script-in-state?(tag-symbol(script-to-check, self), catch-rope)) {
                        go(thrown, 1);
                    }
                }
                lambda {
                    while (!(get-object-distance(tag-symbol(rope-to-check, self), player) < 2.00)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    go(carrying, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!(get-player-carry-object-id() == tag-symbol(rope-to-check, self))) {
                        go(initial, 1);
                    }
                }
            }
        }
    }
    state buddy-has-rope {
        block start {
            track main {
                lambda {
                    wait-script-caption(jesse, "NICE! I think I can swing over like this", -1.00, 0, 0);
                }
                lambda {
                    wait-script-caption(player, "Only one way to find out.", -1.00, 0, 0);
                }
                lambda {
                    enable-action-pack(tap-jump-across-715, 1);
                }
                lambda {
                    enable-action-pack(tap-jump-across-717, 1);
                }
                lambda {
                    enable-action-pack(tap-jump-across-813, 1);
                }
                lambda {
                    enable-action-pack(tap-jump-across-814, 1);
                }
                lambda {
                    npc-enable-skill(jesse, 51);
                }
                lambda {
                    while (!npc-using-traversal-action-pack?(jesse)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    wait(1.50);
                }
                lambda {
                    send-event(
                        rope-detach,
                        player-get-carried-object(),
                        new-boxed-value(7, jesse),
                        new-boxed-value(18, get-joint-locator(jesse, r_hand_prop_attachment))
                    );
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    joypad-disable-commands(interact, inv-drop, object-interact, interact-invisible, fire-press, fire-hold, object-arc-throw);
                }
            }
        }
    }
}
