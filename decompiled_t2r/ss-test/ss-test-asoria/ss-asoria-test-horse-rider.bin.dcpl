// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-asoria\ss-asoria-test-horse-rider.bin     #
// #   filesize: 7654 bytes                                                                           #
// #   total entries: 3                                                                               #
// #   functions: 17                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

u16 play-anim() {
    u16 var_1;
    if (get-float(speed, self) == 0.00) {
        set-symbol(anim, horse-mm-ambi-idle, self);
        var_1 = set-symbol(anim-rider, horse-mm-ambi-idle--npc, self);
    } else if (get-float(speed, self) > 0.00) {
        set-symbol(anim, horse-mm-gallop-fw-loop, self);
        var_1 = set-symbol(anim-rider, horse-mm-gallop-fw-loop--npc, self);
    } else {
        var_1 = 0;
    }
    return var_1;
}

u64? horse-ap() {
    return get-joint-parented-bound-frame(tag-symbol(horse, self), hero_attachment);
}

statescript {
    options {
        player
    }
    declarations {
        symbol anim = nullptr
        symbol anim-rider = nullptr
        f32 speed = nullptr
        f32 angle = nullptr
        f32 distance = nullptr
    }
    state setup {
        block start {
            track main {
                lambda {
                    play-anim();
                }
                lambda {
                    u64? var_2 = #%alloc-array(single-frame, 16, 1, 1);
                    *(i32*)*(*var_2 + (0 * 8)) = 28;
                    *(u64*)(*(*var_2 + (0 * 8)) + 8) = 0;
                    *(f32*)(*(*var_2 + (0 * 8)) + 8) = 0.50;
                    animate_(tag-symbol(horse, self), get-symbol(anim, self), var_2);
                }
                lambda {
                    u64? var_2 = #%alloc-array(single-frame, 16, 1, 1);
                    *(i32*)*(*var_2 + (0 * 8)) = 28;
                    *(u64*)(*(*var_2 + (0 * 8)) + 8) = 0;
                    *(f32*)(*(*var_2 + (0 * 8)) + 8) = 0.50;
                    animate_(get-self-id(), get-symbol(anim-rider, self), var_2);
                }
            }
        }
        block event start-chase {
            track main {
                lambda {
                    go(move, 1);
                }
            }
        }
    }
    state move {
        block start {
            track main {
                lambda {
                    follow-spline(tag-symbol(horse, self), tag-symbol(spline, self), 0);
                }
                lambda {
                    set-symbol(anim, horse-mm-gallop-fw-loop, self);
                }
                lambda {
                    set-symbol(anim-rider, horse-mm-gallop-fw-loop--npc, self);
                }
                lambda {
                    u64? var_2 = #%alloc-array(single-frame, 16, 1, 1);
                    *(i32*)*(*var_2 + (0 * 8)) = 28;
                    *(u64*)(*(*var_2 + (0 * 8)) + 8) = 0;
                    *(f32*)(*(*var_2 + (0 * 8)) + 8) = 0.50;
                    animate_(tag-symbol(horse, self), get-symbol(anim, self), var_2);
                }
                lambda {
                    u64? var_2 = #%alloc-array(single-frame, 16, 1, 1);
                    *(i32*)*(*var_2 + (0 * 8)) = 28;
                    *(u64*)(*(*var_2 + (0 * 8)) + 8) = 0;
                    *(f32*)(*(*var_2 + (0 * 8)) + 8) = 0.50;
                    animate_(get-self-id(), get-symbol(anim-rider, self), var_2);
                }
            }
        }
        block update {
            track main {
                lambda {
                    set-spline-follow-speed(tag-symbol(horse, self), get-float(speed, self), 0);
                }
                lambda {
                    set-object-locator(get-self-id(), get-joint-locator(tag-symbol(horse, self), hero_attachment));
                }
                lambda {
                    set-float(distance, get-object-distance(player, tag-symbol(horse, self)), self);
                }
                lambda {
                    set-float(angle, get-object-facing-angle-plus(get-object-locator(tag-symbol(horse, self)), get-object-locator(player), 2, 1), self);
                }
                lambda {
                    if (get-float(distance, self) >= 2.00 && absf(get-float(angle, self)) >= 90.00) {
                        set-float(speed, 0.00, self);
                    } else if (get-float(distance, self) >= 20.00 && absf(get-float(angle, self)) <= 90.00) {
                        set-float(speed, 15.00, self);
                    } else if (get-float(distance, self) >= 5.00 && absf(get-float(angle, self)) <= 90.00) {
                        set-float(speed, 3.00, self);
                    } else if (get-float(distance, self) <= 1.00 && absf(get-float(angle, self)) <= 90.00) {
                        set-float(speed, get-player-speed(), self);
                    }
                }
            }
        }
        block event chase-part-2 {
            track main {
                lambda {
                    go(move, 1);
                }
            }
        }
    }
}
