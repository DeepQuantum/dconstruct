// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-mgallant\ss-proto-slow-bullets.bin        #
// #   filesize: 9161 bytes                                                                           #
// #   total entries: 3                                                                               #
// #   functions: 15                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    state setup {
        block start {
            track main {
                lambda {
                    while (!(get-symbol(npc, self) == 0) && !!(get-symbol(bullet-spline, self) == 0)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    disable-collision(self, 0);
                }
                lambda {
                    go(create-spline, 1);
                }
                lambda {
                    if (darray-count(bullet-splines) == 0) {
                        darray-reset(bullet-splines);
                    }
                }
                lambda {
                    u64? var_2 = group-get-first-member(bullet-splines);
                    u64? var_4 = spawn-entity-at(proto-slow-bullet, get-joint-locator(npc-get-weapon-process-id(event-arg-symbol(0)), silencer), 0);
                    set-symbol(npc, event-arg-symbol(0), var_4);
                    set-symbol(bullet-spline, var_2, var_4);
                    darray-find-remove(bullet-splines, new-boxed-value(7, var_2), 0);
                }
            }
        }
    }
    state create-spline {
        block start {
            track main {
                lambda {
                    u64? var_2 = get-joint-locator(npc-get-weapon-process-id(get-symbol(npc, self)), silencer);
                    sid var_8;
                    if (player-is-prone?()) {
                        var_8 = headb;
                    } else if (player-is-crouched?()) {
                        var_8 = pelvis;
                    } else {
                        var_8 = spined;
                    }
                    set-spline-point-locator(get-symbol(bullet-spline, self), 0, var_2);
                    set-int32(bullet-raycast, kick-raycast(
                        offset-locator(var_2, alloc-vector(0.00, 0.00, 0.50, 1.00, 0)),
                        offset-locator(rotate-locator-lookat(
                            var_2,
                            offset-locator(get-predicted-player-bound-frame(lerp-scale(0.00, tag-float(bullet-speed, self), 0.00, 1.00, distance-between-objects(player, get-symbol(npc, self)))), alloc-vector(0.00, get-locator-distance-y(get-object-locator(player), get-joint-locator(player, var_8)), 0.00, 1.00, 0)),
                            1.00,
                            1
                        ), alloc-vector(0.00, 0.00, 100.00, 1.00, 0)),
                        player,
                        6147
                    ), self);
                }
                lambda {
                    wait-one-frame();
                }
                lambda {
                    set-spline-point-locator(get-symbol(bullet-spline, self), 1, wait-raycast(get-int32(bullet-raycast, self)));
                }
                lambda {
                    go(lerp-bullet, 1);
                }
            }
        }
    }
    state lerp-bullet {
        block start {
            track main {
                lambda {
                    set-int32(bullet-raycast, -1, self);
                }
                lambda {
                    while (get-float(spline-dist, self) < get-spline-length(get-symbol(bullet-spline, self))) {
                        if (get-int32(bullet-raycast, self) > 0) {
                            u64? var_5 = wait-raycast-get-object(get-int32(bullet-raycast, self));
                            if (!(var_5 == 0) && var_5 != player) {
                                go(die, 1);
                            }
                            if (!(var_5 == 0) && var_5 == player) {
                                var_6 = go(do-damage, 1);
                            }
                        }
                        +=f(spline-dist, get-delta-time() * tag-float(bullet-speed, self), self);
                        clamp-var(spline-dist, 0.00, get-spline-length(get-symbol(bullet-spline, self)), self);
                        set-object-locator(self, spline-locator-at-distance(get-symbol(bullet-spline, self), get-float(spline-dist, self)));
                        set-int32(bullet-raycast, kick-spherecast(
                            get-object-locator(self),
                            offset-locator(get-object-locator(self), alloc-vector(0.00, 0.00, 0.15, 1.00, 0)),
                            0.15,
                            get-self-id(),
                            35
                        ), self);
                        wait-one-frame();
                    }
                }
                lambda {
                    go(die, 1);
                }
            }
        }
    }
    state do-damage {
        block start {
            track main {
                lambda {
                    player-take-melee-damage(get-symbol(npc, self), 20, 1, 1);
                }
                lambda {
                    kill-entity(get-self-id());
                }
            }
        }
    }
    state die {
        block start {
            track main {
                lambda {
                    kill-entity(get-self-id());
                }
            }
        }
    }
}
