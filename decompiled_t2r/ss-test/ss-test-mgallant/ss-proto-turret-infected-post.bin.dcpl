// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-mgallant\ss-proto-turret-infected-post.bin#
// #   filesize: 6740 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 13                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        sp-all
    }
    declarations {
        f32 score = 0.00
        i32 raycast = -1
        bool raycast-blocked = nullptr
        f32 raycast-time = 0.00
        f32 player-dist-xz = 999.00
        symbol assigned-npc = #0000000000000000
        timer update-timer = nullptr
        timer recently-assigned-timer = nullptr
    }
    state score-post {
        block start {
            track main {
                lambda {
                    darray-append(infected-turret-posts, new-boxed-value(7, get-self-id()), 1);
                }
                lambda {
                    set-float(player-dist-xz, get-locator-distance-xz(get-spawner-locator(self, 1), get-object-locator(player)), self);
                }
                lambda {
                    start-timer(update-timer, deterministic-random-float(0.00, 1.00, self), self, 1);
                }
            }
        }
        block event update-timer {
            track main {
                lambda {
                    set-float(player-dist-xz, get-locator-distance-xz(get-spawner-locator(self, 1), get-object-locator(player)), self);
                }
                lambda {
                    if (get-boolean(raycast-blocked, self)) {
                        set-float(score, 0.00, self);
                    } else if (timer-running?(recently-assigned-timer, self)) {
                        set-float(score, 0.00, self);
                    } else if (!is-left?(get-spawner-locator(self, 1), get-object-locator(player), 120.00) && !is-right?(get-spawner-locator(self, 1), get-object-locator(player), 120.00)) {
                        set-float(score, 0.00, self);
                    } else if (get-float(player-dist-xz, self) < 20.00) {
                        set-float(score, lerp-scale(5.00, 20.00, 0.00, 1.00, get-float(player-dist-xz, self)), self);
                    } else {
                        set-float(score, lerp-scale(25.00, 40.00, 1.00, 0.10, get-float(player-dist-xz, self)), self);
                    }
                }
                lambda {
                    if (get-int32(raycast, self) < 0 && time-elapsed(get-float(raycast-time, self)) > 0.50) {
                        set-int32(raycast, kick-raycast(
                            offset-locator(get-spawner-locator(self, 1), alloc-vector(0.00, 2.00, -0.50, 1.00, 0)),
                            get-joint-locator(player, pelvis),
                            player,
                            4097
                        ), self);
                        set-float(raycast-time, current-time(1), self);
                        send-event(raycast-update, self);
                    }
                }
                lambda {
                    start-timer(update-timer, 0.30, self, 1);
                }
            }
        }
        block event raycast-update {
            track main {
                lambda {
                    wait-one-frame();
                }
                lambda {
                    if (!(get-int32(raycast, self) < 0)) {
                        set-boolean(raycast-blocked, !wait-raycast-hit-nothing?(get-int32(raycast, self)), self);
                    }
                }
                lambda {
                    set-int32(raycast, -1, self);
                }
            }
        }
        block event assign-npc {
            track main {
                lambda {
                    set-symbol(assigned-npc, event-arg-symbol(0), self);
                }
            }
        }
        block event unassign-npc {
            track main {
                lambda {
                    set-symbol(assigned-npc, 0, self);
                }
                lambda {
                    start-timer(recently-assigned-timer, 4.00, self, 1);
                }
            }
        }
    }
}
