// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-italian-tomatoes\ss-vox-test-location-regions.bin#
// #   filesize: 12501 bytes                                                                          #
// #   total entries: 1                                                                               #
// #   functions: 34                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        slavalle-test-location-regions
    }
    declarations {
        symbol selected-position = outside
        symbol selected-dialog = spot-player
        string spawn-npc-debug-draw = R3 - Spawn NPC - Outside (vox-location-general-3)
        string spot-pursue-debug-draw = L3 - Force NPCs to Spot Player
    }
    state task-check {
        block event player-requested-interact {
            track main {
                lambda {
                    go(general-1, 1);
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    go(general-2, 1);
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    go(specific-1, 1);
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    go(go-back-to-board, 1);
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    go(go-back-to-board, 1);
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    go(go-back-to-board, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    debug-draw-string-centered(get-string(spawn-npc-debug-draw, self), 530.00, 1, 0.00, 1.50, 1, 0);
                }
                lambda {
                    debug-draw-string-centered(get-string(spot-pursue-debug-draw, self), 560.00, 1, 0.00, 1.50, 1, 0);
                }
                lambda {
                    debug-draw-string-centered("Dpad Right/Left - Change NPC spawn location", 590.00, 1, 0.00, 1.50, 1, 0);
                }
                lambda {
                    if (debug-joypad-command-active?(debug-script-press-l3, 0)) {
                        go(force-spot-player, 1);
                    } else if (debug-joypad-command-active?(debug-script-press-r3, 0)) {
                        go(spawn-npc, 1);
                    } else if (debug-joypad-command-active?(debug-script-press-right, 0)) {
                        go(change-location-next, 1);
                    } else if (debug-joypad-command-active?(debug-script-hold-left, 0)) {
                        go(change-location-prev, 1);
                    }
                }
            }
        }
    }
    state general-1 {
        block start {
            track main {
                lambda {
                    send-event(teleport-player, player, new-boxed-value(18, get-spawner-locator(pos-gen-1, 1)));
                }
                lambda {
                    wait(0.20);
                }
                lambda {
                    go(task-check, 1);
                }
            }
        }
    }
    state general-2 {
        block start {
            track main {
                lambda {
                    send-event(teleport-player, player, new-boxed-value(18, get-spawner-locator(pos-gen-2, 1)));
                }
                lambda {
                    wait(0.20);
                }
                lambda {
                    go(task-check, 1);
                }
            }
        }
    }
    state specific-1 {
        block start {
            track main {
                lambda {
                    send-event(teleport-player, player, new-boxed-value(18, get-spawner-locator(pos-spec-1, 1)));
                }
                lambda {
                    wait(0.20);
                }
                lambda {
                    go(task-check, 1);
                }
            }
        }
    }
    state go-back-to-board {
        block start {
            track main {
                lambda {
                    send-event(teleport-player, player, new-boxed-value(18, get-spawner-locator(pos-go-back-1, 1)));
                }
                lambda {
                    wait(0.20);
                }
                lambda {
                    go(task-check, 1);
                }
            }
        }
    }
    state spawn-npc {
        block start {
            track main {
                lambda {
                    if (get-symbol(selected-position, self) == outside) {
                        var_1 = npc-set-patrol-route(wait-spawn-entity-at(npc-militia-1, get-spawner-locator(pos-npc-speaker-gen-3-outside-1, 1), 0), spl-outside-speaker-1, -1);
                    } else if (get-symbol(selected-position, self) == building-1) {
                        var_1 = npc-set-patrol-route(wait-spawn-entity-at(npc-militia-1, get-spawner-locator(pos-npc-speaker-gen-2-building-1, 1), 0), spl-building-1-speaker-1, -1);
                    } else if (get-symbol(selected-position, self) == building-2) {
                        var_1 = npc-set-patrol-route(wait-spawn-entity-at(npc-militia-1, get-spawner-locator(pos-npc-speaker-gen-2-building-2, 1), 0), spl-building-2-speaker-1, -1);
                    }
                }
                lambda {
                    wait(0.20);
                }
                lambda {
                    go(task-check, 1);
                }
            }
        }
    }
    state change-location-next {
        block start {
            track main {
                lambda {
                    if (get-symbol(selected-position, self) == outside) {
                        set-symbol(selected-position, building-1, self);
                        set-string(spawn-npc-debug-draw, "R3 - Spawn NPC - Building 1 (vox-location-general-2)", self);
                    } else if (get-symbol(selected-position, self) == building-1) {
                        set-symbol(selected-position, building-2, self);
                        set-string(spawn-npc-debug-draw, "R3 - Spawn NPC - Building 2 (vox-location-general-2)", self);
                    } else if (get-symbol(selected-position, self) == building-2) {
                        set-symbol(selected-position, outside, self);
                        set-string(spawn-npc-debug-draw, "R3 - Spawn NPC - Outside (vox-location-general-1)", self);
                    }
                }
                lambda {
                    wait(0.20);
                }
                lambda {
                    go(task-check, 1);
                }
            }
        }
    }
    state change-location-prev {
        block start {
            track main {
                lambda {
                    if (get-symbol(selected-position, self) == outside) {
                        set-symbol(selected-position, building-2, self);
                        set-string(spawn-npc-debug-draw, "R3 - Spawn NPC - Building 2 (vox-location-general-2)", self);
                    } else if (get-symbol(selected-position, self) == building-2) {
                        set-symbol(selected-position, building-1, self);
                        set-string(spawn-npc-debug-draw, "R3 - Spawn NPC - Building 1 (vox-location-general-2)", self);
                    } else if (get-symbol(selected-position, self) == building-1) {
                        set-symbol(selected-position, outside, self);
                        set-string(spawn-npc-debug-draw, "R3 - Spawn NPC - Outside (vox-location-general-1)", self);
                    }
                }
                lambda {
                    wait(0.20);
                }
                lambda {
                    go(task-check, 1);
                }
            }
        }
    }
    state force-spot-player {
        block start {
            track main {
                lambda {
                    foreach (u64? var_1 : all-enemy-npcs) {
                        u16 var_2;
                        if (var_1 && *(u16*)(var_1 + 12) == 7) {
                            var_2 = *(u64*)var_1;
                        } else if (var_1 && *(u16*)(var_1 + 12) == 5) {
                            var_2 = *(u64*)var_1;
                        } else if (var_1 && *(u16*)(var_1 + 12) == 4) {
                            var_2 = *(u64*)var_1;
                        } else {
                            var_2 = 0;
                        }
                        npc-make-aware-of(var_2, player, 2, 0);
                    }
                }
                lambda {
                    wait(0.20);
                }
                lambda {
                    go(task-check, 1);
                }
            }
        }
    }
}
