// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-em\ss-es-floating-pallet-object.bin       #
// #   filesize: 22576 bytes                                                                          #
// #   total entries: 6                                                                               #
// #   functions: 31                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bool is-floating = true
        bool is-supported = nullptr
        bool allow-exit = nullptr
        bool parented = nullptr
        bound-frame lower-point = nullptr
        bound-frame start-point = nullptr
        i32 groupcount = 0
        timer press-delay = nullptr
    }
    state initial {
        block start {
            track main {
                lambda {
                    wait(0.50);
                }
                lambda {
                    if (!get-boolean(is-floating, self)) {
                        go(sink, 1);
                    }
                }
                lambda {
                    go(unsupported, 1);
                }
            }
        }
    }
    state unsupported {
        block start {
            track main {
                lambda {
                    while (1) {
                        while (get-object-parent(player) == get-self-id() || player-in-state?(Edge) || player-in-state?(EdgeClimbUp) || ! != nullptr && !!(0)) {
                            wait-one-frame();
                        }
                        if (tag-boolean(is-debris?, self)) {
                            set-body-buoyancy-multiplier(get-self-id(), 0.00, 0);
                        }
                        wait-one-frame();
                        set-boolean(parented, 1, self);
                        if (!tag-boolean(is-debris?, self)) {
                            set-bound-frame(lower-point, offset-locator(flatten-locator(get-object-locator(get-self-id())), alloc-vector(0.00, -2.00, 0.00, 1.00, 0)), self);
                            set-bound-frame(start-point, get-object-locator(get-self-id()), self);
                            while (get-object-parent(player) == get-self-id()) {
                                set-object-locator(get-self-id(), blend-locator(get-object-locator(get-self-id()), get-bound-frame(lower-point, self), 10.00 * get-delta-time()));
                                wait-frames(2);
                            }
                        }
                        while (!(get-object-parent(player) == get-self-id()) && !(get-locator-distance(get-object-locator(player), get-bound-frame(start-point, self)) > 2.00)) {
                            wait-one-frame();
                        }
                        set-boolean(parented, 0, self);
                        if (tag-boolean(is-debris?, self)) {
                            set-body-buoyancy-multiplier(get-self-id(), 1.00, 0);
                        }
                        if (!tag-boolean(is-debris?, self)) {
                            while (!(get-object-parent(player) == get-self-id())) {
                                set-object-locator(self, blend-locator(get-object-locator(get-self-id()), get-bound-frame(start-point, self), 10.00 * get-delta-time()));
                                wait-frames(2);
                            }
                        }
                        wait-one-frame();
                    }
                }
            }
        }
        block event barrel-under {
            track main {
                lambda {
                    if (!get-boolean(is-full, event-arg-symbol(0))) {
                        darray-append(tag-symbol(my-group, self), new-boxed-value(7, event-arg-symbol(0)), 1);
                        ++(groupcount, self);
                    }
                }
            }
        }
        block event barrel-out {
            track main {
                lambda {
                    if (!get-boolean(is-full, event-arg-symbol(0))) {
                        darray-find-remove(tag-symbol(my-group, self), new-boxed-value(7, event-arg-symbol(0)), 0);
                        --(groupcount, self);
                        if (tag-boolean(is-metal?, self) && get-boolean(is-floating, self) && darray-count(tag-symbol(my-group, self)) == 0) {
                            set-boolean(is-floating, 0, self);
                            send-event(set-buoyancy-settings, get-self-id(), new-boxed-value(7, shi-buoyancy-object-sinking-barrel));
                            go(sink, 1);
                        }
                    }
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    go(move, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!(get-boolean(is-supported, self))) {
                        if (!(tag-boolean(is-metal?, self)) || !(darray-count(tag-symbol(my-group, self)) == 0)) {
                            if (!(get-boolean(is-floating, self)) || !(darray-count(tag-symbol(my-group, self)) == 0)) {
                                if (darray-count(tag-symbol(my-group, self)) > 1 && player-swimming-underwater?()) {
                                    draw-interactable-hud(interact-attack, get-object-locator(self), 0);
                                    if (joypad-command-active?(interact-wall, 6)) {
                                        broadcast-event(connect-barrel, new-boxed-value(7, get-self-id()));
                                        if (get-boolean(is-floating, self)) {
                                            set-boolean(is-supported, 1, self);
                                            go(supported, 1);
                                        }
                                        if (!timer-running?(press-delay, self)) {
                                            start-timer(press-delay, 1.00, self, 1);
                                            set-boolean(is-floating, 1, self);
                                            if (tag-boolean(is-metal?, self)) {
                                                send-event(set-buoyancy-settings, get-self-id(), new-boxed-value(7, shi-buoyancy-object-floating-barrel));
                                                go(raise, 1);
                                            }
                                        }
                                    }
                                }
                            } else if (player-swimming-underwater?()) {}

                        }
                    }
                }
                lambda {
                    if (fact-symbol(player, player-interactable, 0, 0) != 0) {
                        if (!(interactable-disabled?(tag-symbol(interact, self)))) {
                            interactable-disable(tag-symbol(interact, self));
                        }
                    } else if (interactable-disabled?(tag-symbol(interact, self))) {
                        interactable-enable(tag-symbol(interact, self));
                    }
                }
            }
        }
    }
    state raise {
        block start {
            track main {
                lambda {
                    while (!=f(*(f32*)(get-object-position(get-self-id()) + 4), *(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 0)) + 4), 0.01)) {
                        set-object-locator(self, blend-locator(get-object-locator(self), alloc-bound-frame(
                            alloc-point(
                                *(f32*)get-object-position(get-self-id()),
                                *(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 0)) + 4),
                                *(f32*)(get-object-position(get-self-id()) + 8),
                                0
                            ),
                            get-locator-rotation(flatten-locator(get-object-locator(get-self-id()))),
                            0,
                            0,
                            0
                        ), 2.00 * get-delta-time()));
                        wait-one-frame();
                    }
                }
                lambda {
                    go(unsupported, 1);
                }
            }
        }
    }
    state sink {
        block start {
            track main {
                lambda {
                    while (!=f(*(f32*)(get-object-position(get-self-id()) + 4), *(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 1)) + 4), 0.01)) {
                        set-object-locator(self, blend-locator(get-object-locator(self), alloc-bound-frame(
                            alloc-point(
                                *(f32*)get-object-position(get-self-id()),
                                *(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 1)) + 4),
                                *(f32*)(get-object-position(get-self-id()) + 8),
                                0
                            ),
                            get-locator-rotation(flatten-locator(get-object-locator(get-self-id()))),
                            0,
                            0,
                            0
                        ), 2.00 * get-delta-time()));
                        wait-one-frame();
                    }
                }
                lambda {
                    go(unsupported, 1);
                }
            }
        }
    }
    state supported {
        block  {
        }
        block event player-requested-interact {
            track main {
                lambda {
                    go(move, 1);
                }
            }
        }
        block event barrel-under {
            track main {
                lambda {
                    if (!get-boolean(is-full, event-arg-symbol(0))) {
                        darray-append(tag-symbol(my-group, self), new-boxed-value(7, event-arg-symbol(0)), 1);
                        set-boolean(is-supported, 1, self);
                        ++(groupcount, self);
                        go(supported, 1);
                    }
                }
            }
        }
        block event barrel-out {
            track main {
                lambda {
                    if (darray-contains?(tag-symbol(my-group, self), new-boxed-value(7, event-arg-symbol(0)))) {
                        darray-find-remove(tag-symbol(my-group, self), new-boxed-value(7, event-arg-symbol(0)), 0);
                        --(groupcount, self);
                        set-boolean(is-supported, 0, self);
                        go(unsupported, 1);
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (fact-symbol(player, player-interactable, 0, 0) != 0) {
                        if (!(interactable-disabled?(tag-symbol(interact, self)))) {
                            interactable-disable(tag-symbol(interact, self));
                        }
                    } else if (interactable-disabled?(tag-symbol(interact, self))) {
                        interactable-enable(tag-symbol(interact, self));
                    }
                }
            }
        }
    }
    state move {
        block start {
            track main {
                lambda {
                    fact-set(player, player-interactable, new-boxed-value(7, self));
                }
                lambda {
                    enable-collision-gamedriven(self, 0, 0.00);
                }
                lambda {
                    interactable-disable(tag-symbol(interact, self));
                }
                lambda {
                    set-boolean(allow-exit, 0, self);
                }
                lambda {
                    wait(1.00);
                }
                lambda {
                    set-boolean(allow-exit, 1, self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    set-object-locator(self, blend-locator(get-object-locator(self), offset-locator(alloc-bound-frame(get-object-position(player), get-locator-rotation(flatten-locator(camera-locator(0))), 0, 0, 0), alloc-vector(0.25, 0.00, 2.00, 1.00, 0)), 10.00 * get-delta-time()));
                }
                lambda {
                    if (get-boolean(is-supported, self) && tag-exists?(depth-flow-spline, self) && spline-distance-object(tag-symbol(depth-flow-spline, self), player) > 2.50) {
                        player-swim-flow-blend(player, tag-symbol(depth-flow-spline, self), -3.00, -5.00, 0, 0);
                        joypad-disable-commands(dive-swim-down, dive-sprint-hold);
                    } else if (!(get-boolean(is-floating, self))) {}
 else if (tag-exists?(depth-flow-spline, self)) {
                        player-swim-flow-blend(player, tag-symbol(depth-flow-spline, self), 0.00, -2.50, 0, 0);
                    }
                }
                lambda {
                    u16 var_1;
                    if (joypad-command-active?(interact, 6)) {
                        var_1 = 1;
                    } else {
                        var_1 = 0;
                    }
                    if (var_1 && get-boolean(allow-exit, self)) {
                        go(exit, 1);
                    }
                }
            }
        }
    }
    state exit {
        block start {
            track main {
                lambda {
                    if (tag-boolean(is-debris?, self)) {
                        enable-physics(self, 0, 0.00);
                    }
                }
                lambda {
                    if (!(get-boolean(is-floating, self)) && tag-exists?(depth-flow-spline, self)) {
                        while (!=f(*(f32*)(get-object-position(get-self-id()) + 4), *(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 1)) + 4), 0.01)) {
                            set-object-locator(self, blend-locator(get-object-locator(self), alloc-bound-frame(
                                alloc-point(
                                    *(f32*)get-object-position(get-self-id()),
                                    *(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 1)) + 4),
                                    *(f32*)(get-object-position(get-self-id()) + 8),
                                    0
                                ),
                                get-locator-rotation(flatten-locator(get-object-locator(get-self-id()))),
                                0,
                                0,
                                0
                            ), 2.00 * get-delta-time()));
                            wait-one-frame();
                        }
                    } else if (tag-exists?(depth-flow-spline, self)) {
                        while (!=f(*(f32*)(get-object-position(get-self-id()) + 4), *(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 0)) + 4), 0.01)) {
                            set-object-locator(self, blend-locator(get-object-locator(self), alloc-bound-frame(
                                alloc-point(
                                    *(f32*)get-object-position(get-self-id()),
                                    *(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 0)) + 4),
                                    *(f32*)(get-object-position(get-self-id()) + 8),
                                    0
                                ),
                                get-locator-rotation(flatten-locator(get-object-locator(get-self-id()))),
                                0,
                                0,
                                0
                            ), 2.00 * get-delta-time()));
                            wait-one-frame();
                        }
                    } else {
                        wait(1.00);
                    }
                }
                lambda {
                    interactable-enable(tag-symbol(interact, self));
                }
                lambda {
                    fact-set(player, player-interactable, new-boxed-value(7, 0));
                }
                lambda {
                    if (get-boolean(is-supported, self)) {
                        go(supported, 1);
                    } else {
                        go(unsupported, 1);
                    }
                }
            }
        }
    }
}
