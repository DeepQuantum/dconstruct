// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-em\ss-es-floating-barrel-object.bin       #
// #   filesize: 21442 bytes                                                                          #
// #   total entries: 1                                                                               #
// #   functions: 63                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bool is-full = nullptr
        symbol pallet-parent = #0000000000000000
        f32 var1 = 0.00
        bool parented = nullptr
        timer release-water = nullptr
        timer release-air = nullptr
        vector offset = nullptr
    }
    state initial {
        block start {
            track main {
                lambda {
                    wait(0.50);
                }
                lambda {
                    go(ready, 1);
                }
            }
        }
    }
    state ready {
        block start {
            track main {
                lambda {
                    interactable-enable(tag-symbol(interact, self));
                }
            }
        }
        block event release-water {
            track main {
                lambda {
                    kill-entity(tag-symbol(drip, self));
                }
                lambda {
                    set-boolean(is-full, 0, self);
                }
                lambda {
                    send-event(set-buoyancy-settings, get-self-id(), new-boxed-value(7, shi-buoyancy-object-floating-barrel));
                }
            }
        }
        block event release-air {
            track main {
                lambda {
                    kill-entity(tag-symbol(bubbles, self));
                }
                lambda {
                    set-boolean(is-full, 1, self);
                }
                lambda {
                    send-event(set-buoyancy-settings, get-self-id(), new-boxed-value(7, shi-buoyancy-object-sinking-barrel));
                }
                lambda {
                    if (get-boolean(parented, self)) {
                        set-boolean(parented, 0, self);
                        unparent(get-self-id());
                        enable-physics(self, 0, 0.00);
                    }
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    if (get-boolean(parented, self)) {
                        set-boolean(parented, 0, self);
                        unparent(get-self-id());
                    }
                }
                lambda {
                    go(engaged, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (fact-symbol(player, player-interactable, 0, 0) != 0) {
                        if (!(interactable-disabled?(tag-symbol(interact, self)))) {
                            interactable-disable(tag-symbol(interact, self));
                        }
                    } else if (interactable-disabled?(tag-symbol(interact, self))) {
                        interactable-enable(tag-symbol(interact, self));
                    }
                }
                lambda {
                    if (timer-running?(release-air, self) && =f(*(f32*)(get-object-position(get-self-id()) + 4), *(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 0)) + 4), 0.10)) {
                        stop-timer(release-air, self);
                        kill-entity(tag-symbol(bubbles, self));
                    }
                }
            }
        }
    }
    state engaged {
        block start {
            track main {
                lambda {
                    fact-set(player, player-interactable, new-boxed-value(7, self));
                }
                lambda {
                    enable-collision-gamedriven(self, 0, 0.00);
                }
                lambda {
                    interactable-disable(tag-symbol(interact, self));
                }
                lambda {
                    wait(1.00);
                }
                lambda {
                    while (1) {
                        u16 var_1;
                        if (joypad-command-active?(interact, 6)) {
                            var_1 = 1;
                        } else {
                            var_1 = 0;
                        }
                        if (var_1) {
                            go(exit, 1);
                        }
                        wait-one-frame();
                    }
                }
            }
        }
        block event release-water {
            track main {
                lambda {
                    kill-entity(tag-symbol(drip, self));
                }
                lambda {
                    set-boolean(is-full, 0, self);
                }
                lambda {
                    send-event(set-buoyancy-settings, get-self-id(), new-boxed-value(7, shi-buoyancy-object-floating-barrel));
                }
            }
        }
        block event release-air {
            track main {
                lambda {
                    kill-entity(tag-symbol(bubbles, self));
                }
                lambda {
                    set-boolean(is-full, 1, self);
                }
                lambda {
                    send-event(set-buoyancy-settings, get-self-id(), new-boxed-value(7, shi-buoyancy-object-sinking-barrel));
                }
            }
        }
        block event connect-barrel {
            track main {
                lambda {
                    set-vector(offset, vector-between-locators(get-object-locator(event-arg-symbol(0)), get-object-locator(get-self-id())), self);
                }
                lambda {
                    set-symbol(pallet-parent, event-arg-symbol(0), self);
                }
                lambda {
                    go(fasten-barrel, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    player-disable-melee();
                }
                lambda {
                    player-force-walk();
                }
                lambda {
                    if (tag-boolean(leaking, self) && get-boolean(is-full, self) && !player-in-state?(Swim) && !timer-running?(release-water, self)) {
                        start-timer(release-water, 5.00, self, 1);
                        spawn-entity-at(tag-symbol(drip, self), rotate-locator-angle(flatten-locator(offset-locator(get-object-locator(self), alloc-vector(-0.20, 1.10, 0.00, 1.00, 0))), 1, 90.00), 0);
                        parent(tag-symbol(drip, self), get-self-id(), 0);
                    }
                }
                lambda {
                    if (player-in-state?(Swim) && timer-running?(release-water, self)) {
                        stop-timer(release-water, self);
                        kill-entity(tag-symbol(drip, self));
                    }
                }
                lambda {
                    if (tag-boolean(leaking, self) && !(get-boolean(is-full, self)) && player-swimming-underwater?() && !(timer-running?(release-air, self))) {
                        start-timer(release-air, 10.00, self, 1);
                        spawn-entity-at(tag-symbol(bubbles, self), rotate-locator-angle(flatten-locator(offset-locator(get-object-locator(self), alloc-vector(-0.20, 1.10, 0.00, 1.00, 0))), 1, 90.00), 0);
                        parent(tag-symbol(bubbles, self), get-self-id(), 0);
                    }
                }
                lambda {
                    if (!(player-swimming-underwater?()) && timer-running?(release-air, self)) {
                        stop-timer(release-air, self);
                        kill-entity(tag-symbol(bubbles, self));
                    }
                }
                lambda {
                    f32 var_2;
                    if (player-in-state?(Move)) {
                        var_2 = 1.00;
                    } else {
                        var_2 = 0.00;
                    }
                    set-object-locator(self, blend-locator(get-object-locator(self), offset-locator(rotate-locator-angle(alloc-bound-frame(get-locator-position(offset-locator(get-object-locator(player), alloc-vector(0.00, var_2, 0.00, 1.00, 0))), get-locator-rotation(camera-locator(0)), 0, 0, 0), 2, 90.00), alloc-vector(0.25, -0.50, 1.00, 1.00, 0)), 10.00 * get-delta-time()));
                }
                lambda {
                    set-float(var1, spline-distance-object(tag-symbol(depth-flow-spline, self), player), self);
                }
                lambda {
                    if (get-boolean(is-full, self)) {
                        joypad-set-stick-scale(move, 0.51);
                    } else if (timer-running?(release-air, self) && tag-exists?(depth-flow-spline, self) && spline-distance-object(tag-symbol(depth-flow-spline, self), player) > 2.50) {
                        player-swim-flow-blend(player, tag-symbol(depth-flow-spline, self), -0.20, -3.00, 0, 0);
                        joypad-disable-commands(dive-swim-down, dive-sprint-hold);
                    } else if (tag-exists?(depth-flow-spline, self)) {
                        player-swim-flow-blend(player, tag-symbol(depth-flow-spline, self), -0.20, -3.00, 0, 0);
                    }
                }
            }
        }
    }
    state fasten-barrel {
        block start {
            track main {
                lambda {
                    set-boolean(parented, 1, self);
                }
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 3, 1);
                    *(i32*)*(*var_0 + (0 * 8)) = 28;
                    *(u64*)(*(*var_0 + (0 * 8)) + 8) = 0;
                    *(f32*)(*(*var_0 + (0 * 8)) + 8) = 0.40;
                    *(i32*)*(*var_0 + (1 * 8)) = 30;
                    *(u64*)(*(*var_0 + (1 * 8)) + 8) = 0;
                    *(f32*)(*(*var_0 + (1 * 8)) + 8) = 0.40;
                    *(i32*)*(*var_0 + (2 * 8)) = 24;
                    *(u64*)(*(*var_0 + (2 * 8)) + 8) = 0;
                    *(i32*)(*(*var_0 + (2 * 8)) + 8) = 16;
                    animate_(player, swim-attach-idle, var_0);
                }
                lambda {
                    wait(0.75);
                }
                lambda {
                    stop-animating(player, 2, 0.40, 0, 0);
                }
                lambda {
                    fact-set(player, player-interactable, new-boxed-value(7, 0));
                }
                lambda {
                    interactable-enable(tag-symbol(interact, self));
                }
            }
        }
        block event release-air {
            track main {
                lambda {
                    kill-entity(tag-symbol(bubbles, self));
                }
                lambda {
                    set-boolean(is-full, 1, self);
                }
                lambda {
                    send-event(set-buoyancy-settings, get-self-id(), new-boxed-value(7, shi-buoyancy-object-sinking-barrel));
                }
                lambda {
                    if (get-boolean(parented, self)) {
                        set-boolean(parented, 0, self);
                        unparent(get-self-id());
                        enable-physics(self, 0, 0.00);
                        go(ready, 1);
                    }
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    if (get-boolean(parented, self)) {
                        set-boolean(parented, 0, self);
                        unparent(get-self-id());
                    }
                }
                lambda {
                    go(engaged, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    set-object-locator(get-self-id(), alloc-bound-frame(
                        get-locator-position(offset-locator(get-object-locator(get-symbol(pallet-parent, self)), get-vector(offset, self))),
                        get-locator-rotation(rotate-locator-angle(get-object-locator(get-symbol(pallet-parent, self)), 0, 90.00)),
                        0,
                        0,
                        0
                    ));
                }
                lambda {
                    if (tag-boolean(leaking, self) && !(get-boolean(is-full, self)) && *(f32*)(get-object-position(get-self-id()) + 4) < (*(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 0)) + 4) - 0.10) && !(timer-running?(release-air, self))) {
                        start-timer(release-air, 10.00, self, 1);
                        spawn-entity-at(tag-symbol(bubbles, self), rotate-locator-angle(flatten-locator(offset-locator(get-object-locator(self), alloc-vector(-0.20, 1.10, 0.00, 1.00, 0))), 1, 90.00), 0);
                        parent(tag-symbol(bubbles, self), get-self-id(), 0);
                    }
                }
                lambda {
                    if (timer-running?(release-air, self) && =f(*(f32*)(get-object-position(get-self-id()) + 4), *(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 0)) + 4), 0.10)) {
                        stop-timer(release-air, self);
                        kill-entity(tag-symbol(bubbles, self));
                    }
                }
            }
        }
    }
    state exit {
        block start {
            track main {
                lambda {
                    enable-physics(self, 0, 0.00);
                }
                lambda {
                    wait(1.00);
                }
                lambda {
                    fact-set(player, player-interactable, new-boxed-value(7, 0));
                }
                lambda {
                    go(ready, 1);
                }
            }
        }
        block event release-water {
            track main {
                lambda {
                    kill-entity(tag-symbol(drip, self));
                }
                lambda {
                    set-boolean(is-full, 0, self);
                }
                lambda {
                    send-event(set-buoyancy-settings, get-self-id(), new-boxed-value(7, shi-buoyancy-object-floating-barrel));
                }
            }
        }
        block event release-air {
            track main {
                lambda {
                    kill-entity(tag-symbol(bubbles, self));
                }
                lambda {
                    set-boolean(is-full, 1, self);
                }
                lambda {
                    send-event(set-buoyancy-settings, get-self-id(), new-boxed-value(7, shi-buoyancy-object-sinking-barrel));
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (timer-running?(release-air, self) && =f(*(f32*)(get-object-position(get-self-id()) + 4), *(f32*)(get-locator-position(get-spline-point-locator(tag-symbol(depth-flow-spline, self), 0)) + 4), 0.10)) {
                        stop-timer(release-air, self);
                        kill-entity(tag-symbol(bubbles, self));
                    }
                }
            }
        }
    }
}
