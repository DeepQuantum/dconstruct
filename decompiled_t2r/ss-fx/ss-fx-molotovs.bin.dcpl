// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-fx\ss-fx-molotovs.bin                                  #
// #   filesize: 35592 bytes                                                                          #
// #   total entries: 13                                                                              #
// #   functions: 123                                                                                 #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

u64? initialize-molotov-variables() {
    u64? var_1;
    if (player-upgrade-active?(molotov-radius-increase)) {
        var_1 = *(*molotov-explosion-upgraded* + 40);
    } else {
        var_1 = *(*molotov-explosion* + 40);
    }
    f32 var_3;
    if (player-upgrade-active?(molotov-radius-increase)) {
        var_3 = 1.00;
    } else {
        var_3 = 0.00;
    }
    set-float(kill, *(f32*)var_1, self);
    set-float(strong, *(f32*)(var_1 + 4), self);
    set-float(weak, *(f32*)(var_1 + 8), self);
    set-float(flare, var_3, self);
    set-vector(color, alloc-vector(1.50, 1.50, 1.50, 1.00, 0), self);
    set-symbol(impact-smoke, molotov-impact-smoke, self);
    return set-symbol(impact-smoke-snow, molotov-impact-snow-smoke, self);
}

u64? set-molotov-handle-rootvars() {
    set-effect-float(get-uint64(molotov-handle, self), killradius, get-float(kill, self));
    set-effect-float(get-uint64(molotov-handle, self), strongradius, get-float(strong, self));
    set-effect-float(get-uint64(molotov-handle, self), weakradius, get-float(weak, self));
    set-effect-float(get-uint64(molotov-handle, self), ratemult, get-float(rate, self));
    set-effect-float(get-uint64(molotov-handle, self), flare, get-float(flare, self));
    set-effect-vector(get-uint64(molotov-handle, self), upgradecolor, get-vector(color, self));
    set-effect-float(get-uint64(molotov-handle, self), duration, get-float(flame-time, self));
    f32 var_16;
    if (player-upgrade-active?(molotov-radius-increase)) {
        var_16 = 1.00;
    } else {
        var_16 = 0.00;
    }
    set-effect-float(get-uint64(molotov-handle, self), upgradeFx, var_16);
    return set-effect-vector(get-uint64(molotov-handle, self), throwvel, get-vector(velocity-vector, self));
}

u64? handle-npc-splashers() {
    set-symbol(npc-splash-array, darray-create(self, -1, 0), self);
    foreach (u64? var_2 : all-enemy-npcs) {
        u16 var_3;
        if (var_2 && *(u16*)(var_2 + 12) == 7) {
            var_3 = *(u64*)var_2;
        } else if (var_2 && *(u16*)(var_2 + 12) == 5) {
            var_3 = *(u64*)var_2;
        } else if (var_2 && *(u16*)(var_2 + 12) == 4) {
            var_3 = *(u64*)var_2;
        } else {
            var_3 = 0;
        }
        if (npc-alive?(var_3)) {
            if (distance-between-locators(get-bound-frame(location, self), get-object-locator(var_3)) < get-float(weak, self)) {
                u64? var_10 = spawn-particles-at-bound-frame(molotov-cocktail-character-splash, get-bound-frame(location, self), self);
                darray-append(get-symbol(npc-splash-array, self), new-boxed-value(5, var_10), 0);
                set-effect-vector(var_10, directionVector, vector-between-locators(get-bound-frame(location, self), get-object-locator(var_3)));
            }
        }
    }
}

statescript {
    declarations {
        bound-frame location = nullptr
        i32 upgrade-level = nullptr
        symbol attacker-id = nullptr
        i32 attacker-net-id = nullptr
        bool water-surface-valid = nullptr
        bound-frame water-surface = nullptr
        timer main-timer = nullptr
        u64 molotov-handle = 0
        symbol npc-splash-array = nullptr
        f32 kill = 0.00
        f32 weak = 0.00
        f32 strong = 0.00
        f32 rate = 0.00
        f32 flare = 0.00
        vector color = (0.00, 0.00, 0.00, 1.00)
        f32 flame-time = 14.00
        vector velocity-vector = (0.00, 0.00, 0.00, 1.00)
        symbol impact-smoke = #0000000000000000
        symbol impact-smoke-snow = #0000000000000000
        bound-frame location-raycast = nullptr
    }
    state molotov-npc {
        block event spawn-particles {
            track main {
                lambda {
                    initialize-molotov-variables();
                }
                lambda {
                    start-timer(main-timer, get-float(flame-time, self), self, 1);
                }
                lambda {
                    set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail-npc, get-bound-frame(location, self), self), self);
                }
                lambda {
                    set-molotov-handle-rootvars();
                }
                lambda {
                    spawn-particles-at-bound-frame(spore-push-field, get-bound-frame(location, self), none);
                }
                lambda {
                    handle-npc-splashers();
                }
                lambda {
                    go(spawn-flames, 1);
                }
                lambda {
                    set-float(flame-time, 6.00, self);
                }
                lambda {
                    set-symbol(attacker-id, player, self);
                }
                lambda {
                    initialize-molotov-variables();
                }
                lambda {
                    start-timer(main-timer, get-float(flame-time, self), self, 1);
                }
                lambda {
                    set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail-npc, get-bound-frame(location, self), self), self);
                }
                lambda {
                    set-molotov-handle-rootvars();
                }
                lambda {
                    spawn-particles-at-bound-frame(spore-push-field, get-bound-frame(location, self), none);
                }
                lambda {
                    handle-npc-splashers();
                }
                lambda {
                    go(spawn-flames, 1);
                }
                lambda {
                    set-float(flame-time, 6.00, self);
                }
                lambda {
                    set-symbol(attacker-id, player, self);
                }
                lambda {
                    initialize-molotov-variables();
                }
                lambda {
                    start-timer(main-timer, get-float(flame-time, self), self, 1);
                }
                lambda {
                    if (get-boolean(water-surface-valid, self)) {
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, get-bound-frame(water-surface, self), 0), self);
                    } else {
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, get-bound-frame(location, self), 0), self);
                    }
                }
                lambda {
                    set-molotov-handle-rootvars();
                }
                lambda {
                    spawn-particles-at-bound-frame(spore-push-field, get-bound-frame(location, self), none);
                }
                lambda {
                    handle-npc-splashers();
                }
                lambda {
                    if (net-is-active?()) {
                        set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                        add-molotov-flame(
                            get-bound-frame(location, self),
                            1.00,
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    } else {
                        add-molotov-flame(
                            get-bound-frame(location, self),
                            1.00,
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    }
                }
                lambda {
                    if (task-active?(tracking-horde, tra-horde-joel-tommy-rescue)) {
                        set-float(flame-time, 9999.00, self);
                        initialize-molotov-variables();
                    } else {
                        initialize-molotov-variables();
                    }
                }
                lambda {
                    start-timer(main-timer, get-float(flame-time, self), self, 1);
                }
                lambda {
                    set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, get-bound-frame(location, self), self), self);
                }
                lambda {
                    set-molotov-handle-rootvars();
                }
                lambda {
                    spawn-particles-at-bound-frame(spore-push-field, get-bound-frame(location, self), none);
                }
                lambda {
                    handle-npc-splashers();
                }
                lambda {
                    add-molotov-flame(
                        get-bound-frame(location, self),
                        get-float(flame-time, self),
                        get-symbol(attacker-id, self),
                        get-int32(attacker-net-id, self)
                    );
                }
                lambda {
                    initialize-molotov-variables();
                }
                lambda {
                    start-timer(main-timer, get-float(flame-time, self), self, 1);
                }
                lambda {
                    set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail-ceiling, get-bound-frame(location, self), self), self);
                }
                lambda {
                    set-molotov-handle-rootvars();
                }
                lambda {
                    spawn-particles-at-bound-frame(spore-push-field, get-bound-frame(location, self), none);
                }
                lambda {
                    handle-npc-splashers();
                }
                lambda {
                    go(spawn-flames, 1);
                }
                lambda {
                    set-float(flame-time, 6.00, self);
                }
                lambda {
                    set-symbol(attacker-id, player, self);
                }
                lambda {
                    initialize-molotov-variables();
                }
                lambda {
                    start-timer(main-timer, get-float(flame-time, self), self, 1);
                }
                lambda {
                    set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, get-bound-frame(location, self), self), self);
                }
                lambda {
                    set-molotov-handle-rootvars();
                }
                lambda {
                    spawn-particles-at-bound-frame(spore-push-field, get-bound-frame(location, self), none);
                }
                lambda {
                    handle-npc-splashers();
                }
                lambda {
                    add-molotov-flame(
                        get-bound-frame(location, self),
                        get-float(flame-time, self),
                        get-symbol(attacker-id, self),
                        get-int32(attacker-net-id, self)
                    );
                }
                lambda {
                    initialize-molotov-variables();
                }
                lambda {
                    start-timer(main-timer, get-float(flame-time, self), self, 1);
                }
                lambda {
                    if (player-upgrade-active?(molotov-radius-increase)) {
                        set-uint64(gas-handle, spawn-particles-at-bound-frame(molotov-cocktail-outward-gaseous, get-bound-frame(location, self), none), self);
                    }
                }
                lambda {
                    set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail-wall, get-bound-frame(location, self), self), self);
                }
                lambda {
                    set-molotov-handle-rootvars();
                }
                lambda {
                    spawn-particles-at-bound-frame(spore-push-field, get-bound-frame(location, self), none);
                }
                lambda {
                    handle-npc-splashers();
                }
                lambda {
                    go(spawn-flames, 1);
                }
                lambda {
                    set-float(flame-time, 6.00, self);
                }
                lambda {
                    set-symbol(attacker-id, player, self);
                }
                lambda {
                    initialize-molotov-variables();
                }
                lambda {
                    start-timer(main-timer, get-float(flame-time, self), self, 1);
                }
                lambda {
                    set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail-ceiling, get-bound-frame(location, self), self), self);
                }
                lambda {
                    set-molotov-handle-rootvars();
                }
                lambda {
                    spawn-particles-at-bound-frame(spore-push-field, get-bound-frame(location, self), none);
                }
                lambda {
                    handle-npc-splashers();
                }
                lambda {
                    go(spawn-flames, 1);
                }
                lambda {
                    set-float(flame-time, 6.00, self);
                }
                lambda {
                    set-symbol(attacker-id, player, self);
                }
                lambda {
                    initialize-molotov-variables();
                }
                lambda {
                    start-timer(main-timer, get-float(flame-time, self), self, 1);
                }
                lambda {
                    if (player-upgrade-active?(molotov-radius-increase)) {
                        set-uint64(gas-handle, spawn-particles-at-bound-frame(molotov-cocktail-outward-gaseous, get-bound-frame(location, self), none), self);
                    }
                }
                lambda {
                    set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail-wall, get-bound-frame(location, self), self), self);
                }
                lambda {
                    set-molotov-handle-rootvars();
                }
                lambda {
                    spawn-particles-at-bound-frame(spore-push-field, get-bound-frame(location, self), none);
                }
                lambda {
                    handle-npc-splashers();
                }
                lambda {
                    go(spawn-flames, 1);
                }
                lambda {
                    initialize-molotov-variables();
                }
                lambda {
                    start-timer(main-timer, get-float(flame-time, self), self, 1);
                }
                lambda {
                    if (get-boolean(water-surface-valid, self)) {
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, get-bound-frame(water-surface, self), 0), self);
                    } else {
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, get-bound-frame(location, self), 0), self);
                    }
                }
                lambda {
                    set-molotov-handle-rootvars();
                }
                lambda {
                    spawn-particles-at-bound-frame(spore-push-field, get-bound-frame(location, self), none);
                }
                lambda {
                    handle-npc-splashers();
                }
                lambda {
                    if (net-is-active?()) {
                        set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                        add-molotov-flame(
                            get-bound-frame(location, self),
                            1.00,
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    } else {
                        add-molotov-flame(
                            get-bound-frame(location, self),
                            1.00,
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    }
                }
            }
        }
        block event main-timer {
            track main {
                lambda {
                    terminate();
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail/scorch_collision_emitter && event-arg-int32(3) != 28 && event-arg-int32(3) != 27 && event-arg-int32(3) != 38 && *(f32*)(event-arg-vector(1) + 4) > 0.80) {
                        set-uint64(smoke-handle, spawn-particles-at-bound-frame(get-symbol(impact-smoke, self), event-arg-bound-frame(0), none), self);
                        set-effect-float(get-uint64(smoke-handle, self), killradius, get-float(kill, self));
                    }
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail/scorch_collision_emitter || event-arg-int32(3) == 28 && event-arg-int32(3) == 27 && event-arg-int32(3) != 63 && event-arg-int32(3) != 38 && *(f32*)(event-arg-vector(1) + 4) > 0.80) {
                        spawn-particles-at-bound-frame(get-symbol(impact-smoke-snow, self), event-arg-bound-frame(0), none);
                    }
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail/scorch_collision_emitter && event-arg-int32(3) != 28 && event-arg-int32(3) != 27 && event-arg-int32(3) != 38 && *(f32*)(event-arg-vector(1) + 4) > 0.80) {
                        set-uint64(smoke-handle, spawn-particles-at-bound-frame(get-symbol(impact-smoke, self), event-arg-bound-frame(0), none), self);
                        set-effect-float(get-uint64(smoke-handle, self), killradius, get-float(kill, self));
                    }
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail/scorch_collision_emitter || event-arg-int32(3) == 28 && event-arg-int32(3) == 27 && event-arg-int32(3) != 63 && event-arg-int32(3) != 38 && *(f32*)(event-arg-vector(1) + 4) > 0.80) {
                        spawn-particles-at-bound-frame(get-symbol(impact-smoke-snow, self), event-arg-bound-frame(0), none);
                    }
                }
                lambda {
                    terminate();
                }
            }
        }
        block event main-timer {
            track main {
                lambda {
                    terminate();
                }
                lambda {
                    terminate();
                }
            }
        }
    }
    state spawn-flames {
        block start {
            track main {
                lambda {
                    set-bound-frame(location-raycast, offset-locator-world-space(get-bound-frame(location, self), alloc-vector(0.00, -3.00, 0.00, 1.00, 0)), self);
                }
                lambda {
                    wait-one-frame();
                    u64? var_3 = wait-raycast-t(kick-spherecast(get-bound-frame(location, self), get-bound-frame(location-raycast, self), 0.01, 0, 6145));
                    if (net-is-active?()) {
                        set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                        var_5 = add-molotov-flame(
                            get-bound-frame(location, self),
                            5.00,
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    } else {
                        u64? var_11;
                        if (var_3 >= 0.00) {
                            var_11 = lerp-locator(get-bound-frame(location, self), get-bound-frame(location-raycast, self), var_3);
                        } else {
                            var_11 = get-bound-frame(location-raycast, self);
                        }
                        var_5 = add-molotov-flame(
                            var_11,
                            get-float(flame-time, self),
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    }
                }
                lambda {
                    wait(0.10);
                }
                lambda {
                    send-event(direct-molotov-impact, self);
                }
                lambda {
                    set-bound-frame(location-raycast, offset-locator-world-space(get-bound-frame(location, self), alloc-vector(0.00, -3.00, 0.00, 1.00, 0)), self);
                }
                lambda {
                    wait-one-frame();
                    u64? var_3 = wait-raycast-t(kick-spherecast(get-bound-frame(location, self), get-bound-frame(location-raycast, self), 0.01, 0, 6145));
                    if (net-is-active?()) {
                        set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                        var_5 = add-molotov-flame(
                            get-bound-frame(location, self),
                            5.00,
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    } else {
                        u64? var_11;
                        if (var_3 >= 0.00) {
                            var_11 = lerp-locator(get-bound-frame(location, self), get-bound-frame(location-raycast, self), var_3);
                        } else {
                            var_11 = get-bound-frame(location-raycast, self);
                        }
                        var_5 = add-molotov-flame(
                            var_11,
                            get-float(flame-time, self),
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    }
                }
                lambda {
                    wait(0.10);
                }
                lambda {
                    send-event(direct-molotov-impact, self);
                }
                lambda {
                    set-bound-frame(location-raycast, offset-locator-world-space(get-bound-frame(location, self), alloc-vector(0.00, -3.00, 0.00, 1.00, 0)), self);
                }
                lambda {
                    wait-one-frame();
                    u64? var_3 = wait-raycast-t(kick-spherecast(get-bound-frame(location, self), get-bound-frame(location-raycast, self), 0.01, 0, 6145));
                    if (net-is-active?()) {
                        set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                        var_5 = add-molotov-flame(
                            get-bound-frame(location, self),
                            5.00,
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    } else {
                        u64? var_11;
                        if (var_3 >= 0.00) {
                            var_11 = lerp-locator(get-bound-frame(location, self), get-bound-frame(location-raycast, self), var_3);
                        } else {
                            var_11 = get-bound-frame(location-raycast, self);
                        }
                        var_5 = add-molotov-flame(
                            var_11,
                            get-float(flame-time, self),
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    }
                }
                lambda {
                    set-bound-frame(location-raycast, offset-locator-world-space(get-bound-frame(location, self), alloc-vector(0.00, -3.00, 0.00, 1.00, 0)), self);
                }
                lambda {
                    wait-one-frame();
                    u64? var_3 = wait-raycast-t(kick-spherecast(get-bound-frame(location, self), get-bound-frame(location-raycast, self), 0.01, 0, 6145));
                    u64? var_4;
                    if (var_3 >= 0.00) {
                        var_4 = lerp-locator(get-bound-frame(location, self), get-bound-frame(location-raycast, self), var_3);
                    } else {
                        var_4 = get-bound-frame(location-raycast, self);
                    }
                    add-molotov-flame(
                        var_4,
                        get-float(flame-time, self),
                        get-symbol(attacker-id, self),
                        get-int32(attacker-net-id, self)
                    );
                }
                lambda {
                    set-bound-frame(location-raycast, offset-locator-world-space(get-bound-frame(location, self), alloc-vector(0.00, -3.00, 0.00, 1.00, 0)), self);
                }
                lambda {
                    wait-one-frame();
                    u64? var_3 = wait-raycast-t(kick-spherecast(get-bound-frame(location, self), get-bound-frame(location-raycast, self), 0.01, 0, 6145));
                    if (net-is-active?()) {
                        set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                        var_5 = add-molotov-flame(
                            get-bound-frame(location, self),
                            5.00,
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    } else {
                        u64? var_11;
                        if (var_3 >= 0.00) {
                            var_11 = lerp-locator(get-bound-frame(location, self), get-bound-frame(location-raycast, self), var_3);
                        } else {
                            var_11 = get-bound-frame(location-raycast, self);
                        }
                        var_5 = add-molotov-flame(
                            var_11,
                            get-float(flame-time, self),
                            get-symbol(attacker-id, self),
                            get-int32(attacker-net-id, self)
                        );
                    }
                }
                lambda {
                    set-bound-frame(location-raycast, offset-locator-world-space(get-bound-frame(location, self), alloc-vector(0.00, -3.00, 0.00, 1.00, 0)), self);
                }
                lambda {
                    wait-one-frame();
                    u64? var_3 = wait-raycast-t(kick-spherecast(get-bound-frame(location, self), get-bound-frame(location-raycast, self), 0.01, 0, 6145));
                    u64? var_4;
                    if (var_3 >= 0.00) {
                        var_4 = lerp-locator(get-bound-frame(location, self), get-bound-frame(location-raycast, self), var_3);
                    } else {
                        var_4 = get-bound-frame(location-raycast, self);
                    }
                    add-molotov-flame(
                        var_4,
                        get-float(flame-time, self),
                        get-symbol(attacker-id, self),
                        get-int32(attacker-net-id, self)
                    );
                }
            }
        }
        block event main-timer {
            track main {
                lambda {
                    terminate();
                }
                lambda {
                    terminate();
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail-ceiling/smoke_event_emitter && *(f32*)(event-arg-vector(1) + 4) > 0.80 && event-arg-int32(3) != 28 && event-arg-int32(3) != 27 && event-arg-int32(3) != 38) {
                        set-uint64(smoke-handle, spawn-particles-at-bound-frame(get-symbol(impact-smoke, self), event-arg-bound-frame(0), none), self);
                        set-effect-float(get-uint64(smoke-handle, self), killradius, get-float(kill, self));
                        if (net-is-active?()) {
                            set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        } else {
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        }
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, event-arg-bound-frame(0), self), self);
                    }
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail-ceiling/smoke_event_emitter || event-arg-int32(3) == 28 && event-arg-int32(3) == 27 && event-arg-int32(3) != 63 && event-arg-int32(3) != 38 && *(f32*)(event-arg-vector(1) + 4) > 0.80) {
                        spawn-particles-at-bound-frame(get-symbol(impact-smoke-snow, self), event-arg-bound-frame(0), none);
                        if (net-is-active?()) {
                            set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        } else {
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        }
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, event-arg-bound-frame(0), self), self);
                    }
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail-wall/smoke_event_emitter && *(f32*)(event-arg-vector(1) + 4) > 0.80 && event-arg-int32(3) != 28 && event-arg-int32(3) != 27 && event-arg-int32(3) != 38) {
                        set-uint64(smoke-handle, spawn-particles-at-bound-frame(get-symbol(impact-smoke, self), event-arg-bound-frame(0), none), self);
                        set-effect-float(get-uint64(smoke-handle, self), killradius, get-float(kill, self));
                        if (net-is-active?()) {
                            set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        } else {
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        }
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, event-arg-bound-frame(0), self), self);
                    }
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail-wall/smoke_event_emitter || event-arg-int32(3) == 28 && event-arg-int32(3) == 27 && event-arg-int32(3) != 63 && event-arg-int32(3) != 38 && *(f32*)(event-arg-vector(1) + 4) > 0.80) {
                        spawn-particles-at-bound-frame(get-symbol(impact-smoke-snow, self), event-arg-bound-frame(0), none);
                        if (net-is-active?()) {
                            set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        } else {
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        }
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, event-arg-bound-frame(0), self), self);
                    }
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail-ceiling/smoke_event_emitter && *(f32*)(event-arg-vector(1) + 4) > 0.80 && event-arg-int32(3) != 28 && event-arg-int32(3) != 27 && event-arg-int32(3) != 38) {
                        set-uint64(smoke-handle, spawn-particles-at-bound-frame(get-symbol(impact-smoke, self), event-arg-bound-frame(0), none), self);
                        set-effect-float(get-uint64(smoke-handle, self), killradius, get-float(kill, self));
                        if (net-is-active?()) {
                            set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        } else {
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        }
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, event-arg-bound-frame(0), self), self);
                    }
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail-ceiling/smoke_event_emitter || event-arg-int32(3) == 28 && event-arg-int32(3) == 27 && event-arg-int32(3) != 63 && event-arg-int32(3) != 38 && *(f32*)(event-arg-vector(1) + 4) > 0.80) {
                        spawn-particles-at-bound-frame(get-symbol(impact-smoke-snow, self), event-arg-bound-frame(0), none);
                        if (net-is-active?()) {
                            set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        } else {
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        }
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, event-arg-bound-frame(0), self), self);
                    }
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail-wall/smoke_event_emitter && *(f32*)(event-arg-vector(1) + 4) > 0.80 && event-arg-int32(3) != 28 && event-arg-int32(3) != 27 && event-arg-int32(3) != 38) {
                        set-uint64(smoke-handle, spawn-particles-at-bound-frame(get-symbol(impact-smoke, self), event-arg-bound-frame(0), none), self);
                        set-effect-float(get-uint64(smoke-handle, self), killradius, get-float(kill, self));
                        if (net-is-active?()) {
                            set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        } else {
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        }
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, event-arg-bound-frame(0), self), self);
                    }
                }
                lambda {
                    if (event-arg-symbol(4) == molotov-cocktail-wall/smoke_event_emitter || event-arg-int32(3) == 28 && event-arg-int32(3) == 27 && event-arg-int32(3) != 63 && event-arg-int32(3) != 38 && *(f32*)(event-arg-vector(1) + 4) > 0.80) {
                        spawn-particles-at-bound-frame(get-symbol(impact-smoke-snow, self), event-arg-bound-frame(0), none);
                        if (net-is-active?()) {
                            set-effect-float(get-uint64(molotov-handle, self), fireLifeMult, 1.00);
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        } else {
                            add-molotov-flame(
                                event-arg-bound-frame(0),
                                get-float(flame-time, self),
                                get-symbol(attacker-id, self),
                                get-int32(attacker-net-id, self)
                            );
                        }
                        set-uint64(molotov-handle, spawn-particles-at-bound-frame(molotov-cocktail, event-arg-bound-frame(0), self), self);
                    }
                }
            }
        }
        block event main-timer {
            track main {
                lambda {
                    terminate();
                }
                lambda {
                    terminate();
                }
                lambda {
                    terminate();
                }
                lambda {
                    terminate();
                }
            }
        }
    }
}
