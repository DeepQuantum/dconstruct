// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-patrol\ss-flashlight-react-dina.bin                    #
// #   filesize: 9462 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 18                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        dina
    }
    declarations {
        bool flashlight-facing? = nullptr
        bool dina-facing? = nullptr
        bool distance? = nullptr
        bool ok-to-react? = true
    }
    state idle {
        block update {
            track main {
                lambda {
                    if (player-flashlight-on?()) {
                        set-boolean(flashlight-facing?, locator-in-player-flashlight?(get-joint-locator(dina, headb), -20.00, 0), self);
                        set-boolean(dina-facing?, facing-point-xz?(get-object-locator(dina), get-object-locator(player), 120.00), self);
                        set-boolean(distance?, get-locator-distance(get-object-locator(dina), get-object-locator(player)) <= 6.00, self);
                        if (!(npc-get-entered-ap-type(dina) == traversal-action-pack) && !(npc-get-entered-ap-type(dina) == cinematic-action-pack) && tension-mode-is?(1) && !npc-is-in-scripted-anim?(dina) && get-boolean(ok-to-react?, self) && get-locator-distance(get-object-locator(dina), get-object-locator(player)) <= 6.00 && locator-in-player-flashlight?(get-joint-locator(dina, headb), -20.00, 0) && facing-point-xz?(get-object-locator(dina), get-object-locator(player), 120.00) && gesture-idles-current-idle(dina) == 0) {
                            go(shining, 1);
                        }
                    }
                }
            }
        }
    }
    state shining {
        block start {
            track main {
                lambda {
                    wait(0.40);
                }
                lambda {
                    go(flashlight-react, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-locator-distance(get-object-locator(dina), get-object-locator(player)) <= 6.00 && locator-in-player-flashlight?(get-joint-locator(dina, headb), -20.00, 0)) {
                        if (!facing-point-xz?(get-object-locator(dina), get-object-locator(player), 120.00)) {
                            go(idle, 1);
                        }
                    }
                }
            }
        }
    }
    state flashlight-react {
        block start {
            track main {
                lambda {
                    fact-set-bool-in-dict(fact-get-dictionary(dina), blinded?, 1);
                }
                lambda {
                    broadcast-event(buddy-flashlight-react-start, boxed_symbol(dina));
                }
                lambda {
                    u64? var_0 = alloc-gesture-play-params();
                    *(u8*)(var_0 + 33) = 1;
                    *(u8*)(var_0 + 32) = 1;
                    gesture_(dina, dina-ambi-flashlight-react-intro-gesture, var_0);
                }
                lambda {
                    go(flashlight-in-eyes, 1);
                }
            }
        }
    }
    state flashlight-in-eyes {
        block start {
            track main {
                lambda {
                    gesture_(dina, dina-ambi-flashlight-react-loop-gesture, alloc-gesture-play-params());
                }
            }
        }
        block update {
            track main {
                lambda {
                    set-boolean(flashlight-facing?, locator-in-player-flashlight?(get-joint-locator(dina, headb), -20.00, 0), self);
                }
                lambda {
                    set-boolean(dina-facing?, facing-point-xz?(get-object-locator(dina), get-object-locator(player), 120.00), self);
                }
                lambda {
                    set-boolean(distance?, get-locator-distance(get-object-locator(dina), get-object-locator(player)) <= 6.00, self);
                }
                lambda {
                    if (!(npc-get-entered-ap-type(dina) == traversal-action-pack) && !(npc-get-entered-ap-type(dina) == cinematic-action-pack) && tension-mode-is?(1) && !npc-is-in-scripted-anim?(dina) && get-boolean(ok-to-react?, self) && player-flashlight-on?()) {
                        if (!(gesture-idles-current-idle(dina) == 0)) {
                            go(flashlight-clear, 1);
                        }
                    }
                }
                lambda {
                    if (get-state-time(self) > 1.00) {
                        if (get-locator-distance(get-object-locator(dina), get-object-locator(player)) <= 6.00 && locator-in-player-flashlight?(get-joint-locator(dina, headb), -20.00, 0)) {
                            if (!facing-point-xz?(get-object-locator(dina), get-object-locator(player), 120.00)) {
                                go(flashlight-clear, 1);
                            }
                        }
                    }
                }
            }
        }
        block end {
            track main {
                lambda {
                    fact-set-bool-in-dict(fact-get-dictionary(dina), blinded?, 0);
                }
            }
        }
    }
    state flashlight-clear {
        block start {
            track main {
                lambda {
                    u64? var_0 = alloc-gesture-play-params();
                    *(u8*)(var_0 + 33) = 1;
                    *(u8*)(var_0 + 32) = 1;
                    gesture_(dina, dina-ambi-flashlight-react-outro-gesture, var_0);
                }
                lambda {
                    broadcast-event(buddy-flashlight-react-end, boxed_symbol(dina));
                }
                lambda {
                    go(idle, 1);
                }
            }
        }
    }
}
