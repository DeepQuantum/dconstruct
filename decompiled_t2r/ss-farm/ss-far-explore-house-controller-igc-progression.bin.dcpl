// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-farm\ss-far-explore-house-controller-igc-progression.bin#
// #   filesize: 13906 bytes                                                                          #
// #   total entries: 1                                                                               #
// #   functions: 36                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        dina
        far-farmhouse-script
    }
    declarations {
        bool record-played? = nullptr
        bool kitchen-dance-done? = nullptr
        bool toy-picked-up? = nullptr
        bool tractor-igc-done? = nullptr
        bool in-house? = nullptr
        bool music-done? = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(farm, far-explore-house)) {
                        set-boolean(record-played?, 1, self);
                        set-boolean(kitchen-dance-done?, 1, self);
                        set-boolean(toy-picked-up?, 1, self);
                        set-boolean(tractor-igc-done?, 1, self);
                        go(complete, 1);
                    }
                }
                lambda {
                    if (task-continue-active?(farm, far-explore-house, default)) {
                        set-svar-as-boolean(far-danced-in-kitchen, 0);
                        set-svar-as-boolean(far-found-tractor-toy, 0);
                        set-svar-as-boolean(far-played-record, 0);
                        go(wait-all-complete, 1);
                    }
                }
                lambda {
                    if (task-continue-active?(farm, far-explore-house, DEBUG--dina-greeted)) {
                        set-svar-as-boolean(far-danced-in-kitchen, 0);
                        set-svar-as-boolean(far-played-record, 0);
                        go(wait-all-complete, 1);
                    }
                }
                lambda {
                    if (task-active?(farm, far-explore-intro-exterior-igc)) {
                        set-svar-as-boolean(far-danced-in-kitchen, 0);
                        set-svar-as-boolean(far-found-tractor-toy, 0);
                        set-svar-as-boolean(far-played-record, 0);
                    }
                }
                lambda {
                    set-boolean(kitchen-dance-done?, get-svar-as-boolean(far-danced-in-kitchen), self);
                }
                lambda {
                    set-boolean(toy-picked-up?, get-svar-as-boolean(far-found-tractor-toy), self);
                }
                lambda {
                    set-boolean(tractor-igc-done?, get-svar-as-boolean(far-found-tractor-toy), self);
                }
                lambda {
                    set-boolean(record-played?, get-svar-as-boolean(far-played-record), self);
                }
                lambda {
                    if (get-boolean(record-played?, self) && !get-boolean(tractor-igc-done?, self) && !get-boolean(kitchen-dance-done?, self)) {
                        task-set-active-continue!(record-played, 0, 0, 0, 0.00, 0, 0);
                    } else if (get-boolean(record-played?, self) && !get-boolean(tractor-igc-done?, self) && get-boolean(kitchen-dance-done?, self)) {
                        task-set-active-continue!(kitchen-dance-done, 0, 0, 0, 0.00, 0, 0);
                    } else if (get-boolean(tractor-igc-done?, self) && !get-boolean(record-played?, self)) {
                        task-set-active-continue!(tractor-done, 0, 0, 0, 0.00, 0, 0);
                    } else if (get-boolean(record-played?, self) && get-boolean(tractor-igc-done?, self) && !get-boolean(kitchen-dance-done?, self)) {
                        task-set-active-continue!(tractor-done, 0, 0, 0, 0.00, 0, 0);
                    }
                }
                lambda {
                    if (task-continue-active?(farm, far-explore-house, record-played)) {
                        set-boolean(record-played?, 1, self);
                        set-svar-as-boolean(far-played-record, 1);
                        go(wait-all-complete, 1);
                    }
                }
                lambda {
                    if (task-continue-active?(farm, far-explore-house, kitchen-dance-done)) {
                        set-boolean(record-played?, 1, self);
                        set-boolean(kitchen-dance-done?, 1, self);
                        set-svar-as-boolean(far-danced-in-kitchen, 1);
                        set-svar-as-boolean(far-played-record, 1);
                        go(wait-all-complete, 1);
                    }
                }
                lambda {
                    if (task-continue-active?(farm, far-explore-house, tractor-igc)) {
                        set-boolean(toy-picked-up?, 1, self);
                        set-svar-as-boolean(far-found-tractor-toy, 1);
                        go(wait-all-complete, 1);
                    }
                }
                lambda {
                    if (task-continue-active?(farm, far-explore-house, tractor-done)) {
                        set-boolean(toy-picked-up?, 1, self);
                        set-boolean(tractor-igc-done?, 1, self);
                        set-svar-as-boolean(far-found-tractor-toy, 1);
                        go(wait-all-complete, 1);
                    }
                }
                lambda {
                    if (task-active?(farm, far-explore-house)) {
                        go(wait-all-complete, 1);
                    }
                }
            }
        }
        block event task-complete {
            track main {
                lambda {
                    if (task-active?(farm, far-explore-intro-exterior-igc)) {
                        set-svar-as-boolean(far-danced-in-kitchen, 0);
                        set-svar-as-boolean(far-found-tractor-toy, 0);
                        set-svar-as-boolean(far-played-record, 0);
                        set-boolean(kitchen-dance-done?, get-svar-as-boolean(far-danced-in-kitchen), self);
                        set-boolean(toy-picked-up?, get-svar-as-boolean(far-found-tractor-toy), self);
                        set-boolean(tractor-igc-done?, get-svar-as-boolean(far-found-tractor-toy), self);
                        set-boolean(record-played?, get-svar-as-boolean(far-played-record), self);
                    }
                }
                lambda {
                    if (task-active?(farm, far-explore-house)) {
                        go(wait-all-complete, 1);
                    }
                }
            }
        }
    }
    state --script-- {
        block start {
            track ollie {
                lambda {
                    wait-boolean(toy-picked-up?, 1, self);
                }
                lambda {
                    set-mesh-visible(player, ellie-farm-explore-toys, 1);
                }
            }
        }
    }
    state wait-all-complete {
        block start {
            track prog {
                lambda {
                    while (get-boolean(record-played?, self) && get-boolean(kitchen-dance-done?, self) && get-boolean(toy-picked-up?, self) || get-boolean(tractor-igc-done?, self) && !get-boolean(music-done?, self)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    while (distance-between-objects-greater-than?(dina, player, 5.50, 1) && !distance-between-objects-greater-than?(obj-far-explore-laundry-basket-1, player, 10.00, 1)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    go(task-complete, 1);
                }
            }
            track door-interact-prog {
                lambda {
                    wait-boolean(kitchen-dance-done?, 1, self);
                }
                lambda {
                    interactable-set-activation-region(far-front-screen-door-1, rgn-far-porch-door-activation-2);
                }
            }
            track accessibility-1 {
                lambda {
                    while (!is-navmesh-loaded?(far-farm-forest)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    set-nav-mesh-enable-by-level(far-farm-forest, 0);
                }
            }
            track accessibility-2 {
                lambda {
                    while (!is-navmesh-loaded?(far-abandoned-house-stair-slice)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    set-nav-mesh-enable-by-level(far-abandoned-house-stair-slice, 0);
                }
            }
            track accessibility-3 {
                lambda {
                    while (!is-navmesh-loaded?(far-farm)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    set-nav-mesh-enable-by-level(far-farm, 0);
                }
            }
        }
    }
    state task-complete {
        block start {
            track main {
                lambda {
                    set-nav-mesh-enable-by-level(far-farm, 1);
                }
                lambda {
                    set-nav-mesh-enable-by-level(far-farm-forest, 1);
                }
                lambda {
                    set-nav-mesh-enable-by-level(far-abandoned-house-stair-slice, 1);
                }
                lambda {
                    while (!(get-black-fade() <= 0.00)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    while (player-in-region?(rgn-vox-far-in-house-1, 2) && !distance-between-objects-less-than?(dina, player, 8.00)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    task-complete!(farm, far-explore-house);
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
    }
}
