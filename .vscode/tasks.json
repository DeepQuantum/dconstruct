{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build . --target dconstruct --parallel --config Debug",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": {
                "kind": "build"
            },
            "problemMatcher": [
                "$msCompile"
            ],
        }
    ]
}