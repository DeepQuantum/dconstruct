; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-santa-barbara\ss-san-beach-fight-abby-gesture-controller.bin#
; #   filesize: 10315 bytes                                                                          #
; #   total entries: 5                                                                               #
; #   functions: 17                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        symbol abby = npc-san-spikes-abby-1
        f32 gesture-timer = 0.00
        f32 gesture-cooldown = 0.00
        symbol queued-gesture = #0000000000000000
        symbol current-gesture = #0000000000000000
    }
    state --all-- {
        block event run-gestures {
            track main {
                lambda 
                    (if (and (not (== (get-script-state ss-san-beach-fight-abby-gesture-controller-1) waiting-on-cooldown)) (not (== (get-script-state ss-san-beach-fight-abby-gesture-controller-1) running-gestures)))) 
                        (go waiting-on-cooldown 1)
                    
                
            }
        }
        block event stop-gestures {
            track main {
                lambda 
                    (go clean-up 1)
                
            }
        }
        block event queue-gesture {
            track main {
                lambda 
                    (set-symbol queued-gesture (event-arg-symbol 0) self)
                
            }
        }
    }
    state clean-up {
        block start {
            track main {
                lambda 
                    (stop-timer gesture-timer self)
                
                lambda 
                    (set-float gesture-cooldown 0.00 self)
                
                lambda 
                    (set-symbol current-gesture 0 self)
                
                lambda 
                    (go sleeping 1)
                
            }
        }
    }
    state  {
    }
    state waiting-on-cooldown {
        block start {
            track main {
                lambda 
                    (set-float gesture-timer 0.00 self)
                
                lambda 
                    (if (npc-enemy-abby-phase-1? (get-symbol abby self))) 
                        (set-float gesture-cooldown (random-float-range 3.00 4.50 0) self)
                     else (if (npc-enemy-abby-phase-2? (get-symbol abby self))) 
                        (set-float gesture-cooldown (random-float-range 3.00 4.50 0) self)
                     else (if (npc-enemy-abby-phase-3? (get-symbol abby self))) 
                        (set-float gesture-cooldown (random-float-range 3.00 4.50 0) self)
                    
                
                lambda 
                    (define var_3 (get-object-distance (get-symbol abby self) player))
                    (new-boxed-value 7 true)
                    (define var_9 (new-boxed-value 7 false))
                    (do-melee-log "characters-in-range? [[f]] [[f]] [[f]] [[s]]" (new-boxed-value 6 var_3) (new-boxed-value 6 2.25) (new-boxed-value 6 999.00) var_9)
                    (let loop () (when (and (and (or (or (and (or (and (and (and (not (get-current-melee-attack (get-symbol abby self))) (not (== (get-symbol abby self) 0))) (not (== player 0))) (< var_3 999.00)) (> var_3 2.25)) (> var_3 2.25)) var_3) var_9) (> var_3 2.25)) (not (> (get-float gesture-timer self) (get-float gesture-cooldown self)))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go running-gestures 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (get-current-melee-attack (get-symbol abby self)))) 
                        (define var_3 (get-object-distance (get-symbol abby self) player))
                        (define var_5 (&& (&& (&& (not (== (get-symbol abby self) 0)) (not (== player 0))) (< var_3 999.00)) (> var_3 2.25)))
                        (define var_9)
                        (if var_5) 
                            (set! var_9 (new-boxed-value 7 true))
                         else 
                            (set! var_9 (new-boxed-value 7 false))
                        
                        (do-melee-log "characters-in-range? [[f]] [[f]] [[f]] [[s]]" (new-boxed-value 6 var_3) (new-boxed-value 6 2.25) (new-boxed-value 6 999.00) var_9)
                        (if var_5) 
                            (set-float gesture-timer (+ (get-float gesture-timer self) (get-delta-time)) self)
                        
                    
                
            }
        }
    }
    state running-gestures {
        block start {
            track main {
                lambda 
                    (if (not (== (get-symbol queued-gesture self) 0))) 
                        (set-symbol current-gesture (get-symbol queued-gesture self) self)
                        (gesture_ (get-symbol abby self) (get-symbol current-gesture self) (alloc-gesture-play-params))
                        (set-symbol queued-gesture 0 self)
                     else 
                        (define var_7)
                        (if (npc-enemy-abby-phase-1? (get-symbol abby self))) 
                            (set! var_7 abby-gestures-fight-phase-1)
                         else (if (npc-enemy-abby-phase-2? (get-symbol abby self))) 
                            (set! var_7 abby-gestures-fight-phase-2)
                         else (if (npc-enemy-abby-phase-3? (get-symbol abby self))) 
                            (set! var_7 abby-gestures-fight-phase-3)
                         else (if (npc-enemy-abby-phase-gassed? (get-symbol abby self))) 
                            (set! var_7 abby-gestures-fight-phase-gassed)
                         else 
                            (set! var_7 0)
                        
                        (set-symbol current-gesture (group-random var_7) self)
                        (gesture_ (get-symbol abby self) (get-symbol current-gesture self) (alloc-gesture-play-params))
                    
                
                lambda 
                    (let loop () (when (and (not (not (== (get-symbol current-gesture self) 0))) (not (not (is-gesture-playing? (get-symbol abby self) (get-symbol current-gesture self) -1)))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (wait (random-float-range 1.00 1.50 0))
                
                lambda 
                    (set-symbol current-gesture 0 self)
                
                lambda 
                    (go waiting-on-cooldown 1)
                
            }
        }
    }
}
