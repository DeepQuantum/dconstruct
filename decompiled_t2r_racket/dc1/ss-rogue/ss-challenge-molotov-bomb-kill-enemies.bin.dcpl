; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-rogue\ss-challenge-molotov-bomb-kill-enemies.bin     #
; #   filesize: 3978 bytes                                                                           #
; #   total entries: 2                                                                               #
; #   functions: 6                                                                                   #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (update-challenge-progress-str-with-current-kill-count) 
    (set-string challenge-progress-str (dc:format "%d/%d" (get-int32 current-kill-count self) (tag-int32 num-kills-needed self)) self)
    (hud-challenge-update-progress-count (get-int32 current-kill-count self) (tag-int32 num-kills-needed self))
)

statescript {
    options {
        player
    }
    declarations {
        i32 current-kill-count = 0
        string challenge-progress-str = 
    }
    state tracking {
        block start {
            track main {
                lambda 
                    (update-challenge-progress-str-with-current-kill-count)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (>= (get-int32 current-kill-count self) (tag-int32 num-kills-needed self))) 
                        (go completed 1)
                    
                
            }
        }
        block event npc-died {
            track main {
                lambda 
                    (define var_0 (event-arg-symbol 0))
                    (define var_3 (lookup-weapon-gameplay-def (event-arg-symbol 2) 0))
                    (if (and (or (and (and (== player (event-arg-symbol 1)) (not (npc-is-buddy? var_0))) (deref (+ var_3 40))) (== (deref (cast u32* (+ (deref (+ var_3 40)) 60))) 8)) (== (deref (cast u32* (+ (deref (+ var_3 40)) 60))) 4))) 
                        (++ current-kill-count self)
                        (update-challenge-progress-str-with-current-kill-count)
                        (if (not (is-final-build?))) 
                            (set! var_5 (display (dc:format "DEB-EC: Player just killed %s with grenade!" (string-debug var_0)) 19))
                        
                    
                
            }
        }
    }
    state completed {
        block start {
            track main {
                lambda 
                    (send-event challenge-complete ss-challenges-controller-1)
                
                lambda 
                    (if (not (is-final-build?))) 
                        (display (dc:format "DEB-EC: Challenge complete %s" (string-debug (get-self-id))) 19)
                    
                
            }
        }
    }
}
