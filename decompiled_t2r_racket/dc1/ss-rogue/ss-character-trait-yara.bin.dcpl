; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-rogue\ss-character-trait-yara.bin                    #
; #   filesize: 6244 bytes                                                                           #
; #   total entries: 3                                                                               #
; #   functions: 7                                                                                   #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (buddy-kills-active?) 
    (define var_3)
    (if (and (player-upgrade-active? buddy-kills) (! (player-upgrade-active? buddy-kills-plus)))) 
        (if (not (is-final-build?))) 
            (display (dc:format "YARA DEBUG: buddy kills upgrade active") 19)
        
        (set! var_3 1)
     else 
        (if (not (is-final-build?))) 
            (display (dc:format "YARA DEBUG: buddy kills upgrade NOT active") 19)
        
        (set! var_3 0)
    
    var_3
)

(define (buddy-kills-plus-active?) 
    (define var_2)
    (if (and (player-upgrade-active? buddy-kills) (player-upgrade-active? buddy-kills-plus))) 
        (if (not (is-final-build?))) 
            (display (dc:format "YARA DEBUG: buddy kills plus upgrade active") 19)
        
        (set! var_2 1)
     else 
        (if (not (is-final-build?))) 
            (display (dc:format "YARA DEBUG: buddy kills plus upgrade NOT active") 19)
        
        (set! var_2 0)
    
    var_2
)

statescript {
    options {
        player
    }
    declarations {
    }
    state tracking {
        block start {
            track main {
                lambda 
                    (define var_0 (get-difficulty-setting-allies))
                    (define var_2)
                    (define var_3)
                    (if (match (list var_0)
                        [0 000001F27E5AD000]
                        [1 000001F27E5AD000]
                        [_ 0]
                    )) 
                        (if (not (is-final-build?))) 
                            (display (dc:format "YARA DEBUG: ally difficulty setting = easy or ultra-easy") 19)
                        
                        (if (buddy-kills-active?)) 
                            (set! var_2 1.00)
                         else (if (buddy-kills-plus-active?)) 
                            (set! var_2 0.60)
                         else 
                            (set! var_2 1.40)
                        
                        (set! var_3 1)
                     else (if (== var_0 2)) 
                        (if (not (is-final-build?))) 
                            (display (dc:format "YARA DEBUG: ally difficulty setting = normal") 19)
                        
                        (if (buddy-kills-active?)) 
                            (set! var_2 1.40)
                         else (if (buddy-kills-plus-active?)) 
                            (set! var_2 1.00)
                         else 
                            (set! var_2 1.80)
                        
                        (set! var_3 1)
                     else (if (== var_0 3)) 
                        (if (not (is-final-build?))) 
                            (display (dc:format "YARA DEBUG: ally difficulty setting = hard") 19)
                        
                        (if (buddy-kills-active?)) 
                            (set! var_2 2.00)
                         else (if (buddy-kills-plus-active?)) 
                            (set! var_2 1.50)
                         else 
                            (set! var_2 2.50)
                        
                        (set! var_3 1)
                     else (if (== var_0 4)) 
                        (if (not (is-final-build?))) 
                            (display (dc:format "YARA DEBUG: ally difficulty setting = ultra-hard") 19)
                        
                        (if (buddy-kills-active?)) 
                            (set! var_2 3.00)
                         else (if (buddy-kills-plus-active?)) 
                            (set! var_2 2.00)
                         else 
                            (set! var_2 4.00)
                        
                        (set! var_3 1)
                     else (if (== var_0 5)) 
                        (if (not (is-final-build?))) 
                            (display (dc:format "YARA DEBUG: ally difficulty setting = grounded") 19)
                        
                        (if (buddy-kills-active?)) 
                            (set! var_2 4.00)
                         else (if (buddy-kills-plus-active?)) 
                            (set! var_2 3.00)
                         else 
                            (set! var_2 5.00)
                        
                        (set! var_3 1)
                     else (if (not (is-final-build?))) 
                        (display (dc:format "YARA DEBUG ERROR: ally difficulty setting not found, override disabled") 19)
                    
                    (if var_3) 
                        (if (not (is-final-build?))) 
                            (display (dc:format "YARA DEBUG: Set player-to-buddy-kill-ratio %f" var_2) 19)
                        
                        (set! var_26 (override-player-to-buddy-kill-ratio var_2))
                    
                
            }
        }
        block event run-complete {
            track main {
                lambda 
                    (if (not (get-svar-as-boolean rogue-cheat-mod-ever-enabled))) 
                        (increment-stat runs-complete-yara 1)
                        (increment-stat meta-runs-complete 1)
                        (set-stat-bit trophy-win-with-all-characters 6)
                    
                
            }
        }
        block event refresh-buddy-kill-ratio {
            track main {
                lambda 
                    (go reset 1)
                
            }
        }
    }
    state reset {
        block start {
            track main {
                lambda 
                    (if (not (is-final-build?))) 
                        (display (dc:format "YARA DEBUG: buddy kill upgrade unlocked, resetting buddy kill ratio") 19)
                    
                
                lambda 
                    (go tracking 1)
                
            }
        }
    }
}
