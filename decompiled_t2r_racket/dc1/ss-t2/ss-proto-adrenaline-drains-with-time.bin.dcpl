; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-t2\ss-proto-adrenaline-drains-with-time.bin          #
; #   filesize: 5724 bytes                                                                           #
; #   total entries: 2                                                                               #
; #   functions: 13                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (proto-reduce-adrenaline-by arg_0) 
    (set-float adrenaline (- (character-get-adrenaline player) arg_0) self)
    (clamp-var adrenaline 0.00 100.00 self)
    (character-set-adrenaline player (get-float adrenaline self))
)

statescript {
    options {
        player
    }
    declarations {
        f32 adrenaline = 0.00
        f32 health = 0.00
        timer attacked-timer = nullptr
        timer pause-timer = nullptr
        f32 focus-inc = 2.00
    }
    state inactive {
        block update {
            track main {
                lambda 
                    (if (deref (cast u8* (+ *deprecated-prototypes* 2)))) 
                        (set-float adrenaline (character-get-adrenaline player) self)
                        (start-timer pause-timer 2.00 self 1)
                        (go adjust-adrenaline 1)
                    
                
            }
        }
    }
    state adjust-adrenaline {
        block update {
            track main {
                lambda 
                    (if (not (deref (cast u8* (+ *deprecated-prototypes* 2))))) 
                        (go inactive 1)
                    
                
                lambda 
                    (set-float adrenaline (character-get-adrenaline player) self)
                
                lambda 
                    (set-float health (cast f32 (get-player-health)) self)
                
                lambda 
                    (define var_1)
                    (if (> (get-player-health) 70)) 
                        (set! var_1 2.00)
                     else (if (> (get-player-health) 20)) 
                        (set! var_1 1.00)
                     else 
                        (set! var_1 0.00)
                    
                    (set-float focus-inc var_1 self)
                
                lambda 
                    (if (and (tension-mode-is? 3) (distance-between-objects-less-than? player (get-closest-enemy-to-player 9999.00 0 15) 5.00))) 
                        (set! var_3 (start-timer pause-timer 3.00 self 1))
                    
                
                lambda 
                    (if (not (timer-running? pause-timer self))) 
                        (proto-reduce-adrenaline-by (* (get-delta-time) (get-float focus-inc self)))
                    
                
            }
        }
        block event player-attacked {
            track main {
                lambda 
                    (if (not (timer-running? attacked-timer self))) 
                        (proto-reduce-adrenaline-by 30.00)
                        (start-timer attacked-timer (frames-to-sec 15) self 1)
                    
                
                lambda 
                    (start-timer pause-timer 3.00 self 1)
                
            }
        }
        block event player-melee-move-started {
            track main {
                lambda 
                    (start-timer pause-timer 3.00 self 1)
                
            }
        }
        block event suspend-adrenaline-prototypes {
            track main {
                lambda 
                    (go suspended 1)
                
            }
        }
    }
    state suspended {
        block event unsuspend-adrenaline-prototypes {
            track main {
                lambda 
                    (go inactive 1)
                
            }
        }
    }
}
