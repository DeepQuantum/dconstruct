; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss\ss-region-utilities.bin                              #
; #   filesize: 8288 bytes                                                                           #
; #   total entries: 1                                                                               #
; #   functions: 15                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    declarations {
        i32 action-count = 1
        bool loop-started = nullptr
        bool should-disable = nullptr
    }
    state initial {
        block start {
            track main {
                lambda 
                    (if (! (tag-specified? start-disabled self))) 
                        (go event-loop 1)
                    
                
                lambda 
                    (print-string (dc:format "REGION: %s  -STARTING DISABLED!!!!!!!! " (string-debug (get-object-id self))) self)
                
                lambda 
                    (go disabled 1)
                
            }
        }
    }
    state disabled {
        block start {
            track main {
                lambda 
                    (print-string (dc:format "REGION: %s  -IS DISABLED!!!!!!!! " (string-debug (get-object-id self))) self)
                
            }
        }
        block event enable {
            track main {
                lambda 
                    (print-string (dc:format "REGION: %s  -GOING TO ENABLE REGION!!!!!!!! " (string-debug (get-object-id self))) self)
                
                lambda 
                    (go event-loop 1)
                
            }
        }
    }
    state event-loop {
        block start {
            track main {
                lambda 
                    (print-string (dc:format "REGION: %s  -REGION ENABLED!!!!!!!! " (string-debug (get-object-id self))) self)
                
            }
        }
        block event disable {
            track main {
                lambda 
                    (print-string (dc:format "REGION: %s  -GOING TO DISABLE REGION!!!!!!!! " (string-debug (get-object-id self))) self)
                
                lambda 
                    (if (! (get-boolean loop-started self))) 
                        (go disabled 1)
                     else 
                        (set-boolean should-disable 1 self)
                    
                
            }
        }
        block event enter {
            track main {
                lambda 
                    (print-string (dc:format "REGION: %s  -EVENT LOOP!!!!!!!! " (string-debug (get-object-id self))) self)
                
                lambda 
                    (let loop () (when 1 
                        (set-boolean loop-started 1 self)
                        (if (tag-exists? (symbol-concat-int32 event (get-int32 action-count self)) self)) 
                            (send-event (tag-symbol (symbol-concat-int32 event (get-int32 action-count self)) self) (tag-symbol (string-to-symbol (dc:format "event%d-target" (get-int32 action-count self))) self))
                            (print-string (dc:format "EVENT: %d - %s - SENT TO %s !!!!!!!! " (get-int32 action-count self) (string-debug (tag-symbol (symbol-concat-int32 event (get-int32 action-count self)) self)) (string-debug (tag-symbol (string-to-symbol (dc:format "event%d-target" (get-int32 action-count self))) self))) self)
                         else 
                            (print-string (dc:format "REGION EVENT LOOP: %s  -DONE!!!!!!!! " (string-debug (get-object-id self))) self)
                            (set-boolean loop-started 0 self)
                            (if (get-boolean should-disable self)) 
                                (set-boolean should-disable 0 self)
                                (go disabled 1)
                            
                            (go done 1)
                        
                        (++ action-count self)
                     (loop)))
                
            }
        }
        block end {
            track main {
                lambda 
                    (print-string (dc:format "REGION EVENT LOOP: %s  -LEAVING ACTION LOOP!!!!!!!! " (string-debug (get-object-id self))) self)
                
            }
        }
    }
    state done {
        block start {
            track main {
                lambda 
                    (print-string (dc:format "ss region send event once done for REGION: %s  !!! " (string-debug (get-object-id self))) self)
                
            }
        }
        block event enable {
            track main {
                lambda 
                    (print-string (dc:format "REGION: %s  -RESETTING TO ENABLE REGION!!!!!!!! " (string-debug (get-object-id self))) self)
                
                lambda 
                    (go event-loop 1)
                
            }
        }
    }
}
