; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-test\ss-test-dmattson\ss-proto-half-pull-low-cover-peek.bin#
; #   filesize: 4116 bytes                                                                           #
; #   total entries: 1                                                                               #
; #   functions: 11                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        f32 t-position = 0.00
    }
    state initial {
        block start {
            track main {
                lambda 
                    (if (is-bond-enabled?)) 
                        (go waiting 1)
                    
                
                lambda 
                    (go bond-disabled 1)
                
            }
        }
    }
    state waiting {
        block update {
            track main {
                lambda 
                    (set-float t-position (get-joypad-analog-l2) self)
                
                lambda 
                    (if (player-is-crouched?)) 
                        (if (! (&& (|| (player-in-state? GunMove) (player-in-state? BowMove)) 0))) 
                            (go waiting-for-peek 1)
                        
                    
                
            }
        }
    }
    state waiting-for-peek {
        block update {
            track main {
                lambda 
                    (set-float t-position (get-joypad-analog-l2) self)
                
                lambda 
                    (if (player-is-crouched?)) 
                        (if (not (! (&& (|| (player-in-state? GunMove) (player-in-state? BowMove)) 0)))) 
                            (go waiting 1)
                        
                    
                
                lambda 
                    (camera-set-overlay *camera-remap-proto-peek-crouched* 1 2.00)
                
                lambda 
                    (joypad-disable-commands aim aim-press aim-hold aim-hold-long)
                
                lambda 
                    (if (> (get-joypad-analog-l2) 0.01)) 
                        (go peek 1)
                    
                
            }
        }
    }
    state peek {
        block update {
            track main {
                lambda 
                    (set-float t-position (get-joypad-analog-l2) self)
                
                lambda 
                    (define var_0 (get-joypad-analog-l2))
                    (if (< var_0 0.50)) 
                        (camera-set-overlay *camera-remap-proto-peek-crouched-peeking* 1 2.00)
                        (joypad-disable-commands aim aim-press aim-hold aim-hold-long)
                     else 
                        (go waiting 1)
                    
                    (if (< var_0 0.01)) 
                        (set! var_1 (go waiting-for-peek 1))
                    
                
            }
        }
    }
}
