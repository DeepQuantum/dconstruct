; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-test\ss-test-pellis\ss-test-pellis-traps-tripwire-wire-room.bin#
; #   filesize: 16884 bytes                                                                          #
; #   total entries: 1                                                                               #
; #   functions: 47                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        i32 raycast = -1
        i32 raycast-2 = -1
        i32 raycast-3 = -1
        i32 raycast-4 = -1
        i32 raycast-5 = -1
        i32 raycast-6 = -1
        i32 raycast-7 = -1
        i32 raycast-8 = -1
        i32 raycast-9 = -1
        i32 raycast-10 = -1
        i32 raycast-11 = -1
    }
    state raycast-full-room {
        block event player-requested-interact {
            track main {
                lambda 
                    (go dismantle-full-room 1)
                
            }
        }
        block event Attack {
            track main {
                lambda 
                    (go trigger-full-room-trap 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (< (get-int32 raycast self) 0))) 
                        (if (not (wait-raycast-hit-nothing? (get-int32 raycast self)))) 
                            (go trigger-full-room-trap 1)
                        
                    
                
                lambda 
                    (if (not (< (get-int32 raycast-2 self) 0))) 
                        (if (not (wait-raycast-hit-nothing? (get-int32 raycast-2 self)))) 
                            (go trigger-full-room-trap 1)
                        
                    
                
                lambda 
                    (if (not (< (get-int32 raycast-3 self) 0))) 
                        (if (not (wait-raycast-hit-nothing? (get-int32 raycast-3 self)))) 
                            (go trigger-full-room-trap 1)
                        
                    
                
                lambda 
                    (if (not (< (get-int32 raycast-4 self) 0))) 
                        (if (not (wait-raycast-hit-nothing? (get-int32 raycast-4 self)))) 
                            (go trigger-full-room-trap 1)
                        
                    
                
                lambda 
                    (if (not (< (get-int32 raycast-5 self) 0))) 
                        (if (not (wait-raycast-hit-nothing? (get-int32 raycast-5 self)))) 
                            (go trigger-full-room-trap 1)
                        
                    
                
                lambda 
                    (if (not (< (get-int32 raycast-6 self) 0))) 
                        (if (not (wait-raycast-hit-nothing? (get-int32 raycast-6 self)))) 
                            (go trigger-full-room-trap 1)
                        
                    
                
                lambda 
                    (if (not (< (get-int32 raycast-7 self) 0))) 
                        (if (not (wait-raycast-hit-nothing? (get-int32 raycast-7 self)))) 
                            (go trigger-full-room-trap 1)
                        
                    
                
                lambda 
                    (if (not (< (get-int32 raycast-8 self) 0))) 
                        (if (not (wait-raycast-hit-nothing? (get-int32 raycast-8 self)))) 
                            (go trigger-full-room-trap 1)
                        
                    
                
                lambda 
                    (if (not (< (get-int32 raycast-9 self) 0))) 
                        (if (not (wait-raycast-hit-nothing? (get-int32 raycast-9 self)))) 
                            (go trigger-full-room-trap 1)
                        
                    
                
                lambda 
                    (if (not (< (get-int32 raycast-10 self) 0))) 
                        (if (not (wait-raycast-hit-nothing? (get-int32 raycast-10 self)))) 
                            (go trigger-full-room-trap 1)
                        
                    
                
                lambda 
                    (if (not (< (get-int32 raycast-11 self) 0))) 
                        (if (not (wait-raycast-hit-nothing? (get-int32 raycast-11 self)))) 
                            (go trigger-full-room-trap 1)
                        
                    
                
                lambda 
                    (set-int32 raycast (kick-raycast (get-spawner-locator pos-tripwire-raycast-check-5 1) (get-spawner-locator pos-tripwire-raycast-check-6 1) pe-test-tripwire-room-1 4130) self)
                
                lambda 
                    (set-int32 raycast-2 (kick-raycast (get-spawner-locator pos-tripwire-raycast-check-7 1) (get-spawner-locator pos-tripwire-raycast-check-8 1) pe-test-tripwire-room-1 4130) self)
                
                lambda 
                    (set-int32 raycast-3 (kick-raycast (get-spawner-locator pos-tripwire-raycast-check-10 1) (get-spawner-locator pos-tripwire-raycast-check-11 1) pe-test-tripwire-room-1 4130) self)
                
                lambda 
                    (set-int32 raycast-4 (kick-raycast (get-spawner-locator pos-tripwire-raycast-check-12 1) (get-spawner-locator pos-tripwire-raycast-check-13 1) pe-test-tripwire-room-1 4130) self)
                
                lambda 
                    (set-int32 raycast-5 (kick-raycast (get-spawner-locator pos-tripwire-raycast-check-14 1) (get-spawner-locator pos-tripwire-raycast-check-at-box-1 1) pe-test-tripwire-room-1 4130) self)
                
                lambda 
                    (set-int32 raycast-6 (kick-raycast (get-spawner-locator pos-tripwire-raycast-check-15 1) (get-spawner-locator pos-tripwire-raycast-check-at-box-1 1) pe-test-tripwire-room-1 4130) self)
                
                lambda 
                    (set-int32 raycast-7 (kick-raycast (get-spawner-locator pos-tripwire-raycast-check-16 1) (get-spawner-locator pos-tripwire-raycast-check-at-box-1 1) pe-test-tripwire-room-1 4130) self)
                
                lambda 
                    (set-int32 raycast-8 (kick-raycast (get-spawner-locator pos-tripwire-raycast-check-18 1) (get-spawner-locator pos-tripwire-raycast-check-19 1) pe-test-tripwire-room-1 4130) self)
                
                lambda 
                    (set-int32 raycast-9 (kick-raycast (get-spawner-locator pos-tripwire-raycast-check-20 1) (get-spawner-locator pos-tripwire-raycast-check-21 1) pe-test-tripwire-room-1 4130) self)
                
                lambda 
                    (set-int32 raycast-10 (kick-raycast (get-spawner-locator pos-tripwire-raycast-check-22 1) (get-spawner-locator pos-tripwire-raycast-check-23 1) pe-test-tripwire-room-1 4130) self)
                
                lambda 
                    (set-int32 raycast-11 (kick-raycast (get-spawner-locator pos-tripwire-raycast-check-24 1) (get-spawner-locator pos-tripwire-raycast-check-25 1) pe-test-tripwire-room-1 4130) self)
                
            }
        }
        block end {
            track main {
                lambda 
                    (set-int32 raycast -1 self)
                
                lambda 
                    (set-int32 raycast-2 -1 self)
                
                lambda 
                    (set-int32 raycast-3 -1 self)
                
                lambda 
                    (set-int32 raycast-4 -1 self)
                
                lambda 
                    (set-int32 raycast-5 -1 self)
                
                lambda 
                    (set-int32 raycast-6 -1 self)
                
                lambda 
                    (set-int32 raycast-7 -1 self)
                
                lambda 
                    (set-int32 raycast-8 -1 self)
                
                lambda 
                    (set-int32 raycast-9 -1 self)
                
                lambda 
                    (set-int32 raycast-10 -1 self)
                
                lambda 
                    (set-int32 raycast-11 -1 self)
                
            }
        }
    }
    state trigger-full-room-trap {
        block start {
            track main {
                lambda 
                    (animate_ pe-test-tripwire-room-1 tripwire-drop (#%alloc-array single-frame 16 0 1))
                
                lambda 
                    (disable-nav-blocker-object nav-blocker-rect-845)
                
                lambda 
                    (spawn-explosion (get-spawner-position pos-testellis-tripwire-explosion-1 1) *trap-bomb-explosion* 0 0 -1.00 0 0 0.00 0)
                
                lambda 
                    (spawn-explosion (get-spawner-position pos-testellis-tripwire-explosion-2 1) *trap-bomb-explosion* 0 0 -1.00 0 0 0.00 0)
                
                lambda 
                    (spawn-explosion (get-spawner-position pos-testellis-tripwire-explosion-3 1) *trap-bomb-explosion* 0 0 -1.00 0 0 0.00 0)
                
                lambda 
                    (spawn-explosion (get-spawner-position pos-testellis-tripwire-explosion-4 1) *trap-bomb-explosion* 0 0 -1.00 0 0 0.00 0)
                
                lambda 
                    (spawn-explosion (get-spawner-position pos-testellis-tripwire-explosion-5 1) *trap-bomb-explosion* 0 0 -1.00 0 0 0.00 0)
                
                lambda 
                    (go complete 1)
                
            }
        }
    }
    state dismantle-full-room {
        block start {
            track main {
                lambda 
                    (animate_ pe-test-tripwire-room-1 tripwire-drop (#%alloc-array single-frame 16 0 1))
                
                lambda 
                    (disable-nav-blocker-object nav-blocker-rect-845)
                
                lambda 
                    (interactable-disable int-petest-black-box-dismantle-1)
                
                lambda 
                    (go complete 1)
                
            }
        }
    }
}
