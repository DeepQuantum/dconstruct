; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-test\ss-test-nlance\ss-aqu-owen-zone-ambush.bin      #
; #   filesize: 8666 bytes                                                                           #
; #   total entries: 2                                                                               #
; #   functions: 22                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        symbol curr-owen = nullptr
        symbol spawn-pos = nullptr
        symbol zone-region = nullptr
        i32 owen-health = 400
        bound-frame player-loc = nullptr
    }
    state --all-- {
        block event npc-died {
            track main {
                lambda 
                    (if (== (get-symbol curr-owen self) (event-arg-symbol 0))) 
                        (go owen-dead 1)
                    
                
            }
        }
    }
    state wait-player-in-ambush-zone {
        block event ambush-player {
            track main {
                lambda 
                    (if (and (task-active? nlance owen-reef) (task-active? nlance owen-reef-vs-owen-and-dog))) 
                        (set-symbol zone-region (event-arg-symbol 0) self)
                        (set-symbol spawn-pos (event-arg-symbol 1) self)
                        (go ambush-player 1)
                    
                
            }
        }
    }
    state ambush-player {
        block start {
            track main {
                lambda 
                    (set-symbol curr-owen (wait-spawn-entity-at npc-owen-enemy-3 (get-spawner-locator (get-symbol spawn-pos self) 1) 0) self)
                
                lambda 
                    (let loop () (when (not (entity-exists? (get-symbol curr-owen self))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (signal ready 0)
                
                lambda 
                    (set-health-parameters (get-symbol curr-owen self) 0.00 0.00 (get-int32 owen-health self) (get-int32 owen-health self))
                
                lambda 
                    (npc-set-zone-region (get-symbol curr-owen self) (get-symbol zone-region self))
                
                lambda 
                    (npc-make-aware-of (get-symbol curr-owen self) player 2 0)
                
                lambda 
                    (npc-set-perfect-aim (get-symbol curr-owen self) 1)
                
            }
            track flashlight-vision {
                lambda 
                    (wait-for ready)
                
                lambda 
                    (let loop () (when 1 
                        (let loop () (when (not (player-flashlight-on?)) 
                            (wait-one-frame)
                         (loop)))
                        (player-set-npc-vision-multiplier-extra 2.00)
                        (let loop () (when (player-flashlight-on?) 
                            (wait-one-frame)
                         (loop)))
                        (player-set-npc-vision-multiplier-extra 0.25)
                        (wait-one-frame)
                     (loop)))
                
            }
        }
        block event npc-attacked {
            track main {
                lambda 
                    (if (== (event-arg-symbol 1) (get-symbol curr-owen self))) 
                        (go owen-escape 1)
                    
                
            }
        }
    }
    state owen-escape {
        block start {
            track main {
                lambda 
                    (set-bound-frame player-loc (get-object-locator player) self)
                
                lambda 
                    (npc-set-movement-speed-scale (get-symbol curr-owen self) 1.20)
                
                lambda 
                    (npc-move-to-spawner (get-symbol curr-owen self) (get-furthest-in-group owen-escape-points player) 1 0 0 0 0 0 0)
                
                lambda 
                    (let loop () (when (not (npc-navigation-in-progress? (get-symbol curr-owen self))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (let loop () (when (and (or (not (npc-on-screen-and-visible? (get-symbol curr-owen self) -1 0)) (object-further-than-distance-to-object-xz? (get-symbol curr-owen self) player 6.00 1)) (not (not (npc-navigation-in-progress? (get-symbol curr-owen self))))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (set-int32 owen-health (cast i32 (get-object-health (get-symbol curr-owen self))) self)
                
                lambda 
                    (kill-entity (get-symbol curr-owen self))
                
                lambda 
                    (let loop () (when (not (locator-further-than-distance-to-locator-xz? (get-bound-frame player-loc self) (get-object-locator player) 8.00 1)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (wait 6.00)
                
                lambda 
                    (go wait-player-in-ambush-zone 1)
                
            }
        }
    }
}
