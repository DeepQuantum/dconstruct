; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-test\ss-test-aeinhorn\ss-ae-buddy-tweaker.bin        #
; #   filesize: 3482 bytes                                                                           #
; #   total entries: 2                                                                               #
; #   functions: 6                                                                                   #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (allow-buddy-tweaking arg_0) 
    (deref (cast u8* (+ *buddy-prototypes* 19)))
)

statescript {
    options {
        player
        sp-all
    }
    state disabled {
        block start {
            track main {
                lambda 
                    (if (allow-buddy-tweaking (get-self-id))) 
                        (go enable-editing 1)
                    
                
            }
        }
        block event designer-menu-changed {
            track main {
                lambda 
                    (if (allow-buddy-tweaking (get-self-id))) 
                        (go enable-editing 1)
                    
                
            }
        }
    }
    state enable-editing {
        block start {
            track low-health {
                lambda 
                    (let loop () (when 1 
                        (let loop () (when (not (deref (cast u8* (+ *buddy-prototypes* 29)))) 
                            (wait-one-frame)
                         (loop)))
                        (if (and (entity-exists? dina) (deref (cast u8* (+ *buddy-prototypes* 25))))) 
                            (set-object-health dina 110.00)
                        
                        (if (and (entity-exists? yara) (deref (cast u8* (+ *buddy-prototypes* 26))))) 
                            (set-object-health yara 110.00)
                        
                        (if (and (entity-exists? lev) (deref (cast u8* (+ *buddy-prototypes* 27))))) 
                            (set-object-health lev 110.00)
                        
                        (set! (deref (cast u8* (+ *buddy-prototypes* 29))) 0)
                     (loop)))
                
            }
            track full-health {
                lambda 
                    (let loop () (when 1 
                        (let loop () (when (not (deref (cast u8* (+ *buddy-prototypes* 30)))) 
                            (wait-one-frame)
                         (loop)))
                        (if (and (entity-exists? dina) (deref (cast u8* (+ *buddy-prototypes* 25))))) 
                            (set-object-health dina (get-object-max-health dina))
                        
                        (if (and (entity-exists? yara) (deref (cast u8* (+ *buddy-prototypes* 26))))) 
                            (set-object-health yara (get-object-max-health yara))
                        
                        (if (and (entity-exists? lev) (deref (cast u8* (+ *buddy-prototypes* 27))))) 
                            (set-object-health lev (get-object-max-health lev))
                        
                        (set! (deref (cast u8* (+ *buddy-prototypes* 30))) 0)
                     (loop)))
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (allow-buddy-tweaking (get-self-id)))) 
                        (go disabled 1)
                    
                
            }
        }
    }
}
