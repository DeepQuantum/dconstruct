; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-test\ss-test-aeinhorn\ss-ae-struggle-save-settings.bin#
; #   filesize: 5188 bytes                                                                           #
; #   total entries: 5                                                                               #
; #   functions: 8                                                                                   #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (increment-buddy-saves-index) 
    (set-int32 menu-index (get-next-buddy-save-task-index) self)
)

(define (get-current-subtask-symbol) 
    (define var_1 (darray-at buddy-struggle-save-subtasks (get-int32 menu-index self)))
    (define var_2)
    (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 7))) 
        (set! var_2 (deref (cast u64* var_1)))
     else (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 5))) 
        (set! var_2 (deref (cast u64* var_1)))
     else (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 4))) 
        (set! var_2 (deref (cast u64* var_1)))
     else 
        (set! var_2 0)
    
    var_2
)

(define (get-next-buddy-save-task-index) 
    (mod-int32 (+ (get-int32 menu-index self) 1) (darray-count buddy-struggle-save-subtasks))
)

statescript {
    options {
        player
    }
    declarations {
        i32 menu-index = 0
        f32 top-menu-y-start = 20.00
        f32 top-menu-x-start = 50.00
    }
    state task-check {
        block start {
            track main {
                lambda 
                    (for ([var_1 buddy-struggle-save-subtasks]) 
                        (define var_2)
                        (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 7))) 
                            (set! var_2 (deref (cast u64* var_1)))
                         else (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 5))) 
                            (set! var_2 (deref (cast u64* var_1)))
                         else (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 4))) 
                            (set! var_2 (deref (cast u64* var_1)))
                         else 
                            (set! var_2 0)
                        
                        (if (task-active? melee-buddies var_2)) 
                            (go menu 1)
                        
                        (++ menu-index self)
                    )
                
            }
        }
    }
    state menu {
        block start {
            track setup {
                lambda 
                    (let loop () (when (not (joypad-command-active? cancel-hold 6)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (task-skip-to! melee-buddies (get-current-subtask-symbol) 1 0 0)
                
            }
            track menu-change {
                lambda 
                    (let loop () (when 1 
                        (let loop () (when (not (joypad-command-active? interact 6)) 
                            (wait-one-frame)
                         (loop)))
                        (increment-buddy-saves-index)
                        (let loop () (when (not (not (joypad-command-active? interact 6))) 
                            (wait-one-frame)
                         (loop)))
                     (loop)))
                
            }
        }
        block update {
            track main {
                lambda 
                    (debug-draw-string-2d (alloc-point (get-float top-menu-x-start self) (get-float top-menu-y-start self) 0.00 0) (dc:format "Press [Triangle] to cycle choice. Press [Circle] to jump to %s" (string-debug (get-current-subtask-symbol))) 7 0.00 0.60 0 0 0 1.00)
                
            }
        }
    }
}
