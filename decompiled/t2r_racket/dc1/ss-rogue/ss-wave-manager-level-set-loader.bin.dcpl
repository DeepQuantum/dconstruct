; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-rogue\ss-wave-manager-level-set-loader.bin           #
; #   filesize: 12549 bytes                                                                          #
; #   total entries: 3                                                                               #
; #   functions: 19                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (set-wave-faction-to-replay-current-task! arg_0) 
    (if (== arg_0 (get-symbol current-task ss-wave-manager-level-set-loader-1))) 
        (set-symbol next-encounter-task (get-symbol current-task ss-wave-manager-level-set-loader-1) ss-wave-manager-level-set-loader-1)
        (set-int32 next-encounter-faction (get-int32 last-encounter-faction ss-wave-manager-level-set-loader-1) ss-wave-manager-level-set-loader-1)
    
    (if (not (is-final-build?))) 
        (display (dc:format "DEB-WM-LS: Replaying rogue encounter") 19)
    
    (if (not (is-final-build?))) 
        (display (dc:format "DEB-WM-LS:     task:    %s" (string-debug arg_0)) 19)
    
    (define var_9)
    (if (not (is-final-build?))) 
        (set! var_9 (display (dc:format "DEB-WM-LS:     faction: %s" (get-t2r-rogue-wave-faction-as-string (get-int32 next-encounter-faction ss-wave-manager-level-set-loader-1))) 19))
     else 
        (set! var_9 0)
    
    var_9
)

(define (set-waves-faction-for-next-task! arg_0 arg_1) 
    (let loop () (when (not (object-spawned? ss-wave-manager-level-set-loader-1)) 
        (wait-one-frame)
     (loop)))
    (set-symbol next-encounter-task arg_0 ss-wave-manager-level-set-loader-1)
    (set-int32 next-encounter-faction arg_1 ss-wave-manager-level-set-loader-1)
    (if (not (is-final-build?))) 
        (display (dc:format "DEB-WM-LS: Faction for next rogue encounter set") 19)
    
    (if (not (is-final-build?))) 
        (display (dc:format "DEB-WM-LS:      next task: %s" (string-debug arg_0)) 19)
    
    (if (not (is-final-build?))) 
        (display (dc:format "DEB-WM-LS:      faction:   %s" (get-t2r-rogue-wave-faction-as-string arg_1)) 19)
    
    (define var_9)
    (if (!= *encounter-tweaks* nullptr)) 
        (set! (deref (cast i32* (+ *encounter-tweaks* 20))) -1)
        (set! var_9 -1)
     else 
        (set! var_9 0)
    
    var_9
)

statescript {
    options {
    }
    declarations {
        bool ignore-debug-waves? = nullptr
        bool is-encounter-manager-task? = nullptr
        symbol current-task = #0000000000000000
        symbol next-encounter-task = #0000000000000000
        i32 next-encounter-faction = -1
        symbol last-task = #0000000000000000
        i32 last-encounter-faction = -1
        bool was-encounter-manager-task? = nullptr
    }
    state --script-- {
        block start {
            track main {
                lambda 
                    (if (not (is-final-build?))) 
                        (display (dc:format "DEB-WM-LS: ss-wave-manager-level-set-loader started!") 19)
                    
                
                lambda 
                    (set-svar rogue-wave-faction -1)
                
            }
        }
        block end {
            track main {
                lambda 
                    (if (not (is-final-build?))) 
                        (display (dc:format "DEB-WM-LS: ss-wave-manager-level-set-loader ended!") 19)
                    
                
            }
        }
    }
    state --all-- {
        block update {
            track main {
                lambda 
                    (if (and (== *invalid-symbol* (get-symbol current-task self)) (not (== (task-get-current-symbol) 0)))) 
                        (set-symbol last-task (get-symbol current-task self) self)
                        (set-symbol current-task (task-get-current-symbol) self)
                        (go wait-for-task 1)
                    
                
            }
        }
        block event on-set-active-continue {
            track main {
                lambda 
                    (define var_0 (event-arg-symbol 0))
                    (if (not (is-final-build?))) 
                        (display (dc:format "DEB-WM-LS: On set active continue..") 19)
                    
                    (if (not (is-final-build?))) 
                        (display (dc:format "DEB-WM-LS:    next task: %s" (string-debug var_0)) 19)
                    
                    (if (!= var_0 *invalid-symbol*)) 
                        (set-symbol last-task (get-symbol current-task self) self)
                        (set-symbol current-task var_0 self)
                        (set! var_6 (go wait-for-task 1))
                    
                
            }
        }
    }
    state wait-for-task {
        block start {
            track main {
                lambda 
                    (let loop () (when (not (not (== (get-symbol current-task self) 0))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (if (and (and (== (get-boolean was-encounter-manager-task? self) 1) (== (get-boolean is-encounter-manager-task? self) 0)) (== (get-symbol current-task self) (get-symbol last-task self)))) 
                        (set-wave-faction-to-replay-current-task! (get-symbol last-task self))
                    
                
                lambda 
                    (go tasked-in 1)
                
            }
        }
    }
    state tasked-in {
        block start {
            track main {
                lambda 
                    (if (not (is-final-build?))) 
                        (display (dc:format "DEB-WM-LS: Tasked in %s!" (string-debug (get-symbol current-task self))) 19)
                    
                
                lambda 
                    (set-boolean ignore-debug-waves? 0 self)
                
                lambda 
                    (set-boolean is-encounter-manager-task? 0 self)
                
                lambda 
                    (if (get-wave-manager-task-config (get-symbol current-task self))) 
                        (define var_3 (get-debug-waves-for-task (get-symbol current-task self)))
                        (define var_4)
                        (if var_3) 
                            (set! var_4 (deref (cast i32* (+ (+ (deref (deref (+ var_3 40))) (* 0 40)) 8))))
                         else 
                            (set! var_4 -1)
                        
                        (define var_5)
                        (if (!= *encounter-tweaks* nullptr)) 
                            (set! var_5 (deref (cast i32* (+ *encounter-tweaks* 20))))
                         else 
                            (set! var_5 -1)
                        
                        (if (and (> var_5 -1) (< var_5 4))) 
                            (set-boolean ignore-debug-waves? 1 self)
                            (set-svar rogue-wave-faction var_5)
                            (if (not (is-final-build?))) 
                                (display (dc:format "DEB-WM-LS:   -> *encounter-tweaks* set svar") 19)
                            
                         else (if (and (and (and (not (== next-encounter-task 0)) (== (get-symbol current-task self) (get-symbol next-encounter-task self))) (> (get-int32 next-encounter-faction self) -1)) (< (get-int32 next-encounter-faction self) 4))) 
                            (set-boolean ignore-debug-waves? 1 self)
                            (set-svar rogue-wave-faction (get-int32 next-encounter-faction self))
                            (if (not (is-final-build?))) 
                                (display (dc:format "DEB-WM-LS:   -> ss-encounter-manager set svar!") 19)
                            
                         else (if (and (> var_4 -1) (< var_4 4))) 
                            (set-svar rogue-wave-faction var_4)
                            (if (not (is-final-build?))) 
                                (display (dc:format "DEB-WM-LS:   -> debug-waves set svar") 19)
                            
                         else 
                            (display-error "Failed to set waves faction! Wave manager won't function as intended.")
                            (if (not (is-final-build?))) 
                                (display (dc:format "DEB-WM-LS:   -> ERROR: Couldn't set svar :(") 19)
                            
                        
                        (if (and (not (== next-encounter-task 0)) (== (get-symbol current-task self) (get-symbol next-encounter-task self)))) 
                            (set-boolean is-encounter-manager-task? 1 self)
                        
                        (set-int32 last-encounter-faction (get-int32 next-encounter-faction self) self)
                        (set-int32 next-encounter-faction -1 self)
                        (set-symbol next-encounter-task *invalid-symbol* self)
                        (if (not (is-final-build?))) 
                            (set! var_23 (display (dc:format "DEB-WM-LS:      rogue-wave-faction:     %d (%s)" (get-svar rogue-wave-faction) (get-t2r-rogue-wave-faction-as-string (get-svar rogue-wave-faction))) 19))
                        
                        (let loop () (when (not (is-level-set-loaded? t2r-lvl-wave-manager-enemy-npcs)) 
                            (wait-one-frame)
                         (loop)))
                        (if (not (is-final-build?))) 
                            (display (dc:format "DEB-WM-LS:      Level-set fully loaded: t2r-lvl-wave-manager-enemy-npcs") 19)
                        
                    
                
            }
        }
        block end {
            track main {
                lambda 
                    (set-boolean was-encounter-manager-task? (get-boolean is-encounter-manager-task? self) self)
                
                lambda 
                    (set-boolean is-encounter-manager-task? 0 self)
                
                lambda 
                    (set-boolean ignore-debug-waves? 0 self)
                
                lambda 
                    (set-svar rogue-wave-faction -1)
                
                lambda 
                    (if (not (is-final-build?))) 
                        (display (dc:format "DEB-WM-LS: Tasking out..") 19)
                    
                
            }
        }
    }
}
