; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-rogue\ss-challenge-smoke-bomb-stun-enemies.bin       #
; #   filesize: 8308 bytes                                                                           #
; #   total entries: 2                                                                               #
; #   functions: 9                                                                                   #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (update-challenge-progress-str-with-current-stun-count) 
    (set-string challenge-progress-str (dc:format "%d/%d" (get-int32 num-successfull-stuns self) (tag-int32 num-stuns-needed self)) self)
    (hud-challenge-update-progress-count (get-int32 num-successfull-stuns self) (tag-int32 num-stuns-needed self))
)

statescript {
    options {
        player
    }
    declarations {
        i32 num-successfull-stuns = 0
        string challenge-progress-str = 
        symbol ---------------------------- = ---------------
        i32 num-currently-stunned-npcs = 0
        symbol group-currently-stunned-npcs = #0000000000000000
        timer timer-clear-stun-bomb = nullptr
        symbol stun-bomb-explosion-process = #0000000000000000
        bound-frame stun-bomb-explosion-location = nullptr
    }
    state tracking {
        block start {
            track main {
                lambda 
                    (set-symbol group-currently-stunned-npcs (darray-create self -1 0) self)
                
                lambda 
                    (update-challenge-progress-str-with-current-stun-count)
                
            }
        }
        block update {
            track main {
                lambda 
                    (for ([var_1 all-enemy-npcs]) 
                        (define var_2)
                        (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 7))) 
                            (set! var_2 (deref (cast u64* var_1)))
                         else (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 5))) 
                            (set! var_2 (deref (cast u64* var_1)))
                         else (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 4))) 
                            (set! var_2 (deref (cast u64* var_1)))
                         else 
                            (set! var_2 0)
                        
                        (define var_3 (npc-alive? var_2))
                        (define var_4 (npc-is-stunned? var_2))
                        (define var_7 (darray-contains? (get-symbol group-currently-stunned-npcs self) (new-boxed-value 7 var_2)))
                        (if (not (npc-is-buddy? var_2))) 
                            (if (and (and (and var_3 var_4) (not var_7)) (&& (&& var_3 (not (== (get-symbol stun-bomb-explosion-process self) 0))) (> 5.00 (distance-between-locators (get-object-locator var_2) (get-bound-frame stun-bomb-explosion-location self)))))) 
                                (darray-append (get-symbol group-currently-stunned-npcs self) (new-boxed-value 7 var_2) 1)
                                (send-event npc-just-got-stunned self (new-boxed-value 7 var_2))
                             else (if (and (and var_3 (not var_4)) var_7)) 
                                (darray-find-remove (get-symbol group-currently-stunned-npcs self) (new-boxed-value 7 var_2) 0)
                             else (if (and (not var_3) var_7)) 
                                (darray-find-remove (get-symbol group-currently-stunned-npcs self) (new-boxed-value 7 var_2) 0)
                            
                        
                        (set-int32 num-currently-stunned-npcs (darray-count (get-symbol group-currently-stunned-npcs self)) self)
                    )
                
            }
        }
        block event projectile-throwable-explode {
            track main {
                lambda 
                    (define var_0 (event-arg-symbol 0))
                    (define var_1 (event-arg-bound-frame 1))
                    (if (and (== (event-arg-symbol 2) player) (== (event-arg-symbol 3) *stun-bomb-explosion*))) 
                        (set-symbol stun-bomb-explosion-process var_0 self)
                        (set-bound-frame stun-bomb-explosion-location var_1 self)
                        (start-timer timer-clear-stun-bomb 1.00 self 1)
                        (define var_5 (get-locator-position var_1))
                        (if (not (is-final-build?))) 
                            (display (dc:format "DEB-EC: Player's stun bomb exploded") 19)
                        
                        (if (not (is-final-build?))) 
                            (display (dc:format "DEB-EC:   > id = %s" (string-debug var_0)) 19)
                        
                        (define var_12)
                        (if (not (is-final-build?))) 
                            (set! var_12 (display (dc:format "DEB-EC:   > loc = (%.1f, %.1f, %.1f)" (deref (cast f32* var_5)) (deref (cast f32* (+ var_5 4))) (deref (cast f32* (+ var_5 8)))) 19))
                         else 
                            (set! var_12 0)
                        
                        (set! var_4 var_12)
                    
                
            }
        }
        block event timer-clear-stun-bomb {
            track main {
                lambda 
                    (set-symbol stun-bomb-explosion-process *invalid-symbol* self)
                
            }
        }
        block event npc-just-got-stunned {
            track main {
                lambda 
                    (if (not (is-final-build?))) 
                        (display (dc:format "DEB-EC: Counting %s as stunned" (string-debug (event-arg-symbol 0))) 19)
                    
                    (++ num-successfull-stuns self)
                    (if (and (!= *encounter-tweaks* nullptr) (>= (deref (cast f32* (+ *encounter-tweaks* 88))) 0.00))) 
                        (wait (deref (cast f32* (+ *encounter-tweaks* 88))))
                     else 
                        (wait 0.50)
                    
                    (update-challenge-progress-str-with-current-stun-count)
                    (if (>= (get-int32 num-successfull-stuns self) (tag-int32 num-stuns-needed self))) 
                        (set! var_7 (go completed 1))
                    
                
            }
        }
    }
    state completed {
        block start {
            track main {
                lambda 
                    (send-event challenge-complete ss-challenges-controller-1)
                
                lambda 
                    (if (not (is-final-build?))) 
                        (display (dc:format "DEB-EC: Challenge complete %s" (string-debug (get-self-id))) 19)
                    
                
            }
        }
    }
}
