; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-rogue\ss-challenge-melee-kill-enemies.bin            #
; #   filesize: 5505 bytes                                                                           #
; #   total entries: 3                                                                               #
; #   functions: 8                                                                                   #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (wait-count-melee-kill-towards-progress! arg_0 arg_1) 
    (++ current-kill-count self)
    (if (and (!= *encounter-tweaks* nullptr) (>= (deref (cast f32* (+ *encounter-tweaks* 88))) 0.00))) 
        (wait (deref (cast f32* (+ *encounter-tweaks* 88))))
     else 
        (wait 0.25)
    
    (update-challenge-progress-str-with-current-melee-kill-count)
    (define var_3)
    (if (>= (get-int32 current-kill-count self) (tag-int32 num-kills-needed self))) 
        (set! var_3 (go completed 1))
     else 
        (set! var_3 0)
    
    var_3
)

(define (update-challenge-progress-str-with-current-melee-kill-count) 
    (set-string challenge-progress-str (dc:format "%d/%d" (get-int32 current-kill-count self) (tag-int32 num-kills-needed self)) self)
    (hud-challenge-update-progress-count (get-int32 current-kill-count self) (tag-int32 num-kills-needed self))
)

statescript {
    options {
        player
    }
    declarations {
        i32 current-kill-count = 0
        string challenge-progress-str = 
        bool is-grab-gun-finisher? = nullptr
    }
    state tracking {
        block start {
            track main {
                lambda 
                    (update-challenge-progress-str-with-current-melee-kill-count)
                
            }
        }
        block event player-grab-gun-finisher {
            track main {
                lambda 
                    (set-boolean is-grab-gun-finisher? 1 self)
                
            }
        }
        block event npc-died-from-melee {
            track main {
                lambda 
                    (define var_0 (event-arg-symbol 0))
                    (define var_1 (event-arg-symbol 1))
                    (define var_2 (event-arg-symbol 2))
                    (if (and (and (== player var_1) (not (== var_1 0))) (not (get-boolean is-grab-gun-finisher? self)))) 
                        (if (not (is-final-build?))) 
                            (display (dc:format "DEB-EC: Player just melee killed %s with %s. Custom event!" (string-debug var_0) (string-debug var_2)) 19)
                        
                        (wait-count-melee-kill-towards-progress! var_0 var_2)
                    
                    (set-boolean is-grab-gun-finisher? 0 self)
                
            }
        }
        block event npc-died {
            track main {
                lambda 
                    (define var_0 (event-arg-symbol 0))
                    (define var_2 (event-arg-symbol 2))
                    (if (== player (event-arg-symbol 1))) 
                        (define var_3 (lookup-weapon-gameplay-def var_2 1))
                        (define var_4)
                        (if (not (== var_3 0))) 
                            (set! var_4 (deref (cast i32* var_3)))
                         else 
                            (set! var_4 0)
                        
                        (if (and (and (== var_4 3) (character-in-finisher? var_0)) (not (get-boolean is-grab-gun-finisher? self)))) 
                            (if (not (is-final-build?))) 
                                (display (dc:format "DEB-EC: Player just melee killed %s with %s" (string-debug var_0) (string-debug var_2)) 19)
                            
                            (wait-count-melee-kill-towards-progress! var_0 var_2)
                        
                    
                    (set-boolean is-grab-gun-finisher? 0 self)
                
            }
        }
    }
    state completed {
        block start {
            track main {
                lambda 
                    (send-event challenge-complete ss-challenges-controller-1)
                
                lambda 
                    (if (not (is-final-build?))) 
                        (display (dc:format "DEB-EC: Challenge complete %s" (string-debug (get-self-id))) 19)
                    
                
            }
        }
    }
}
