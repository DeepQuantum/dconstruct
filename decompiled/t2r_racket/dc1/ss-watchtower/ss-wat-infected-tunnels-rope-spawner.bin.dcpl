; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-watchtower\ss-wat-infected-tunnels-rope-spawner.bin  #
; #   filesize: 8905 bytes                                                                           #
; #   total entries: 1                                                                               #
; #   functions: 28                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
        wat-infected-tunnels-intro-station-script
    }
    state task-check {
        block start {
            track main {
                lambda 
                    (if (task-complete? watchtower wat-infected-tunnels-rope-puzzle)) 
                        (if (task-active? watchtower wat-infected-tunnels-shambler-intro)) 
                            (go spawn-rope-2 1)
                        
                        (go complete 1)
                    
                
                lambda 
                    (if (task-active? watchtower wat-infected-tunnels-rope-puzzle)) 
                        (go spawn-rope-1 1)
                    
                
            }
        }
        block event task-complete {
            track main {
                lambda 
                    (if (task-active? watchtower wat-infected-tunnels-rope-puzzle)) 
                        (go spawn-rope-1 1)
                    
                
            }
        }
    }
    state spawn-rope-1 {
        block start {
            track spawn-rope {
                lambda 
                    (spawn-entity-at carry-object-wat-rope-1 (get-spawner-locator carry-object-wat-rope-1 1) 0)
                
                lambda 
                    (interactable-disable carry-object-wat-rope-1)
                
                lambda 
                    (wait 1.00)
                
                lambda 
                    (set-instance-flag-hero (get-carry-rope-rope carry-object-wat-rope-1) 1)
                
            }
            track rope-tutorial {
                lambda 
                    (let loop () (when (and (player-in-region? rgn-wat-player-on-rope-1 2) (not (player-in-state? RopeClimb))) 
                        (wait 0.10)
                     (loop)))
                
                lambda 
                    (signal first-tutorial 0)
                
                lambda 
                    (if (is-text-to-speech-enabled?)) 
                        (if (not (is-traversal-audio-cues?))) 
                            (hud-play-tutorial HUD_FTT_TUTORIAL_ROPE3_PC 10.00 2 rope-swing 1.00 0 0 0)
                        
                    
                
                lambda 
                    (let loop () (when (not (joypad-command-active-and-enabled? rope-swing 2)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (wait 1.00)
                
                lambda 
                    (let loop () (when (and (player-in-region? rgn-wat-player-on-rope-2 2) (not (player-in-state? RopeClimb))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (hud-play-tutorial HUD_FTT_TUTORIAL_ROPE4 10.00 2 jump 1.00 0 0 0)
                
            }
            track tts-tutorial {
                lambda 
                    (signal first-tutorial 0)
                
                lambda 
                    (if (and (is-text-to-speech-enabled?) (is-traversal-audio-cues?))) 
                        (let loop () (when 1 
                            (let loop () (when (not (player-in-state? RopeClimb)) 
                                (wait-one-frame)
                             (loop)))
                            (hud-play-tutorial TIP_ACCESS_ROPE_SWING -1.00 2 none 1.00 0 0 0)
                            (let loop () (when (player-in-state? RopeClimb) 
                                (wait-one-frame)
                             (loop)))
                            (hud-clear-tutorials 0)
                         (loop)))
                    
                
            }
        }
        block event task-complete {
            track main {
                lambda 
                    (if (task-complete? watchtower wat-infected-tunnels-shambler-intro)) 
                        (kill-entity-if-spawned obj-wat-rope-throw-correction-1)
                        (kill-entity-if-spawned obj-wat-rope-throw-correction-2)
                        (kill-entity-if-spawned carry-object-wat-rope-1)
                        (hud-clear-tutorials 0)
                        (go complete 1)
                    
                
            }
        }
        block event enable-rope {
            track main {
                lambda 
                    (interactable-enable carry-object-wat-rope-1)
                
            }
        }
        block event disable-rope {
            track main {
                lambda 
                    (interactable-disable carry-object-wat-rope-1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (entity-exists? (get-carry-rope-rope carry-object-wat-rope-1))) 
                        (if (player-in-state? RopeClimb)) 
                            (enable-collision ic-wat-rope-climb-top-collision-1 1 0 0.00)
                            (set-show-in-flashlight-flag (get-carry-rope-rope carry-object-wat-rope-1) 0)
                         else 
                            (set-show-in-flashlight-flag (get-carry-rope-rope carry-object-wat-rope-1) 1)
                            (enable-collision ic-wat-rope-climb-top-collision-1 0 0 0.00)
                        
                    
                
            }
        }
    }
    state spawn-rope-2 {
        block start {
            track main {
                lambda 
                    (kill-entity-if-spawned obj-wat-rope-throw-correction-1)
                
                lambda 
                    (kill-entity-if-spawned obj-wat-rope-throw-correction-2)
                
                lambda 
                    (spawn-entity-at carry-object-wat-rope-1 (get-spawner-locator pos-wat-pin-rope-1 1) 1)
                
                lambda 
                    (wait 1.00)
                
                lambda 
                    (send-event drop carry-object-wat-rope-1)
                
                lambda 
                    (set-instance-flag-hero (get-carry-rope-rope carry-object-wat-rope-1) 1)
                
            }
        }
        block event task-complete {
            track main {
                lambda 
                    (if (task-complete? watchtower wat-infected-tunnels-shambler-intro)) 
                        (kill-entity-if-spawned carry-object-wat-rope-1)
                        (go complete 1)
                    
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (player-in-state? RopeClimb)) 
                        (set-show-in-flashlight-flag (get-carry-rope-rope carry-object-wat-rope-1) 0)
                     else 
                        (set-show-in-flashlight-flag (get-carry-rope-rope carry-object-wat-rope-1) 1)
                    
                
            }
        }
    }
}
