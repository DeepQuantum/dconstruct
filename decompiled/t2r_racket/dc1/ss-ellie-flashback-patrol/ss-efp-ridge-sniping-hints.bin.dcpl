; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-ellie-flashback-patrol\ss-efp-ridge-sniping-hints.bin#
; #   filesize: 12245 bytes                                                                          #
; #   total entries: 3                                                                               #
; #   functions: 38                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (clear-marks) 
    (for ([var_1 all-enemy-npcs]) 
        (define var_2)
        (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 7))) 
            (set! var_2 (deref (cast u64* var_1)))
         else (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 5))) 
            (set! var_2 (deref (cast u64* var_1)))
         else (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 4))) 
            (set! var_2 (deref (cast u64* var_1)))
         else 
            (set! var_2 0)
        
        (if (npc-alive? var_2)) 
            (hud2-buddy-unmark-npc var_2)
        
    )
)

(define (npc-in-group-on-screen?) 
    (set-boolean enemy-on-screen? 0 self)
    (if (fact-bool player is-zoomed-in 0 0)) 
        (for ([var_2 all-enemy-npcs]) 
            (define var_3)
            (if (and var_2 (== (deref (cast u16* (+ var_2 12))) 7))) 
                (set! var_3 (deref (cast u64* var_2)))
             else (if (and var_2 (== (deref (cast u16* (+ var_2 12))) 5))) 
                (set! var_3 (deref (cast u64* var_2)))
             else (if (and var_2 (== (deref (cast u16* (+ var_2 12))) 4))) 
                (set! var_3 (deref (cast u64* var_2)))
             else 
                (set! var_3 0)
            
            (if (npc-alive? var_3)) 
                (if (npc-on-screen? var_3 0.00)) 
                    (set-boolean enemy-on-screen? 1 self)
                
            
        )
    
    (set-timer-paused hint-look-at-timer (get-boolean enemy-on-screen? self) self)
)

statescript {
    options {
        player
    }
    declarations {
        i32 hint-count = 0
        timer hint-look-at-timer = nullptr
        f32 hint-look-at-delay-time = 22.00
        symbol hinted-enemy = nullptr
        bool enemy-on-screen? = nullptr
        symbol wave-hint-at = look-at-enemy-hint-1
    }
    state task-check {
        block start {
            track main {
                lambda 
                    (if (task-complete? ellie-flashback-patrol efp-walking-with-tommy-sniping-end)) 
                        (go complete 1)
                    
                
                lambda 
                    (if (task-active? ellie-flashback-patrol efp-walking-with-tommy-sniping-signpost-ridge)) 
                        (go wait-sign-hit 1)
                    
                
                lambda 
                    (if (task-active? ellie-flashback-patrol efp-walking-with-tommy-sniping-deer-ridge)) 
                        (set-symbol wave-hint-at look-at-enemy-hint-2 self)
                        (go monitor 1)
                    
                
                lambda 
                    (if (task-active? ellie-flashback-patrol efp-walking-with-tommy-sniping-ski-lift-ridge)) 
                        (set-symbol wave-hint-at look-at-enemy-hint-3 self)
                        (go monitor 1)
                    
                
            }
        }
        block event task-complete {
            track main {
                lambda 
                    (if (task-active? ellie-flashback-patrol efp-walking-with-tommy-sniping)) 
                        (go wait-sign-hit 1)
                    
                
            }
        }
    }
    state wait-sign-hit {
        block event efp-watch-player-hit-sign {
            track main {
                lambda 
                    (go monitor 1)
                
            }
        }
    }
    state monitor {
        block start {
            track main {
                lambda 
                    (set-timer-paused hint-look-at-timer 0 self)
                
                lambda 
                    (start-timer hint-look-at-timer (get-float hint-look-at-delay-time self) self 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (and (and (! (== (get-symbol hinted-enemy self) 0)) (npc-alive? (get-symbol hinted-enemy self))) (> (npc-group-alive-count all-enemy-npcs) 0))) 
                        (set-spawner-locator (get-symbol wave-hint-at self) (get-object-locator (get-symbol hinted-enemy self)))
                    
                
                lambda 
                    (npc-in-group-on-screen?)
                
            }
        }
        block event npc-died {
            track main {
                lambda 
                    (if (== (npc-group-alive-count all-enemy-npcs) 0)) 
                        (go no-hints 1)
                    
                
                lambda 
                    (set-symbol hinted-enemy (npc-group-find-first-alive all-enemy-npcs) self)
                
                lambda 
                    (clear-marks)
                
                lambda 
                    (start-timer hint-look-at-timer (get-float hint-look-at-delay-time self) self 1)
                
            }
        }
        block event player-requested-interact {
            track main {
                lambda 
                    (add-int32 hint-count 1 self)
                
                lambda 
                    (if (and (== (get-symbol hinted-enemy self) 0) (! (npc-alive? (get-symbol hinted-enemy self))))) 
                        (set-symbol hinted-enemy (npc-group-find-first-alive all-enemy-npcs) self)
                    
                
                lambda 
                    (if (and (> (npc-group-alive-count all-enemy-npcs) 0) (== (mod-int32 (get-int32 hint-count self) 2) 0))) 
                        (clear-marks)
                        (hud2-buddy-mark-npc (get-symbol hinted-enemy self))
                    
                
                lambda 
                    (interactable-disable (get-symbol wave-hint-at self))
                
                lambda 
                    (start-timer hint-look-at-timer (get-float hint-look-at-delay-time self) self 1)
                
            }
        }
        block event stop-hints {
            track main {
                lambda 
                    (go no-hints 1)
                
            }
        }
        block event hint-look-at-timer {
            track main {
                lambda 
                    (if (and (== (get-symbol hinted-enemy self) 0) (! (npc-alive? (get-symbol hinted-enemy self))))) 
                        (set-symbol hinted-enemy (npc-group-find-first-alive all-enemy-npcs) self)
                    
                
                lambda 
                    (interactable-enable (get-symbol wave-hint-at self))
                
            }
        }
        block event switch-hint-look-at {
            track main {
                lambda 
                    (interactable-disable (get-symbol wave-hint-at self))
                
                lambda 
                    (set-symbol wave-hint-at (event-arg-symbol 0) self)
                
            }
        }
        block event task-complete {
            track main {
                lambda 
                    (if (task-complete? ellie-flashback-patrol efp-walking-with-tommy-sniping-end)) 
                        (clear-marks)
                        (go deactivate 1)
                    
                
            }
        }
    }
    state no-hints {
        block start {
            track main {
                lambda 
                    (interactable-disable (get-symbol wave-hint-at self))
                
                lambda 
                    (set-timer-paused hint-look-at-timer 1 self)
                
                lambda 
                    (clear-marks)
                
            }
        }
        block event allow-hints {
            track main {
                lambda 
                    (go monitor 1)
                
            }
        }
        block event switch-hint-look-at {
            track main {
                lambda 
                    (interactable-disable (get-symbol wave-hint-at self))
                
                lambda 
                    (set-symbol wave-hint-at (event-arg-symbol 0) self)
                
            }
        }
        block event task-complete {
            track main {
                lambda 
                    (if (task-complete? ellie-flashback-patrol efp-walking-with-tommy-sniping-end)) 
                        (clear-marks)
                        (go deactivate 1)
                    
                
            }
        }
    }
    state deactivate {
        block start {
            track main {
                lambda 
                    (interactable-disable (get-symbol wave-hint-at self))
                
                lambda 
                    (set-timer-paused hint-look-at-timer 1 self)
                
                lambda 
                    (clear-marks)
                
                lambda 
                    (go complete 1)
                
            }
        }
    }
}
