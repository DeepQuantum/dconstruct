; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-finding-nora\ss-nor-ellie-alone-task-controller.bin  #
; #   filesize: 10360 bytes                                                                          #
; #   total entries: 1                                                                               #
; #   functions: 20                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
        nor-theater-streets-script
    }
    declarations {
        bool can-set-task = true
        bool task-queued = nullptr
        symbol queued-task = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda 
                    (if (task-complete? find-nora nor-ellie-alone-streets)) 
                        (go complete 1)
                    
                
                lambda 
                    (if (task-active? find-nora nor-ellie-alone-streets)) 
                        (go clear-stats 1)
                    
                
            }
        }
        block event task-complete {
            track main {
                lambda 
                    (if (task-active? find-nora nor-ellie-alone-streets)) 
                        (go clear-stats 1)
                    
                
            }
        }
    }
    state clear-stats {
        block start {
            track task {
                lambda 
                    (let loop () (when (not (can-set-combat-checkpoints?)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (enable-combat-checkpoints find-nora nor-ellie-alone-streets)
                
                lambda 
                    (set-combat-checkpoint find-nora nor-ellie-alone-streets (task-get-active-continue) 0 0)
                
                lambda 
                    (let loop () (when 1 
                        (wait-boolean task-queued 1 self)
                        (wait-boolean can-set-task 1 self)
                        (let loop () (when (not (can-set-combat-checkpoints?)) 
                            (wait-one-frame)
                         (loop)))
                        (let loop () (when (and (and (and (not (player-in-state? Death)) (not (player-in-state? Fall))) (not (player-in-state? GroundLanding))) (not (not (player-in-state? Jump)))) 
                            (wait-one-frame)
                         (loop)))
                        (task-set-active-continue-safely! (get-symbol queued-task self) 1 1)
                        (set-combat-checkpoint find-nora nor-ellie-alone-streets (get-symbol queued-task self) 0 1)
                        (set-boolean task-queued 0 self)
                        (wait-one-frame)
                     (loop)))
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (is-combat-or-search?)) 
                        (set-boolean can-set-task 0 self)
                     else 
                        (set-boolean can-set-task 1 self)
                    
                
            }
        }
        block event streets-lower {
            track main {
                lambda 
                    (if (not (== (get-symbol queued-task self) streets-lower))) 
                        (set-symbol queued-task streets-lower self)
                        (set-boolean task-queued 1 self)
                    
                
            }
        }
        block event streets-upper {
            track main {
                lambda 
                    (if (not (== (get-symbol queued-task self) streets-upper))) 
                        (set-symbol queued-task streets-upper self)
                        (set-boolean task-queued 1 self)
                    
                
            }
        }
        block event rope-puzzle-start {
            track main {
                lambda 
                    (if (not (== (get-symbol queued-task self) rope-puzzle-start))) 
                        (if (not (== (get-svar nor-alone-rope-puzzle-done) 1))) 
                            (set-symbol queued-task rope-puzzle-start self)
                            (set-boolean task-queued 1 self)
                        
                    
                
            }
        }
        block event bar-combat-done {
            track main {
                lambda 
                    (if (not (== (get-symbol queued-task self) bar-combat-done))) 
                        (if (not (== (get-svar nor-alone-bar-infected-killed) 0))) 
                            (set-symbol queued-task bar-combat-done self)
                            (set-boolean task-queued 1 self)
                        
                    
                
            }
        }
        block event ambush-apartment {
            track main {
                lambda 
                    (if (not (== (get-symbol queued-task self) ambush-apartment))) 
                        (if (not (== (get-svar nor-alone-workbench-ambush-done) 1))) 
                            (set-symbol queued-task ambush-apartment self)
                            (set-boolean task-queued 1 self)
                        
                    
                
            }
        }
        block event stairway-exit {
            track main {
                lambda 
                    (if (not (== (get-symbol queued-task self) stairway-exit))) 
                        (set-symbol queued-task stairway-exit self)
                        (set-boolean task-queued 1 self)
                    
                
            }
        }
        block event hospital-vantage-point {
            track main {
                lambda 
                    (if (get-svar-as-boolean nor-alone-look-at-hospital-done)) 
                        (if (not (== (get-symbol queued-task self) hospital-vantage-point))) 
                            (set-symbol queued-task hospital-vantage-point self)
                            (set-boolean task-queued 1 self)
                        
                    
                
            }
        }
        block event dumpster-puzzle-start {
            track main {
                lambda 
                    (if (not (== (get-symbol queued-task self) dumpster-puzzle-start))) 
                        (if (not (== (get-svar nor-alone-dumpster-puzzle-done) 1))) 
                            (set-symbol queued-task dumpster-puzzle-start self)
                            (set-boolean task-queued 1 self)
                        
                    
                
            }
        }
        block event task-complete {
            track main {
                lambda 
                    (if (task-complete? find-nora nor-ellie-alone-streets)) 
                        (go complete 1)
                    
                
            }
        }
        block event on-set-active-continue {
            track main {
                lambda 
                    (if (task-continue-active? find-nora nor-ellie-alone-streets rope-puzzle-done)) 
                        (set-symbol queued-task rope-puzzle-done self)
                        (set-combat-checkpoint find-nora nor-ellie-alone-streets (get-symbol queued-task self) 0 0)
                    
                
                lambda 
                    (if (task-continue-active? find-nora nor-ellie-alone-streets dumpster-puzzle-done)) 
                        (set-symbol queued-task dumpster-puzzle-done self)
                        (set-combat-checkpoint find-nora nor-ellie-alone-streets (get-symbol queued-task self) 0 0)
                    
                
                lambda 
                    (if (task-continue-active? find-nora nor-ellie-alone-streets ambush-done)) 
                        (set-symbol queued-task ambush-done self)
                        (set-combat-checkpoint find-nora nor-ellie-alone-streets (get-symbol queued-task self) 0 0)
                    
                
            }
        }
    }
}
