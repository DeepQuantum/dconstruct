; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-tool\ss-tool-anim-debugger.bin                       #
; #   filesize: 8954 bytes                                                                           #
; #   total entries: 1                                                                               #
; #   functions: 19                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    declarations {
        symbol active-object = nullptr
        bool is-attached = nullptr
        symbol expected-anim-ddict = nullptr
        symbol break-anim-ddict = nullptr
        symbol curr-anim = nullptr
        symbol temp-anim = nullptr
    }
    state initial {
        block start {
            track main {
                lambda 
                    (set-symbol expected-anim-ddict (ddict-create self -1 0) self)
                
                lambda 
                    (set-symbol break-anim-ddict (ddict-create self -1 0) self)
                
                lambda 
                    (go initialized 1)
                
            }
        }
    }
    state initialized {
        block start {
            track main {
                lambda 
                    (set-symbol active-object null self)
                
                lambda 
                    (set-symbol curr-anim null self)
                
                lambda 
                    (set-symbol temp-anim null self)
                
                lambda 
                    (set-boolean is-attached 0 self)
                
                lambda 
                    (ddict-clear (get-symbol break-anim-ddict self))
                
                lambda 
                    (ddict-clear (get-symbol expected-anim-ddict self))
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (get-boolean is-attached self)) 
                        (set-symbol temp-anim (get-object-anim (get-symbol active-object self) 1) self)
                        (if (not (== (get-symbol temp-anim self) (get-symbol curr-anim self)))) 
                            (if (and (> (ddict-key-count (get-symbol expected-anim-ddict self)) 0) (not (ddict-contains-key? (get-symbol expected-anim-ddict self) (get-symbol temp-anim self))))) 
                                (debug-draw-string-centered "Unexpected anim hit" 50.00 7 0.00 1.00 0 0)
                                (debug-pause)
                            
                            (if (ddict-contains-key? (get-symbol break-anim-ddict self) (get-symbol temp-anim self))) 
                                (debug-draw-string-centered "Break anim hit" 50.00 7 0.00 1.00 0 0)
                                (debug-pause)
                            
                            (set-symbol curr-anim (get-symbol temp-anim self) self)
                        
                    
                
            }
        }
        block event attach-to-selected {
            track main {
                lambda 
                    (if (not (string-eq? "(INVALID_STRING_ID_64)" (string-debug (get-selected-object-id))))) 
                        (set-symbol active-object (get-selected-object-id) self)
                        (set-symbol curr-anim (get-object-anim (get-symbol active-object self) 1) self)
                        (set-symbol temp-anim (get-object-anim (get-symbol active-object self) 1) self)
                        (set-boolean is-attached 1 self)
                    
                
            }
        }
        block event attach-to-object {
            track main {
                lambda 
                    (if (entity-exists? (event-arg-symbol 0))) 
                        (set-symbol active-object (event-arg-symbol 0) self)
                        (set-symbol curr-anim (get-object-anim (get-symbol active-object self) 1) self)
                        (set-symbol temp-anim (get-object-anim (get-symbol active-object self) 1) self)
                        (set-boolean is-attached 1 self)
                    
                
            }
        }
        block event add-expected-anim {
            track main {
                lambda 
                    (ddict-set (get-symbol expected-anim-ddict self) (event-arg-symbol 0) (new-boxed-value 1 1))
                
            }
        }
        block event remove-expected-anim {
            track main {
                lambda 
                    (if (ddict-contains-key? (get-symbol expected-anim-ddict self) (event-arg-symbol 0))) 
                        (ddict-set (get-symbol expected-anim-ddict self) (event-arg-symbol 0) (new-boxed-value 0 0))
                    
                
            }
        }
        block event clear-expected-anims {
            track main {
                lambda 
                    (ddict-clear (get-symbol expected-anim-ddict self))
                
            }
        }
        block event add-break-anim {
            track main {
                lambda 
                    (ddict-set (get-symbol break-anim-ddict self) (event-arg-symbol 0) (new-boxed-value 1 1))
                
            }
        }
        block event remove-break-anim {
            track main {
                lambda 
                    (if (ddict-contains-key? (get-symbol break-anim-ddict self) (event-arg-symbol 0))) 
                        (ddict-set (get-symbol break-anim-ddict self) (event-arg-symbol 0) (new-boxed-value 0 0))
                    
                
            }
        }
        block event clear-break-anims {
            track main {
                lambda 
                    (ddict-clear (get-symbol break-anim-ddict self))
                
            }
        }
        block event detach {
            track main {
                lambda 
                    (go initialized 1)
                
            }
        }
    }
}
