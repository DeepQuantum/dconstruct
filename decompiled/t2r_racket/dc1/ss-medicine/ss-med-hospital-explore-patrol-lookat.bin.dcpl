; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-medicine\ss-med-hospital-explore-patrol-lookat.bin   #
; #   filesize: 3466 bytes                                                                           #
; #   total entries: 1                                                                               #
; #   functions: 5                                                                                   #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
    }
    state task-check {
        block start {
            track main {
                lambda 
                    (if (task-complete? medicine med-hospital-explore)) 
                        (go already-complete 1)
                    
                
                lambda 
                    (if (task-active? medicine med-hospital-explore)) 
                        (go look-at 1)
                    
                
            }
        }
        block event task-complete {
            track main {
                lambda 
                    (if (task-active? medicine med-hospital-explore)) 
                        (go look-at 1)
                    
                
            }
        }
    }
    state look-at {
        block start {
            track main {
                lambda 
                    (let loop () (when 1 
                        (let loop () (when (and (and (distance-between-objects-less-than? self player 3.00) (is-in-front? (get-object-locator self) (get-object-locator player) 2 160.00)) (not (is-in-front? (get-object-locator player) (get-object-locator self) 2 180.00))) 
                            (wait 0.50)
                         (loop)))
                        (define var_14 (is-in-front? (get-object-locator player) (get-object-locator self) 2 180.00))
                        (let loop () (when (and (or (or (and (distance-between-objects-less-than? self player 3.00) (is-in-front? (get-object-locator self) (get-object-locator player) 2 160.00)) var_14) (! var_14)) (not (> (- (get-current-time 0) (get-current-time 0)) (random-float-range 2.00 4.00 0)))) 
                            (look-at-object self player r_eyeball 0.30)
                            (wait 0.20)
                         (loop)))
                        (wait-random 2.50 4.00)
                     (loop)))
                
            }
        }
    }
    state already-complete {
        block start {
            track main {
                lambda 
                    (go complete 1)
                
            }
        }
    }
}
