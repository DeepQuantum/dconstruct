; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-medicine\ss-med-flooded-street-overindulgent-brush.bin#
; #   filesize: 7041 bytes                                                                           #
; #   total entries: 1                                                                               #
; #   functions: 21                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bool player-entering? = nullptr
        bool player-exiting? = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda 
                    (if (task-complete? medicine med-flooded-street-highway)) 
                        (go complete 1)
                    
                
                lambda 
                    (if (task-active? medicine med-flooded-street-swim)) 
                        (go wait-for-player 1)
                    
                
            }
        }
        block event task-complete {
            track main {
                lambda 
                    (if (task-active? medicine med-flooded-street-swim)) 
                        (go wait-for-player 1)
                    
                
            }
        }
    }
    state wait-for-player {
        block update {
            track main {
                lambda 
                    (if (< (get-locator-distance (get-object-locator player) (get-spawner-locator ss-med-flooded-street-overindulgent-brush-1 1)) 7.00)) 
                        (if (player-in-region? rgn-med-flooded-street-brush-enter-1 2)) 
                            (go wait-for-enter 1)
                         else (if (player-in-region? rgn-med-flooded-street-brush-exit-1 2)) 
                            (go wait-for-exit 1)
                        
                    
                
            }
        }
    }
    state wait-for-enter {
        block start {
            track main {
                lambda 
                    (let loop () (when (not (player-in-region? rgn-med-flooded-street-brush-enter-2 2)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (define var_0 (alloc-gesture-play-params))
                    (set! (deref (cast u64* (+ var_0 24))) (get-spawner-position pos-med-flooded-street-brush-enter-1 1))
                    (set! (deref (cast i32* (+ var_0 36))) 12)
                    (set! (deref (cast u8* (+ var_0 16))) 1)
                    (set! (deref (cast u8* (+ var_0 34))) 1)
                    (gesture_ player ellie-uneasy-raised-point-left-arm-gesture var_0)
                
                lambda 
                    (let loop () (when (not (not (player-in-region? rgn-med-flooded-street-brush-enter-2 2))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (clear-gesture player -1 -1.00 0)
                
                lambda 
                    (let loop () (when (and (and (not (player-in-region? rgn-med-flooded-street-brush-enter-1 2)) (not (player-in-region? rgn-med-flooded-street-brush-exit-1 2))) (not (not (player-in-region? rgn-med-flooded-street-brush-exit-2 2)))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go wait-for-player 1)
                
            }
        }
    }
    state wait-for-exit {
        block start {
            track main {
                lambda 
                    (let loop () (when (not (player-in-region? rgn-med-flooded-street-brush-exit-2 2)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (define var_0 (alloc-gesture-play-params))
                    (set! (deref (cast u64* (+ var_0 24))) (get-spawner-position pos-med-flooded-street-brush-exit-1 1))
                    (set! (deref (cast i32* (+ var_0 36))) 12)
                    (set! (deref (cast u8* (+ var_0 16))) 1)
                    (set! (deref (cast u8* (+ var_0 34))) 1)
                    (gesture_ player ellie-uneasy-raised-point-left-arm-gesture var_0)
                
                lambda 
                    (let loop () (when (not (not (player-in-region? rgn-med-flooded-street-brush-exit-2 2))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (clear-gesture player -1 -1.00 0)
                
                lambda 
                    (let loop () (when (and (and (not (player-in-region? rgn-med-flooded-street-brush-enter-1 2)) (not (player-in-region? rgn-med-flooded-street-brush-enter-2 2))) (not (not (player-in-region? rgn-med-flooded-street-brush-exit-1 2)))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go wait-for-player 1)
                
            }
        }
    }
    state player-brush {
        block start {
            track main {
                lambda 
                    (define var_0 (alloc-gesture-play-params))
                    (define var_2)
                    (if (get-boolean player-entering? self)) 
                        (set! var_2 (get-spawner-position pos-med-flooded-street-brush-2 1))
                     else 
                        (set! var_2 (get-spawner-position pos-med-flooded-street-brush-1 1))
                    
                    (set! (deref (cast u64* (+ var_0 24))) var_2)
                    (set! (deref (cast i32* (+ var_0 36))) 12)
                    (set! (deref (cast u8* (+ var_0 16))) 1)
                    (set! (deref (cast u8* (+ var_0 34))) 1)
                    (gesture_ player ellie-uneasy-raised-point-left-arm-gesture var_0)
                
                lambda 
                    (let loop () (when (not (not (player-in-region? rgn-med-flooded-street-brush-1 2))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (clear-gesture player -1 -1.00 0)
                
                lambda 
                    (wait 2.00)
                
                lambda 
                    (go wait-for-player 1)
                
            }
        }
    }
}
