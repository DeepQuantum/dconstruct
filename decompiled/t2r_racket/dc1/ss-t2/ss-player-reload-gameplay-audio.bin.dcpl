; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-t2\ss-player-reload-gameplay-audio.bin               #
; #   filesize: 5895 bytes                                                                           #
; #   total entries: 2                                                                               #
; #   functions: 18                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (ss-player-reload-gameplay-audio--should-play-event?) 
    (&& (get-boolean manual-reload self) (!= (get-symbol weapon-id self) bow-ellie))
)

statescript {
    options {
        player
    }
    declarations {
        symbol weapon-id = nullptr
        symbol weapon-instance = nullptr
        bool manual-reload = nullptr
    }
    state initial {
        block start {
            track main {
                lambda 
                    (if (== (get-difficulty-setting-stealth) 5)) 
                        (go wait-for-reload 1)
                    
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (and (nth-frame? 15 0) (== (get-difficulty-setting-stealth) 5))) 
                        (go wait-for-reload 1)
                    
                
            }
        }
        block event options-menu-changed {
            track main {
                lambda 
                    (if (== (get-difficulty-setting-stealth) 5)) 
                        (go wait-for-reload 1)
                    
                
            }
        }
    }
    state wait-for-reload {
        block event player-start-reload {
            track main {
                lambda 
                    (set-symbol weapon-id (event-arg-symbol 0) self)
                
                lambda 
                    (set-symbol weapon-instance (event-arg-symbol 1) self)
                
                lambda 
                    (set-boolean manual-reload (event-arg-boolean 2) self)
                
                lambda 
                    (if (ss-player-reload-gameplay-audio--should-play-event?)) 
                        (go trigger-gameplay-audio-event 1)
                    
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (and (nth-frame? 15 0) (!= (get-difficulty-setting-stealth) 5))) 
                        (go initial 1)
                    
                
            }
        }
        block event options-menu-changed {
            track main {
                lambda 
                    (if (!= (get-difficulty-setting-stealth) 5)) 
                        (go initial 1)
                    
                
            }
        }
    }
    state trigger-gameplay-audio-event {
        block start {
            track main {
                lambda 
                    (audio-gameplay-event weapon-reload (get-object-position (get-symbol weapon-instance self)) (get-symbol weapon-instance self))
                    (wait-one-frame)
                    (let loop () (when (player-is-reloading?) 
                        (audio-gameplay-event weapon-reload (get-object-position (get-symbol weapon-instance self)) (get-symbol weapon-instance self))
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go wait-for-reload 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (and (nth-frame? 15 0) (!= (get-difficulty-setting-stealth) 5))) 
                        (go initial 1)
                    
                
            }
        }
        block event player-start-reload {
            track main {
                lambda 
                    (set-symbol weapon-id (event-arg-symbol 0) self)
                
                lambda 
                    (set-symbol weapon-instance (event-arg-symbol 1) self)
                
                lambda 
                    (set-boolean manual-reload (event-arg-boolean 2) self)
                
                lambda 
                    (if (get-boolean manual-reload self)) 
                        (go trigger-gameplay-audio-event 1)
                    
                
            }
        }
        block event options-menu-changed {
            track main {
                lambda 
                    (if (!= (get-difficulty-setting-stealth) 5)) 
                        (go initial 1)
                    
                
            }
        }
    }
}
