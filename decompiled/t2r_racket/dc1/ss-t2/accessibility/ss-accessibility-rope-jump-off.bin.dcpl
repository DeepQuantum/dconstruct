; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-t2\accessibility\ss-accessibility-rope-jump-off.bin  #
; #   filesize: 5046 bytes                                                                           #
; #   total entries: 1                                                                               #
; #   functions: 9                                                                                   #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
        sp-all
    }
    declarations {
        bool cancel-jump = nullptr
    }
    state inactive {
        block start {
            track main {
                lambda 
                    (if (and (is-traversal-audio-cues?) (is-auto-traversal-accessibility?))) 
                        (go wait-for-enter 1)
                    
                
            }
        }
        block event options-menu-changed {
            track main {
                lambda 
                    (if (and (is-traversal-audio-cues?) (is-auto-traversal-accessibility?))) 
                        (go wait-for-enter 1)
                    
                
            }
        }
    }
    state wait-for-enter {
        block start {
            track main {
                lambda 
                    (if (player-in-region? self 2)) 
                        (go jump-prompt 1)
                    
                
            }
        }
        block event enter {
            track main {
                lambda 
                    (go jump-prompt 1)
                
            }
        }
        block event options-menu-changed {
            track main {
                lambda 
                    (if (and (is-traversal-audio-cues?) (not (is-auto-traversal-accessibility?)))) 
                        (go inactive 1)
                    
                
            }
        }
    }
    state jump-prompt {
        block start {
            track main {
                lambda 
                    (let loop () (when (player-in-region? self 2) 
                        (if (and (and (and (is-traversal-audio-cues?) (player-in-state? RopeClimb)) (joypad-command-enabled? jump-accessibility)) (not (tag-boolean no-audio-cue self)))) 
                            (play-accessibility-jump-sound-at-position (get-spawner-position (tag-symbol jump-target self) 1) 0 0)
                        
                        (wait-frames 60)
                     (loop)))
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (and (or (player-in-state? RopeClimb) (is-traversal-audio-cues?)) (is-auto-traversal-accessibility?))) 
                        (if (tag-boolean edge-clamber-target self)) 
                            (set-player-clamber-spawner (tag-symbol jump-target self))
                         else 
                            (define var_7)
                            (if (tag-boolean edge-grab-target self)) 
                                (set! var_7 1)
                             else 
                                (set! var_7 2)
                            
                            (set-player-jump-spawner (tag-symbol jump-target self) var_7)
                        
                        (player-force-move-stick-dir (get-z-vec-from-bound-frame (get-spawner-locator (tag-symbol jump-target self) 1)))
                        (if (is-auto-traversal-accessibility?)) 
                            (joypad-force-command rope-jump)
                        
                    
                
            }
        }
        block event exit {
            track main {
                lambda 
                    (go wait-for-enter 1)
                
            }
        }
        block event options-menu-changed {
            track main {
                lambda 
                    (if (and (is-traversal-audio-cues?) (not (is-auto-traversal-accessibility?)))) 
                        (go inactive 1)
                    
                
            }
        }
    }
}
