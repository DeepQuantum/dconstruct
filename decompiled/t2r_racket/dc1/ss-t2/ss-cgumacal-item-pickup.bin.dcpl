; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-t2\ss-cgumacal-item-pickup.bin                       #
; #   filesize: 25822 bytes                                                                          #
; #   total entries: 21                                                                              #
; #   functions: 42                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
        cgumacal-item-pickup
    }
    declarations {
        f32 count = 0.00
        f32 original-group-count = 0.00
        symbol to-delete = none
    }
    state setup {
        block start {
            track main {
                lambda 
                    (print-int32 (darray-count all-pickups) self)
                
                lambda 
                    (for ([var_1 all-pickups]) 
                        (define var_2)
                        (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 7))) 
                            (set! var_2 (deref (cast u64* var_1)))
                         else (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 5))) 
                            (set! var_2 (deref (cast u64* var_1)))
                         else (if (and var_1 (== (deref (cast u16* (+ var_1 12))) 4))) 
                            (set! var_2 (deref (cast u64* var_1)))
                         else 
                            (set! var_2 0)
                        
                        (if (== (item-pickup-get-item-id var_2) alcohol)) 
                            (darray-append alcohol (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) rag)) 
                            (darray-append rag (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) explosive)) 
                            (darray-append explosive (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) canister)) 
                            (darray-append canister (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) blade)) 
                            (darray-append blade (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) binding)) 
                            (darray-append binding (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-pistol)) 
                            (darray-append ammo-pistol (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-revolver)) 
                            (darray-append ammo-revolver (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-xcaliber)) 
                            (darray-append ammo-xcaliber (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-xcaliber-shot-shell)) 
                            (darray-append ammo-xcaliber-shot-shell (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-rifle)) 
                            (darray-append ammo-rifle (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-rifle-vepr)) 
                            (darray-append ammo-rifle-vepr (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-rifle-galil)) 
                            (darray-append ammo-rifle-galil (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-shotgun)) 
                            (darray-append ammo-shotgun (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-shotgun-dragons-breath)) 
                            (darray-append ammo-shotgun-dragons-breath (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-arrow)) 
                            (darray-append ammo-arrow (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-crossbow)) 
                            (darray-append ammo-crossbow (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-arrow-explosive)) 
                            (darray-append ammo-arrow-explosive (new-boxed-value 7 var_2) 1)
                        
                        (if (== (item-pickup-get-item-id var_2) ammo-flamethrower)) 
                            (darray-append ammo-flamethrower (new-boxed-value 7 var_2) 1)
                        
                    )
                
                lambda 
                    (go delete 1)
                
            }
        }
    }
    state delete {
        block start {
            track main {
                lambda 
                    (if (not (is-final-build?))) 
                        (display (dc:format "delete started") 19)
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count alcohol)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count alcohol)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random alcohol) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove alcohol (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count rag)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count rag)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random rag) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove rag (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count explosive)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count explosive)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random explosive) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove explosive (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count canister)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count canister)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random canister) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove canister (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count blade)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count blade)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random blade) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove blade (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count binding)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count binding)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random binding) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove binding (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-pistol)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-pistol)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-pistol) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-pistol (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-revolver)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-revolver)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-revolver) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-revolver (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-xcaliber)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-xcaliber)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-xcaliber) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-xcaliber (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-xcaliber-shot-shell)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-xcaliber-shot-shell)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-xcaliber-shot-shell) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-xcaliber-shot-shell (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-rifle)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-rifle)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-rifle) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-rifle (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-rifle-vepr)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-rifle-vepr)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-rifle-vepr) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-rifle-vepr (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-rifle-galil)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-rifle-galil)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-rifle-galil) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-rifle-galil (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-shotgun)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-shotgun)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-shotgun) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-shotgun (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-shotgun-dragons-breath)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-shotgun-dragons-breath)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-shotgun-dragons-breath) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-shotgun-dragons-breath (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-arrow)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-arrow)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-arrow) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-arrow (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-crossbow)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-crossbow)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-crossbow) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-crossbow (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-arrow-explosive)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-arrow-explosive)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-arrow-explosive) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-arrow-explosive (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
                lambda 
                    (set-float original-group-count (cast f32 (darray-count ammo-flamethrower)) self)
                
                lambda 
                    (if (not (== (get-float original-group-count self) 0.00))) 
                        (let loop () (when (< 0.50 (/ (cast f32 (darray-count ammo-flamethrower)) (get-float original-group-count self))) 
                            (set-symbol to-delete (group-random ammo-flamethrower) self)
                            (kill-entity (get-symbol to-delete self))
                            (darray-find-remove ammo-flamethrower (new-boxed-value 7 (get-symbol to-delete self)) 0)
                         (loop)))
                    
                
            }
        }
    }
}
