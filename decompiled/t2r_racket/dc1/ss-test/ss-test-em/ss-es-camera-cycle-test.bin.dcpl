; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-test\ss-test-em\ss-es-camera-cycle-test.bin          #
; #   filesize: 3291 bytes                                                                           #
; #   total entries: 1                                                                               #
; #   functions: 2                                                                                   #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bool test-mode-on = nullptr
    }
    state default {
        block start {
            track main {
                lambda 
                    (go test 1)
                
            }
        }
    }
    state test {
        block start {
            track main {
                lambda 
                    (let loop () (when 1 
                        (script-caption player "default - 24mm" 1.00 0 0)
                        (let loop () (when (! (joypad-command-active? interact 6)) 
                            (wait-one-frame)
                         (loop)))
                        (wait 0.25)
                        (script-caption player "26mm" 1.00 0 0)
                        (let loop () (when (! (joypad-command-active? interact 6)) 
                            (wait-one-frame)
                         (loop)))
                        (wait 0.25)
                        (script-caption player "28mm" 1.00 0 0)
                        (let loop () (when (! (joypad-command-active? interact 6)) 
                            (wait-one-frame)
                         (loop)))
                        (wait 0.25)
                        (script-caption player "35mm" 1.00 0 0)
                        (let loop () (when (! (joypad-command-active? interact 6)) 
                            (wait-one-frame)
                         (loop)))
                        (wait 0.25)
                        (script-caption player "28mm" 1.00 0 0)
                        (let loop () (when (! (joypad-command-active? interact 6)) 
                            (wait-one-frame)
                         (loop)))
                        (wait 0.25)
                        (script-caption player "26mm" 1.00 0 0)
                        (let loop () (when (! (joypad-command-active? interact 6)) 
                            (wait-one-frame)
                         (loop)))
                        (wait 0.25)
                        (script-caption player "default - 24mm" 1.00 0 0)
                        (let loop () (when (! (joypad-command-active? interact 6)) 
                            (wait-one-frame)
                         (loop)))
                        (wait 0.25)
                        (script-caption player "20mm" 1.00 0 0)
                        (let loop () (when (! (joypad-command-active? interact 6)) 
                            (wait-one-frame)
                         (loop)))
                        (wait 0.25)
                        (wait-one-frame)
                     (loop)))
                
            }
        }
    }
}
