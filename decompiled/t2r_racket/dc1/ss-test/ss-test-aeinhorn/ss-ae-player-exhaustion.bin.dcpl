; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-test\ss-test-aeinhorn\ss-ae-player-exhaustion.bin    #
; #   filesize: 5302 bytes                                                                           #
; #   total entries: 2                                                                               #
; #   functions: 27                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (player-exhaustion-should-be-active) 
    (&& (deref (cast u8* (+ *player-prototypes* 43))) (> (get-float fatigue ss-ae-player-exhaustion) 500.00))
)

statescript {
    options {
        player
    }
    state inactive {
        block start {
            track main {
                lambda 
                    (if (deref (cast u8* (+ *player-prototypes* 43)))) 
                        (go no-gesture 1)
                    
                
            }
        }
        block event designer-menu-changed {
            track main {
                lambda 
                    (if (deref (cast u8* (+ *player-prototypes* 43)))) 
                        (go no-gesture 1)
                    
                
            }
        }
    }
    state no-gesture {
        block start {
            track aiming {
                lambda 
                    (let loop () (when (not (player-in-state? GunMove)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go aiming 1)
                
            }
            track walking {
                lambda 
                    (let loop () (when (not (player-in-state? Move)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go walking 1)
                
            }
            track main {
                lambda 
                    (let loop () (when (not (not (deref (cast u8* (+ *player-prototypes* 43))))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go clean-up 1)
                
            }
        }
    }
    state walking {
        block start {
            track main {
                lambda 
                    (let loop () (when (not (not (deref (cast u8* (+ *player-prototypes* 43))))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go clean-up 1)
                
            }
            track move {
                lambda 
                    (let loop () (when (not (player-in-state? GunMove)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (wait-one-frame)
                
                lambda 
                    (go aiming 1)
                
            }
            track gesture {
                lambda 
                    (define var_0 (alloc-gesture-play-params))
                    (set! (deref (cast u64* (+ var_0 8))) 0)
                    (set! (deref (cast i32* (+ var_0 36))) 29)
                    (set! (deref (cast u8* (+ var_0 2))) 1)
                    (set! (deref (cast u8* (+ var_0 34))) 1)
                    (gesture_ player ellie-breathing-exhausted-gesture var_0)
                
            }
            track stop-playing {
                lambda 
                    (let loop () (when (and (or (or (and (or (and (or (not (player-in-state? GunMove)) (not (player-in-state? Move))) (joypad-command-active? sprint-hold 6)) 1) (joypad-command-active? sprint-hold-long 6)) 1) 0) (not 0)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go no-gesture 1)
                
            }
        }
    }
    state aiming {
        block start {
            track main {
                lambda 
                    (let loop () (when (not (not (deref (cast u8* (+ *player-prototypes* 43))))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go clean-up 1)
                
            }
            track move {
                lambda 
                    (let loop () (when (not (player-in-state? Move)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (wait-one-frame)
                
                lambda 
                    (go walking 1)
                
            }
            track gesture {
                lambda 
                    (define var_0 (alloc-gesture-play-params))
                    (set! (deref (cast u64* (+ var_0 8))) 0)
                    (set! (deref (cast i32* (+ var_0 36))) 29)
                    (set! (deref (cast u8* (+ var_0 2))) 1)
                    (set! (deref (cast u8* (+ var_0 34))) 1)
                    (gesture_ player ellie-breathing-exhausted-gesture var_0)
                
            }
            track stop-playing {
                lambda 
                    (let loop () (when (and (or (or (and (or (and (or (not (player-in-state? GunMove)) (not (player-in-state? Move))) (joypad-command-active? sprint-hold 6)) 1) (joypad-command-active? sprint-hold-long 6)) 1) 0) (not 0)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go no-gesture 1)
                
            }
        }
    }
    state clean-up {
        block start {
            track main {
                lambda 
                    (clear-gesture player -1 -1.00 0)
                
                lambda 
                    (go inactive 1)
                
            }
        }
    }
}
