; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-test\ss-test-aeinhorn\ss-ae-buddy-in-position-callout.bin#
; #   filesize: 4725 bytes                                                                           #
; #   total entries: 1                                                                               #
; #   functions: 17                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
    }
    state off {
        block update {
            track main {
                lambda 
                    (if (deref (cast u8* (+ *buddy-prototypes* 14)))) 
                        (go initial 1)
                    
                
            }
        }
    }
    state initial {
        block start {
            track main {
                lambda 
                    (let loop () (when (not (>= (vector-magnitude (get-object-velocity (get-self-id))) 1.00)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go moving 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (deref (cast u8* (+ *buddy-prototypes* 14))))) 
                        (go off 1)
                    
                
            }
        }
    }
    state reset {
        block start {
            track main {
                lambda 
                    (go moving 1)
                
            }
        }
    }
    state moving {
        block start {
            track check-for-still {
                lambda 
                    (let loop () (when (and (<= (vector-magnitude (get-object-velocity (get-self-id))) 0.01) (not (npc-using-skill? (get-self-id) 36))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (signal stopped 0)
                
                lambda 
                    (wait 1.00)
                
                lambda 
                    (go in-position 1)
                
            }
            track check-for-moving {
                lambda 
                    (wait-for stopped)
                
                lambda 
                    (let loop () (when (not (>= (vector-magnitude (get-object-velocity (get-self-id))) 1.00)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go reset 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (deref (cast u8* (+ *buddy-prototypes* 14))))) 
                        (go off 1)
                    
                
            }
        }
    }
    state in-position {
        block start {
            track main {
                lambda 
                    (send-event pulse-highlight (get-self-id))
                
                lambda 
                    (let loop () (when (not (not (== (npc-get-motion-type (get-self-id)) 3))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go moving 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (deref (cast u8* (+ *buddy-prototypes* 14))))) 
                        (go off 1)
                    
                
            }
        }
    }
}
