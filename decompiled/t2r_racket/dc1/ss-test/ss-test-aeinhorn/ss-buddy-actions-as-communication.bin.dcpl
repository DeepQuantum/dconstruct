; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-test\ss-test-aeinhorn\ss-buddy-actions-as-communication.bin#
; #   filesize: 9559 bytes                                                                           #
; #   total entries: 4                                                                               #
; #   functions: 22                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (current-distance-from-centroid) 
    (distance-between-points (get-point conversation-centroid self) (get-object-position player))
)

(define (distance-delta-from-centroid) 
    (print-float (- (current-distance-from-centroid) (get-float distance-to-centroid self)) self)
    (- (current-distance-from-centroid) (get-float distance-to-centroid self))
)

(define (enable-conversation-system) 
    0
)

statescript {
    options {
        player
        sp-all
    }
    declarations {
        point conversation-centroid = nullptr
        f32 distance-to-centroid = nullptr
    }
    state initial {
        block update {
            track main {
                lambda 
                    (if (enable-conversation-system)) 
                        (go waiting 1)
                    
                
            }
        }
    }
    state waiting {
        block start {
            track main {
                lambda 
                    (let loop () (when (not (entity-exists? dina)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (let loop () (when 1 
                        (if (and (npc-on-screen-and-visible? dina -1 0) (< (distance-between-objects player dina) 5.00))) 
                            (go initialise-conversation 1)
                        
                        (wait 5.00)
                     (loop)))
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (enable-conversation-system))) 
                        (go initial 1)
                    
                
            }
        }
    }
    state initialise-conversation {
        block start {
            track main {
                lambda 
                    (define var_0 (darray-create self -1 0))
                    (for ([var_2 all-enemy-npcs]) 
                        (define var_3)
                        (if (and var_2 (== (deref (cast u16* (+ var_2 12))) 7))) 
                            (set! var_3 (deref (cast u64* var_2)))
                         else (if (and var_2 (== (deref (cast u16* (+ var_2 12))) 5))) 
                            (set! var_3 (deref (cast u64* var_2)))
                         else (if (and var_2 (== (deref (cast u16* (+ var_2 12))) 4))) 
                            (set! var_3 (deref (cast u64* var_2)))
                         else 
                            (set! var_3 0)
                        
                        (if (and (< (vector-magnitude (vector-flatten (vector-between-objects var_3 player) 1)) 15.00) (< (distance-y-between-objects var_3 player) 3.00))) 
                            (darray-append var_0 (new-boxed-value 7 var_3) 1)
                        
                    )
                    (set-point conversation-centroid (get-group-centroid var_0) self)
                    (set-float distance-to-centroid (current-distance-from-centroid) self)
                    (darray-destroy var_0)
                
                lambda 
                    (debug-draw-sphere (get-point conversation-centroid self) 0.50 1 5.00 0 0)
                
                lambda 
                    (go conversation-thing 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (enable-conversation-system))) 
                        (go initial 1)
                    
                
            }
        }
    }
    state conversation-thing {
        block start {
            track main {
                lambda 
                    (script-caption dina "OK, what are we gonna do about these guys?" -1.00 0 0)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (enable-conversation-system))) 
                        (go initial 1)
                    
                
                lambda 
                    (if (> (distance-delta-from-centroid) 2.00)) 
                        (go walked-away 1)
                     else (if (< (distance-delta-from-centroid) -2.00)) 
                        (go snuck-forward 1)
                     else (if (tension-mode-is? 3)) 
                        (go attacked-enemies 1)
                     else (if (> (get-state-time self) 12.00)) 
                        (go timeout 1)
                    
                
            }
        }
    }
    state walked-away {
        block start {
            track main {
                lambda 
                    (script-caption dina "OK yeah, let's back off." -1.00 0 0)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (enable-conversation-system))) 
                        (go initial 1)
                    
                
            }
        }
    }
    state snuck-forward {
        block start {
            track main {
                lambda 
                    (script-caption dina "OK, I'll try and take one out quietly." -1.00 0 0)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (enable-conversation-system))) 
                        (go initial 1)
                    
                
            }
        }
    }
    state attacked-enemies {
        block start {
            track main {
                lambda 
                    (script-caption dina "Arghhh." -1.00 0 0)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (enable-conversation-system))) 
                        (go initial 1)
                    
                
            }
        }
    }
    state timeout {
        block start {
            track main {
                lambda 
                    (script-caption dina "OK... let's wait it out..." -1.00 0 0)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (enable-conversation-system))) 
                        (go initial 1)
                    
                
            }
        }
    }
}
