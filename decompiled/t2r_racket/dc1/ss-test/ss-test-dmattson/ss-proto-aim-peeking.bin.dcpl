; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-test\ss-test-dmattson\ss-proto-aim-peeking.bin       #
; #   filesize: 7833 bytes                                                                           #
; #   total entries: 1                                                                               #
; #   functions: 35                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bool is-shoulder-swapped? = nullptr
    }
    state initial {
        block start {
            track main {
                lambda 
                    (if (deref (cast u8* (+ *player-prototypes* 14)))) 
                        (go waiting 1)
                    
                
                lambda 
                    (go disabled 1)
                
            }
        }
    }
    state waiting {
        block start {
            track main {
                lambda 
                    (if (get-boolean is-shoulder-swapped? self)) 
                        (set-boolean is-shoulder-swapped? 0 self)
                    
                
                lambda 
                    (let loop () (when (not (player-in-state? GunMove)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go waiting-for-peek 1)
                
            }
        }
    }
    state waiting-for-peek {
        block start {
            track main {
                lambda 
                    (draw-reticle)
                
                lambda 
                    (let loop () (when (player-in-state? GunMove) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (go waiting 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (joypad-command-active? peek-right 6)) 
                        (go peek-right 1)
                     else (if (joypad-command-active? peek-left 6)) 
                        (go peek-left 1)
                    
                
                lambda 
                    (joypad-disable-commands shoulder-switch sprint-press sprint-release sprint-hold-long sprint-hold crouch-press crouch-release prone-toggle)
                
            }
        }
    }
    state peek-right {
        block start {
            track main {
                lambda 
                    (set-overlay-set player anim-overlay-player-lean-aim-right)
                
                lambda 
                    (draw-reticle)
                
                lambda 
                    (force-refresh-player-anims 1.00)
                
                lambda 
                    (if (get-boolean is-shoulder-swapped? self)) 
                        (joypad-force-command shoulder-switch)
                        (set-boolean is-shoulder-swapped? 0 self)
                    
                
                lambda 
                    (wait-one-frame)
                
                lambda 
                    (let loop () (when (and (joypad-command-active? peek-right 6) (player-in-state? GunMove)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (if (player-in-state? GunMove)) 
                        (go waiting-for-peek 1)
                    
                
                lambda 
                    (go waiting 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (joypad-disable-commands shoulder-switch sprint-press sprint-release sprint-hold-long sprint-hold crouch-press crouch-release prone-toggle)
                
                lambda 
                    (camera-set-overlay *camera-remap-proto-peek* 1 2.00 2 2.00)
                
            }
        }
        block end {
            track main {
                lambda 
                    (set-overlay-set player anim-overlay-player-lean-aim-default)
                
                lambda 
                    (force-refresh-player-anims 0.50)
                
            }
        }
    }
    state peek-left {
        block start {
            track main {
                lambda 
                    (draw-reticle)
                
                lambda 
                    (set-overlay-set player anim-overlay-player-lean-aim-left)
                
                lambda 
                    (force-refresh-player-anims 1.00)
                
                lambda 
                    (if (not (get-boolean is-shoulder-swapped? self))) 
                        (joypad-force-command shoulder-switch)
                        (set-boolean is-shoulder-swapped? 1 self)
                    
                
                lambda 
                    (wait-one-frame)
                
                lambda 
                    (let loop () (when (and (joypad-command-active? peek-left 6) (player-in-state? GunMove)) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (if (player-in-state? GunMove)) 
                        (go waiting-for-peek 1)
                    
                
                lambda 
                    (go waiting 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (joypad-disable-commands shoulder-switch sprint-press sprint-release sprint-hold-long sprint-hold crouch-press crouch-release prone-toggle)
                
                lambda 
                    (camera-set-overlay *camera-remap-proto-peek* 1 2.00 2 2.00)
                
            }
        }
        block end {
            track main {
                lambda 
                    (set-overlay-set player anim-overlay-player-lean-aim-default)
                
                lambda 
                    (force-refresh-player-anims 0.50)
                
            }
        }
    }
    state disabled {
        block event designer-menu-changed {
            track main {
                lambda 
                    (if (deref (cast u8* (+ *player-prototypes* 14)))) 
                        (go waiting 1)
                    
                
            }
        }
    }
}
