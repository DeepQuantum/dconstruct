; ####################################################################################################
; #                                                                                                  #
; #   generated by dconstruct ver. 1.52.0                                                            #
; #   listing for file: .\game_dumps\t2r\dc1\ss-test\ss-test-mgallant\ss-proto-cavalry-passenger.bin #
; #   filesize: 5745 bytes                                                                           #
; #   total entries: 2                                                                               #
; #   functions: 15                                                                                  #
; #   has state script: true                                                                         #
; #   game type: t2r                                                                                 #
; #   optimizations enabled: true                                                                    #
; #                                                                                                  #
; ####################################################################################################

(define (cavalry-passenger-can-dismount?) 
    (&& (== (tag-symbol dismount-region self) 0) (object-in-region? self (tag-symbol dismount-region self)))
)

statescript {
    options {
        player
        sp-all
    }
    declarations {
    }
    state setup {
        block start {
            track main {
                lambda 
                    (wait-one-frame)
                
                lambda 
                    (let loop () (when (not (object-spawned? (tag-symbol horse self))) 
                        (wait-one-frame)
                     (loop)))
                
                lambda 
                    (wait-one-frame)
                
                lambda 
                    (npc-force-on-horse self (tag-symbol horse self) 1)
                
                lambda 
                    (wait-one-frame)
                
                lambda 
                    (go wait-for-dismount 1)
                
            }
        }
    }
    state wait-for-dismount {
        block update {
            track main {
                lambda 
                    (if (not (npc-alive? self))) 
                        (go dead 1)
                    
                
                lambda 
                    (if (deterministic-nth-frame? 30)) 
                        (if (and (and (and (npc-is-aware-of? self player) (not (npc-target-lost? self player))) (< (get-locator-distance (get-object-locator self) (alloc-bound-frame (npc-get-entity-position self player) 0 0 0 0)) 25.00)) (cavalry-passenger-can-dismount?))) 
                            (set! var_8 (go dismount 1))
                        
                    
                
            }
        }
        block event Attack {
            track main {
                lambda 
                    (if (and (and (npc-alive? self) (event-attack-source? player)) (cavalry-passenger-can-dismount?))) 
                        (go dismount 1)
                    
                
            }
        }
        block event npc-died {
            track main {
                lambda 
                    (if (and (== (event-arg-symbol 0) (tag-symbol rider self)) (cavalry-passenger-can-dismount?))) 
                        (go dismount 1)
                     else (if (== (event-arg-symbol 0) (get-self-id))) 
                        (go dead 1)
                    
                
            }
        }
        block event horse-died {
            track main {
                lambda 
                    (if (== (event-arg-symbol 0) (tag-symbol horse self))) 
                        (npc-die-if-alive self)
                        (go dead 1)
                    
                
            }
        }
    }
    state dismount {
        block start {
            track main {
                lambda 
                    (script-caption self "Dismounting!" 3.00 0 0)
                
                lambda 
                    (npc-force-off-horse self 1)
                
            }
        }
        block update {
            track main {
                lambda 
                    (if (not (npc-alive? self))) 
                        (go dead 1)
                    
                
            }
        }
    }
}
