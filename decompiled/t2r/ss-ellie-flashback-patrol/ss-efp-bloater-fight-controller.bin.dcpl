// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...packed/dc1\ss-ellie-flashback-patrol\ss-efp-bloater-fight-controller.bin  #
// #   filesize: 13711 bytes                                                                          #
// #   total entries: 1                                                                               #
// #   functions: 35                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        efp-bloater-fight-script
    }
    declarations {
        f32 allow-kill-igc-health-threshold = 320.00
        f32 force-kill-igc-health-threshold = 60.00
        bool bloater-using-no-damage-weapon? = nullptr
        bool allow-kill? = nullptr
        bool force-kill? = nullptr
        f32 task-began-timestamp = 0.00
        f32 time-in-fight = 0.00
        f32 total-time-in-fight = 0.00
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-played-from-dev-menu?()) {
                        set-svar(seconds-since-task-start, 0);
                    }
                }
                lambda {
                    if (task-complete?(ellie-flashback-patrol, efp-bloater-fight)) {
                        go(complete, 1);
                    }
                }
                lambda {
                    if (task-active?(ellie-flashback-patrol, efp-bloater-fight)) {
                        if (is-new-game-plus?() && get-difficulty-setting-player() == 3 && get-player-health() < 25) {
                            set-player-health(25);
                        }
                        go(setup, 1);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-active?(ellie-flashback-patrol, efp-bloater-fight)) {
                        set-svar(seconds-since-task-start, 0);
                        go(setup, 1);
                    }
                }
            }
        }
    }
    state setup {
        block start {
            track main {
                lambda {
                    u64? var_0 = get-difficulty-detail-enemies();
                    +=f(allow-kill-igc-health-threshold, *(f32 *)(var_0 + 284), self);
                    +=f(force-kill-igc-health-threshold, *(f32 *)(var_0 + 284), self);
                }
                lambda {
                    set-accessibility-pathfind-unavailable();
                }
                lambda {
                    set-float(task-began-timestamp, current-time(1) - (f32)get-svar(seconds-since-task-start), self);
                }
                lambda {
                    if (!object-spawned?(npc-efp-bloater-1)) {
                        spawn-entity(npc-efp-bloater-1, 0, 0);
                    }
                }
                lambda {
                    character-set-invincibility(npc-efp-bloater-1, 1, 0);
                }
                lambda {
                    npc-disable-skill(npc-efp-bloater-1, 16);
                }
                lambda {
                    melee-fact-set-bool(npc-efp-bloater-1, force-disable-throw?, 1);
                }
                lambda {
                    melee-fact-set-bool(npc-efp-bloater-1, bloater-disable-charge, 1);
                }
                lambda {
                    if (task-continue-active?(ellie-flashback-patrol, efp-bloater-fight, default)) {
                        go(wait-reach-health-threshold, 1);
                    } else if (task-continue-active?(ellie-flashback-patrol, efp-bloater-fight, debug-bloater-hit-trigger)) {
                        set-object-health(npc-efp-bloater-1, get-float(allow-kill-igc-health-threshold, self));
                        npc-give-weapon(npc-efp-bloater-1, infected-bloater-unarmed-no-damage, 1, 1);
                        set-boolean(bloater-using-no-damage-weapon?, 1, self);
                        go(wait-reach-health-threshold, 1);
                    } else if (task-continue-active?(ellie-flashback-patrol, efp-bloater-fight, debug-force-grab-trigger)) {
                        set-object-health(npc-efp-bloater-1, get-float(force-kill-igc-health-threshold, self));
                        npc-give-weapon(npc-efp-bloater-1, infected-bloater-unarmed-no-damage, 1, 1);
                        set-boolean(bloater-using-no-damage-weapon?, 1, self);
                        go(wait-reach-health-threshold, 1);
                    }
                }
            }
        }
    }
    state wait-reach-health-threshold {
        block start {
            track main {
                lambda {
                    wait(6.00);
                }
                lambda {
                    melee-fact-set-bool(npc-efp-bloater-1, bloater-disable-charge, 0);
                }
                lambda {
                    melee-fact-set-bool(npc-efp-bloater-1, force-disable-throw?, 0);
                }
                lambda {
                    while (task-continue-active?(ellie-flashback-patrol, efp-bloater-fight, debug-force-grab-trigger) || task-continue-active?(ellie-flashback-patrol, efp-bloater-fight, debug-bloater-hit-trigger) && get-character-health(npc-efp-bloater-1) <= (i32)get-float(allow-kill-igc-health-threshold, self) || get-float(time-in-fight, self) > 65.00 || is-golden-gun-enabled?() || is-golden-melee-enabled?() && get-character-health(npc-efp-bloater-1) <= (i32)get-float(force-kill-igc-health-threshold, self) || get-float(time-in-fight, self) > 50.00 || is-golden-gun-enabled?() || is-golden-melee-enabled?() || get-float(total-time-in-fight, self) > 120.00 && stat-value?(task-death-count) > 2 || get-float(time-in-fight, self) > 40.00 && !(get-float(time-in-fight, self) > 100.00)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    broadcast-event(reached-allow-kill-threshold);
                }
                lambda {
                    set-boolean(allow-kill?, 1, self);
                }
                lambda {
                    f32 var_1;
                    if (1.20 < (1.06 * *(f32 *)(get-difficulty-detail-enemies() + 164))) {
                        var_1 = 1.20;
                    } else {
                        var_1 = 1.06 * *(f32 *)(get-difficulty-detail-enemies() + 164);
                    }
                    send-event(set-bloater-speed-mult, npc-efp-bloater-1, boxed_f32(var_1));
                }
                lambda {
                    send-event(set-target-speed, npc-efp-bloater-1, boxed_f32(1.20), boxed_f32(30.00));
                }
                lambda {
                    melee-fact-set-bool(npc-efp-bloater-1, disable-melee-difficulty-adjustments, 1);
                }
                lambda {
                    melee-fact-set-bool(npc-efp-bloater-1, force-worse-dodges, 1);
                }
                lambda {
                    melee-fact-set-bool(npc-efp-bloater-1, force-disable-throw?, 1);
                }
                lambda {
                    while (task-continue-active?(ellie-flashback-patrol, efp-bloater-fight, debug-force-grab-trigger) && get-character-health(npc-efp-bloater-1) <= (i32)get-float(force-kill-igc-health-threshold, self) || is-golden-gun-enabled?() || is-golden-melee-enabled?() || get-float(time-in-fight, self) > 140.00 && get-float(time-in-fight, self) > 90.00 && efp-bloater-ammo-value() < 90.00 && !(get-float(total-time-in-fight, self) > 120.00)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    broadcast-event(reached-force-kill-threshold);
                }
                lambda {
                    set-boolean(force-kill?, 1, self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!(player-is-accessibility-invisible?()) && get-boolean(allow-kill?, ss-efp-bloater-fight-controller-1)) {
                        npc-make-aware-of(npc-efp-bloater-1, player, 2, 0);
                    } else {
                        npc-make-aware-of-at(npc-efp-bloater-1, player, accessibility-invisible-start-location(), 2, 0, 0);
                    }
                }
                lambda {
                    npc-make-aware-of(npc-efp-bloater-1, joel, 2, 0);
                }
                lambda {
                    override-allow-always-on-threat-indicators/f();
                }
                lambda {
                    set-float(time-in-fight, get-state-time(self), self);
                }
                lambda {
                    set-float(total-time-in-fight, time-elapsed(get-float(task-began-timestamp, self)), self);
                }
                lambda {
                    if (get-boolean(allow-kill?, ss-efp-bloater-fight-controller-1)) {
                        set-sprint-type(slow-sprint-tense);
                    } else {
                        set-sprint-type(sprint-tense);
                    }
                }
            }
        }
        block event_npc-attacked {
            track main {
                lambda {
                    if (event-arg-symbol(1) == npc-efp-bloater-1 && get-boolean(allow-kill?, self) && !(get-boolean(bloater-using-no-damage-weapon?, self))) {
                        npc-give-weapon(npc-efp-bloater-1, infected-bloater-unarmed-no-damage, 1, 1);
                        set-boolean(bloater-using-no-damage-weapon?, 1, self);
                    }
                }
            }
        }
        block event_npc-died {
            track main {
                lambda {
                    if (event-arg-symbol(0) == npc-efp-bloater-1) {
                        broadcast-event(bloater-dead);
                        set-accessibility-pathfind-goal(0);
                        task-complete!(ellie-flashback-patrol, efp-bloater-fight);
                        go(complete, 1);
                    }
                }
            }
        }
    }
}
