// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...ain/bin_unpacked/dc1\ss-abby-ellie-fight\ss-aef-trap-door-controller.bin  #
// #   filesize: 7748 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 14                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        f32 last-trap-door-drop-timestamp = 0.00
        f32 phase-1-desired-time = 120.00
        f32 phase-2-desired-time = 150.00
        f32 phase-1-excessive-time = 240.00
        f32 phase-2-excessive-time = 240.00
        f32 phase-3-excessive-time = 240.00
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(abby-ellie-fight, aef-fight-ellie-hunt-3)) {
                        go(complete, 1);
                    }
                }
                lambda {
                    if (task-range-active?(abby-ellie-fight, aef-fight-ellie-hunt, abby-ellie-fight, aef-fight-ellie-hunt-3)) {
                        go(active, 1);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-active?(abby-ellie-fight, aef-fight-ellie-hunt)) {
                        u64? var_1 = get-trapdoor-level(1);
                        if (var_1 > 0) {
                            set-trapdoor-level(var_1 - 1, 5);
                        }
                        set-svar(timed-trap-door-level, get-trapdoor-level(1));
                        set-svar(cached-difficulty-level, get-difficulty-setting-stealth());
                        set-svar(seconds-since-last-trap-door-drop, 0);
                        set-svar(seconds-since-task-start, 0);
                        go(active, 1);
                    }
                }
            }
        }
    }
    state active {
        block start {
            track main {
                lambda {
                    if (get-svar(cached-difficulty-level) != get-difficulty-setting-stealth()) {
                        set-svar(cached-difficulty-level, get-difficulty-setting-stealth());
                        set-svar(timed-trap-door-level, 0);
                    }
                }
                lambda {
                    set-trapdoor-level(get-svar(timed-trap-door-level), 5);
                }
                lambda {
                    set-float(last-trap-door-drop-timestamp, current-time(1) - (f32)get-svar(seconds-since-last-trap-door-drop), self);
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    set-svar(seconds-since-task-start, get-svar(seconds-since-task-start) + (i32)get-state-time(self));
                }
                lambda {
                    if (task-complete?(abby-ellie-fight, aef-fight-ellie-hunt-3)) {
                        go(complete, 1);
                    }
                }
                lambda {
                    u64? var_0 = event-arg-symbol(0);
                    if (var_0 == aef-fight-ellie-hunt-2 && var_0 == aef-fight-ellie-hunt) {
                        u64? var_4;
                        if (task-complete?(abby-ellie-fight, aef-fight-ellie-hunt-2)) {
                            var_4 = get-float(phase-2-desired-time, self);
                        } else {
                            var_4 = get-float(phase-1-desired-time, self);
                        }
                        u16 var_7;
                        if ((f32)get-svar(seconds-since-task-start) < var_4) {
                            u64? var_8 = get-svar(timed-trap-door-level);
                            u16 var_9;
                            if (var_8 > 0) {
                                set-trapdoor-level(var_8 - 1, 5);
                                set-svar(timed-trap-door-level, get-trapdoor-level(1));
                                if (!is-final-build?()) {
                                    var_9 = display(dc:format("AEF TRAPDOOR CONTROLLER: Reducing trapdoor due to quick progression"), 19);
                                } else {
                                    var_9 = 0;
                                }
                            } else {
                                var_9 = 0;
                            }
                            var_7 = var_9;
                        } else {
                            var_7 = 0;
                        }
                        var_1 = var_7;
                    }
                }
                lambda {
                    set-svar(seconds-since-last-trap-door-drop, 0);
                }
                lambda {
                    set-svar(seconds-since-task-start, 0);
                }
            }
        }
        block event_player-died {
            track main {
                lambda {
                    u64? var_3;
                    if (task-complete?(abby-ellie-fight, aef-fight-ellie-hunt-2)) {
                        var_3 = get-float(phase-3-excessive-time, self);
                    } else if (task-complete?(abby-ellie-fight, aef-fight-ellie-hunt)) {
                        var_3 = get-float(phase-2-excessive-time, self);
                    } else {
                        var_3 = get-float(phase-1-excessive-time, self);
                    }
                    if ((get-current-time(0) - get-float(last-trap-door-drop-timestamp, self)) > var_3) {
                        if (!is-final-build?()) {
                            display(dc:format("AEF TRAPDOOR CONTROLLER: Bumping trapdoor due to excessive time in phase"), 19);
                        }
                        if (get-svar(timed-trap-door-level) < 3) {
                            set-trapdoor-level(get-svar(timed-trap-door-level) + 1, 5);
                            set-svar(timed-trap-door-level, get-trapdoor-level(1));
                            var_8 = set-svar(seconds-since-last-trap-door-drop, 0);
                        }
                    } else {
                        set-svar(seconds-since-last-trap-door-drop, get-svar(seconds-since-last-trap-door-drop) + (i32)get-state-time(self));
                        if (!is-final-build?()) {
                            var_8 = display(dc:format("AEF TRAPDOOR CONTROLLER: Seconds since last trap door drop: %d", get-svar(seconds-since-last-trap-door-drop)), 19);
                        }
                    }
                }
            }
        }
        block end {
            track main {
                lambda {
                    set-svar(seconds-since-task-start, get-svar(seconds-since-task-start) + (i32)get-state-time(self));
                }
                lambda {
                    if (!is-final-build?()) {
                        display(dc:format("AEF TRAPDOOR CONTROLLER: Seconds since last task start: %d", get-svar(seconds-since-task-start)), 19);
                    }
                }
            }
        }
    }
}
