// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...ed/dc1\ss-t2\accessibility\ss-accessibility-pathfind-override-region.bin  #
// #   filesize: 4530 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 9                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        sp-all
    }
    declarations {
        bool active = true
    }
    state override-goal {
        block start {
            track main {
                lambda {
                    set-boolean(active, tag-boolean(active-on-spawn, self), self);
                }
                lambda {
                    if (player-in-region?(get-self-id(), 2) && get-boolean(active, self)) {
                        send-event(add-override-region, ss-accessibility-pathfind, boxed_symbol(get-self-id()));
                    }
                }
            }
        }
        block event_enter {
            track main {
                lambda {
                    if (get-boolean(active, self)) {
                        send-event(add-override-region, ss-accessibility-pathfind, boxed_symbol(get-self-id()));
                    }
                }
            }
        }
        block event_exit {
            track main {
                lambda {
                    send-event(remove-override-region, ss-accessibility-pathfind, boxed_symbol(get-self-id()));
                }
            }
        }
        block event_enable {
            track main {
                lambda {
                    set-boolean(active, 1, self);
                }
                lambda {
                    if (player-in-region?(get-self-id(), 2)) {
                        send-event(enter, self);
                    }
                }
            }
        }
        block event_disable {
            track main {
                lambda {
                    set-boolean(active, 0, self);
                }
                lambda {
                    if (player-in-region?(get-self-id(), 2)) {
                        send-event(exit, self);
                    }
                }
            }
        }
        block end {
            track main {
                lambda {
                    if (is-script-running?(ss-accessibility-pathfind)) {
                        send-event(remove-override-region, ss-accessibility-pathfind, boxed_symbol(get-self-id()));
                    }
                }
            }
        }
    }
}
