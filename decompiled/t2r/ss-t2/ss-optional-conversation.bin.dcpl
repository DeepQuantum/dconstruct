// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...art II/build/pc/main/bin_unpacked/dc1\ss-t2\ss-optional-conversation.bin  #
// #   filesize: 4709 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 12                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bool in-optional-conversation = nullptr
        bool force-walk = true
        bool disable-interacts = true
        bool use-camera-overlay = true
        symbol camera-overlay = *camera-remap-close*
        symbol current-interactable = nullptr
    }
    state not-in-optional-conversation {
        block event_start-optional-conversation {
            track main {
                lambda {
                    set-boolean(in-optional-conversation, 1, self);
                }
                lambda {
                    set-boolean(force-walk, event-arg-boolean(0), self);
                }
                lambda {
                    set-boolean(disable-interacts, event-arg-boolean(1), self);
                }
                lambda {
                    if (event-arg-count() > 2) {
                        set-boolean(use-camera-overlay, event-arg-boolean(2), self);
                    } else {
                        set-boolean(use-camera-overlay, 1, self);
                    }
                }
                lambda {
                    if (get-boolean(use-camera-overlay, self)) {
                        if (event-arg-count() > 3) {
                            set-symbol(camera-overlay, event-arg-symbol(3), self);
                        } else {
                            set-symbol(camera-overlay, *camera-remap-close*, self);
                        }
                    }
                }
                lambda {
                    go(in-optional-conversation, 1);
                }
            }
        }
    }
    state in-optional-conversation {
        block start {
            track main {
                lambda {
                    broadcast-event(optional-conversation-start);
                }
            }
        }
        block update {
            track main {
                lambda {
                    player-conversation-mode(get-boolean(force-walk, self), get-boolean(disable-interacts, self), 1, 1);
                }
                lambda {
                    if (get-boolean(use-camera-overlay, self)) {
                        camera-set-overlay(get-symbol(camera-overlay, self), 6, 1000, 1, 1.50, 2, 1.50);
                    }
                }
            }
        }
        block event_end-optional-conversation {
            track main {
                lambda {
                    set-boolean(in-optional-conversation, 0, self);
                }
                lambda {
                    broadcast-event(optional-conversation-end);
                }
                lambda {
                    go(not-in-optional-conversation, 1);
                }
            }
        }
    }
}
