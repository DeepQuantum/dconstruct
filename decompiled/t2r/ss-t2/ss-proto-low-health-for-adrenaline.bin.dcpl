// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...ld/pc/main/bin_unpacked/dc1\ss-t2\ss-proto-low-health-for-adrenaline.bin  #
// #   filesize: 4819 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 9                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        f32 adrenaline = 0.00
        timer attack-timer = nullptr
        timer attacked-timer = nullptr
        f32 focus-inc = 0.00
    }
    state inactive {
        block update {
            track main {
                lambda {
                    if (*(u8 *)(*deprecated-prototypes* + 1)) {
                        set-float(adrenaline, character-get-adrenaline(player), self);
                        go(gain-adrenaline, 1);
                    }
                }
            }
        }
    }
    state gain-adrenaline {
        block update {
            track main {
                lambda {
                    if (!*(u8 *)(*deprecated-prototypes* + 1)) {
                        go(inactive, 1);
                    }
                }
                lambda {
                    if (!timer-running?(attack-timer, self) && object-anim-playing?(player, ellie-1hblade-dodge-fw-heavy-atk, 0)) {
                        -=f(adrenaline, 49.00, self);
                        var_2 = start-timer(attack-timer, frames-to-sec(70), self, 1);
                    }
                }
                lambda {
                    if (get-player-health() < 25) {
                        +=f(adrenaline, get-delta-time() * 6.00, self);
                    } else if (get-player-health() < 50) {
                        +=f(adrenaline, get-delta-time() * 4.00, self);
                    } else if (get-player-health() < 75) {
                        +=f(adrenaline, get-delta-time() * 0.50, self);
                    } else if (get-player-health() >= 90) {
                        +=f(adrenaline, get-delta-time() * -2.00, self);
                    }
                }
                lambda {
                    clamp-var(adrenaline, 0.00, 100.00, self);
                }
                lambda {
                    character-set-adrenaline(player, get-float(adrenaline, self));
                }
            }
        }
        block event_player-attacked {
            track main {
                lambda {
                    if (!timer-running?(attacked-timer, self)) {
                        +=f(adrenaline, 10.00, self);
                        start-timer(attacked-timer, frames-to-sec(15), self, 1);
                    }
                }
            }
        }
        block event_suspend-adrenaline-prototypes {
            track main {
                lambda {
                    go(suspended, 1);
                }
            }
        }
    }
    state suspended {
        block event_unsuspend-adrenaline-prototypes {
            track main {
                lambda {
                    go(inactive, 1);
                }
            }
        }
    }
}
