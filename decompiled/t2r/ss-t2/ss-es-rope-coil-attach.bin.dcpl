// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ... Part II/build/pc/main/bin_unpacked/dc1\ss-t2\ss-es-rope-coil-attach.bin  #
// #   filesize: 8865 bytes                                                                           #
// #   total entries: 2                                                                               #
// #   functions: 23                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        symbol rope-obj = #0000000000000000
    }
    state disabled {
        block start {
            track main {
                lambda {
                    interactable-disable(self);
                }
            }
        }
        block event_ready {
            track main {
                lambda {
                    go(ready, 1);
                }
                lambda {
                    set-symbol(rope-obj, event-arg-symbol(0), self);
                }
                lambda {
                    broadcast-event(thrown-over, boxed_symbol(get-self-id()), boxed_symbol(get-symbol(rope-obj, self)));
                }
                lambda {
                    if (task-node-active?(medicine)) {
                        broadcast-event(hack-throw-temp, boxed_symbol(get-self-id()));
                    }
                }
                lambda {
                    go(down, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-state-time(self) > 1.00 && !(player-get-carried-object() == 0) && !task-active?(focus-test-tutorial, ftt-inventory)) {
                        if (get-boolean(is-attached, self)) {
                            go(attached, 1);
                        } else {
                            go(ready, 1);
                        }
                    }
                }
            }
        }
    }
    state ready {
        block start {
            track main {
                lambda {
                    wait(1.00);
                }
                lambda {
                    interactable-enable(self);
                }
                lambda {
                    set-boolean(is-attached, 0, self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (player-get-carried-object() == 0) {
                        go(disabled, 1);
                    }
                }
                lambda {
                    set-symbol(rope-obj, 0, self);
                }
                lambda {
                    go(initial, 1);
                }
            }
        }
        block event_player-requested-interact {
            track main {
                lambda {
                    if (!(tag-symbol(attach-point, self) == 0)) {
                        send-event(
                            attach-rope-to-spawner,
                            player-get-carried-object(),
                            boxed_symbol(get-self-id()),
                            boxed_boundframe(get-spawner-locator(tag-symbol(attach-point, self), 1))
                        );
                    } else {
                        send-event(attach-rope-to-spawner, player-get-carried-object(), boxed_symbol(get-self-id()), boxed_boundframe(get-spawner-locator(get-self-id(), 1)));
                    }
                }
                lambda {
                    go(attached, 1);
                }
            }
        }
        block event_disable {
            track main {
                lambda {
                    go(disabled, 1);
                }
            }
        }
    }
    state attached {
        block start {
            track main {
                lambda {
                    set-boolean(is-attached, 1, self);
                }
                lambda {
                    wait(1.00);
                }
                lambda {
                    interactable-enable(self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (player-get-carried-object() == 0) {
                        go(disabled, 1);
                    }
                }
            }
        }
        block event_player-requested-interact {
            track main {
                lambda {
                    if (!(tag-symbol(attach-point, self) == 0)) {
                        send-event(
                            detach-rope,
                            player-get-carried-object(),
                            boxed_symbol(get-self-id()),
                            boxed_boundframe(get-spawner-locator(tag-symbol(attach-point, self), 1))
                        );
                    } else {
                        send-event(detach-rope, player-get-carried-object(), boxed_symbol(get-self-id()), boxed_boundframe(get-spawner-locator(get-self-id(), 1)));
                    }
                }
                lambda {
                    go(ready, 1);
                }
            }
        }
        block event_disable {
            track main {
                lambda {
                    go(disabled, 1);
                }
            }
        }
    }
}
