// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...acked/dc1\ss-abby-fights-militia\ss-afm-horse-chase-controller-horse.bin  #
// #   filesize: 34629 bytes                                                                          #
// #   total entries: 7                                                                               #
// #   functions: 98                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

u64? ss-afm-horse-controller-horse/f() {
    npc-set-script-only-no-perception(lev);
    player-disable-flinches();
    disable-systemic-horse-rider-gestures/f(lev);
    player-disable-auto-holster();
    player-set-override-aim-settings(res-downhill);
    joypad-force-command(horse-jump);
    joypad-disable-commands(horse-dismount, quick-swap-weapon, flashlight-toggle, shoulder-switch, toggle-silencer, listen-hold, listen-toggle, listen-hold-long);
    joypad-disable-all-inventory-actions();
    horse-disable-circle-strafe/f(player-horse);
    if (!get-boolean(capital-entrance-slow-down-2?, self)) {
        horse-prevent-stopping/f(player-horse);
        set-player-horse-move-settings/f(*horse-move-settings-force-run-afm-horse-chase*);
    }
    u64? var_3;
    if (timer-running?(post-branch-magnetize-timer, ss-afm-horse-chase-spline-manager-1) && is-any-branch-spline-active?()) {
        var_3 = get-float(max-turn-rate--branching, self);
    } else if (horse-is-any-barrier-spline-active?(player-horse)) {
        var_3 = get-float(max-turn-rate-barrier-active, self);
    } else {
        var_3 = get-float(max-turn-rate, self);
    }
    return horse-set-max-turn-rate-dps/f(var_3, player-horse);
}

u64? ss-afm-horse-controller-horse--ammo-check() {
    if (get-player-weapon-total-ammo() < get-weapon-mag-capacity-for-weapon(pistol-glock)) {
        set-player-weapon-total-ammo(get-weapon-mag-capacity-for-weapon(pistol-glock), pistol-glock);
    }
    set-player-weapon-clip-ammo(0, pistol-glock);
    return player-reload();
}

u64? ss-afm-horse-controller-set-camera-overlay/f() {
    u64? var_1 = vector-magnitude(get-world-space-stick-input-xz());
    u64? var_3;
    if (get-boolean(grappled?, self)) {
        var_3 = camera-set-overlay(get-symbol(camera-overlay-close-regular, self), 3, 15.00);
    } else if (joypad-command-active?(sprint-hold, 6) && var_1 > 0.20) {
        if (get-boolean(inside-close-camera-region?, self)) {
            var_3 = camera-set-overlay(get-symbol(camera-overlay-close-fast, self), 3, 15.00);
        } else {
            var_3 = camera-set-overlay(get-symbol(camera-overlay-current-fast, self), 3, 15.00);
        }
    } else if (var_1 > 0.20) {
        if (get-boolean(inside-close-camera-region?, self)) {
            var_3 = camera-set-overlay(get-symbol(camera-overlay-close-regular, self), 3, 15.00);
        } else {
            var_3 = camera-set-overlay(get-symbol(camera-overlay-current-regular, self), 3, 15.00);
        }
    } else if (get-boolean(inside-close-camera-region?, self)) {
        var_3 = camera-set-overlay(get-symbol(camera-overlay-close-slow, self), 3, 15.00);
    } else {
        var_3 = camera-set-overlay(get-symbol(camera-overlay-current-slow, self), 3, 15.00);
    }
    return var_3;
}

u16 is-any-branch-spline-active?() {
    u16 var_1;
    for (u64 i = 0; i < horse-get-num-active-barrier-splines(player-horse); i++) {
        u16 var_5;
        if (darray-contains?(ss-afm-horse-chase-branch-splines, boxed_symbol(horse-get-active-barrier-spline-from-index(i, player-horse)))) {
            var_5 = 1;
        }
        var_1 = var_5;
    }
    return var_1;
}

u64? ss-afm-horse-controller-set-speed/f() {
    u64? var_1;
    if (get-boolean(grappled?, self)) {
        var_1 = horse-force-speed/f(get-float(max-speed-grapple, self), 0, player-horse);
    } else {
        var_1 = horse-force-speed/f(get-float(max-speed, self), 0, player-horse);
    }
    return var_1;
}

statescript {
    options {
        player
    }
    declarations {
        f32 max-turn-rate = 60.00
        f32 max-turn-rate--branching = 150.00
        f32 max-turn-rate-grapple = 60.00
        f32 max-turn-rate-barrier-active = 90.00
        f32 max-speed = 9.50
        f32 max-speed-grapple = 6.00
        bool grappled? = nullptr
        bool grappled-left? = nullptr
        bool grapple-npc-will-be-killed? = nullptr
        bool capital-entrance-slow-down-1? = nullptr
        bool capital-entrance-slow-down-2? = nullptr
        bool capital-entrance-done? = nullptr
        bool approaching-fall-off? = nullptr
        bool pipe-bomb-section-active? = nullptr
        symbol current-camera-overlay = #0000000000000000
        bool first-camera-overlay-initialized? = nullptr
        bool hide-hud? = nullptr
        bool use-abby-anim-overlay? = nullptr
        bool player-aimed-weapon? = nullptr
        symbol camera-overlay-close-slow = *camera-remap-afm-horse-chase--close-slow*
        symbol camera-overlay-close-regular = *camera-remap-afm-horse-chase--close-regular*
        symbol camera-overlay-close-fast = *camera-remap-afm-horse-chase--close-fast*
        symbol camera-overlay-regular-slow = *camera-remap-afm-horse-chase--regular-slow*
        symbol camera-overlay-regular-regular = *camera-remap-afm-horse-chase--regular-regular*
        symbol camera-overlay-regular-fast = *camera-remap-afm-horse-chase--regular-fast*
        symbol camera-overlay-interior = *camera-remap-afm-horse-chase--interior*
        symbol camera-overlay-pre-field-reveal = *camera-remap-afm-horse-chase--pre-field-reveal*
        symbol camera-overlay-field-reveal = *camera-remap-afm-horse-chase--field-reveal*
        symbol camera-overlay-field-slow = *camera-remap-afm-horse-chase--field-slow*
        symbol camera-overlay-field-regular = *camera-remap-afm-horse-chase--field-regular*
        symbol camera-overlay-field-fast = *camera-remap-afm-horse-chase--field-fast*
        symbol camera-overlay-capital-slow = *camera-remap-afm-horse-chase--capital-slow*
        symbol camera-overlay-capital-regular = *camera-remap-afm-horse-chase--capital-regular*
        symbol camera-overlay-capital-fast = *camera-remap-afm-horse-chase--capital-fast*
        symbol camera-overlay-final-enemy = *camera-remap-afm-horse-chase--final-enemy*
        bool inside-close-camera-region? = nullptr
        symbol camera-overlay-current-slow = *camera-remap-afm-horse-chase--close-slow*
        symbol camera-overlay-current-regular = *camera-remap-afm-horse-chase--close-regular*
        symbol camera-overlay-current-fast = *camera-remap-afm-horse-chase--close-fast*
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-active?(abby-fights-militia, afm-horse-chase-start)) {
                        set-boolean(hide-hud?, 1, self);
                        set-boolean(use-abby-anim-overlay?, 0, self);
                        set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-close-slow, self), self);
                        set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-close-regular, self), self);
                        set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-close-fast, self), self);
                        go(main, 1);
                    }
                }
                lambda {
                    if (task-active?(abby-fights-militia, afm-horse-chase-scar-flee)) {
                        set-boolean(hide-hud?, 1, self);
                        set-boolean(use-abby-anim-overlay?, 0, self);
                        set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-close-slow, self), self);
                        set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-close-regular, self), self);
                        set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-close-fast, self), self);
                        go(main, 1);
                    }
                }
                lambda {
                    if (task-active?(abby-fights-militia, afm-horse-chase-crossfire)) {
                        set-boolean(use-abby-anim-overlay?, 1, self);
                        set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-regular-slow, self), self);
                        set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-regular-regular, self), self);
                        set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-regular-fast, self), self);
                        go(main, 1);
                    }
                }
                lambda {
                    if (task-active?(abby-fights-militia, afm-horse-chase-cavalry)) {
                        set-boolean(use-abby-anim-overlay?, 0, self);
                        set-boolean(hide-hud?, 1, self);
                        set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-close-slow, self), self);
                        set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-close-regular, self), self);
                        set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-close-fast, self), self);
                        go(main, 1);
                    }
                }
                lambda {
                    if (task-active?(abby-fights-militia, afm-horse-chase-field-arrive)) {
                        set-boolean(use-abby-anim-overlay?, 0, self);
                        set-boolean(hide-hud?, 1, self);
                        set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-close-slow, self), self);
                        set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-close-regular, self), self);
                        set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-close-fast, self), self);
                        go(main, 1);
                    }
                }
                lambda {
                    if (task-active?(abby-fights-militia, afm-horse-chase-capitol-arrive)) {
                        set-boolean(use-abby-anim-overlay?, 1, self);
                        set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-regular-slow, self), self);
                        set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-regular-regular, self), self);
                        set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-regular-fast, self), self);
                        go(main, 1);
                    }
                }
                lambda {
                    if (task-active?(abby-fights-militia, afm-horse-chase-knock-off)) {
                        set-boolean(hide-hud?, 1, self);
                        go(hide-hud, 1);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-range-active?(abby-fights-militia, afm-horse-chase-start, abby-fights-militia, afm-horse-chase-capitol-arrive)) {
                        set-boolean(use-abby-anim-overlay?, 0, self);
                        set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-close-slow, self), self);
                        set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-close-regular, self), self);
                        set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-close-fast, self), self);
                        go(main, 1);
                    }
                }
            }
        }
    }
    state main {
        block start {
            track main {
                lambda {
                    npc-set-look-aim-mode(lev, 13);
                }
                lambda {
                    equip-player-weapon(pistol-glock);
                }
                lambda {
                    holster-player-weapon(4, 1.00);
                }
                lambda {
                    if (task-range-active?(abby-fights-militia, afm-horse-chase-scar-flee, abby-fights-militia, afm-horse-chase-knock-off)) {
                        equip-player-weapon(pistol-glock);
                    }
                }
                lambda {
                    if (task-active?(abby-fights-militia, afm-horse-chase-start)) {
                        set-boolean(hide-hud?, 1, self);
                    }
                }
                lambda {
                    npc-set-combat-params(lev, *buddy-combat-params-no-flinch*);
                }
                lambda {
                    if (task-range-active?(abby-fights-militia, afm-horse-chase-scar-flee, abby-fights-militia, afm-horse-chase-capitol-arrive)) {
                        while (!!(get-player-horse-id(1, 1) == 0)) {
                            wait-one-frame();
                        }
                        npc-force-on-horse(lev, get-player-horse-id(1, 1), 1);
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    ss-afm-horse-controller-set-camera-overlay/f();
                }
                lambda {
                    ss-afm-horse-controller-set-speed/f();
                }
                lambda {
                    ss-afm-horse-controller-horse/f();
                }
                lambda {
                    if (joypad-command-enabled?(aim) && joypad-command-active?(reload, 6)) {
                        if (get-weapon-ammo-in-clip(get-weapon-id(player, -1)) < get-weapon-mag-capacity-for-weapon(pistol-glock)) {
                            ss-afm-horse-controller-horse--ammo-check();
                        }
                    }
                }
                lambda {
                    if (!get-boolean(player-aimed-weapon?, self) && is-player-horse-control-allowed?()) {
                        if (player-is-aiming-desired?()) {
                            set-boolean(player-aimed-weapon?, 1, self);
                        }
                    }
                }
                lambda {
                    if (get-boolean(use-abby-anim-overlay?, self)) {
                        set-overlay-set(player, anim-overlay-player-level-specific-afm-horse-chase);
                    } else {
                        set-overlay-set(player, anim-overlay-player-level-specific-default);
                    }
                }
                lambda {
                    if (hud-tutorial-active?(0) && player-is-aiming-desired?()) {
                        hud2-clear-tutorials(1);
                    }
                }
            }
        }
        block event_player-fired-weapon {
            track main {
                lambda {
                    if (get-weapon-ammo-in-clip(get-weapon-id(player, -1)) == 0) {
                        ss-afm-horse-controller-horse--ammo-check();
                    }
                }
            }
        }
        block event_enter {
            track main {
                lambda {
                    if (event-arg-symbol(1) == rgn-task-to-scar-flee-1) {
                        set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-regular-slow, self), self);
                        set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-regular-regular, self), self);
                        set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-regular-fast, self), self);
                    }
                }
            }
        }
        block event_enter-scar-flee {
            track main {
                lambda {
                    if (get-player-weapon-total-ammo() < get-weapon-mag-capacity-for-weapon(pistol-glock)) {
                        set-player-weapon-total-ammo(get-weapon-mag-capacity-for-weapon(pistol-glock), pistol-glock);
                    }
                }
                lambda {
                    if (!player-is-aiming-desired?()) {
                        set-player-weapon-clip-ammo(0, pistol-glock);
                        player-reload();
                    }
                }
                lambda {
                    wait(0.50);
                }
                lambda {
                    if (!get-boolean(player-aimed-weapon?, self) && !player-is-aiming-desired?()) {
                        hud2-play-tutorial(HINT_AIM, 3.00, aim, 0, 2, 0, 0, 0);
                    }
                }
                lambda {
                    wait(1.00);
                }
                lambda {
                    set-boolean(hide-hud?, 0, self);
                }
                lambda {
                    wait(1.00);
                }
                lambda {
                    set-boolean(use-abby-anim-overlay?, 1, self);
                }
                lambda {
                    set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-regular-slow, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-regular-regular, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-regular-fast, self), self);
                }
            }
        }
        block event_close-camera-region-enter {
            track main {
                lambda {
                    set-boolean(inside-close-camera-region?, 1, self);
                }
            }
        }
        block event_close-camera-region-exit {
            track main {
                lambda {
                    set-boolean(inside-close-camera-region?, 0, self);
                }
            }
        }
        block event_grapple-start {
            track main {
                lambda {
                    set-boolean(use-abby-anim-overlay?, 0, self);
                }
                lambda {
                    set-boolean(hide-hud?, 1, self);
                }
                lambda {
                    set-boolean(grappled?, 1, self);
                }
                lambda {
                    set-boolean(grappled-left?, 0, self);
                }
                lambda {
                    set-boolean(grapple-npc-will-be-killed?, event-arg-boolean(0), self);
                }
            }
        }
        block event_grapple-end {
            track main {
                lambda {
                    set-boolean(hide-hud?, 0, self);
                }
                lambda {
                    set-boolean(grappled?, 0, self);
                }
                lambda {
                    set-boolean(use-abby-anim-overlay?, 1, self);
                }
                lambda {
                    set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-regular-slow, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-regular-regular, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-regular-fast, self), self);
                }
            }
        }
        block event_afm-horse-chase-camera--see-haven {
            track main {
                lambda {
                    set-boolean(use-abby-anim-overlay?, 0, self);
                }
                lambda {
                    set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-pre-field-reveal, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-pre-field-reveal, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-pre-field-reveal, self), self);
                }
                lambda {
                    set-boolean(hide-hud?, 1, self);
                }
            }
        }
        block event_enter-forest {
            track main {
                lambda {
                    set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-close-slow, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-close-regular, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-close-fast, self), self);
                }
            }
        }
        block event_enter {
            track main {
                lambda {
                    set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-pre-field-reveal, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-pre-field-reveal, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-pre-field-reveal, self), self);
                }
            }
        }
        block event_exit {
            track main {
                lambda {
                    set-boolean(use-abby-anim-overlay?, 1, self);
                }
                lambda {
                    set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-field-reveal, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-field-reveal, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-field-reveal, self), self);
                }
                lambda {
                    wait(2.00);
                }
                lambda {
                    set-boolean(hide-hud?, 0, self);
                }
                lambda {
                    set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-field-slow, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-field-regular, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-field-fast, self), self);
                }
            }
        }
        block event_enter-capitol {
            track main {
                lambda {
                    set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-capital-slow, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-capital-regular, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-capital-fast, self), self);
                }
            }
        }
        block event_enter {
            track main {
                lambda {
                    set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-interior, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-interior, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-interior, self), self);
                }
            }
        }
        block event_exit {
            track main {
                lambda {
                    set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-capital-slow, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-capital-regular, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-capital-fast, self), self);
                }
            }
        }
        block event_afm-horse-chase-camera--final-enemy {
            track main {
                lambda {
                    set-symbol(camera-overlay-current-slow, get-symbol(camera-overlay-final-enemy, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-regular, get-symbol(camera-overlay-final-enemy, self), self);
                }
                lambda {
                    set-symbol(camera-overlay-current-fast, get-symbol(camera-overlay-final-enemy, self), self);
                }
            }
        }
        block event_afm-horse-chase-camera--pre-knock-off {
            track main {
                lambda {
                    set-boolean(use-abby-anim-overlay?, 0, self);
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    set-boolean(hide-hud?, 1, self);
                }
                lambda {
                    npc-set-script-high-priority(lev);
                }
                lambda {
                    go(hide-hud, 1);
                }
            }
        }
        block event_enter {
            track main {
                lambda {
                    u64? var_0 = alloc-gesture-play-params();
                    *(i32 *)(var_0 + 36) = 19;
                    *(i32 *)(var_0 + 116) = 2;
                    *(u8 *)(var_0 + 34) = 1;
                    *(u8 *)(var_0 + 112) = 1;
                    gesture_(player, afm-abby-flinch-light-fw-gesture, var_0);
                }
                lambda {
                    u64? var_0 = alloc-gesture-play-params();
                    *(i32 *)(var_0 + 36) = 19;
                    *(i32 *)(var_0 + 116) = 2;
                    *(u8 *)(var_0 + 34) = 1;
                    *(u8 *)(var_0 + 112) = 1;
                    gesture_(lev, afm-lev-flinch-light-fw-gesture, var_0);
                }
                lambda {
                    wait(0.50);
                }
                lambda {
                    rumble-curve(0.50, 0.50, 0.50, 1, 0);
                }
            }
        }
        block event_horse-chase-complete {
            track main {
                lambda {
                    set-boolean(hide-hud?, 0, self);
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
    }
    state hide-hud {
        block update {
            track main {
                lambda {
                    if (get-boolean(hide-hud?, self)) {
                        hud2-hide-health-ammo/f(0, 1);
                    }
                }
                lambda {
                    if (task-continue-active?(abby-fights-militia, afm-horse-chase-knock-off, knocked-off)) {
                        set-boolean(hide-hud?, 0, self);
                        npc-set-combat-params(lev, *buddy-combat-params*);
                        go(complete, 1);
                    }
                }
            }
        }
    }
    state already-complete {
        block start {
            track main {
                lambda {
                    go(complete, 1);
                }
            }
        }
    }
}
