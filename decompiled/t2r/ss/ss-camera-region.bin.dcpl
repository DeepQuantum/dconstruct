// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...ast of Us Part II/build/pc/main/bin_unpacked/dc1\ss\ss-camera-region.bin  #
// #   filesize: 3693 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 3                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        symbol camera-overlay = nullptr
        f32 fade-in-sec = 1.00
        f32 fade-out-sec = 1.00
        f32 fade-in-dist = 0.00
        f32 fade-out-dist = -1.00
        i32 script-priority = 100
        bool allow-stack? = nullptr
    }
    state apply-overlay {
        block  {
        }
        block event_enter {
            track main {
                lambda {
                    if (!is-final-build?()) {
                        string var_9;
                        if (get-boolean(allow-stack?, self)) {
                            var_9 = "stacking allowed";
                        } else {
                            var_9 = "stacking not allowed";
                        }
                        display(dc:format(
                            "%s: Fading in camera overlay %s over %f seconds and %f distance at priority %d with %s",
                            string-debug(get-self-id()),
                            string-debug(tag-symbol(camera-overlay, self)),
                            get-float(fade-in-sec, self),
                            get-float(fade-in-dist, self),
                            get-int32(script-priority, self),
                            var_9
                        ), 19);
                    }
                }
            }
        }
        block event_inside {
            track main {
                lambda {
                    camera-set-overlay(
                        tag-symbol(camera-overlay, self),
                        1,
                        get-float(fade-in-sec, self),
                        3,
                        get-float(fade-in-dist, self),
                        2,
                        get-float(fade-out-sec, self),
                        4,
                        get-float(fade-out-dist, self),
                        6,
                        get-int32(script-priority, self),
                        8,
                        get-boolean(allow-stack?, self)
                    );
                }
            }
        }
        block event_exit {
            track main {
                lambda {
                    if (!is-final-build?()) {
                        display(dc:format(
                            "%s: Fading out camera overlay %s over %f seconds and %f distance",
                            string-debug(get-self-id()),
                            string-debug(tag-symbol(camera-overlay, self)),
                            get-float(fade-out-sec, self),
                            get-float(fade-out-dist, self)
                        ), 19);
                    }
                }
            }
        }
    }
}
