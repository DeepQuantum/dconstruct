// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-santa-barbara\ss-san-beach-fight-player-gesture-controller.bin#
// #   filesize: 11465 bytes                                                                          #
// #   total entries: 5                                                                               #
// #   functions: 21                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        npc-san-spikes-abby-1
    }
    declarations {
        symbol abby = npc-san-spikes-abby-1
        f32 gesture-timer = 0.00
        f32 gesture-cooldown = 0.00
        symbol queued-gesture = #0000000000000000
        symbol current-gesture = #0000000000000000
    }
    state --all-- {
        block event run-gestures {
            track main {
                lambda {
                    if (!(get-script-state(ss-san-beach-fight-abby-gesture-controller-1) == waiting-on-cooldown) && !(get-script-state(ss-san-beach-fight-abby-gesture-controller-1) == running-gestures)) {
                        go(waiting-on-cooldown, 1);
                    }
                }
            }
        }
        block event stop-gestures {
            track main {
                lambda {
                    go(clean-up, 1);
                }
            }
        }
        block event queue-gesture {
            track main {
                lambda {
                    set-symbol(queued-gesture, event-arg-symbol(0), self);
                }
            }
        }
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(santa-barbara, san-beach-fight-fist-phase)) {
                        go(sleeping, 1);
                    }
                }
                lambda {
                    if (task-range-active?(santa-barbara, san-beach-fight-knife-phase-1, santa-barbara, san-beach-fight-fist-phase)) {
                        go(waiting-on-cooldown, 1);
                    }
                }
            }
        }
        block event task-complete {
            track main {
                lambda {
                    if (task-active?(santa-barbara, san-beach-fight-knife-phase-1)) {
                        go(waiting-on-cooldown, 1);
                    }
                }
            }
        }
    }
    state clean-up {
        block start {
            track main {
                lambda {
                    stop-timer(gesture-timer, self);
                }
                lambda {
                    set-float(gesture-cooldown, 0.00, self);
                }
                lambda {
                    set-symbol(current-gesture, 0, self);
                }
                lambda {
                    go(sleeping, 1);
                }
            }
        }
    }
    state  {
    }
    state waiting-on-cooldown {
        block start {
            track main {
                lambda {
                    set-float(gesture-timer, 0.00, self);
                }
                lambda {
                    if (npc-enemy-abby-phase-1?(get-symbol(abby, self))) {
                        set-float(gesture-cooldown, random-float-range(0.25, 1.00, 0), self);
                    } else if (npc-enemy-abby-phase-2?(get-symbol(abby, self))) {
                        set-float(gesture-cooldown, random-float-range(0.25, 1.00, 0), self);
                    } else if (npc-enemy-abby-phase-3?(get-symbol(abby, self))) {
                        set-float(gesture-cooldown, random-float-range(0.25, 1.00, 0), self);
                    } else {
                        set-float(gesture-cooldown, random-float-range(0.25, 1.00, 0), self);
                    }
                }
                lambda {
                    u64? var_2 = get-object-distance(get-symbol(abby, self), player);
                    boxed_symbol(true);
                    u64? var_8 = boxed_symbol(false);
                    do-melee-log(
                        "characters-in-range? [[f]] [[f]] [[f]] [[s]]",
                        boxed_f32(var_2),
                        boxed_f32(2.25),
                        boxed_f32(999.00),
                        var_8
                    );
                    while (!get-current-melee-attack(player) && !(get-symbol(abby, self) == 0) && !(player == 0) && var_2 < 999.00 || var_2 > 2.25 && var_2 > 2.25 || var_2 || var_8 && var_2 > 2.25 && !(get-float(gesture-timer, self) > get-float(gesture-cooldown, self))) {
                        wait-one-frame();
                    }
                }
                lambda {
                    go(running-gestures, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!get-current-melee-attack(player)) {
                        u64? var_2 = get-object-distance(get-symbol(abby, self), player);
                        bool var_4 = !(get-symbol(abby, self) == 0) && !(player == 0) && var_2 < 999.00 && var_2 > 2.50;
                        u64? var_8;
                        if (var_4) {
                            var_8 = boxed_symbol(true);
                        } else {
                            var_8 = boxed_symbol(false);
                        }
                        do-melee-log(
                            "characters-in-range? [[f]] [[f]] [[f]] [[s]]",
                            boxed_f32(var_2),
                            boxed_f32(2.50),
                            boxed_f32(999.00),
                            var_8
                        );
                        if (var_4) {
                            set-float(gesture-timer, get-float(gesture-timer, self) + get-delta-time(), self);
                        }
                    }
                }
            }
        }
    }
    state running-gestures {
        block start {
            track main {
                lambda {
                    if (!(get-symbol(queued-gesture, self) == 0)) {
                        set-symbol(current-gesture, get-symbol(queued-gesture, self), self);
                        gesture_(player, get-symbol(current-gesture, self), alloc-gesture-play-params());
                        set-symbol(queued-gesture, 0, self);
                    } else {
                        sid var_6;
                        if (npc-enemy-abby-phase-1?(get-symbol(abby, self))) {
                            var_6 = player-gestures-fight-phase-1;
                        } else if (npc-enemy-abby-phase-2?(get-symbol(abby, self))) {
                            var_6 = player-gestures-fight-phase-2;
                        } else if (npc-enemy-abby-phase-3?(get-symbol(abby, self))) {
                            var_6 = player-gestures-fight-phase-3;
                        } else if (npc-enemy-abby-phase-gassed?(get-symbol(abby, self))) {
                            var_6 = player-gestures-fight-phase-gassed;
                        } else {
                            var_6 = player-gestures-fight-phase-1;
                        }
                        set-symbol(current-gesture, group-random(var_6), self);
                    }
                }
                lambda {
                    while (!!(get-symbol(current-gesture, self) == 0) || !is-gesture-playing?(player, get-symbol(current-gesture, self), -1) && !get-current-melee-attack(player)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    clear-gesture-instantly(player, -1);
                }
                lambda {
                    wait(random-float-range(1.00, 1.50, 0));
                }
                lambda {
                    set-symbol(current-gesture, 0, self);
                }
                lambda {
                    go(waiting-on-cooldown, 1);
                }
            }
        }
    }
}
