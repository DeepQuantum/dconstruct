// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...bin_unpacked/dc1\ss-santa-barbara\ss-san-beach-fight-abby-controller.bin  #
// #   filesize: 4039 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 8                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        npc-san-spikes-abby-1
    }
    declarations {
        symbol abby = npc-san-spikes-abby-1
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(santa-barbara, san-beach-fight-fist-phase)) {
                        go(inactive, 1);
                    }
                }
                lambda {
                    if (task-range-active?(santa-barbara, san-beach-fight-knife-phase-1, santa-barbara, san-beach-fight-fist-phase)) {
                        go(active, 1);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-active?(santa-barbara, san-beach-fight-knife-phase-1)) {
                        go(active, 1);
                    }
                }
            }
        }
    }
    state active {
        block start {
            track main {
                lambda {
                    set-character-wet-mask-value(get-symbol(abby, self), 0.60);
                }
                lambda {
                    set-mesh-visible(get-symbol(abby, self), abby-santa-barbara-prisoner-wet-hair, 1);
                }
                lambda {
                    npc-set-ambient-rim-light-enabled(get-symbol(abby, self), 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!get-current-melee-attack(get-symbol(abby, self))) {
                        u16 var_5;
                        if (npc-enemy-abby-phase-1?(get-symbol(abby, self))) {
                            var_5 = exhausted-ambient-no-idle;
                        } else if (npc-enemy-abby-phase-2?(get-symbol(abby, self))) {
                            var_5 = exhausted-anger-ambient;
                        } else if (npc-enemy-abby-phase-3?(get-symbol(abby, self))) {
                            var_5 = exhausted-anger-ambient;
                        } else if (npc-enemy-abby-phase-gassed?(get-symbol(abby, self))) {
                            var_5 = exhausted-02-ambient;
                        } else {
                            var_5 = 0;
                        }
                        npc-set-emotion(get-symbol(abby, self), var_5, 0.00, 0.60, 1.00);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-complete?(santa-barbara, san-beach-fight-fist-phase)) {
                        go(inactive, 1);
                    }
                }
            }
        }
    }
}
