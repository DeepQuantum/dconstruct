// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...in_unpacked/dc1\ss-watchtower\ss-wat-infected-tunnels-boost-to-cable.bin  #
// #   filesize: 16059 bytes                                                                          #
// #   total entries: 1                                                                               #
// #   functions: 49                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bool at-ladder = nullptr
        bool initial-boosted = nullptr
        bool rope-in-region? = true
    }
    state initial {
        block start {
            track main {
                lambda {
                    if (task-complete?(watchtower, wat-infected-tunnels-rope-puzzle)) {
                        go(done, 1);
                    } else if (task-active?(watchtower, wat-infected-tunnels-rope-puzzle)) {
                        go(check-bad-jump, 1);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-active?(watchtower, wat-infected-tunnels-rope-puzzle)) {
                        go(check-bad-jump, 1);
                    }
                }
            }
        }
    }
    state check-bad-jump {
        block start {
            track main {
                lambda {
                    interactable-enable(int-rope-boost-1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-boolean(at-ladder, self)) {
                        u16 var_2;
                        if (joypad-command-active?(debug-script-press-x, 6)) {
                            var_2 = 1;
                        } else if (joypad-command-active?(debug-script-hold-x, 6)) {
                            var_2 = 1;
                        } else {
                            var_2 = 0;
                        }
                        if (var_2 && player-facing-position?(get-spawner-position(ap-boost-to-cable-1, 1), 60.00)) {
                            interactable-disable(int-rope-boost-1);
                            go(dina-will-help, 1);
                        }
                    }
                }
                lambda {
                    if (player-in-region?(ss-rgn-boost-to-cable-1, 2)) {
                        joypad-disable-commands(jump, jump-hold);
                    }
                }
            }
        }
        block event_player-requested-interact {
            track main {
                lambda {
                    interactable-disable(int-rope-boost-1);
                }
                lambda {
                    go(dina-will-help, 1);
                }
            }
        }
    }
    state dina-will-help {
        block start {
            track dina {
                lambda {
                    set-collision-layer(dina, small, 0);
                }
                lambda {
                    npc-request-hold(dina, 10.00);
                }
                lambda {
                    wait-for(ellie-ready);
                }
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 2, 1);
                    *(i32 *)*(*var_0 + (0 * 8)) = 10;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = 0;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = get-spawner-locator(ap-boost-to-cable-1, 1);
                    *(i32 *)*(*var_0 + (1 * 8)) = 28;
                    *(u64 *)(*(*var_0 + (1 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (1 * 8)) + 8) = 0.50;
                    wait-animate_(dina, wat-infected-tunnels-boost-to-cable--dina, var_0);
                }
                lambda {
                    signal(dina-done, 0);
                }
            }
            track rope {
                lambda {
                    wait-stub-object-lerp(carry-object-wat-rope-1, pos-rope-drop-1, 7.00, 1, 1.00);
                }
                lambda {
                    wait-stub-object-lerp(carry-object-wat-rope-1, pos-rope-drop-2, 7.00, 1, 1.00);
                }
                lambda {
                    send-event(drop, carry-object-wat-rope-1);
                }
                lambda {
                    signal(rope-done, 0);
                }
            }
            track ellie {
                lambda {
                    wait-player-move-to-entry(
                        wat-infected-tunnels-boost-to-cable-intro--ellie,
                        get-spawner-locator(ap-boost-to-cable-1, 1),
                        1,
                        0,
                        0.40,
                        apReference-camera1,
                        apReference,
                        0.00,
                        0,
                        1,
                        0,
                        1,
                        0.00,
                        0,
                        1
                    );
                }
                lambda {
                    camera-enable-animated(2.00, -1.00, 1, 1000, 0, 0, 0);
                }
                lambda {
                    signal(ellie-ready, 0);
                }
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 2, 1);
                    *(i32 *)*(*var_0 + (0 * 8)) = 10;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = 0;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = get-spawner-locator(ap-boost-to-cable-1, 1);
                    *(i32 *)*(*var_0 + (1 * 8)) = 28;
                    *(u64 *)(*(*var_0 + (1 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (1 * 8)) + 8) = 0.50;
                    wait-animate_(player, wat-infected-tunnels-boost-to-cable-intro--ellie, var_0);
                }
                lambda {
                    signal(jump-done, 0);
                }
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 2, 1);
                    *(i32 *)*(*var_0 + (0 * 8)) = 10;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = 0;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = get-spawner-locator(ap-boost-to-cable-1, 1);
                    *(i32 *)*(*var_0 + (1 * 8)) = 28;
                    *(u64 *)(*(*var_0 + (1 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (1 * 8)) + 8) = 0.50;
                    animate_(player, wat-infected-tunnels-boost-to-cable-idle--ellie, var_0);
                }
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 2, 1);
                    *(i32 *)*(*var_0 + (0 * 8)) = 10;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = 0;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = get-spawner-locator(ap-boost-to-cable-1, 1);
                    *(i32 *)*(*var_0 + (1 * 8)) = 28;
                    *(u64 *)(*(*var_0 + (1 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (1 * 8)) + 8) = 0.50;
                    wait-animate_(player, wat-infected-tunnels-boost-to-cable--ellie, var_0);
                }
                lambda {
                    camera-fade-out-animated(2.00, 0);
                }
                lambda {
                    wait-for(rope-done);
                }
                lambda {
                    wait-for(dina-done);
                }
                lambda {
                    set-collision-layer(dina, npc, 0);
                }
                lambda {
                    wait(6.00);
                }
                lambda {
                    go(check-redo, 1);
                }
            }
            track vox {
                lambda {
                    send-event(buddy-boost-train, ss-vox-wat-infected-tunnels-1);
                }
            }
        }
    }
    state check-redo {
        block update {
            track main {
                lambda {
                    if (!(is-rope-draped?(carry-object-wat-rope-1)) && player-get-carried-object() != carry-object-wat-rope-1) {
                        interactable-enable(int-rope-boost-1);
                        go(wait-ask-redo, 1);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-active?(watchtower, wat-infected-tunnels-shambler-intro)) {
                        go(done, 1);
                    }
                }
            }
        }
    }
    state wait-ask-redo {
        block event_player-requested-interact {
            track main {
                lambda {
                    interactable-disable(int-rope-boost-1);
                }
                lambda {
                    go(dina-throws-again, 1);
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-active?(watchtower, wat-infected-tunnels-shambler-intro)) {
                        go(done, 1);
                    }
                }
            }
        }
    }
    state dina-throws-again {
        block start {
            track rope {
                lambda {
                    kill-entity(carry-object-wat-rope-1);
                }
                lambda {
                    set-collision-layer(dina, small, 0);
                }
                lambda {
                    wait-spawn-entity-at(carry-object-wat-rope-1, get-spawner-locator(carry-object-wat-rope-1, 1), 0);
                }
                lambda {
                    interactable-disable(carry-object-wat-rope-1);
                }
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 3, 1);
                    *(i32 *)*(*var_0 + (0 * 8)) = 10;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = 0;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = get-spawner-locator(ap-boost-to-cable-1, 1);
                    *(i32 *)*(*var_0 + (1 * 8)) = 28;
                    *(u64 *)(*(*var_0 + (1 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (1 * 8)) + 8) = 0.50;
                    *(i32 *)*(*var_0 + (2 * 8)) = 58;
                    *(u64 *)(*(*var_0 + (2 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (2 * 8)) + 8) = 0.60;
                    animate_(dina, wat-infected-tunnels-boost-to-cable--dina, var_0);
                }
                lambda {
                    interactable-disable(int-rope-boost-1);
                }
                lambda {
                    wait-stub-object-lerp(carry-object-wat-rope-1, pos-rope-drop-1, 3.00, 1, 1.00);
                }
                lambda {
                    wait-stub-object-lerp(carry-object-wat-rope-1, pos-rope-drop-2, 3.00, 1, 1.00);
                }
                lambda {
                    send-event(drop, carry-object-wat-rope-1);
                }
                lambda {
                    set-collision-layer(dina, npc, 0);
                }
                lambda {
                    wait(8.00);
                }
                lambda {
                    go(check-redo, 1);
                }
            }
            track vox {
                lambda {
                    script-caption(player, "Hey, throw me that rope again.", -1.00, 0, 0);
                }
            }
        }
    }
    state done {
        block start {
            track main {
                lambda {
                    interactable-disable(int-rope-boost-1);
                }
            }
        }
    }
}
