// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...n/bin_unpacked/dc1\ss-rogue\modifiers\ss-modifier-unlock-all-recipes.bin  #
// #   filesize: 4664 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 12                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        symbol grp-cached-unlocked-recipes = nullptr
    }
    state off {
        block event_turn-on {
            track main {
                lambda {
                    go(on, 1);
                }
            }
        }
    }
    state on {
        block start {
            track main {
                lambda {
                    set-symbol(grp-cached-unlocked-recipes, darray-create(self, -1, 0), self);
                }
                lambda {
                    if (!is-final-build?()) {
                        display(dc:format("DEB-SK: cached recipes being added to group..."), 19);
                    }
                    foreach (u16 var_5 : skull--get-collected-recipes()) {
                        if (!is-final-build?()) {
                            display(dc:format("DEB-SK: 	%s", string-debug(var_5)), 19);
                        }
                        darray-append(get-symbol(grp-cached-unlocked-recipes, self), boxed_symbol(var_5), 1);
                    }
                }
                lambda {
                    broadcast-event(skull-activate, boxed_symbol(unlock-all-recipes));
                }
                lambda {
                    skull--give-all-recipes();
                }
            }
        }
        block event_encounter-cleanup {
            track main {
                lambda {
                    if (!is-final-build?()) {
                        display(dc:format("DEB-SK: [ss-unlock-all-recipes] encounter-cleanup"), 19);
                    }
                }
                lambda {
                    disable-inventory();
                }
                lambda {
                    wait-one-frame();
                }
                lambda {
                    skull--restore-cached-recipes(get-symbol(grp-cached-unlocked-recipes, self));
                }
                lambda {
                    go(off, 1);
                }
            }
        }
        block end {
            track main {
                lambda {
                    if (!is-final-build?()) {
                        display(dc:format("DEB-SK: [ss-unlock-all-recipes] end"), 19);
                    }
                }
                lambda {
                    skull--restore-cached-recipes(get-symbol(grp-cached-unlocked-recipes, self));
                }
            }
        }
    }
}
