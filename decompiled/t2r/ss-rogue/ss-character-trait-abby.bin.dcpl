// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...t II/build/pc/main/bin_unpacked/dc1\ss-rogue\ss-character-trait-abby.bin  #
// #   filesize: 4940 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 5                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        i32 amount-to-restore-kill = 10
        i32 amount-to-restore = 3
        bool is-grab-gun-finisher? = nullptr
    }
    state tracking {
        block start {
            track main {
                lambda {
                    if (!is-final-build?()) {
                        display(dc:format("Character Trait Initiated"), 19);
                    }
                }
            }
        }
        block event_player-grab-gun-finisher {
            track main {
                lambda {
                    set-boolean(is-grab-gun-finisher?, 1, self);
                }
            }
        }
        block event_melee-attack-hit {
            track main {
                lambda {
                    wait-frames(2);
                }
                lambda {
                    u64? var_0 = event-arg-symbol(0);
                    if (is-npc-and-is-not-buddy?(var_0) && !(get-boolean(is-grab-gun-finisher?, self))) {
                        if (player-upgrade-active?(heal-on-hit)) {
                            if (is-script-running?(ss-meta-abby-heal-1)) {
                                u64? var_10;
                                if (get-int32(amount-to-restore, self) < (get-player-max-health() - get-player-health())) {
                                    var_10 = get-int32(amount-to-restore, self);
                                } else {
                                    var_10 = get-player-max-health() - get-player-health();
                                }
                                send-event(character-healed, ss-meta-abby-heal-1, boxed_i32(var_10));
                            }
                            u64? var_18;
                            if (get-player-max-health() < (get-player-health() + get-int32(amount-to-restore, self))) {
                                var_18 = get-player-max-health();
                            } else {
                                var_18 = get-player-health() + get-int32(amount-to-restore, self);
                            }
                            set-player-health(var_18);
                            spawn-particles-at-joint(
                                pos-perk-health-burst,
                                player,
                                spined,
                                0,
                                16,
                                root,
                                0,
                                0,
                                0,
                                0
                            );
                            if (!skull--is-skull-active?(melee-restore-health)) {
                                play-sound(sfx-hud-rl-skulls-boost-health-small, 0);
                            }
                        }
                        if (get-object-health(var_0) <= 0.00) {
                            if (is-script-running?(ss-meta-abby-heal-1)) {
                                u64? var_27;
                                if (get-int32(amount-to-restore-kill, self) < (get-player-max-health() - get-player-health())) {
                                    var_27 = get-int32(amount-to-restore-kill, self);
                                } else {
                                    var_27 = get-player-max-health() - get-player-health();
                                }
                                send-event(character-healed, ss-meta-abby-heal-1, boxed_i32(var_27));
                            }
                            u64? var_35;
                            if (get-player-max-health() < (get-player-health() + get-int32(amount-to-restore-kill, self))) {
                                var_35 = get-player-max-health();
                            } else {
                                var_35 = get-player-health() + get-int32(amount-to-restore-kill, self);
                            }
                            set-player-health(var_35);
                            spawn-particles-at-joint(
                                pos-perk-health-burst,
                                player,
                                spined,
                                0,
                                16,
                                root,
                                0,
                                0,
                                0,
                                0
                            );
                            if (!skull--is-skull-active?(melee-restore-health)) {
                                play-sound(sfx-hud-rl-skulls-boost-health-small, 0);
                            }
                        }
                    }
                    set-boolean(is-grab-gun-finisher?, 0, self);
                }
            }
        }
        block event_run-complete {
            track main {
                lambda {
                    if (!get-svar-as-boolean(rogue-cheat-mod-ever-enabled)) {
                        increment-stat(runs-complete-abby, 1);
                        increment-stat(meta-runs-complete, 1);
                        set-stat-bit(trophy-win-with-all-characters, 1);
                    }
                }
            }
        }
    }
}
