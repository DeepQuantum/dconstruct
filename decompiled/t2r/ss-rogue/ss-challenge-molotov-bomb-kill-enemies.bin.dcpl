// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...ain/bin_unpacked/dc1\ss-rogue\ss-challenge-molotov-bomb-kill-enemies.bin  #
// #   filesize: 3978 bytes                                                                           #
// #   total entries: 2                                                                               #
// #   functions: 6                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

u64? update-challenge-progress-str-with-current-kill-count() {
    set-string(challenge-progress-str, dc:format("%d/%d", get-int32(current-kill-count, self), tag-int32(num-kills-needed, self)), self);
    return hud-challenge-update-progress-count(get-int32(current-kill-count, self), tag-int32(num-kills-needed, self));
}

statescript {
    options {
        player
    }
    declarations {
        i32 current-kill-count = 0
        string challenge-progress-str = 
    }
    state tracking {
        block start {
            track main {
                lambda {
                    update-challenge-progress-str-with-current-kill-count();
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-int32(current-kill-count, self) >= tag-int32(num-kills-needed, self)) {
                        go(completed, 1);
                    }
                }
            }
        }
        block event_npc-died {
            track main {
                lambda {
                    u64? var_0 = event-arg-symbol(0);
                    u64? var_3 = lookup-weapon-gameplay-def(event-arg-symbol(2), 0);
                    if (player == event-arg-symbol(1) && !npc-is-buddy?(var_0) && *(var_3 + 40) || *(u32 *)(*(var_3 + 40) + 60) == 8 && *(u32 *)(*(var_3 + 40) + 60) == 4) {
                        ++(current-kill-count, self);
                        update-challenge-progress-str-with-current-kill-count();
                        if (!is-final-build?()) {
                            var_5 = display(dc:format("DEB-EC: Player just killed %s with grenade!", string-debug(var_0)), 19);
                        }
                    }
                }
            }
        }
    }
    state completed {
        block start {
            track main {
                lambda {
                    send-event(challenge-complete, ss-challenges-controller-1);
                }
                lambda {
                    if (!is-final-build?()) {
                        display(dc:format("DEB-EC: Challenge complete %s", string-debug(get-self-id())), 19);
                    }
                }
            }
        }
    }
}
