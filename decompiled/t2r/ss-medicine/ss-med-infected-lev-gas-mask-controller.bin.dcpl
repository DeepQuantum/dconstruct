// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...bin_unpacked/dc1\ss-medicine\ss-med-infected-lev-gas-mask-controller.bin  #
// #   filesize: 7622 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 21                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        lev
        med-infected-global-script
    }
    declarations {
        symbol lev-gas-mask-id = nullptr
        bool lev-gas-mask-spawned = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-active?(medicine, med-infected-building-return-to-lev)) {
                        go(cine-setup, 1);
                    } else if (task-active?(medicine, med-hotel-look-for-gasmask-found)) {
                        go(cine-playing, 1);
                    } else if (task-range-active?(medicine, med-infected-building-rope-descent-start, medicine, med-infected-building-death-pit)) {
                        go(equip-gas-mask, 1);
                    } else if (task-complete?(medicine, med-infected-building-death-pit)) {
                        go(complete, 1);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-active?(medicine, med-infected-building-return-to-lev)) {
                        go(cine-setup, 1);
                    }
                }
            }
        }
    }
    state cine-setup {
        block start {
            track main {
                lambda {
                    set-gas-mask-setting-id(lev, *lev-gas-mask-setup*);
                }
                lambda {
                    set-symbol(lev-gas-mask-id, spawn-gas-mask(lev), self);
                }
                lambda {
                    set-visible(get-symbol(lev-gas-mask-id, self), 0);
                }
                lambda {
                    set-boolean(lev-gas-mask-spawned, 1, self);
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    go(cine-playing, 1);
                }
            }
        }
    }
    state cine-playing {
        block start {
            track main {
                lambda {
                    if (get-symbol(lev-gas-mask-id, self) == 0) {
                        set-gas-mask-setting-id(lev, *lev-gas-mask-setup*);
                        set-symbol(lev-gas-mask-id, spawn-gas-mask(lev), self);
                        set-boolean(lev-gas-mask-spawned, 1, self);
                    }
                }
                lambda {
                    set-visible(get-symbol(lev-gas-mask-id, self), 1);
                }
            }
        }
        block event_attach-gas-mask {
            track main {
                lambda {
                    go(cine-clean-up, 1);
                }
            }
        }
    }
    state cine-clean-up {
        block start {
            track main {
                lambda {
                    wait-equip-gas-mask(lev, get-symbol(lev-gas-mask-id, self), 1);
                }
                lambda {
                    go(inside-infected-building, 1);
                }
            }
        }
    }
    state equip-gas-mask {
        block start {
            track main {
                lambda {
                    set-gas-mask-setting-id(lev, *lev-gas-mask-setup*);
                }
                lambda {
                    set-symbol(lev-gas-mask-id, wait-equip-gas-mask(lev, 0, 1), self);
                }
                lambda {
                    set-boolean(lev-gas-mask-spawned, 1, self);
                }
                lambda {
                    go(inside-infected-building, 1);
                }
            }
        }
    }
    state inside-infected-building {
        block event_left-infected-building {
            track main {
                lambda {
                    go(remove-gas-mask, 1);
                }
            }
        }
    }
    state remove-gas-mask {
        block start {
            track main {
                lambda {
                    wait-remove-gas-mask(lev, 0, 1);
                }
                lambda {
                    set-boolean(gas-mask-removed, 1, ss-vox-med-infected-building-death-pit-1);
                }
                lambda {
                    set-boolean(lev-gas-mask-spawned, 0, self);
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
    }
}
