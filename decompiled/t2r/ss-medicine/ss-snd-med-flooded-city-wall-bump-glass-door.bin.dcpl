// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...npacked/dc1\ss-medicine\ss-snd-med-flooded-city-wall-bump-glass-door.bin  #
// #   filesize: 3571 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 8                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        med-const-audio
    }
    declarations {
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-active?(medicine, med-flooded-street-highway)) {
                        go(enter, 1);
                    }
                }
                lambda {
                    if (task-complete?(medicine, med-flooded-street-highway)) {
                        go(complete, 1);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-active?(medicine, med-flooded-street-highway)) {
                        go(enter, 1);
                    }
                }
            }
        }
    }
    state enter {
        block start {
            track main {
                lambda {
                    while (object-anim-playing?(player, ellie-jump^wall-hit^fall, 0) || object-anim-playing?(player, ellie-wall-land-med-fw^idle-tense, 0) && !object-anim-playing?(player, ellie-jump^wall-hit^fall, 0)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    if (object-anim-playing?(player, ellie-jump^wall-hit^fall, 0)) {
                        wait-until-anim-frame(player, ellie-jump^wall-hit^fall, 9.00, 2);
                    } else if (object-anim-playing?(player, ellie-wall-land-med-fw^idle-tense, 0)) {
                        wait-until-anim-frame(player, ellie-wall-land-med-fw^idle-tense, 9.00, 2);
                    } else if (object-anim-playing?(player, ellie-jump^wall-hit^fall, 0)) {
                        wait-until-anim-frame(player, ellie-jump^wall-hit^fall, 9.00, 2);
                    }
                }
                lambda {
                    play-sound-at-joint(sfx-igc-med-flooded-street-glass-door-thump, player, r_clavicle_helper, 0, 0);
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-complete?(medicine, med-flooded-street-highway)) {
                        go(complete, 1);
                    }
                }
            }
        }
    }
}
