// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...cked/dc1\ss-medicine\hospital-annex\ss-med-ratking-fight-lower-phase.bin  #
// #   filesize: 11964 bytes                                                                          #
// #   total entries: 4                                                                               #
// #   functions: 31                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

f32 ratking-stalker-lower-phase-starting-health() {
    f32 var_2;
    if (get-difficulty-setting-enemies() == 0 && get-svar(timed-trap-door-level) >= 9) {
        var_2 = 600.00;
    } else if (get-difficulty-setting-enemies() == 0 && get-svar(timed-trap-door-level) >= 6) {
        var_2 = 600.00;
    } else if (get-difficulty-setting-enemies() == 0 && get-svar(timed-trap-door-level) >= 3) {
        var_2 = 600.00;
    } else if (600.00 > (f32)get-svar(med-superstalker-health-after-chase)) {
        var_2 = 600.00;
    } else {
        var_2 = (f32)get-svar(med-superstalker-health-after-chase);
    }
    return var_2;
}

f32 ratking-lower-phase-starting-health() {
    f32 var_2;
    if (get-difficulty-setting-enemies() == 0 && get-svar(timed-trap-door-level) >= 9) {
        var_2 = 600.00;
    } else if (get-difficulty-setting-enemies() == 0 && get-svar(timed-trap-door-level) >= 6) {
        var_2 = 710.00;
    } else if (get-difficulty-setting-enemies() == 0 && get-svar(timed-trap-door-level) >= 3) {
        var_2 = 790.00;
    } else if (850.00 > (f32)get-svar(med-ratking-health-after-chase)) {
        var_2 = 850.00;
    } else {
        var_2 = (f32)get-svar(med-ratking-health-after-chase);
    }
    return var_2;
}

u64? ratking-spawn-lower-phase() {
    get-spawner-locator(pos-ratking-lower-phase-start-1, 1);
    u64? var_2;
    if (med-ratking-player-using-blind-settings?()) {
        var_2 = set-locator-z-direction(get-spawner-locator(ap-ratking-split-20, 1), vector-between-points(get-spawner-position(ap-ratking-split-20, 1), get-object-position(player)), 0);
    }
    return spawn-entity-at(npc-ratking-bloater-1, var_2, 0);
}

statescript {
    options {
        player
        med-hospital-annex-basement-script
    }
    declarations {
        f32 hit-time = 0.00
        bool said-need-to-fight? = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(medicine, med-hospital-basement-ratking-basement-fight)) {
                        go(complete, 1);
                    }
                }
                lambda {
                    if (task-active?(medicine, med-hospital-basement-ratking-basement-fight)) {
                        go(resume-spawn, 1);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-active?(medicine, med-hospital-basement-ratking-basement-fight)) {
                        set-svar(timed-trap-door-level, 0);
                        go(spawn-basement, 1);
                    }
                }
            }
        }
    }
    state spawn-basement {
        block start {
            track main {
                lambda {
                    if (!(entity-exists?(npc-ratking-bloater-1)) && !(npc-alive?(npc-ratking-bloater-1))) {
                        ratking-spawn-lower-phase();
                    }
                }
                lambda {
                    set-object-health(npc-ratking-bloater-1, ratking-lower-phase-starting-health());
                }
                lambda {
                    set-object-health(npc-superstalker-3, ratking-stalker-lower-phase-starting-health());
                }
                lambda {
                    go(wait-reach-damage-threshhold, 1);
                }
            }
        }
    }
    state resume-spawn {
        block start {
            track main {
                lambda {
                    ratking-spawn-lower-phase();
                }
                lambda {
                    wait-for-entity-spawned(npc-superstalker-3);
                }
                lambda {
                    set-object-health(npc-ratking-bloater-1, ratking-lower-phase-starting-health());
                }
                lambda {
                    set-object-health(npc-superstalker-3, ratking-stalker-lower-phase-starting-health());
                }
                lambda {
                    if (task-continue-active?(medicine, med-hospital-basement-ratking-basement-fight, split-igc-play)) {
                        go(wait-split, 1);
                    } else {
                        go(wait-reach-damage-threshhold, 1);
                    }
                }
            }
        }
    }
    state wait-reach-damage-threshhold {
        block start {
            track main {
                lambda {
                    set-float(hit-time, current-time(1), self);
                }
                lambda {
                    wait-one-frame();
                }
                lambda {
                    if (task-continue-active?(medicine, med-hospital-basement-ratking-basement-fight, split-igc-setup)) {
                        set-object-health(npc-ratking-bloater-1, 10.00);
                        set-object-health(npc-superstalker-3, 10.00);
                    }
                }
                lambda {
                    wait(0.20);
                }
                lambda {
                    say(npc-ratking-bloater-1, vox-inf-igc-med-hospital-basement-ratking-basement-fight-start-bellow, 2, 0);
                }
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 4, 1);
                    *(i32 *)*(*var_0 + (0 * 8)) = 4;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = 0;
                    *(u8 *)(*(*var_0 + (0 * 8)) + 8) = 1;
                    *(i32 *)*(*var_0 + (1 * 8)) = 3;
                    *(u64 *)(*(*var_0 + (1 * 8)) + 8) = 0;
                    *(u8 *)(*(*var_0 + (1 * 8)) + 8) = 1;
                    *(i32 *)*(*var_0 + (2 * 8)) = 28;
                    *(u64 *)(*(*var_0 + (2 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (2 * 8)) + 8) = 0.30;
                    *(i32 *)*(*var_0 + (3 * 8)) = 30;
                    *(u64 *)(*(*var_0 + (3 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (3 * 8)) + 8) = 0.30;
                    wait-animate_(npc-ratking-bloater-1, ratking-ratking-charge-tell-bellow, var_0);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (npc-alive?(npc-ratking-bloater-1) && (get-object-health(npc-ratking-bloater-1) + get-object-health(npc-superstalker-3)) < 1100.00) {
                        broadcast-event(enable-split);
                        go(wait-split, 1);
                    }
                }
                lambda {
                    if (npc-alive?(npc-ratking-bloater-1) && (get-object-health(npc-ratking-bloater-1) + get-object-health(npc-superstalker-3)) < 1250.00) {
                        melee-fact-set-bool(npc-ratking-bloater-1, shambler-explode-disabled, 1);
                        decay-all-infected-clouds(2.00);
                    }
                }
            }
        }
        block event_ratking-attacked {
            track main {
                lambda {
                    set-float(hit-time, current-time(1), self);
                }
            }
        }
        block event_checked-exit {
            track main {
                lambda {
                    f32 var_2;
                    if ((current-time(1) - 20.00) < get-float(hit-time, self)) {
                        var_2 = current-time(1) - 20.00;
                    } else {
                        var_2 = get-float(hit-time, self);
                    }
                    set-float(hit-time, var_2, self);
                }
            }
        }
        block event_player-died {
            track main {
                lambda {
                    set-svar(timed-trap-door-level, 1 + get-svar(timed-trap-door-level));
                }
            }
        }
    }
    state wait-split {
        block event_split-complete {
            track main {
                lambda {
                    go(phase-end, 1);
                }
            }
        }
    }
    state phase-end {
        block start {
            track main {
                lambda {
                    task-complete!(medicine, med-hospital-basement-ratking-basement-fight);
                }
                lambda {
                    wait-for-event(
                        task-complete,
                        0,
                        0,
                        0,
                        0,
                        0,
                        0,
                        0,
                        0,
                        0
                    );
                }
                lambda {
                    send-event(set-split-default-checkpoint, ss-med-ratking-fight-split-checkpoints-1);
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
    }
}
