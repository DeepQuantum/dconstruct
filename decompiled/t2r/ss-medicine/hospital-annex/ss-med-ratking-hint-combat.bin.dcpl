// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...n_unpacked/dc1\ss-medicine\hospital-annex\ss-med-ratking-hint-combat.bin  #
// #   filesize: 8073 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 24                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bool inside-back-room? = nullptr
        bool changed-area? = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(medicine, med-hospital-boss-return-to-lev)) {
                        go(complete, 1);
                    }
                }
                lambda {
                    if (task-active?(medicine, med-hospital-basement-finding-exit)) {
                        go(wait-hint-after-combat, 1);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-active?(medicine, med-hospital-basement-finding-exit)) {
                        go(wait-hint-after-combat, 1);
                    }
                }
            }
        }
    }
    state wait-hint-after-combat {
        block start {
            track main {
                lambda {
                    send-event(turn-on, hint-waypoint-ratking-dead-1);
                }
            }
        }
        block event_hint-displayed {
            track main {
                lambda {
                    go(do-hint-after-combat, 1);
                }
            }
        }
        block event_enter {
            track main {
                lambda {
                    send-event(turn-off, hint-waypoint-ratking-dead-1);
                }
                lambda {
                    set-boolean(inside-back-room?, 1, self);
                }
            }
        }
        block event_hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-dead-1) {
                        go(wait-hint-climb, 1);
                    }
                }
            }
        }
    }
    state do-hint-after-combat {
        block  {
        }
        block event_enter {
            track main {
                lambda {
                    send-event(turn-off, hint-waypoint-ratking-dead-1);
                }
                lambda {
                    set-boolean(inside-back-room?, 1, self);
                }
            }
        }
        block event_hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-dead-1) {
                        go(wait-hint-climb, 1);
                    }
                }
            }
        }
    }
    state wait-hint-climb {
        block start {
            track main {
                lambda {
                    send-event(turn-on, hint-waypoint-ratking-dead-2);
                }
            }
        }
        block event_hint-displayed {
            track main {
                lambda {
                    go(do-hint-climb, 1);
                }
            }
        }
        block event_enter {
            track main {
                lambda {
                    send-event(turn-off, hint-waypoint-ratking-dead-1);
                }
                lambda {
                    set-boolean(inside-back-room?, 1, self);
                }
            }
        }
        block event_exit {
            track main {
                lambda {
                    set-boolean(inside-back-room?, 0, self);
                }
            }
        }
        block event_hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-dead-2) {
                        go(complete, 1);
                    }
                }
            }
        }
    }
    state do-hint-climb {
        block start {
            track main {
                lambda {
                    if (!get-boolean(inside-back-room?, self)) {
                        send-event(turn-on, hint-waypoint-ratking-dead-zero-time-1);
                        send-event(turn-off, hint-waypoint-ratking-dead-2);
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-boolean(changed-area?, self)) {
                        set-boolean(changed-area?, 0, self);
                        if (get-boolean(inside-back-room?, self)) {
                            send-event(turn-on, hint-waypoint-ratking-dead-zero-time-2);
                            send-event(turn-off, hint-waypoint-ratking-dead-1);
                            send-event(turn-off, hint-waypoint-ratking-dead-zero-time-1);
                        } else if (!get-boolean(inside-back-room?, self)) {
                            send-event(turn-on, hint-waypoint-ratking-dead-zero-time-1);
                            send-event(turn-off, hint-waypoint-ratking-dead-2);
                            send-event(turn-off, hint-waypoint-ratking-dead-zero-time-2);
                        }
                    }
                }
            }
        }
        block event_enter {
            track main {
                lambda {
                    set-boolean(inside-back-room?, 1, self);
                }
                lambda {
                    set-boolean(changed-area?, 1, self);
                }
            }
        }
        block event_exit {
            track main {
                lambda {
                    set-boolean(inside-back-room?, 0, self);
                }
                lambda {
                    set-boolean(changed-area?, 1, self);
                }
            }
        }
        block event_enter {
            track main {
                lambda {
                    go(complete, 1);
                }
            }
        }
    }
}
