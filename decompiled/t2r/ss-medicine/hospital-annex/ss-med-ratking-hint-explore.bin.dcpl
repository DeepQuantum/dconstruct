// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-medicine\hospital-annex\ss-med-ratking-hint-explore.bin#
// #   filesize: 11887 bytes                                                                          #
// #   total entries: 1                                                                               #
// #   functions: 32                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bool vox-clickers-dead? = nullptr
        bool inside-valve-pause? = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(medicine, med-hospital-basement-ratking-chase-past-squeeze)) {
                        go(complete, 1);
                    }
                }
                lambda {
                    if (task-active?(medicine, med-hospital-basement-power-on)) {
                        go(disable-find-power-hints, 1);
                    }
                }
                lambda {
                    if (task-range-active?(medicine, med-hospital-basement-outside-quarantine, medicine, med-hospital-basement-finding-power)) {
                        go(wait-hint-surgeryentrance, 1);
                    }
                }
                lambda {
                    if (task-active?(medicine, med-hospital-basement-back-rooms)) {
                        go(wait-hint-valve, 1);
                    }
                }
                lambda {
                    if (task-active?(medicine, med-hospital-annex-descend)) {
                        go(wait-hint-dropdown, 1);
                    }
                }
            }
        }
        block event task-complete {
            track main {
                lambda {
                    if (task-active?(medicine, med-hospital-annex-descend)) {
                        go(wait-hint-dropdown, 1);
                    }
                }
            }
        }
    }
    state wait-hint-dropdown {
        block event hint-displayed {
            track main {
                lambda {
                    go(do-hint-dropdown, 1);
                }
            }
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-drop-down-1) {
                        go(wait-hint-surgeryentrance, 1);
                    }
                }
            }
        }
    }
    state do-hint-dropdown {
        block  {
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-drop-down-1) {
                        go(wait-hint-surgeryentrance, 1);
                    }
                }
            }
        }
    }
    state wait-hint-surgeryentrance {
        block event hint-displayed {
            track main {
                lambda {
                    go(do-hint-surgeryentrance, 1);
                }
            }
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-surgeryentrance-1) {
                        go(wait-hint-valve, 1);
                    }
                }
            }
        }
    }
    state do-hint-surgeryentrance {
        block  {
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-surgeryentrance-1) {
                        go(wait-hint-valve, 1);
                    }
                }
            }
        }
    }
    state wait-hint-valve {
        block event hint-displayed {
            track main {
                lambda {
                    go(do-hint-valve, 1);
                }
            }
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-valve-1) {
                        go(disable-find-power-hints, 1);
                    }
                }
            }
        }
        block event pause-valve-hint {
            track main {
                lambda {
                    set-boolean(inside-valve-pause?, 1, self);
                }
            }
        }
        block event unpause-valve-hint {
            track main {
                lambda {
                    set-boolean(inside-valve-pause?, 0, self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-boolean(inside-valve-pause?, self)) {
                        send-event(pause-timer, hint-waypoint-ratking-valve-1);
                    }
                }
            }
        }
    }
    state do-hint-valve {
        block  {
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-valve-1) {
                        go(disable-find-power-hints, 1);
                    }
                }
            }
        }
    }
    state disable-find-power-hints {
        block start {
            track main {
                lambda {
                    send-event(turn-off, hint-waypoint-ratking-surgeryentrance-1);
                }
                lambda {
                    send-event(turn-off, hint-waypoint-ratking-valve-1);
                }
                lambda {
                    send-event(turn-off, hint-waypoint-ratking-drop-down-1);
                }
                lambda {
                    go(wait-hint-surgery-exit, 1);
                }
            }
        }
    }
    state wait-hint-surgery-exit {
        block event hint-displayed {
            track main {
                lambda {
                    go(do-hint-surgery-exit, 1);
                }
            }
        }
        block event all-clickers-dead {
            track main {
                lambda {
                    if (!get-boolean(vox-clickers-dead?, self)) {
                        set-boolean(vox-clickers-dead?, 1, self);
                        send-event(turn-on, hint-waypoint-ratking-surgeryexit-1);
                    }
                }
            }
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-surgeryexit-1) {
                        go(wait-hint-ambulance-garage, 1);
                    }
                }
            }
        }
    }
    state do-hint-surgery-exit {
        block  {
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-surgeryexit-1) {
                        go(wait-hint-ambulance-garage, 1);
                    }
                }
            }
        }
    }
    state wait-hint-ambulance-garage {
        block event hint-displayed {
            track main {
                lambda {
                    go(do-hint-ambulance-garage, 1);
                }
            }
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-ambulance-garage-1) {
                        go(wait-hint-ambulance, 1);
                    }
                }
            }
        }
    }
    state do-hint-ambulance-garage {
        block  {
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-ambulance-garage-1) {
                        go(wait-hint-ambulance, 1);
                    }
                }
            }
        }
    }
    state wait-hint-ambulance {
        block event hint-displayed {
            track main {
                lambda {
                    go(do-hint-ambulance, 1);
                }
            }
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-ambulance-1) {
                        go(complete, 1);
                    }
                }
            }
        }
    }
    state do-hint-ambulance {
        block  {
        }
        block event hint-off {
            track main {
                lambda {
                    if (event-sender() == hint-waypoint-ratking-ambulance-1) {
                        go(complete, 1);
                    }
                }
            }
        }
    }
}
