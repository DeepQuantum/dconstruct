// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-broner\ss-test-broner-prone.bin           #
// #   filesize: 24306 bytes                                                                          #
// #   total entries: 11                                                                              #
// #   functions: 42                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

u64? rb-prone-alloc-mul-quat(f32 arg_0, f32 arg_1) {
    return alloc-quat(
        (((*(f32*)arg_0 * *(f32*)(arg_1 + 12)) + (*(f32*)(arg_0 + 12) * *(f32*)arg_1)) + ((-1.00 * *(f32*)(arg_0 + 8)) * *(f32*)(arg_1 + 4))) + (*(f32*)(arg_0 + 4) * *(f32*)(arg_1 + 8)),
        (((*(f32*)(arg_0 + 4) * *(f32*)(arg_1 + 12)) + (*(f32*)(arg_0 + 8) * *(f32*)arg_1)) + (*(f32*)(arg_0 + 12) * *(f32*)(arg_1 + 4))) + ((-1.00 * *(f32*)arg_0) * *(f32*)(arg_1 + 8)),
        (((*(f32*)(arg_0 + 8) * *(f32*)(arg_1 + 12)) + ((-1.00 * *(f32*)(arg_0 + 4)) * *(f32*)arg_1)) + (*(f32*)arg_0 * *(f32*)(arg_1 + 4))) + (*(f32*)(arg_0 + 12) * *(f32*)(arg_1 + 8)),
        (((*(f32*)(arg_0 + 12) * *(f32*)(arg_1 + 12)) + ((-1.00 * *(f32*)arg_0) * *(f32*)arg_1)) + ((-1.00 * *(f32*)(arg_0 + 4)) * *(f32*)(arg_1 + 4))) + ((-1.00 * *(f32*)(arg_0 + 8)) * *(f32*)(arg_1 + 8)),
        0
    );
}

u64? rb-prone-set-width-x(u64? arg_0, u64? arg_1, u64? arg_2) {
    u64? var_0 = get-object-position(arg_0);
    *(f32*)var_0 = *(f32*)var_0 + arg_2;
    return rb-set-object-position(arg_1, var_0);
}

u64? rb-prone-set-height(u64? arg_0, u64? arg_1) {
    u64? var_0 = get-object-position(arg_0);
    *(f32*)(var_0 + 4) = arg_1;
    return rb-set-object-position(arg_0, var_0);
}

u64? rb-prone-move-object(u64? arg_0, u64? arg_1) {
    return set-object-position(arg_0, offset-point(get-object-position(arg_0), arg_1));
}

u64? rb-prone-adjust-ground-height(u64? arg_0, u64? arg_1, u64? arg_2) {
    return rb-prone-move-object(arg_0, alloc-vector(0.00, rb-prone-get-ground-height(arg_0, arg_1, arg_2), 0.00, 1.00, 0));
}

u64? rb-prone-set-rot(u64? arg_0, u64? arg_1, u64? arg_2) {
    return set-object-rotation(arg_0, rb-prone-get-target-rot(arg_1, arg_2, get-locator-rotation(get-spawner-locator(arg_0, 1))));
}

u64? rb-prone-set-bump-height(u64? arg_0, u64? arg_1) {
    return send-event(set-bump-height, rb-prone-hill-1, boxed_f32(arg_0), boxed_f32(arg_1));
}

u64? rb-set-object-position(u64? arg_0, u64? arg_1) {
    return set-object-locator(arg_0, alloc-bound-frame(arg_1, get-object-rotation(arg_0), get-object-parent(arg_0), 0, 0));
}

u64? rb-prone-get-target-rot(u64? arg_0, u64? arg_1, u64? arg_2) {
    return rb-prone-alloc-mul-quat(quat-from-axis-angle(rotate-vector("", quat-from-axis-angle("", arg_1)), arg_0), arg_2);
}

u64? rb-prone-get-ground-height(u64? arg_0, u64? arg_1, u64? arg_2) {
    u64? var_0 = get-object-position(arg_0);
    u64? var_5 = rotate-vector("", rb-prone-get-target-rot(arg_1, arg_2, alloc-quat(0.00, 0.00, 0.00, 1.00, 0)));
    return -dot-product(alloc-vector(
        *(f32*)var_0,
        0.00,
        *(f32*)(var_0 + 8),
        1.00,
        0
    ), var_5) / dot-product("", var_5);
}

statescript {
    options {
        player
    }
    declarations {
        f32 bump-dist = 0.00
        f32 dist = 0.00
        f32 rot-tilt = 0.00
        f32 rot-dir = 0.00
        f32 bump-height = 0.50
        f32 bump-width = 2.00
        f32 climb-height = 0.25
        f32 ceiling-height = 1.00
        f32 ground-rot-tilt = 0.00
        f32 ground-rot-dir = 0.00
        f32 obj-rot-tilt = 0.00
        f32 obj-rot-dir = 0.00
        f32 wall-width = 1.00
        f32 test-width = 1.00
        f32 test-height = 0.50
    }
    state initial {
        block start {
            track main {
                lambda {
                    rb-prone-set-bump-height(get-float(bump-height, self), get-float(bump-width, self));
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (joypad-command-active?(interact, 6)) {
                        go(control, 1);
                    }
                }
            }
        }
    }
    state control {
        block  {
        }
        block update {
            track main {
                lambda {
                    joypad-disable-all-commands-except(
                        debug-script-press-up,
                        debug-script-press-down,
                        debug-script-press-left,
                        debug-script-press-right,
                        debug-script-hold-l1,
                        debug-script-hold-r1,
                        debug-script-hold-l2,
                        debug-script-hold-r2,
                        debug-script-release-triangle,
                        debug-script-hold-circle
                    );
                }
                lambda {
                    set-float(rot-tilt, 5.00, self);
                }
                lambda {
                    set-float(rot-dir, 15.00, self);
                }
                lambda {
                    set-float(dist, 0.05, self);
                }
                lambda {
                    set-float(bump-dist, 0.10, self);
                }
                lambda {
                    if (joypad-command-active?(debug-script-hold-l1, 6) && !joypad-command-active?(debug-script-hold-r1, 6)) {
                        set-float(rot-tilt, get-float(rot-tilt, self) / 100.00, self);
                        set-float(rot-dir, get-float(rot-dir, self) / 100.00, self);
                        set-float(dist, get-float(dist, self) / 100.00, self);
                        set-float(bump-dist, get-float(bump-dist, self) / 100.00, self);
                    }
                }
                lambda {
                    if (joypad-command-active?(debug-script-hold-r1, 6) && !joypad-command-active?(debug-script-hold-l1, 6)) {
                        set-float(rot-tilt, get-float(rot-tilt, self) / 10.00, self);
                        set-float(rot-dir, get-float(rot-dir, self) / 10.00, self);
                        set-float(dist, get-float(dist, self) / 10.00, self);
                        set-float(bump-dist, get-float(bump-dist, self) / 10.00, self);
                    }
                }
                lambda {
                    if (joypad-command-active?(debug-script-hold-l1, 6) && joypad-command-active?(debug-script-hold-r1, 6)) {
                        set-float(rot-tilt, get-float(rot-tilt, self) * 10.00, self);
                        set-float(rot-dir, get-float(rot-dir, self) * 10.00, self);
                        set-float(dist, get-float(dist, self) * 10.00, self);
                        set-float(bump-dist, get-float(bump-dist, self) * 10.00, self);
                    }
                }
                lambda {
                    if (joypad-command-active?(debug-script-press-up, 6)) {
                        if (joypad-command-active?(debug-script-hold-circle, 6)) {
                            set-float(bump-height, get-float(bump-height, self) + get-float(bump-dist, self), self);
                            rb-prone-set-bump-height(get-float(bump-height, self), get-float(bump-width, self));
                        } else if (joypad-command-active?(debug-script-hold-l2, 6)) {
                            set-float(ground-rot-tilt, get-float(ground-rot-tilt, self) + get-float(rot-tilt, self), self);
                        } else if (joypad-command-active?(debug-script-hold-r2, 6)) {
                            set-float(obj-rot-tilt, get-float(obj-rot-tilt, self) + get-float(rot-tilt, self), self);
                        } else {
                            set-float(climb-height, get-float(climb-height, self) + get-float(dist, self), self);
                            set-float(wall-width, get-float(wall-width, self) + get-float(dist, self), self);
                        }
                    }
                }
                lambda {
                    if (joypad-command-active?(debug-script-press-down, 6)) {
                        if (joypad-command-active?(debug-script-hold-circle, 6)) {
                            set-float(bump-height, get-float(bump-height, self) + -get-float(bump-dist, self), self);
                            rb-prone-set-bump-height(get-float(bump-height, self), get-float(bump-width, self));
                        } else if (joypad-command-active?(debug-script-hold-l2, 6)) {
                            set-float(ground-rot-tilt, get-float(ground-rot-tilt, self) + -get-float(rot-tilt, self), self);
                        } else if (joypad-command-active?(debug-script-hold-r2, 6)) {
                            set-float(obj-rot-tilt, get-float(obj-rot-tilt, self) + -get-float(rot-tilt, self), self);
                        } else {
                            set-float(climb-height, get-float(climb-height, self) + -get-float(dist, self), self);
                            set-float(wall-width, get-float(wall-width, self) + -get-float(dist, self), self);
                        }
                    }
                }
                lambda {
                    if (joypad-command-active?(debug-script-press-right, 6)) {
                        if (joypad-command-active?(debug-script-hold-circle, 6)) {
                            set-float(bump-width, get-float(bump-width, self) + get-float(bump-dist, self), self);
                            rb-prone-set-bump-height(get-float(bump-height, self), get-float(bump-width, self));
                        } else if (joypad-command-active?(debug-script-hold-l2, 6)) {
                            set-float(ground-rot-dir, get-float(ground-rot-dir, self) + get-float(rot-dir, self), self);
                        } else if (joypad-command-active?(debug-script-hold-r2, 6)) {
                            set-float(obj-rot-dir, get-float(obj-rot-dir, self) + get-float(rot-dir, self), self);
                        } else {
                            set-float(ceiling-height, get-float(ceiling-height, self) + get-float(dist, self), self);
                        }
                    }
                }
                lambda {
                    if (joypad-command-active?(debug-script-press-left, 6)) {
                        if (joypad-command-active?(debug-script-hold-circle, 6)) {
                            set-float(bump-width, get-float(bump-width, self) + -get-float(bump-dist, self), self);
                            rb-prone-set-bump-height(get-float(bump-height, self), get-float(bump-width, self));
                        } else if (joypad-command-active?(debug-script-hold-l2, 6)) {
                            set-float(ground-rot-dir, get-float(ground-rot-dir, self) + -get-float(rot-dir, self), self);
                        } else if (joypad-command-active?(debug-script-hold-r2, 6)) {
                            set-float(obj-rot-dir, get-float(obj-rot-dir, self) + -get-float(rot-dir, self), self);
                        } else {
                            set-float(ceiling-height, get-float(ceiling-height, self) + -get-float(dist, self), self);
                        }
                    }
                }
                lambda {
                    rb-prone-set-width-x(rb-prone-vent-wall-1, rb-prone-vent-wall-2, get-float(wall-width, self));
                }
                lambda {
                    rb-prone-set-rot(rb-prone-ground-plane-1, get-float(ground-rot-tilt, self), get-float(ground-rot-dir, self));
                }
                lambda {
                    rb-prone-set-height(rb-prone-block-1, get-float(climb-height, self));
                }
                lambda {
                    rb-prone-set-height(rb-prone-roof-1, (1.00 + get-float(climb-height, self)) + get-float(ceiling-height, self));
                }
                lambda {
                    rb-prone-set-height(rb-prone-roof-2, 1.00 + get-float(ceiling-height, self));
                }
                lambda {
                    rb-prone-set-rot(rb-prone-block-1, get-float(obj-rot-tilt, self), get-float(obj-rot-dir, self));
                }
                lambda {
                    rb-prone-set-rot(rb-prone-roof-1, get-float(obj-rot-tilt, self), get-float(obj-rot-dir, self));
                }
                lambda {
                    rb-prone-set-rot(rb-prone-roof-2, get-float(obj-rot-tilt, self), get-float(obj-rot-dir, self));
                }
                lambda {
                    debug-draw-string-2d(
                        alloc-point(440.00, 230.00, 0.00, 0),
                        dc:format("[     U/D] Climb Height:    %.3f", get-float(climb-height, self)),
                        1,
                        0.00,
                        1.00,
                        1,
                        0,
                        0,
                        1.00
                    );
                }
                lambda {
                    debug-draw-string-2d(
                        alloc-point(440.00, 250.00, 0.00, 0),
                        dc:format("[     U/D] Wall Width:      %.3f", get-float(wall-width, self)),
                        1,
                        0.00,
                        1.00,
                        1,
                        0,
                        0,
                        1.00
                    );
                }
                lambda {
                    debug-draw-string-2d(
                        alloc-point(440.00, 270.00, 0.00, 0),
                        dc:format("[     L/R] Ceiling Height:  %.3f", get-float(ceiling-height, self)),
                        1,
                        0.00,
                        1.00,
                        1,
                        0,
                        0,
                        1.00
                    );
                }
                lambda {
                    debug-draw-string-2d(
                        alloc-point(440.00, 290.00, 0.00, 0),
                        dc:format("[L2 + U/D] Ground Rot Tilt: %.3f", get-float(ground-rot-tilt, self)),
                        1,
                        0.00,
                        1.00,
                        1,
                        0,
                        0,
                        1.00
                    );
                }
                lambda {
                    debug-draw-string-2d(
                        alloc-point(440.00, 310.00, 0.00, 0),
                        dc:format("[L2 + L/R] Ground Rot Dir:  %.3f", get-float(ground-rot-dir, self)),
                        1,
                        0.00,
                        1.00,
                        1,
                        0,
                        0,
                        1.00
                    );
                }
                lambda {
                    debug-draw-string-2d(
                        alloc-point(440.00, 330.00, 0.00, 0),
                        dc:format("[R2 + U/D] Object Rot Tilt: %.3f", get-float(obj-rot-tilt, self)),
                        1,
                        0.00,
                        1.00,
                        1,
                        0,
                        0,
                        1.00
                    );
                }
                lambda {
                    debug-draw-string-2d(
                        alloc-point(440.00, 350.00, 0.00, 0),
                        dc:format("[R2 + L/R] Object Rot Dir:  %.3f", get-float(obj-rot-dir, self)),
                        1,
                        0.00,
                        1.00,
                        1,
                        0,
                        0,
                        1.00
                    );
                }
                lambda {
                    debug-draw-string-2d(
                        alloc-point(440.00, 370.00, 0.00, 0),
                        dc:format("[ O + U/D] Terrain Height:  %.3f", get-float(bump-height, self)),
                        1,
                        0.00,
                        1.00,
                        1,
                        0,
                        0,
                        1.00
                    );
                }
                lambda {
                    debug-draw-string-2d(
                        alloc-point(440.00, 390.00, 0.00, 0),
                        dc:format("[ O + L/R] Terrain Width:   %.3f", get-float(bump-width, self)),
                        1,
                        0.00,
                        1.00,
                        1,
                        0,
                        0,
                        1.00
                    );
                }
                lambda {
                    if (joypad-command-active?(debug-script-release-triangle, 6)) {
                        go(initial, 1);
                    }
                }
            }
        }
    }
}
