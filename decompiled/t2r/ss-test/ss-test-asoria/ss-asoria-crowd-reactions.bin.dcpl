// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-asoria\ss-asoria-crowd-reactions.bin      #
// #   filesize: 12545 bytes                                                                          #
// #   total entries: 4                                                                               #
// #   functions: 33                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

u64? update-distance-and-frontness() {
    return set-float(dist-to-player, get-object-distance(player, get-self-id()), self);
}

u16 select-reaction-anim-new() {
    u64? var_1 = get-object-locator(get-self-id());
    u64? var_2 = get-object-locator(player);
    u64? var_3 = get-object-facing-angle-plus(var_2, var_1, 2, 1);
    u64? var_4 = get-object-facing-angle-plus(var_1, var_2, 2, 1);
    u16 var_5;
    if (var_4 > 0.00 && var_4 < 90.00 && var_3 > 0.00 && var_3 < 90.00) {
        var_5 = set-symbol(react-anim, snpc-squeeze-react-front-right-gesture, self);
    } else if (var_4 > 0.00 && var_4 < 90.00 && var_3 > -90.00 && var_3 < 0.00) {
        var_5 = set-symbol(react-anim, snpc-squeeze-react-right-front-gesture, self);
    } else if (var_4 < 0.00 && var_4 > -90.00 && var_3 > 0.00 && var_3 < 90.00) {
        var_5 = set-symbol(react-anim, snpc-squeeze-react-left-front-gesture, self);
    } else if (var_4 < 0.00 && var_4 > -90.00 && var_3 > -90.00 && var_3 < 0.00) {
        var_5 = set-symbol(react-anim, snpc-squeeze-react-front-left-gesture, self);
    } else if (var_4 < -90.00 && var_4 > -180.00 && var_3 > 0.00 && var_3 < 90.00) {
        var_5 = set-symbol(react-anim, snpc-squeeze-react-back-left-gesture, self);
    } else if (var_4 < -90.00 && var_4 > -180.00 && var_3 > -90.00 && var_3 < 0.00) {
        var_5 = set-symbol(react-anim, snpc-squeeze-react-left-back-gesture, self);
    } else if (var_4 > 90.00 && var_4 < 180.00 && var_3 > 0.00 && var_3 < 90.00) {
        var_5 = set-symbol(react-anim, snpc-squeeze-react-right-back-gesture, self);
    } else if (var_4 > 90.00 && var_4 < 180.00 && var_3 > -90.00 && var_3 < 0.00) {
        var_5 = set-symbol(react-anim, snpc-squeeze-react-back-right-gesture, self);
    } else {
        var_5 = 0;
    }
    return var_5;
}

u16 select-reaction-idle-lean() {
    get-object-locator(get-self-id());
    get-object-locator(player);
    u64? var_4 = get-object-facing-angle-plus(get-object-locator(get-self-id()), get-object-locator(player), 2, 1);
    u16 var_5;
    if (var_4 < -45.00 && var_4 > -135.00) {
        var_5 = set-symbol(react-idle-lean, snpc-idle-lean-rt-gesture, self);
    } else if (var_4 > 135.00 && var_4 < -135.00) {
        var_5 = set-symbol(react-idle-lean, snpc-idle-lean-fw-gesture, self);
    } else if (var_4 > -45.00 && var_4 < 45.00) {
        var_5 = set-symbol(react-idle-lean, snpc-idle-lean-bw-gesture, self);
    } else if (var_4 > 45.00 && var_4 < 135.00) {
        var_5 = set-symbol(react-idle-lean, snpc-idle-lean-lt-gesture, self);
    } else {
        var_5 = 0;
    }
    return var_5;
}

statescript {
    options {
        player
    }
    declarations {
        f32 frontness = 0.00
        f32 dist-to-player = 0.00
        f32 phase = 0.00
        f32 angle-npc = 0.00
        f32 angle-player = 0.00
        f32 frame = 0.00
        symbol look-gesture = npc-m-head-look-generic-gesture
        symbol overlay-idle = #0000000000000000
        symbol react-anim = npc-m-nw-mm-ambi-idle
        symbol react-idle-anim = #0000000000000000
        symbol react-idle-lean = #0000000000000000
        symbol old-react-anim = #0000000000000000
    }
    state initial {
        block start {
            track main {
                lambda {
                    while (!entity-exists?(get-self-id())) {
                        wait-one-frame();
                    }
                }
                lambda {
                    wait(0.10);
                }
                lambda {
                    set-symbol(overlay-idle, get-object-anim(get-object-id(self), 1), self);
                }
                lambda {
                    go(normal, 1);
                }
            }
        }
    }
    state normal {
        block start {
            track main {
                lambda {
                    npc-clear-look-at(get-object-id(self));
                }
                lambda {
                    stop-animating(get-object-id(self), 2, 1.00, 0, 0);
                }
                lambda {
                    clear-gesture(get-self-id(), -1, -1.00, 0);
                }
            }
        }
        block event simple-npc-near-player {
            track main {
                lambda {
                    update-distance-and-frontness();
                }
                lambda {
                    if (get-float(dist-to-player, self) < 2.50) {
                        go(normal-near-player, 1);
                    }
                }
            }
        }
    }
    state normal-near-player {
        block update {
            track main {
                lambda {
                    update-distance-and-frontness();
                }
                lambda {
                    if (get-float(dist-to-player, self) >= 2.50) {
                        npc-clear-look-at(get-self-id());
                        go(normal, 1);
                    } else {
                        go(pre-react, 1);
                    }
                }
            }
        }
    }
    state pre-react {
        block  {
        }
        block update {
            track main {
                lambda {
                    update-distance-and-frontness();
                }
                lambda {
                    select-reaction-anim-new();
                }
                lambda {
                    select-reaction-idle-lean();
                }
                lambda {
                    set-float(angle-player, get-object-facing-angle-plus(get-object-locator(get-self-id()), get-object-locator(player), 2, 1), self);
                }
                lambda {
                    if (get-float(dist-to-player, self) < 0.80) {
                        go(react, 1);
                    }
                }
                lambda {
                    if (get-float(dist-to-player, self) > 2.50) {
                        go(normal, 1);
                    }
                }
            }
        }
    }
    state react {
        block start {
            track main {
                lambda {
                    set-symbol(old-react-anim, get-symbol(react-anim, self), self);
                }
                lambda {
                    u64? var_2 = alloc-gesture-play-params();
                    *(u64*)(var_2 + 8) = player;
                    *(u8*)(var_2 + 2) = 1;
                    gesture_(get-self-id(), get-symbol(react-anim, self), var_2);
                }
            }
        }
        block update {
            track main {
                lambda {
                    update-distance-and-frontness();
                }
                lambda {
                    select-reaction-anim-new();
                }
                lambda {
                    if (get-float(dist-to-player, self) > 0.80) {
                        go(normal, 1);
                    }
                }
                lambda {
                    if (!(get-symbol(react-anim, self) == get-symbol(old-react-anim, self)) && get-float(dist-to-player, self) < 0.80) {
                        go(react-2, 1);
                    }
                }
            }
        }
    }
    state react-2 {
        block start {
            track main {
                lambda {
                    set-symbol(old-react-anim, get-symbol(react-anim, self), self);
                }
                lambda {
                    u64? var_2 = alloc-gesture-play-params();
                    *(u64*)(var_2 + 8) = player;
                    *(u8*)(var_2 + 2) = 1;
                    gesture_(get-self-id(), get-symbol(react-anim, self), var_2);
                }
            }
        }
        block update {
            track main {
                lambda {
                    select-reaction-anim-new();
                }
                lambda {
                    select-reaction-idle-lean();
                }
                lambda {
                    update-distance-and-frontness();
                }
                lambda {
                    if (get-float(dist-to-player, self) > 1.00) {
                        go(normal, 1);
                    }
                }
                lambda {
                    if (!(get-symbol(react-anim, self) == get-symbol(old-react-anim, self)) && get-float(dist-to-player, self) < 0.80) {
                        go(react, 1);
                    }
                }
            }
        }
    }
}
