// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-em\ss-es-puzzle-manual-stacker.bin        #
// #   filesize: 16848 bytes                                                                          #
// #   total entries: 2                                                                               #
// #   functions: 33                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

u16 manual-stacker-drop(u64? arg_0, u64? arg_1) {
    u16 var_2;
    if (timer-running?(stop-drop-timer, self) && get-boolean(lock-height?, self)) {
        u16 var_4;
        if (joypad-command-active?(radio-down, 6)) {
            var_4 = 1;
        } else if (joypad-command-active?(read-mode-scroll-down, 6)) {
            var_4 = 1;
        } else {
            var_4 = 0;
        }
        if (!!var_4) {
            var-approach-target-per-second(drop-speed, arg_0, arg_1, 0.00, 0.20);
            -=f(rise, get-float(drop-speed, self), self);
            if (get-float(rise, self) < 0.00) {
                set-float(rise, 0.00, self);
            }
            var_2 = set-animation-phase(self, get-float(rise, self), 2, 0);
        } else {
            var_2 = 0;
        }
    }
    return var_2;
}

statescript {
    options {
        player
    }
    declarations {
        f32 auto-drop-speed = 0.00
        f32 force-drop-speed = 1.50
        f32 rise = 0.00
        f32 rise-targ = 0.00
        bound-frame interact-loc = nullptr
        timer acc-timer = nullptr
        f32 drop-speed = 0.00
        bool standing-on = nullptr
        bound-frame object-offset-from-player = nullptr
        timer freeze-timer = nullptr
        timer stop-drop-timer = nullptr
        bool lock-height? = nullptr
    }
    state initial {
        block start {
            track main {
                lambda {
                    wait(0.50);
                }
                lambda {
                    set-bound-frame(interact-loc, offset-locator(get-object-locator(self), alloc-vector(0.00, 1.25, -0.50, 1.00, 0)), self);
                }
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 2, 1);
                    *(i32*)*var_0[0] = 56;
                    *(u64*)(*var_0[0] + 8) = 0;
                    *(f32*)(*var_0[0] + 8) = 0.00;
                    *(i32*)*var_0[1] = 58;
                    *(u64*)(*var_0[1] + 8) = 0;
                    *(f32*)(*var_0[1] + 8) = tag-float(rise-start, self);
                    animate_(self, lift, var_0);
                }
                lambda {
                    set-float(rise, tag-float(rise-start, self), self);
                }
                lambda {
                    set-bound-frame(object-offset-from-player, transform-locator-world-to-local(get-object-locator(self), get-spawner-locator(tag-symbol(player-pos, self), 1)), self);
                }
                lambda {
                    go(ready, 1);
                }
            }
        }
    }
    state --all-- {
        block event stop-drop {
            track main {
                lambda {
                    start-timer(stop-drop-timer, 0.10, self, 1);
                }
            }
        }
        block event force-drop {
            track main {
                lambda {
                    go(force-drop, 1);
                }
            }
        }
    }
    state ready {
        block start {
            track main {
                lambda {
                    interactable-enable(tag-symbol(interact, self));
                }
                lambda {
                    while (1) {
                        if (tag-boolean(auto-drop, self)) {
                            if (get-boolean(standing-on, self)) {
                                set-boolean(standing-on, 0, self);
                                manual-stacker-drop(0.10, 0.04);
                            } else {
                                manual-stacker-drop(get-float(auto-drop-speed, self), 0.08);
                            }
                        }
                        wait-one-frame();
                    }
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    go(engaged, 1);
                }
            }
        }
        block event standing-on {
            track main {
                lambda {
                    if (*(f32*)(point-to-vector(transform-point-world-to-local(get-object-position(player), get-object-locator(self))) + 8) > 0.25) {
                        set-boolean(standing-on, 1, self);
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (fact-symbol(player, player-interactable, 0, 0) != 0) {
                        if (!(interactable-disabled?(tag-symbol(interact, self)))) {
                            interactable-disable(tag-symbol(interact, self));
                        }
                    } else if (interactable-disabled?(tag-symbol(interact, self))) {
                        interactable-enable(tag-symbol(interact, self));
                    }
                }
            }
        }
    }
    state engaged {
        block start {
            track main {
                lambda {
                    fact-set(player, player-interactable, boxed_symbol(self));
                }
                lambda {
                    interactable-disable(tag-symbol(interact, self));
                }
                lambda {
                    wait(0.50);
                }
                lambda {
                    while (1) {
                        u16 var_1;
                        if (joypad-command-active?(interact, 6)) {
                            var_1 = 1;
                        } else {
                            var_1 = 0;
                        }
                        if (var_1) {
                            go(exit, 1);
                        } else {
                            u16 var_3;
                            if (joypad-command-active?(radio-up, 6)) {
                                var_3 = 1;
                            } else {
                                var_3 = 0;
                            }
                            if (var_3) {
                                set-float(drop-speed, 0.00, self);
                                set-float(rise-targ, get-float(rise, self) + tag-float(rise-inc, self), self);
                                if (get-float(rise-targ, self) > tag-float(max-height, self)) {
                                    set-float(rise-targ, tag-float(max-height, self), self);
                                }
                                u64? var_10 = get-float(rise-targ, self);
                                u64? var_11 = get-current-time(0);
                                while (!((get-current-time(0) - var_11) >= 0.50)) {
                                    set-animation-phase(
                                        self,
                                        curve-scale(0.00, 0.50, get-float(rise, self), var_10, get-current-time(0) - var_11, 3),
                                        2,
                                        0
                                    );
                                    joypad-disable-commands(move);
                                    wait-one-frame();
                                }
                                set-animation-phase(self, var_10, 2, 0);
                                joypad-disable-commands(move);
                                set-float(rise, get-float(rise-targ, self), self);
                                wait(0.25);
                            } else {
                                u16 var_18;
                                if (joypad-command-active?(radio-down, 6)) {
                                    var_18 = 1;
                                } else if (joypad-command-active?(read-mode-scroll-down, 6)) {
                                    var_18 = 1;
                                } else {
                                    var_18 = 0;
                                }
                                if (var_18) {
                                    manual-stacker-drop(0.30, 0.01);
                                } else {
                                    if (tag-boolean(auto-drop, self)) {
                                        manual-stacker-drop(get-float(auto-drop-speed, self), 0.04);
                                    }
                                    set-bound-frame(interact-loc, offset-locator(get-object-locator(self), alloc-vector(0.25, 1.25, -0.50, 1.00, 0)), self);
                                    draw-interactable-hud(interact-dpad-up, get-bound-frame(interact-loc, self), 0);
                                    draw-interactable-hud(interact-dpad-down, offset-locator(get-bound-frame(interact-loc, self), alloc-vector(0.00, -0.15, 0.00, 1.00, 0)), 0);
                                }
                            }
                        }
                        wait-one-frame();
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    player-disable-melee();
                }
                lambda {
                    player-disable-jump();
                }
                lambda {
                    player-force-walk();
                }
                lambda {
                    set-player-no-switch-weapon();
                }
                lambda {
                    joypad-disable-commands(consumable-press, consumable-hold, quick-turn, menu-up, menu-right, menu-left, menu-down);
                }
                lambda {
                    joypad-disable-commands(weapon-cross-up, weapon-cross-down, weapon-cross-left, weapon-cross-right, inv-open-subcross, weapon-cross-craft-press, weapon-cross-shiv-craft-press);
                }
                lambda {
                    if (!player-in-state?(Move) && player-is-crouched?()) {
                        go(exit, 1);
                    }
                }
                lambda {
                    set-object-locator(self, blend-locator(get-object-locator(self), offset-locator(transform-locator-local-to-world(get-bound-frame(object-offset-from-player, self), alloc-bound-frame(get-object-position(player), get-locator-rotation(flatten-locator(camera-locator(0))), 0, 0, 0)), alloc-vector(0.00, 0.00, 0.25, 1.00, 0)), 10.00 * get-delta-time()));
                }
                lambda {
                    joypad-set-stick-scale(camera, 0.40);
                }
            }
        }
    }
    state exit {
        block start {
            track main {
                lambda {
                    wait(1.00);
                }
                lambda {
                    fact-set(player, player-interactable, boxed_symbol(0));
                }
                lambda {
                    go(ready, 1);
                }
            }
        }
    }
    state force-drop {
        block start {
            track main {
                lambda {
                    while (!(get-float(rise, self) <= 0.00)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    go(exit, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    manual-stacker-drop(get-float(force-drop-speed, self), 0.25);
                }
            }
        }
    }
}
