// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...in/bin_unpacked/dc1\ss-test\ss-test-em\ss-es-test-infected-filaments.bin  #
// #   filesize: 6159 bytes                                                                           #
// #   total entries: 2                                                                               #
// #   functions: 9                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        f32 far-dist = 10.00
        f32 near-dist = 5.00
        f32 min-dist = 3.00
        f32 player-dist-pct = 1.00
        bound-frame start-loc = nullptr
        f32 wait-time = 0.75
        f32 anim-speed = 0.40
        f32 anim-speed-targ = 0.40
        f32 grow-speed = 0.00
        f32 offset-rest = 0.00
        f32 offset = 0.00
        f32 offset-targ = 0.00
    }
    state initial {
        block start {
            track main {
                lambda {
                    set-float(offset-rest, random-float-min-max(-0.30, -0.10, 0), self);
                }
                lambda {
                    set-bound-frame(start-loc, get-object-locator(self), self);
                }
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 1, 1);
                    *(i32 *)*(*var_0 + (0 * 8)) = 56;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (0 * 8)) + 8) = get-float(anim-speed, self);
                    animate_(self, idle, var_0);
                }
                lambda {
                    go(monitor, 1);
                }
            }
        }
    }
    state monitor {
        block start {
            track main {
                lambda {
                    while (1) {
                        u64? var_2 = get-locator-distance(get-bound-frame(start-loc, self), get-object-locator(player));
                        set-float(anim-speed-targ, lerp-scale(get-float(min-dist, self), get-float(near-dist, self), 2.00, 0.20, var_2), self);
                        set-float(offset-targ, lerp-scale(
                            get-float(min-dist, self),
                            get-float(near-dist, self),
                            0.00,
                            get-float(offset-rest, self),
                            var_2
                        ), self);
                        f32 var_11;
                        if (var_2 > get-float(far-dist, self)) {
                            var_11 = 0.75;
                        } else {
                            var_11 = 0.10;
                        }
                        set-float(wait-time, var_11, self);
                        set-float(grow-speed, 0.00, self);
                        if (get-locator-distance(get-bound-frame(start-loc, self), get-object-locator(mattress-test-2)) < 1.50) {
                            set-float(offset-targ, -1.00, self);
                            set-float(grow-speed, 0.10, self);
                        }
                        wait(get-float(wait-time, self));
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    var-approach-target(anim-speed, get-float(anim-speed-targ, self), 0.05, 0.20, 2.00);
                }
                lambda {
                    var-approach-target(offset, get-float(offset-targ, self), get-float(grow-speed, self), -3.00, 0.00);
                }
                lambda {
                    set-animation-speed(self, get-float(anim-speed, self), 2, 0);
                }
                lambda {
                    set-object-locator(get-self-id(), offset-locator(get-bound-frame(start-loc, self), alloc-vector(0.00, 0.00, get-float(offset, self), 1.00, 0)));
                }
            }
        }
    }
}
