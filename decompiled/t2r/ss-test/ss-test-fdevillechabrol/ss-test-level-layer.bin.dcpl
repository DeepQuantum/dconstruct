// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...bin_unpacked/dc1\ss-test\ss-test-fdevillechabrol\ss-test-level-layer.bin  #
// #   filesize: 4319 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 12                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    declarations {
        i32 load-enum = 3
    }
    state initial {
        block  {
        }
        block update {
            track main {
                lambda {
                    go(running, 1);
                }
            }
        }
    }
    state running {
        block event_entered-region-gameplay-off {
            track main {
                lambda {
                    set-int32(load-enum, 1, self);
                }
            }
        }
        block event_entered-region-gameplay {
            track main {
                lambda {
                    set-int32(load-enum, 2, self);
                }
            }
        }
        block event_entered-region-all {
            track main {
                lambda {
                    set-int32(load-enum, 3, self);
                }
            }
        }
        block event_entered-region-geometry {
            track main {
                lambda {
                    set-int32(load-enum, 4, self);
                }
            }
        }
        block event_entered-region-geometry-off {
            track main {
                lambda {
                    set-int32(load-enum, 5, self);
                }
            }
        }
        block event_entered-region-gameplay-load-only-1 {
            track main {
                lambda {
                    set-int32(load-enum, 6, self);
                }
            }
        }
        block event_entered-region-gameplay-load-only-2 {
            track main {
                lambda {
                    set-int32(load-enum, 6, self);
                }
            }
        }
        block event_entered-region-collision {
            track main {
                lambda {
                    set-int32(load-enum, 7, self);
                }
            }
        }
        block event_entered-region-navmesh {
            track main {
                lambda {
                    set-int32(load-enum, 8, self);
                }
            }
        }
        block event_entered-region-none {
            track main {
                lambda {
                    set-int32(load-enum, 0, self);
                }
            }
        }
        block event_entered-region-all-display-false {
            track main {
                lambda {
                    set-int32(load-enum, 9, self);
                }
            }
        }
    }
}
