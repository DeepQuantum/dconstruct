// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-afrost\ss-proto-infected-clicker-ambient-bark.bin#
// #   filesize: 6574 bytes                                                                           #
// #   total entries: 2                                                                               #
// #   functions: 9                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

u64? wait-get-bark-target() {
    u64? var_1 = npc-get-ideal-post(get-self-id(), 77);
    u64 i = 77;
    u64? var_4;
    while (ai-post-is-valid?(var_1) && !(*(i32*)(var_1 + 12) != get-int32(bark-post-id, self))) {
        wait(0.30);
        u64? var_6 = npc-get-ideal-post(get-self-id(), 77);
        var_4 = var_6;
        i = var_6;
    }
    return set-int32(bark-post-id, *(i32*)(var_4 + 12), self);
}

statescript {
    options {
        player
    }
    declarations {
        timer bark-cd = nullptr
        bool bark-active? = nullptr
        timer ambient-bark-cd = nullptr
        i32 bark-post-id = -1
    }
    state check-enabled? {
        block start {
            track main {
                lambda {
                    if (tag-boolean(clicker-ambient-prototypes-enabled?, self)) {
                        go(initial, 1);
                    }
                }
                lambda {
                    go(disabled, 1);
                }
            }
        }
    }
    state  {
    }
    state initial {
        block start {
            track main {
                lambda {
                    start-timer(ambient-bark-cd, random-float-range(5.00, 20.00, 0), self, 1);
                }
                lambda {
                    npc-set-script-low-priority(self);
                }
            }
        }
        block event ambient-bark-cd {
            track main {
                lambda {
                    if (!(timer-running?(bark-cd, self)) && !(get-boolean(bark-active?, self)) && npc-get-demeanor(self) == 0 && !(npc-using-skill?(self, 11))) {
                        if (!(timer-running?(bark-cd, self))) {
                            set-boolean(bark-active?, 1, self);
                            wait-get-bark-target();
                            if (!(ai-get-post(get-int32(bark-post-id, self)) == 0)) {
                                wait-infected-bark-at(self, ai-post-get-position(ai-get-post(get-int32(bark-post-id, self))));
                            } else {
                                wait-infected-bark-forward(self);
                            }
                            start-timer(bark-cd, 14.00, self, 1);
                            set-boolean(bark-active?, 0, self);
                        }
                    }
                }
                lambda {
                    start-timer(ambient-bark-cd, random-float-range(15.00, 20.00, 0), self, 1);
                }
            }
        }
        block event patrol-wait-end {
            track main {
                lambda {
                    if (!(get-boolean(bark-active?, self)) && !(timer-running?(bark-cd, self))) {
                        set-boolean(bark-active?, 1, self);
                        wait-infected-bark-forward(self);
                        start-timer(bark-cd, 14.00, self, 1);
                        stop-timer(ambient-bark-cd, self);
                        start-timer(ambient-bark-cd, random-float-range(15.00, 16.00, 0), self, 1);
                        set-boolean(bark-active?, 0, self);
                    }
                }
            }
        }
        block event force-bark {
            track main {
                lambda {
                    if (!(get-boolean(bark-active?, self))) {
                        set-boolean(bark-active?, 1, self);
                        wait-infected-bark-at(self, get-locator-position(event-arg-bound-frame(0)));
                        wait(3.00);
                        start-timer(bark-cd, 14.00, self, 1);
                        stop-timer(ambient-bark-cd, self);
                        start-timer(ambient-bark-cd, random-float-range(15.00, 16.00, 0), self, 1);
                        set-boolean(bark-active?, 0, self);
                    }
                }
            }
        }
    }
}
