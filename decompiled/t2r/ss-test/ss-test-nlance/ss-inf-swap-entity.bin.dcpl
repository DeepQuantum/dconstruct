// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...d/pc/main/bin_unpacked/dc1\ss-test\ss-test-nlance\ss-inf-swap-entity.bin  #
// #   filesize: 7012 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 10                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        symbol swap-from-entity = nullptr
        symbol behavior-script-instance = nullptr
    }
    state wait-start {
        block start {
            track main {
                lambda {
                    while (!entity-exists?(get-self-id())) {
                        wait-one-frame();
                    }
                }
                lambda {
                    wait(0.10);
                }
                lambda {
                    if (!(get-symbol(swap-from-entity, self) == 0)) {
                        go(swap-in, 1);
                    } else {
                        go(wait-swap, 1);
                    }
                }
            }
        }
    }
    state swap-in {
        block start {
            track main {
                lambda {
                    u64? var_3 = #%alloc-array(single-frame, 16, 3, 1);
                    *(i32 *)*(*var_3 + (0 * 8)) = 58;
                    *(u64 *)(*(*var_3 + (0 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_3 + (0 * 8)) + 8) = get-animation-phase(get-symbol(swap-from-entity, self), 2, 0);
                    *(i32 *)*(*var_3 + (1 * 8)) = 30;
                    *(u64 *)(*(*var_3 + (1 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_3 + (1 * 8)) + 8) = 0.50;
                    *(i32 *)*(*var_3 + (2 * 8)) = 32;
                    *(u64 *)(*(*var_3 + (2 * 8)) + 8) = 0;
                    *(u8 *)(*(*var_3 + (2 * 8)) + 8) = 1;
                    animate_(get-self-id(), get-object-anim(get-symbol(swap-from-entity, self), 1), var_3);
                    wait-one-frame();
                    set-visible(get-self-id(), 1);
                    stop-animating(get-self-id(), 2, 0.50, 0, 0);
                    kill-entity(get-symbol(swap-from-entity, self));
                    go(wait-swap, 1);
                }
            }
        }
    }
    state wait-swap {
        block start {
            track main {
                lambda {
                    if (tag-specified?(behavior-script, self) && get-symbol(behavior-script-instance, self) == 0) {
                        set-symbol(behavior-script-instance, spawn-script-multiple(tag-string(behavior-script, self, 0, 0), get-self-id(), 0), self);
                        wait-one-frame();
                    }
                }
                lambda {
                    if (!(get-symbol(behavior-script-instance, self) == 0)) {
                        send-event(start, get-symbol(behavior-script-instance, self));
                    }
                }
            }
        }
        block event_swap {
            track main {
                lambda {
                    go(swap-to-entity, 1);
                }
            }
        }
        block end {
            track main {
                lambda {
                    if (!(get-symbol(behavior-script-instance, self) == 0)) {
                        send-event(stop, get-symbol(behavior-script-instance, self));
                    }
                }
            }
        }
    }
    state swap-to-entity {
        block start {
            track main {
                lambda {
                    if (!is-final-build?()) {
                        display(dc:format("existing entity %s's look is %s", string-debug(get-self-id()), string-debug(get-object-look(get-self-id(), 0))), 19);
                    }
                }
                lambda {
                    u64? var_5 = spawn-entity-at-with-look(tag-symbol(swap-to-multi-spawner, self), get-object-locator(get-self-id()), get-object-look(get-self-id(), 0));
                    while (!entity-exists?(var_5)) {
                        wait-one-frame();
                    }
                    set-visible(var_5, 0);
                    if (!is-final-build?()) {
                        display(dc:format("new entity %s's look is %s", string-debug(var_5), string-debug(get-object-look(var_5, 0))), 19);
                    }
                    broadcast-event(swap-groups, boxed_symbol(get-self-id()), boxed_symbol(var_5));
                    set-symbol(swap-from-entity, get-self-id(), var_5);
                }
            }
        }
    }
}
