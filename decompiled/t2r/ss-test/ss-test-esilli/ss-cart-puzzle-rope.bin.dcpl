// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-esilli\ss-cart-puzzle-rope.bin            #
// #   filesize: 25119 bytes                                                                          #
// #   total entries: 2                                                                               #
// #   functions: 58                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        point end-position = (0.00, (0.00, (0.00)
        symbol attach-spawner = pos-cart-puzz-cart-attach-1
        bool attached? = nullptr
        bool free? = nullptr
        bound-frame closest-nav-locator = nullptr
        bool fake-attach? = nullptr
        bool pick-up-finished? = nullptr
    }
    state setup {
        block start {
            track attached {
                lambda {
                    wait-boolean(attached?, 1, self);
                }
                lambda {
                    set-boolean(attached?, 1, self);
                }
                lambda {
                    go(attached, 1);
                }
            }
            track free {
                lambda {
                    while (get-boolean(free?, self) && !((get-current-time(0) - get-current-time(0)) > 0.50)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    if (!get-boolean(free?, self)) {
                        set-point(end-position, get-spawner-position(tag-symbol(connected-position, self), 1), self);
                        set-boolean(free?, 1, self);
                    }
                }
                lambda {
                    set-object-position(tag-symbol(int-rope-end, self), get-point(end-position, self));
                }
                lambda {
                    go(free, 1);
                }
            }
        }
    }
    state free {
        block start {
            track main {
                lambda {
                    set-boolean(free?, 1, self);
                }
                lambda {
                    interactable-enable(tag-symbol(int-rope-end, self));
                }
            }
        }
        block update {
            track main {
                lambda {
                    rope-keyframe-point(self, tag-float(rope-length, self), get-point(end-position, self), 0, 0);
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    if (event-arg-symbol(0) == tag-symbol(int-rope-end, self)) {
                        go(held, 1);
                    }
                }
            }
        }
    }
    state dropping {
        block start {
            track player {
                lambda {
                    interactable-disable(int-cart-puzz-attach-1);
                }
                lambda {
                    u64? var_0 = alloc-gesture-play-params();
                    *(u8*)(var_0 + 33) = 1;
                    *(i32*)(var_0 + 36) = 19;
                    *(u8*)(var_0 + 32) = 1;
                    *(u8*)(var_0 + 34) = 1;
                    gesture_(player, ellie-rope-drop-gesture, var_0);
                }
                lambda {
                    signal(done-anim, 0);
                }
                lambda {
                    wait-for(done);
                }
                lambda {
                    go(free, 1);
                }
            }
            track rope {
                lambda {
                    wait-for(done-anim);
                }
                lambda {
                    player-drop-rope();
                }
                lambda {
                    if (get-locator-distance(get-spawner-locator(tag-symbol(start-position, self), 1), get-object-locator(player)) < (tag-float(rope-length, self) - 1.30)) {
                        set-bound-frame(closest-nav-locator, find-closest-point-on-nav-mesh(get-object-locator(player), 5.00, 5.00, 0.00, 0, 0), self);
                    }
                }
                lambda {
                    set-point(end-position, get-locator-position(get-bound-frame(closest-nav-locator, self)), self);
                }
                lambda {
                    set-object-position(tag-symbol(int-rope-end, self), get-point(end-position, self));
                }
                lambda {
                    interactable-enable(tag-symbol(int-rope-end, self));
                }
                lambda {
                    signal(done, 0);
                }
            }
            track cart {
                lambda {
                    wait-for(done-anim);
                }
                lambda {
                    interactable-enable(obj-cart-puzz-cart-1);
                }
            }
        }
    }
    state held {
        block start {
            track player {
                lambda {
                    set-boolean(pick-up-finished?, 0, self);
                }
                lambda {
                    if (get-boolean(free?, self)) {
                        signal(anim-start, 0);
                        camera-enable-animated(1.30, -1.00, 1, 1000, 0, 0, 0);
                        u64? var_1 = #%alloc-array(single-frame, 16, 3, 1);
                        *(i32*)*var_1[0] = 10;
                        *(u64*)(*var_1[0] + 8) = 0;
                        *(u64*)(*var_1[0] + 8) = rotate-locator-angle(rotate-locator-lookat-object(flatten-locator(get-object-locator(tag-symbol(int-rope-end, self))), player, 1.00, 0), 1, 180.00);
                        *(i32*)*var_1[1] = 28;
                        *(u64*)(*var_1[1] + 8) = 0;
                        *(f32*)(*var_1[1] + 8) = 0.80;
                        *(i32*)*var_1[2] = 30;
                        *(u64*)(*var_1[2] + 8) = 0;
                        *(f32*)(*var_1[2] + 8) = 0.80;
                        wait-animate_(player, ellie-rope-pick-up, var_1);
                        camera-fade-out-animated(0.80, 0);
                        signal(done-anim, 0);
                    } else if (get-boolean(attached?, self)) {
                        signal(anim-start, 0);
                        camera-enable-animated(1.30, -1.00, 1, 1000, 0, 0, 0);
                        u64? var_8 = #%alloc-array(single-frame, 16, 3, 1);
                        *(i32*)*var_8[0] = 10;
                        *(u64*)(*var_8[0] + 8) = 0;
                        *(u64*)(*var_8[0] + 8) = rotate-locator-angle(flatten-locator(rotate-object-lookat-locator(obj-cart-puzz-cart-1, alloc-bound-frame(get-point(end-position, self), 0, 0, 0, 0), 1.00, 0)), 1, 180.00);
                        *(i32*)*var_8[1] = 28;
                        *(u64*)(*var_8[1] + 8) = 0;
                        *(f32*)(*var_8[1] + 8) = 0.80;
                        *(i32*)*var_8[2] = 30;
                        *(u64*)(*var_8[2] + 8) = 0;
                        *(f32*)(*var_8[2] + 8) = 0.80;
                        wait-animate_(player, ellie-rope-tie, var_8);
                        camera-fade-out-animated(0.80, 0);
                        signal(done-anim, 0);
                    }
                }
            }
            track rope {
                lambda {
                    interactable-disable(tag-symbol(int-rope-end, self));
                }
                lambda {
                    wait-for(anim-start);
                }
                lambda {
                    if (get-boolean(free?, self)) {
                        wait-frames(20);
                        set-boolean(free?, 0, self);
                        set-boolean(fake-attach?, 1, self);
                        wait-for(done-anim);
                    } else if (get-boolean(attached?, self)) {
                        wait-frames(40);
                        set-boolean(attached?, 0, self);
                        set-boolean(fake-attach?, 1, self);
                        wait-for(done-anim);
                        rope-clear-body-constraint(self);
                    }
                }
                lambda {
                    wait-frames(3);
                }
                lambda {
                    player-pick-up-rope(self);
                }
                lambda {
                    set-boolean(fake-attach?, 0, self);
                }
                lambda {
                    set-boolean(pick-up-finished?, 1, self);
                }
                lambda {
                    interactable-enable(int-cart-puzz-attach-1);
                }
            }
            track cart {
                lambda {
                    interactable-disable(obj-cart-puzz-cart-1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    joypad-disable-commands(crouch-press, crouch-release, crouch-idle, prone-toggle);
                }
                lambda {
                    if (get-boolean(free?, self)) {
                        rope-keyframe-point(self, tag-float(rope-length, self), get-point(end-position, self), 0, 0);
                    } else if (get-boolean(attached?, self)) {
                        rope-keyframe-point(
                            self,
                            tag-float(rope-length, self),
                            offset-position(get-spawner-position(get-symbol(attach-spawner, self), 1), alloc-vector(0.00, 0.00, 0.07, 1.00, 0)),
                            1,
                            0
                        );
                    } else if (get-boolean(fake-attach?, self)) {
                        rope-keyframe-point(self, tag-float(rope-length, self), get-joint-position(player, r_hand_prop_attachment), 1, 0);
                    } else if (get-boolean(pick-up-finished?, self)) {
                        rope-keyframe-point(self, tag-float(rope-length, self), get-joint-position(player, r_hand_prop_attachment), 1, 0);
                    } else {
                        rope-keyframe-point(self, tag-float(rope-length, self), get-spawner-position(tag-symbol(start-position, self), 1), 0, 0);
                    }
                }
                lambda {
                    rope-keyframe-point(self, 0.00, get-spawner-position(tag-symbol(start-position, self), 1), 0, 0);
                }
                lambda {
                    u16 var_1;
                    foreach (u64? var_3 : cart-puzz-interact-points) {
                        u16 var_7;
                        if (999.00 > get-locator-distance(get-spawner-locator(var_3, 1), get-object-locator(player))) {
                            var_7 = var_3;
                            var_8 = get-locator-distance(get-spawner-locator(var_3, 1), get-object-locator(player));
                        }
                        var_1 = var_7;
                    }
                    set-symbol(attach-spawner, var_1, self);
                }
                lambda {
                    if (get-locator-distance(get-spawner-locator(tag-symbol(start-position, self), 1), get-object-locator(player)) > (tag-float(rope-length, self) - 1.30)) {
                        set-bound-frame(closest-nav-locator, find-closest-point-on-nav-mesh(get-object-locator(player), 5.00, 5.00, 0.00, 0, 0), self);
                    }
                }
                lambda {
                    if (get-locator-distance(get-spawner-locator(tag-symbol(start-position, self), 1), get-object-locator(player)) > (tag-float(rope-length, self) + 0.10)) {
                        if (!(joypad-stick-pressed-towards?(move, get-spawner-locator(tag-symbol(start-position, self), 1), 0, 45.00, 0.00))) {
                            joypad-disable-commands(move);
                        }
                    }
                }
                lambda {
                    if (get-boolean(pick-up-finished?, self) && joypad-command-active?(drop-winch-hook, 6)) {
                        set-boolean(pick-up-finished?, 0, self);
                        go(dropping, 1);
                    }
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    go(attached, 1);
                }
            }
        }
    }
    state attached {
        block start {
            track player {
                lambda {
                    interactable-disable(tag-symbol(int-rope-end, self));
                }
                lambda {
                    if (!get-boolean(attached?, self)) {
                        camera-enable-animated(1.30, -1.00, 1, 1000, 0, 0, 0);
                        signal(anim-start, 0);
                        u64? var_1 = #%alloc-array(single-frame, 16, 3, 1);
                        *(i32*)*var_1[0] = 10;
                        *(u64*)(*var_1[0] + 8) = 0;
                        *(u64*)(*var_1[0] + 8) = rotate-locator-angle(rotate-locator-lookat-object(flatten-locator(get-object-locator(obj-cart-puzz-cart-1)), player, 1.00, 0), 1, 180.00);
                        *(i32*)*var_1[1] = 28;
                        *(u64*)(*var_1[1] + 8) = 0;
                        *(f32*)(*var_1[1] + 8) = 0.80;
                        *(i32*)*var_1[2] = 30;
                        *(u64*)(*var_1[2] + 8) = 0;
                        *(f32*)(*var_1[2] + 8) = 0.80;
                        wait-animate_(player, ellie-rope-tie, var_1);
                        camera-fade-out-animated(0.80, 0);
                    }
                }
                lambda {
                    signal(anim-done, 0);
                }
            }
            track rope {
                lambda {
                    if (!get-boolean(attached?, self)) {
                        wait-for(anim-start);
                        wait-frames(40);
                        player-drop-rope();
                        set-boolean(attached?, 1, self);
                    }
                }
                lambda {
                    wait-for(anim-done);
                }
                lambda {
                    interactable-enable(obj-cart-puzz-cart-1);
                }
                lambda {
                    wait-one-frame();
                }
                lambda {
                    rope-set-body-constraint(self, obj-cart-puzz-cart-1, root, 1);
                }
                lambda {
                    set-object-position(tag-symbol(int-rope-end, self), offset-point(get-point(end-position, self), alloc-vector(0.00, 15.00, 0.00, 1.00, 0)));
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-boolean(attached?, self)) {
                        rope-keyframe-point(
                            self,
                            tag-float(rope-length, self),
                            offset-position(get-spawner-position(get-symbol(attach-spawner, self), 1), alloc-vector(0.00, 0.00, 0.07, 1.00, 0)),
                            1,
                            0
                        );
                    }
                }
                lambda {
                    if (player-in-state?(PushPull)) {
                        interactable-disable(int-cart-puzz-attach-1);
                    } else {
                        interactable-enable(int-cart-puzz-attach-1);
                    }
                }
            }
        }
        block event player-requested-interact {
            track main {
                lambda {
                    set-point(end-position, get-spawner-position(get-symbol(attach-spawner, self), 1), self);
                }
                lambda {
                    rope-keyframe-point(
                        self,
                        tag-float(rope-length, self),
                        offset-position(get-spawner-position(get-symbol(attach-spawner, self), 1), alloc-vector(0.00, 0.00, 0.07, 1.00, 0)),
                        1,
                        0
                    );
                }
                lambda {
                    go(held, 1);
                }
            }
        }
    }
}
