// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...ain/bin_unpacked/dc1\ss-test\ss-test-pellis\ss-test-pellis-bear-trap.bin  #
// #   filesize: 12785 bytes                                                                          #
// #   total entries: 1                                                                               #
// #   functions: 37                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        pe-trap-test
    }
    declarations {
        f32 meter-progress = 0.00
        symbol trapped-npc = nullptr
    }
    state initial {
        block update {
            track main {
                lambda {
                    if (get-locator-distance(get-object-locator(player), get-object-locator(get-self-id())) < 0.50) {
                        if (!player-in-state?(Jump)) {
                            audio-gameplay-event(distraction, get-object-position(player), player);
                            go(teleport-to-trap-loop, 1);
                        }
                    }
                }
                lambda {
                    foreach (u16 var_2 : all-npcs) {
                        if (get-locator-distance(get-object-locator(var_2), get-object-locator(get-self-id())) < 0.50) {
                            set-symbol(trapped-npc, var_2, self);
                            go(npc-trap, 1);
                        }
                    }
                }
                lambda {
                    if (player-in-state?(Prone) && player-is-crouched?()) {
                        interactable-enable(tag-symbol(interactable-dismantle, self));
                    } else {
                        interactable-disable(tag-symbol(interactable-dismantle, self));
                    }
                }
            }
        }
        block event_player-requested-interact {
            track main {
                lambda {
                    audio-gameplay-event(distraction, get-object-position(get-self-id()), player);
                }
                lambda {
                    go(complete-anim, 1);
                }
            }
        }
        block event_Attack {
            track main {
                lambda {
                    go(complete-anim, 1);
                }
            }
        }
        block end {
            track main {
                lambda {
                    animate_(get-self-id(), anim-close, #%alloc-array(single-frame, 16, 0, 1));
                }
                lambda {
                    interactable-disable(tag-symbol(interactable-dismantle, self));
                }
                lambda {
                    disable-nav-blocker-object(tag-symbol(nav-blocker-trap, self));
                }
                lambda {
                    script-caption(get-self-id(), " *CLANK!* ", 2.00, 0, 0);
                }
            }
        }
    }
    state npc-trap {
        block start {
            track main {
                lambda {
                    if (npc-get-faction(get-symbol(trapped-npc, self), 1) == Infected) {
                        go(npc-trap-infected, 1);
                    }
                }
                lambda {
                    u64? var_1 = #%alloc-array(single-frame, 16, 1, 1);
                    *(i32 *)*(*var_1 + (0 * 8)) = 3;
                    *(u64 *)(*(*var_1 + (0 * 8)) + 8) = 0;
                    *(u8 *)(*(*var_1 + (0 * 8)) + 8) = 1;
                    animate_(get-symbol(trapped-npc, self), bindpose, var_1);
                }
                lambda {
                    set-object-health(get-symbol(trapped-npc, self), get-object-health(get-symbol(trapped-npc, self)) - 25.00);
                }
                lambda {
                    wait(3.00);
                }
                lambda {
                    set-object-health(get-symbol(trapped-npc, self), get-object-health(get-symbol(trapped-npc, self)) - 25.00);
                }
                lambda {
                    wait(3.00);
                }
                lambda {
                    set-object-health(get-symbol(trapped-npc, self), get-object-health(get-symbol(trapped-npc, self)) - 25.00);
                }
                lambda {
                    wait(3.00);
                }
                lambda {
                    stop-animating(get-symbol(trapped-npc, self), 2, 0.00, 0, 0);
                }
                lambda {
                    go(complete-anim, 1);
                }
            }
        }
    }
    state npc-trap-infected {
        block start {
            track main {
                lambda {
                    if (npc-get-limb-state(get-symbol(trapped-npc, self)) == 1) {
                        npc-die(get-symbol(trapped-npc, self), 0, 0, 0, 1);
                    } else {
                        send-event(force-legless, get-symbol(trapped-npc, self));
                    }
                }
                lambda {
                    go(complete-anim, 1);
                }
            }
        }
    }
    state teleport-to-trap-loop {
        block start {
            track main {
                lambda {
                    teleport-character(player, get-self-id());
                }
                lambda {
                    go(trap-loop, 1);
                }
            }
        }
    }
    state trap-loop {
        block start {
            track main {
                lambda {
                    set-float(meter-progress, 0.00, self);
                }
            }
            track damage {
                lambda {
                    if (character-get-invincibility(player) == 0) {
                        if (get-player-health() < 6) {
                            player-take-melee-damage(player, 99999, 1, 1);
                        } else {
                            set-player-health(get-player-health() - 20);
                        }
                    }
                }
            }
            track mash {
                lambda {
                    while (1) {
                        while (!joypad-command-active?(debug-script-press-triangle, 6)) {
                            wait-one-frame();
                        }
                        +=f(meter-progress, get-delta-time(), self);
                        wait-one-frame();
                        while (!((current-time(1) - current-time(1)) > 0.20)) {
                            +=f(meter-progress, get-delta-time(), self);
                            wait-one-frame();
                        }
                        wait(0.10);
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    override-render-settings(
                        *zgame-player-bleed-damage*,
                        0.00,
                        -1.00,
                        0,
                        32767,
                        0,
                        1,
                        0,
                        1.00
                    );
                }
                lambda {
                    treat-character-as-uninterruptible(player);
                }
                lambda {
                    joypad-disable-all-commands-except(
                        aim,
                        object-throw,
                        fire-press,
                        reload,
                        menu-left,
                        menu-right,
                        menu-down,
                        menu-up,
                        camera,
                        debug-script-press-triangle
                    );
                }
                lambda {
                    if (get-float(meter-progress, self) > 1.00) {
                        go(trap-escape, 1);
                    }
                }
            }
        }
    }
    state trap-escape {
        block start {
            track main {
                lambda {
                    stop-animating(player, 2, 0.00, 0, 0);
                }
                lambda {
                    go(complete-anim, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    treat-character-as-uninterruptible(player);
                }
            }
        }
    }
    state complete-anim {
        block start {
            track main {
                lambda {
                    wait(0.50);
                }
                lambda {
                    animate_(get-self-id(), anim-fall, #%alloc-array(single-frame, 16, 0, 1));
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
    }
}
