// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-bsingh\ss-bsingh-puzzle-forklift-able.bin #
// #   filesize: 4408 bytes                                                                           #
// #   total entries: 2                                                                               #
// #   functions: 9                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        symbol parent = nullptr
        bound-frame offset-from-parent = nullptr
    }
    state init {
        block start {
            track main {
                lambda {
                    darray-append(ss-forklift-able-group, boxed_symbol(get-self-id()), 1);
                }
                lambda {
                    if (!is-final-build?()) {
                        display(dc:format("adding: %s", string-debug(get-self-id())), 19);
                    }
                }
                lambda {
                    if (tag-boolean(spawn-attached, self) && object-spawned?(tag-symbol(spawn-attached-obj, self))) {
                        set-symbol(parent, tag-symbol(spawn-attached-obj, self), self);
                        go(attached, 1);
                    }
                }
                lambda {
                    go(idle, 1);
                }
            }
        }
    }
    state idle {
        block event forklift-interact {
            track main {
                lambda {
                    set-symbol(parent, event-arg-symbol(0), self);
                }
                lambda {
                    go(attached, 1);
                }
            }
        }
    }
    state attached {
        block start {
            track main {
                lambda {
                    set-bound-frame(offset-from-parent, transform-locator-world-to-local(get-object-locator(tag-symbol(target-object, self)), get-object-locator(get-symbol(parent, self))), self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    set-object-locator(tag-symbol(target-object, self), transform-locator-local-to-world(get-bound-frame(offset-from-parent, self), get-object-locator(get-symbol(parent, self))));
                }
            }
        }
        block event forklift-interact {
            track main {
                lambda {
                    go(idle, 1);
                }
            }
        }
    }
}
