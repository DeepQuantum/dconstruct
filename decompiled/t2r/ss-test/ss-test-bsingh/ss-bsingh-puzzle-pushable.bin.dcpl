// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...in/bin_unpacked/dc1\ss-test\ss-test-bsingh\ss-bsingh-puzzle-pushable.bin  #
// #   filesize: 7609 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 20                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        bound-frame object-offset-from-player = nullptr
    }
    state init {
        block start {
            track main {
                lambda {
                    set-bound-frame(object-offset-from-player, transform-locator-world-to-local(get-object-locator(tag-symbol(target-object, self)), get-spawner-locator(tag-symbol(pos-player, self), 1)), self);
                }
                lambda {
                    go(idle, 1);
                }
            }
        }
    }
    state idle {
        block start {
            track main {
                lambda {
                    interactable-enable(tag-symbol(interactable, self));
                }
                lambda {
                    enable-collision(tag-symbol(target-object, self), 1, 0, 0.00);
                }
                lambda {
                    disable-collision(tag-symbol(interactable, self), 0);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!interactable-enabled?(tag-symbol(interactable, self)) && player-in-state?(Move) && fact-symbol(player, player-interactable, 0, 0) == 0) {
                        interactable-enable(tag-symbol(interactable, self));
                    }
                }
                lambda {
                    if (interactable-enabled?(tag-symbol(interactable, self)) || !player-in-state?(Move) && fact-symbol(player, player-interactable, 0, 0) != 0) {
                        interactable-disable(tag-symbol(interactable, self));
                    }
                }
            }
        }
        block event_player-requested-interact {
            track main {
                lambda {
                    if (event-arg-symbol(0) == tag-symbol(interactable, self)) {
                        go(grab-init, 1);
                    }
                }
            }
        }
    }
    state grab-init {
        block start {
            track main {
                lambda {
                    wait-one-frame();
                }
                lambda {
                    interactable-disable(tag-symbol(interactable, self));
                }
                lambda {
                    disable-collision(tag-symbol(target-object, self), 0);
                }
                lambda {
                    disable-collision(tag-symbol(interactable, self), 0);
                }
                lambda {
                    fact-set(player, player-interactable, boxed_symbol(tag-symbol(target-object, self)));
                }
                lambda {
                    go(grabbed, 1);
                }
            }
        }
    }
    state grabbed {
        block update {
            track main {
                lambda {
                    player-force-walk();
                }
                lambda {
                    joypad-disable-commands(consumable-press, consumable-hold);
                }
                lambda {
                    set-object-locator(tag-symbol(target-object, self), blend-locator(get-object-locator(tag-symbol(target-object, self)), transform-locator-local-to-world(get-bound-frame(object-offset-from-player, self), alloc-bound-frame(get-object-position(player), get-locator-rotation(flatten-locator(camera-locator(0))), 0, 0, 0)), 10.00 * get-delta-time()));
                }
                lambda {
                    if (joypad-command-active?(interact-invisible, 6) || joypad-command-active?(interactable-cancel, 6) && !player-in-state?(Move)) {
                        go(un-grab, 1);
                    }
                }
            }
        }
    }
    state un-grab {
        block start {
            track main {
                lambda {
                    fact-set(player, player-interactable, boxed_symbol(0));
                }
                lambda {
                    go(idle, 1);
                }
            }
        }
    }
}
