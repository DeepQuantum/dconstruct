// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: .../bin_unpacked/dc1\ss-test\ss-test-mjudkins\ss-mjudkins-trap-infected.bin  #
// #   filesize: 20402 bytes                                                                          #
// #   total entries: 2                                                                               #
// #   functions: 100                                                                                 #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

u16 get-infected-attack-anim(sid arg_0) {
    return match (arg_0) {
        front3 -> trap-infected-f-attack--infected-testlevel,
        front4 -> trap-infected-f-attack--infected,
        front5 -> trap-infected-e-attack--infected,
        front6 -> trap-infected-a-attack--infected,
        back4  -> trap-infected-10-attack--infected,
        back5  -> trap-infected-e-45-attack--infected,
        back6  -> trap-infected-c-attack--infected,
        else   -> 0
    };
}

statescript {
    options {
        player
        sp-all
    }
    declarations {
        i32 raycast = -1
        bound-frame ap-ref-var = nullptr
        bound-frame forground-piece-ap-ref-var = nullptr
        bool trap-has-emerged? = nullptr
        bool live = nullptr
        bool attack-player = nullptr
        bool loop = nullptr
        bool loop-idle-debug = nullptr
        bool emerged-once = nullptr
    }
    state setup {
        block start {
            track main {
                lambda {
                    if (tag-exists?(ap-spawner, self)) {
                        set-bound-frame(ap-ref-var, get-spawner-locator(tag-symbol(ap-spawner, self), 1), self);
                    } else {
                        set-bound-frame(ap-ref-var, get-spawner-locator(self, 1), self);
                    }
                }
                lambda {
                    npc-set-script-only-no-perception(self);
                }
                lambda {
                    set-focusable-ignore(self, 0);
                }
                lambda {
                    kill-all-particles();
                }
                lambda {
                    u64? var_2 = #%alloc-array(single-frame, 16, 2, 1);
                    *(i32 *)*(*var_2 + (0 * 8)) = 56;
                    *(u64 *)(*(*var_2 + (0 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_2 + (0 * 8)) + 8) = 0.00;
                    *(i32 *)*(*var_2 + (1 * 8)) = 10;
                    *(u64 *)(*(*var_2 + (1 * 8)) + 8) = 0;
                    *(u64 *)(*(*var_2 + (1 * 8)) + 8) = get-bound-frame(ap-ref-var, self);
                    animate_(self, get-infected-attack-anim(tag-symbol(letter, self)), var_2);
                }
                lambda {
                    if (tag-exists?(forground-piece, self)) {
                        set-bound-frame(forground-piece-ap-ref-var, get-spawner-locator(tag-symbol(forground-piece, self), 1), self);
                        enable-collision(tag-symbol(forground-piece, self), 0, 0, 0.00);
                        u64? var_5 = #%alloc-array(single-frame, 16, 1, 1);
                        *(i32 *)*(*var_5 + (0 * 8)) = 10;
                        *(u64 *)(*(*var_5 + (0 * 8)) + 8) = 0;
                        *(u64 *)(*(*var_5 + (0 * 8)) + 8) = get-bound-frame(forground-piece-ap-ref-var, self);
                        animate_(tag-symbol(forground-piece, self), idle, var_5);
                    }
                }
                lambda {
                    go(wait-to-activate, 1);
                }
            }
        }
    }
    state wait-to-activate {
        block start {
            track main {
                lambda {
                    set-focusable-ignore(self, 0);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-boolean(live, self)) {
                        u64? var_1 = distance-between-objects(self, player);
                        if (get-int32(raycast, self) >= 0 || var_1 < 2.00 && is-object-on-screen?(self, 0.00) && var_1 < 3.80 || var_1 < 1.30) {
                            if (!(get-player-speed() < 1.50 && player-is-crouched?()) && !player-in-state?(DebugFly) && !get-boolean(emerged-once, self)) {
                                set-boolean(emerged-once, 1, self);
                                go(emerge, 1);
                            }
                        } else if (!player-in-state?(DebugFly) && !get-boolean(emerged-once, self)) {}

                        if (get-int32(raycast, self) >= 0 && get-player-speed() > 1.50 && get-boolean(emerged-once, self)) {
                            var_15 = set-boolean(emerged-once, 0, self);
                        }
                        set-int32(raycast, kick-raycast(
                            offset-locator-world-space(get-object-locator(self), vector-add(alloc-vector(0.00, 0.50, 0.00, 1.00, 0), vector-multiply(get-z-vec-from-object(self), 0.10))),
                            get-joint-locator(player, pelvis),
                            self,
                            6145
                        ), self);
                    }
                }
                lambda {
                    if (get-boolean(loop, self)) {
                        go(emerge, 1);
                    }
                }
                lambda {
                    if (get-boolean(loop-idle-debug, self)) {
                        go(setup, 1);
                    }
                }
            }
        }
        block event_infected-loop-idle-debug {
            track main {
                lambda {
                    set-boolean(live, 0, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(attack-player, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 1, self);
                }
            }
        }
        block event_infected-test-live {
            track main {
                lambda {
                    set-boolean(live, 1, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
            }
        }
        block event_infected-test-not-live {
            track main {
                lambda {
                    set-boolean(live, 0, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
            }
        }
        block event_infected-test-attack-player {
            track main {
                lambda {
                    set-boolean(attack-player, 1, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
            }
        }
        block event_infected-test-dont-attack-player {
            track main {
                lambda {
                    set-boolean(attack-player, 0, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
            }
        }
        block event_infected-test-loop-attack {
            track main {
                lambda {
                    set-boolean(loop, 1, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
            }
        }
        block event_Attack {
            track main {
                lambda {
                    if (!npc-alive?(self)) {
                        go(dead, 1);
                    }
                }
                lambda {
                    go(grab, 1);
                }
            }
        }
        block event_npc-died {
            track main {
                lambda {
                    go(dead, 1);
                }
            }
        }
    }
    state emerge {
        block start {
            track emerge-anim {
                lambda {
                    if (get-boolean(loop, self)) {
                        wait(1.00);
                    }
                }
                lambda {
                    npc-set-script-only-no-perception(self);
                }
                lambda {
                    set-focusable-ignore(self, 0);
                }
                lambda {
                    if (tag-exists?(forground-piece, self)) {
                        enable-collision(tag-symbol(forground-piece, self), 1, 0, 0.00);
                        u64? var_3 = #%alloc-array(single-frame, 16, 1, 1);
                        *(i32 *)*(*var_3 + (0 * 8)) = 10;
                        *(u64 *)(*(*var_3 + (0 * 8)) + 8) = 0;
                        *(u64 *)(*(*var_3 + (0 * 8)) + 8) = get-bound-frame(forground-piece-ap-ref-var, self);
                        animate_(tag-symbol(forground-piece, self), attack, var_3);
                    }
                }
                lambda {
                    u64? var_2 = #%alloc-array(single-frame, 16, 3, 1);
                    *(i32 *)*(*var_2 + (0 * 8)) = 10;
                    *(u64 *)(*(*var_2 + (0 * 8)) + 8) = 0;
                    *(u64 *)(*(*var_2 + (0 * 8)) + 8) = get-bound-frame(ap-ref-var, self);
                    *(i32 *)*(*var_2 + (1 * 8)) = 28;
                    *(u64 *)(*(*var_2 + (1 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_2 + (1 * 8)) + 8) = 0.20;
                    *(i32 *)*(*var_2 + (2 * 8)) = 30;
                    *(u64 *)(*(*var_2 + (2 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_2 + (2 * 8)) + 8) = 0.30;
                    wait-animate_(self, get-infected-attack-anim(tag-symbol(letter, self)), var_2);
                }
                lambda {
                    if (get-boolean(attack-player, self)) {
                        go(grab, 1);
                    }
                }
                lambda {
                    if (get-boolean(loop, self)) {
                        wait(3.00);
                        go(setup, 1);
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-locator-distance-xz(get-predicted-player-bound-frame(0.30), get-object-locator(self)) < 1.20 && get-locator-distance-xz(get-object-locator(player), get-object-locator(self)) < 1.30 && get-boolean(attack-player, self)) {
                        go(grab, 1);
                    }
                }
            }
        }
        block event_infected-loop-idle-debug {
            track main {
                lambda {
                    set-boolean(live, 0, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(attack-player, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 1, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
        block event_infected-test-live {
            track main {
                lambda {
                    set-boolean(live, 1, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
        block event_infected-test-not-live {
            track main {
                lambda {
                    set-boolean(live, 0, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
        block event_infected-test-attack-player {
            track main {
                lambda {
                    set-boolean(attack-player, 1, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
        block event_infected-test-dont-attack-player {
            track main {
                lambda {
                    set-boolean(attack-player, 0, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
        block event_infected-test-loop-attack {
            track main {
                lambda {
                    set-boolean(loop, 1, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
        block event_Attack {
            track main {
                lambda {
                    if (!npc-alive?(self)) {
                        go(dead, 1);
                    }
                }
                lambda {
                    go(grab, 1);
                }
            }
        }
        block event_npc-died {
            track main {
                lambda {
                    go(dead, 1);
                }
            }
        }
    }
    state grab {
        block start {
            track main {
                lambda {
                    if (!get-boolean(live, self)) {
                        go(setup, 1);
                    }
                }
                lambda {
                    npc-set-script-high-priority(self);
                }
                lambda {
                    npc-make-aware-of(self, player, 2, 0);
                }
                lambda {
                    set-focusable-ignore(self, 0);
                }
                lambda {
                    if (tag-exists?(trap-wrapper, self)) {
                        kill-entity(tag-symbol(trap-wrapper, self));
                    }
                }
                lambda {
                    wait-start-melee-action(
                        self,
                        player,
                        tag-symbol(attack, self),
                        0,
                        0,
                        1,
                        0,
                        0,
                        0
                    );
                }
                lambda {
                    if (get-boolean(loop, self)) {
                        wait(0.15);
                        go(setup, 1);
                    }
                }
            }
        }
        block event_infected-loop-idle-debug {
            track main {
                lambda {
                    set-boolean(live, 0, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(attack-player, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 1, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
        block event_infected-test-live {
            track main {
                lambda {
                    set-boolean(live, 1, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
        block event_infected-test-not-live {
            track main {
                lambda {
                    set-boolean(live, 0, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
        block event_infected-test-attack-player {
            track main {
                lambda {
                    set-boolean(attack-player, 1, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
        block event_infected-test-dont-attack-player {
            track main {
                lambda {
                    set-boolean(attack-player, 0, self);
                }
                lambda {
                    set-boolean(loop, 0, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
        block event_infected-test-loop-attack {
            track main {
                lambda {
                    set-boolean(loop, 1, self);
                }
                lambda {
                    set-boolean(loop-idle-debug, 0, self);
                }
                lambda {
                    go(setup, 1);
                }
            }
        }
    }
    state dead {
        block start {
            track main {
                lambda {
                    if (tag-exists?(forground-piece, self)) {
                        enable-collision(tag-symbol(forground-piece, self), 1, 0, 0.00);
                    }
                }
            }
        }
    }
}
