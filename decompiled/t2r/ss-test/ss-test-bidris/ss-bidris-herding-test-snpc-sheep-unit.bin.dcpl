// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...ed/dc1\ss-test\ss-test-bidris\ss-bidris-herding-test-snpc-sheep-unit.bin  #
// #   filesize: 7520 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 22                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
    }
    declarations {
        f32 herding-radius = 5.50
        f32 flee-dist-scaled = 7.00
        bound-frame destination-direction-loc = nullptr
        f32 distance-to-destination = nullptr
        bound-frame flee-target-loc = nullptr
        point flee-final-loc = nullptr
        bound-frame player-direction-loc = nullptr
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-active?(bidris, bidris-herding-test)) {
                        go(wait-herd, 1);
                    }
                }
            }
        }
    }
    state wait-herd {
        block event_player-herding! {
            track main {
                lambda {
                    go(calculate, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    debug-draw-locator(get-bound-frame(flee-target-loc, self), 0.30, 0.00, 0, 2.00, 0);
                }
            }
        }
        block event_herd-end {
            track main {
                lambda {
                    simple-npc-spline-flock(get-self-id(), spl-1046, 0, 3);
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
    }
    state calculate {
        block start {
            track main {
                lambda {
                    set-float(distance-to-destination, get-locator-distance-xz(get-object-locator(get-self-id()), get-spawner-locator(pos-herd-destination-1, 1)), self);
                }
                lambda {
                    set-bound-frame(destination-direction-loc, rotate-locator-lookat(get-object-locator(get-self-id()), get-spawner-locator(pos-herd-destination-1, 1), 1.00, 0), self);
                }
                lambda {
                    set-bound-frame(flee-target-loc, find-closest-point-on-nav-mesh(
                        offset-locator(get-bound-frame(destination-direction-loc, self), alloc-vector(0.00, 0.00, get-float(flee-dist-scaled, self), 1.00, 0)),
                        get-float(flee-dist-scaled, self),
                        get-float(flee-dist-scaled, self),
                        1.00,
                        0,
                        0
                    ), self);
                }
                lambda {
                    set-bound-frame(player-direction-loc, reverse-locator(rotate-locator-lookat-object(get-object-locator(get-self-id()), player, 1.00, 0)), self);
                }
                lambda {
                    go(move, 1);
                }
            }
        }
    }
    state move {
        block start {
            track main {
                lambda {
                    u64? var_0 = random-float-range(1.00, 1.30, 0);
                    simple-npc-set-movement-speed-scale(get-self-id(), var_0);
                    simple-npc-set-locomotion-speed-scale(get-self-id(), var_0);
                }
            }
        }
        block update {
            track main {
                lambda {
                    simple-npc-move-to-goal(self, get-locator-position(get-bound-frame(flee-target-loc, self)), 0, 0, -1.00, 0, 0);
                }
                lambda {
                    debug-draw-locator(get-bound-frame(flee-target-loc, self), 0.30, 0.00, 0, 2.00, 0);
                }
            }
        }
        block event_player-herding! {
            track main {
                lambda {
                    set-float(distance-to-destination, get-locator-distance-xz(get-object-locator(get-self-id()), get-spawner-locator(pos-herd-destination-1, 1)), self);
                }
                lambda {
                    set-bound-frame(destination-direction-loc, rotate-locator-lookat(get-object-locator(get-self-id()), get-spawner-locator(pos-herd-destination-1, 1), 1.00, 0), self);
                }
                lambda {
                    set-bound-frame(flee-target-loc, find-closest-point-on-nav-mesh(
                        offset-locator(get-bound-frame(destination-direction-loc, self), alloc-vector(0.00, 0.00, get-float(flee-dist-scaled, self), 1.00, 0)),
                        get-float(flee-dist-scaled, self),
                        get-float(flee-dist-scaled, self),
                        1.00,
                        0,
                        0
                    ), self);
                }
                lambda {
                    set-bound-frame(player-direction-loc, reverse-locator(rotate-locator-lookat-object(get-object-locator(get-self-id()), player, 1.00, 0)), self);
                }
            }
        }
        block event_player-too-far! {
            track main {
                lambda {
                    go(move-to-stop, 1);
                }
            }
        }
        block event_herd-end {
            track main {
                lambda {
                    simple-npc-spline-flock(get-self-id(), spl-1046, 0, 3);
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
    }
    state move-to-stop {
        block start {
            track main {
                lambda {
                    simple-npc-force-loco-state(get-self-id(), idle, random-float-range(0.50, 1.00, 0));
                }
                lambda {
                    go(wait-herd, 1);
                }
            }
        }
    }
}
