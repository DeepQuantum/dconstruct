// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-mgallant\ss-proto-burn-grass-in-search.bin#
// #   filesize: 18139 bytes                                                                          #
// #   total entries: 3                                                                               #
// #   functions: 37                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

u16 is-valid-grass-target?(u16 arg_0, sid arg_1) {
    u16 var_1;
    if (!distance-between-objects-in-range?(arg_0, arg_1, 5.00, 9.00, 1)) {
        var_1 = 0;
    } else if (arg_0 != get-closest-in-group(all-enemy-npcs, arg_1, 0, 0, 1, 0)) {
        var_1 = 0;
    } else {
        u64? var_3 = npc-get-awareness(arg_0, player);
        var_1 = !npc-is-aware-of?(arg_0, player) || var_3 == 4 || var_3 == 0 && var_3 == 3 && distance-between-points(npc-get-entity-position(arg_0, player), get-object-position(arg_1)) < 10.00;
    }
    return var_1;
}

statescript {
    options {
        player
        sp-all
    }
    declarations {
        symbol closest-grass = #0000000000000000
        symbol navblocker = #0000000000000000
        i32 raycast = -1
        bound-frame ap-ref-throw = nullptr
        timer cooldown-timer = nullptr
    }
    state --all-- {
        block event npc-died {
            track main {
                lambda {
                    if (get-self-id() == event-arg-symbol(0)) {
                        darray-find-remove(npcs-searching-with-fire, boxed_symbol(get-self-id()), 0);
                        go(done, 1);
                    }
                }
            }
        }
    }
    state setup {
        block start {
            track main {
                lambda {
                    wait-one-frame();
                }
                lambda {
                    if (task-active?(mgallant, proto-burnable-grass)) {
                        npc-start-search(npc-get-faction-id(self));
                    }
                }
                lambda {
                    darray-append(npcs-searching-with-fire, boxed_symbol(get-self-id()), 1);
                }
                lambda {
                    go(search-for-grass, 1);
                }
            }
        }
    }
    state search-for-grass {
        block start {
            track main {
                lambda {
                    set-symbol(closest-grass, 0, self);
                }
                lambda {
                    set-int32(raycast, -1, self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!npc-alive?(self)) {
                        go(done, 1);
                    }
                }
                lambda {
                    if (get-int32(raycast, self) >= 0 || npc-get-target(self) == player && npc-get-target(self) == 0 && wait-raycast-hit-nothing?(get-int32(raycast, self)) && !npc-is-on-fire?(self) && !npc-in-stealth-vegetation?(self) || npc-using-skill?(self, 58) || npc-using-skill?(self, 14) && npc-using-skill?(self, 13) && !(get-symbol(closest-grass, self) == 0) && is-in-front?-symbol(self, get-symbol(closest-grass, self), 2, 180.00)) {
                        go(throw-molotov, 1);
                    }
                }
                lambda {
                    if (npc-using-skill?(self, 58) || npc-using-skill?(self, 14) && !npc-using-skill?(self, 13)) {
                        set-symbol(closest-grass, 0, self);
                        set-int32(raycast, -1, self);
                    } else if (deterministic-nth-frame?(30)) {
                        set-symbol(closest-grass, 0, self);
                        foreach (u64? var_6 : unburnt-grass-targets) {
                            if (get-symbol(closest-grass, self) == 0) {
                                if (is-valid-grass-target?(get-self-id(), var_6)) {
                                    set-symbol(closest-grass, var_6, self);
                                }
                            }
                        }
                    }
                }
                lambda {
                    if (!(get-symbol(closest-grass, self) == 0)) {
                        set-int32(raycast, kick-spherecast(
                            get-joint-locator(self, headb),
                            offset-locator(get-object-locator(get-symbol(closest-grass, self)), alloc-vector(0.00, 1.00, 0.00, 1.00, 0)),
                            0.80,
                            get-self-id(),
                            4099
                        ), self);
                    } else {
                        set-int32(raycast, -1, self);
                    }
                }
                lambda {
                    if (deterministic-nth-frame?(30) && npc-is-on-fire?(self)) {
                        u64? var_2 = darray-create(self, -1, 0);
                        group-copy(unburnt-grass-targets, var_2);
                        foreach (u64? var_5 : var_2) {
                            if (get-object-distance(self, var_5) < 3.00) {
                                send-event(burn, var_5);
                            }
                        }
                        darray-destroy(var_2);
                    }
                }
            }
        }
        block event throwing-molotov {
            track main {
                lambda {
                    go(cooldown, 1);
                }
            }
        }
        block end {
            track main {
                lambda {
                    set-int32(raycast, -1, self);
                }
            }
        }
    }
    state throw-molotov {
        block start {
            track main {
                lambda {
                    npc-set-script-only-with-perception(self);
                }
                lambda {
                    foreach (u64? var_2 : npcs-searching-with-fire) {
                        send-event(throwing-molotov, var_2);
                    }
                }
                lambda {
                    darray-find-remove(unburnt-grass-targets, boxed_symbol(get-symbol(closest-grass, self)), 0);
                }
                lambda {
                    set-symbol(navblocker, spawn-entity-at(nav-blocker-fire-temp-1, get-spawner-locator(get-symbol(closest-grass, self), 1), 0), self);
                }
                lambda {
                    npc-turn-to(self, get-object-position(get-symbol(closest-grass, self)), 0, 0);
                }
                lambda {
                    wait(1.00);
                }
                lambda {
                    set-bound-frame(ap-ref-throw, rotate-locator-lookat(get-object-locator(self), get-object-locator(get-symbol(closest-grass, self)), 1.00, 0), self);
                }
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 4, 1);
                    *(i32*)*var_0[0] = 10;
                    *(u64*)(*var_0[0] + 8) = 0;
                    *(u64*)(*var_0[0] + 8) = get-bound-frame(ap-ref-throw, self);
                    *(i32*)*var_0[1] = 56;
                    *(u64*)(*var_0[1] + 8) = 0;
                    *(f32*)(*var_0[1] + 8) = 0.50;
                    *(i32*)*var_0[2] = 28;
                    *(u64*)(*var_0[2] + 8) = 0;
                    *(f32*)(*var_0[2] + 8) = 0.50;
                    *(i32*)*var_0[3] = 30;
                    *(u64*)(*var_0[3] + 8) = 0;
                    *(f32*)(*var_0[3] + 8) = 0.50;
                    animate_(self, thug-throw, var_0);
                }
                lambda {
                    wait-frames(44);
                }
                lambda {
                    throw-grenade(get-locator-position(offset-locator(get-object-locator(self), alloc-vector(0.00, 1.00, 0.50, 1.00, 0))), get-object-position(get-symbol(closest-grass, self)), molotov);
                }
                lambda {
                    wait(1.00);
                }
                lambda {
                    u64? var_0 = darray-create(self, -1, 0);
                    foreach (u64? var_3 : unburnt-grass) {
                        if (distance-between-objects-less-than?(var_3, get-symbol(closest-grass, self), 3.00)) {
                            darray-append(var_0, boxed_symbol(var_3), 1);
                        }
                    }
                    foreach (u64? var_10 : var_0) {
                        send-event(burn, var_10);
                    }
                    darray-destroy(var_0);
                }
                lambda {
                    wait(3.00);
                }
                lambda {
                    npc-clear-request-movement(self);
                }
                lambda {
                    npc-set-script-high-priority(self);
                }
                lambda {
                    go(cooldown, 1);
                }
            }
        }
        block end {
            track main {
                lambda {
                    disable-nav-blocker-object(get-symbol(navblocker, self));
                }
                lambda {
                    kill-entity(get-symbol(navblocker, self));
                }
                lambda {
                    set-symbol(navblocker, 0, self);
                }
            }
        }
    }
    state cooldown {
        block start {
            track main {
                lambda {
                    start-timer(cooldown-timer, random-float-range(7.00, 9.00, 0), self, 1);
                }
            }
        }
        block event throwing-molotov {
            track main {
                lambda {
                    start-timer(cooldown-timer, random-float-range(7.00, 9.00, 0), self, 1);
                }
            }
        }
        block event cooldown-timer {
            track main {
                lambda {
                    go(search-for-grass, 1);
                }
            }
        }
    }
}
