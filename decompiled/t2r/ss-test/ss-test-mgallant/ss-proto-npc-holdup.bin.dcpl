// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-mgallant\ss-proto-npc-holdup.bin          #
// #   filesize: 33934 bytes                                                                          #
// #   total entries: 8                                                                               #
// #   functions: 70                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

u16 track-last-seen-position() {
    u16 var_2;
    if (tension-mode-is?(3) || !faction-entity-missing?(3, player)) {
        if (is-in-front?-symbol(get-symbol(npc-held-up, self), player, 2, 180.00)) {
            var_2 = set-point(last-seen-pos, get-object-position(player), self);
        } else {
            var_2 = 0;
        }
    }
    return var_2;
}

u16 gun-raycast-blocked?(u64? arg_0) {
    u16 var_1;
    if (get-int32(gun-raycast, self) > 0) {
        if (!wait-raycast-hit-nothing?(get-int32(gun-raycast, self))) {
            var_1 = 1;
        }
    }
    set-int32(gun-raycast, kick-raycast(camera-locator(0), get-joint-locator(arg_0, spinec), arg_0, 4099), self);
    return var_1;
}

u16 npc-should-break-holdup?() {
    u16 var_3;
    if (get-object-health(get-symbol(npc-candidate, self)) < get-float(held-up-health, self)) {
        var_3 = 1;
    } else if (joypad-command-active?(fire-press, 6) && get-player-weapon-id() != bow && player-clip-percent-full() == 0.00) {
        var_3 = 1;
    } else {
        var_3 = 0;
    }
    return var_3;
}

u64? npc-can-be-held-up?(u64? arg_0, f32 arg_1) {
    u64? var_0 = distance-between-objects(arg_0, player);
    return npc-get-faction(arg_0, 1) == Pirate && var_0 <= arg_1 && !npc-using-traversal-action-pack?(arg_0) || var_0 < 2.00 && object-anim-playing?(arg_0, bindpose-anim, 0) && npc-on-screen-and-visible?(arg_0, -1, 0);
}

u64? player-can-hold-up?() {
    return player-is-aiming?();
}

u16 npcs-investigate-holdup() {
    u16 var_1;
    foreach (u64? var_3 : all-enemy-npcs) {
        u16 var_9;
        if (!0 && var_3 != get-symbol(npc-held-up, self) && is-in-front?-symbol(var_3, get-symbol(npc-held-up, self), 2, 180.00) && npc-can-see?(var_3, get-symbol(npc-held-up, self))) {
            var_9 = 1;
        }
        var_1 = var_9;
    }
    u16 var_11;
    if (var_1) {
        var_11 = audio-gameplay-event(footstep-run, get-object-position(get-symbol(npc-held-up, self)), player);
    } else {
        var_11 = 0;
    }
    return var_11;
}

u64? can-continue-holdup?(u64? arg_0, u64? arg_1) {
    return npc-alive?(arg_0) && player-can-hold-up?() && npc-can-be-held-up?(arg_0, arg_1) && !gun-raycast-blocked?(arg_0);
}

statescript {
    options {
        player
        sp-all
    }
    declarations {
        symbol npc-held-up = #0000000000000000
        symbol npc-candidate = #0000000000000000
        f32 held-up-health = 0.00
        i32 gun-raycast = -1
        bound-frame potential-move = nullptr
        bound-frame chosen-move = nullptr
        timer breakout-timer = nullptr
        point last-seen-pos = nullptr
        symbol command-move = #0000000000000000
        symbol command-look = #0000000000000000
        bool command-vox = nullptr
    }
    state inactive {
        block update {
            track main {
                lambda {
                    if (*(u8*)(*npc-prototypes* + 4) && !task-active?(mgallant, hide-skill-test)) {
                        go(wait-for-aim, 1);
                    }
                }
            }
        }
    }
    state wait-for-aim {
        block start {
            track main {
                lambda {
                    stop-timer(breakout-timer, self);
                }
                lambda {
                    set-symbol(npc-held-up, 0, self);
                }
                lambda {
                    set-symbol(npc-candidate, 0, self);
                }
                lambda {
                    set-int32(gun-raycast, -1, self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!*(u8*)(*npc-prototypes* + 4)) {
                        go(inactive, 1);
                    }
                }
                lambda {
                    if (!player-can-hold-up?()) {
                        set-symbol(npc-candidate, 0, self);
                        set-int32(gun-raycast, -1, self);
                    }
                }
                lambda {
                    if (player-can-hold-up?() && !tension-mode-is?(3)) {
                        foreach (u64? var_4 : all-enemy-npcs) {
                            if (get-symbol(npc-candidate, self) == 0 && !npc-on-screen?(get-symbol(npc-candidate, self), 0.00) && npc-can-be-held-up?(var_4, tag-float(max-holdup-dist, self))) {
                                if (is-in-front?-symbol(var_4, player, 2, 180.00) && npc-can-see?(var_4, player)) {
                                    set-symbol(npc-candidate, var_4, self);
                                    set-int32(gun-raycast, -1, self);
                                }
                            }
                        }
                        if (get-symbol(npc-candidate, self) == 0) {}
 else if (!npc-alive?(get-symbol(npc-candidate, self))) {
                            set-symbol(npc-candidate, 0, self);
                        } else if (get-int32(gun-raycast, self) < 0) {
                            gun-raycast-blocked?(get-symbol(npc-candidate, self));
                        } else if (can-continue-holdup?(get-symbol(npc-candidate, self), tag-float(max-holdup-dist, self))) {
                            set-symbol(npc-held-up, get-symbol(npc-candidate, self), self);
                            set-float(held-up-health, get-object-health(get-symbol(npc-candidate, self)), self);
                            go(start-holdup, 1);
                        }
                    }
                }
            }
        }
        block end {
            track main {
                lambda {
                    set-int32(gun-raycast, -1, self);
                }
            }
        }
    }
    state start-holdup {
        block start {
            track main {
                lambda {
                    npc-set-script-only-no-perception(get-symbol(npc-held-up, self));
                }
                lambda {
                    npc-set-patrol-route(get-symbol(npc-held-up, self), 0, -1);
                }
                lambda {
                    melee-fact-set-bool(get-symbol(npc-held-up, self), being-held-up, 1);
                }
                lambda {
                    set-bound-frame(chosen-move, get-object-locator(get-symbol(npc-held-up, self)), self);
                }
                lambda {
                    set-point(last-seen-pos, get-object-position(player), self);
                }
                lambda {
                    set-symbol(command-move, stop, self);
                }
                lambda {
                    set-symbol(command-look, fwd, self);
                }
                lambda {
                    u64? var_0 = random-integer(6, 0, 0);
                    if (var_0 == 0) {
                        var_1 = script-caption(player, "Reach for the skies", 3.00, 0, 0);
                    } else if (var_0 == 1) {
                        var_1 = script-caption(player, "Don't make a sound", 3.00, 0, 0);
                    } else if (var_0 == 2) {
                        var_1 = script-caption(player, "Hands where I can see them", 3.00, 0, 0);
                    } else if (var_0 == 3) {
                        var_1 = script-caption(player, "Got you dead to rights", 3.00, 0, 0);
                    } else if (var_0 == 4) {
                        var_1 = script-caption(player, "Don't do anything stupid", 3.00, 0, 0);
                    } else if (var_0 == 5) {
                        var_1 = script-caption(player, "Hey asshole", 3.00, 0, 0);
                    }
                }
                lambda {
                    u64? var_3 = #%alloc-array(single-frame, 16, 4, 1);
                    *(i32*)*var_3[0] = 10;
                    *(u64*)(*var_3[0] + 8) = 0;
                    *(u64*)(*var_3[0] + 8) = rotate-locator-lookat(get-object-locator(get-symbol(npc-held-up, self)), get-object-locator(player), 1.00, 0);
                    *(i32*)*var_3[1] = 20;
                    *(u64*)(*var_3[1] + 8) = 0;
                    *(f32*)(*var_3[1] + 8) = 20.00;
                    *(i32*)*var_3[2] = 28;
                    *(u64*)(*var_3[2] + 8) = 0;
                    *(f32*)(*var_3[2] + 8) = 0.30;
                    *(i32*)*var_3[3] = 30;
                    *(u64*)(*var_3[3] + 8) = 0;
                    *(f32*)(*var_3[3] + 8) = 0.30;
                    animate_(get-symbol(npc-held-up, self), npc-m-p-ambi-idle^hands-up-fw, var_3);
                }
                lambda {
                    wait-frames(16);
                }
                lambda {
                    u64? var_1 = alloc-gesture-play-params();
                    *(u64*)(var_1 + 8) = 0;
                    *(i32*)(var_1 + 36) = 19;
                    *(u8*)(var_1 + 2) = 1;
                    *(u8*)(var_1 + 34) = 1;
                    gesture_(get-symbol(npc-held-up, self), npc-m-p-hands-up-fw-gesture, var_1);
                }
                lambda {
                    npc-look-at-object(get-symbol(npc-held-up, self), player, 0, 10.00);
                }
                lambda {
                    npc-set-demeanor(get-symbol(npc-held-up, self), 0, 0);
                }
                lambda {
                    if (npc-is-heavy?(get-symbol(npc-held-up, self))) {
                        start-timer(breakout-timer, random-float-range(4.00, 8.00, 0), self, 1);
                    } else {
                        start-timer(breakout-timer, random-float-range(8.00, 30.00, 0), self, 1);
                    }
                }
                lambda {
                    request-dialog(ai-enemy-held-hostage, get-symbol(npc-held-up, self), 0, 0, 0, 0, 0);
                }
                lambda {
                    go(holdup, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!npc-alive?(get-symbol(npc-held-up, self))) {
                        go(wait-for-aim, 1);
                    }
                }
                lambda {
                    if (npc-should-break-holdup?()) {
                        go(alert-npc, 1);
                    }
                }
                lambda {
                    npcs-investigate-holdup();
                }
            }
        }
    }
    state holdup {
        block update {
            track main {
                lambda {
                    if (!npc-alive?(get-symbol(npc-held-up, self))) {
                        go(wait-for-aim, 1);
                    }
                }
                lambda {
                    if (npc-should-break-holdup?()) {
                        go(alert-npc, 1);
                    }
                }
                lambda {
                    if (!can-continue-holdup?(get-symbol(npc-held-up, self), tag-float(max-holdup-dist, self) + 1.00)) {
                        var_3 = go(holdup-sustain, 1);
                    }
                }
                lambda {
                    npcs-investigate-holdup();
                }
                lambda {
                    if (joypad-command-active?(debug-script-press-l3, 6)) {
                        set-boolean(command-vox, 1, self);
                        if (get-symbol(command-move, self) == stop) {
                            set-symbol(command-move, walk, self);
                        } else {
                            set-symbol(command-move, stop, self);
                        }
                    } else if (joypad-command-active?(debug-script-press-r3, 6)) {
                        set-boolean(command-vox, 1, self);
                        if (get-symbol(command-look, self) == fwd) {
                            set-symbol(command-look, away, self);
                        } else {
                            set-symbol(command-look, fwd, self);
                        }
                    }
                }
                lambda {
                    if (joypad-command-active?(debug-script-press-r3, 6) && get-symbol(command-move, self) == walk) {
                        set-symbol(command-move, stop, self);
                    }
                }
                lambda {
                    if (get-boolean(command-vox, self)) {
                        set-boolean(command-vox, 0, self);
                        if (get-symbol(command-move, self) == stop && joypad-command-active?(debug-script-press-r3, 6)) {
                            script-caption(player, "Turn around", 3.00, 0, 0);
                        } else if (get-symbol(command-move, self) == stop && joypad-command-active?(debug-script-press-l3, 6)) {
                            script-caption(player, "Stop", 3.00, 0, 0);
                        } else if (get-symbol(command-move, self) == walk && get-symbol(command-look, self) == fwd && joypad-command-active?(debug-script-press-l3, 6)) {
                            script-caption(player, "Come here", 3.00, 0, 0);
                        } else if (get-symbol(command-move, self) == walk && get-symbol(command-look, self) == away && joypad-command-active?(debug-script-press-l3, 6)) {
                            script-caption(player, "Keep walking", 3.00, 0, 0);
                        }
                    }
                }
                lambda {
                    if (get-symbol(command-move, self) == stop && get-symbol(command-look, self) == fwd) {
                        set-bound-frame(potential-move, rotate-locator-lookat(get-object-locator(get-symbol(npc-held-up, self)), get-object-locator(player), 1.00, 0), self);
                    } else if (get-symbol(command-move, self) == stop && get-symbol(command-look, self) == away) {
                        set-bound-frame(potential-move, reverse-locator(rotate-locator-lookat(get-object-locator(get-symbol(npc-held-up, self)), get-object-locator(player), 1.00, 0)), self);
                    } else if (get-symbol(command-move, self) == walk && get-symbol(command-look, self) == fwd) {
                        set-bound-frame(potential-move, find-closest-point-on-nav-mesh(
                            offset-locator(rotate-locator-lookat(get-object-locator(player), get-object-locator(get-symbol(npc-held-up, self)), 1.00, 0), alloc-vector(0.00, 0.00, tag-float(min-holdup-dist, self), 1.00, 0)),
                            2.00,
                            5.00,
                            0.00,
                            0,
                            0
                        ), self);
                    } else if (get-symbol(command-move, self) == walk && get-symbol(command-look, self) == away) {
                        set-bound-frame(potential-move, reverse-locator(offset-locator(rotate-locator-lookat(get-object-locator(get-symbol(npc-held-up, self)), get-object-locator(player), 1.00, 0), alloc-vector(0.00, 0.00, -3.00, 1.00, 0))), self);
                    }
                }
                lambda {
                    track-last-seen-position();
                }
                lambda {
                    if (get-locator-distance(get-bound-frame(potential-move, self), get-bound-frame(chosen-move, self)) > 0.50 && angle-in-between-locators(get-bound-frame(potential-move, self), get-bound-frame(chosen-move, self), 2) > 10.00) {
                        set-bound-frame(chosen-move, get-bound-frame(potential-move, self), self);
                        var_6 = npc-move-to(get-symbol(npc-held-up, self), get-bound-frame(chosen-move, self), 0, 1, 0, 0, 0, 0);
                    }
                }
                lambda {
                    debug-draw-string-centered("Commands", 590.00, 1, 0.00, 1.50, 1, 0);
                }
                lambda {
                    if (get-symbol(command-move, self) == stop && get-symbol(command-look, self) == fwd) {
                        debug-draw-string-centered("L3: Walk To Me ", 620.00, 1, 0.00, 1.50, 1, 0);
                    } else if (get-symbol(command-move, self) == stop && get-symbol(command-look, self) == away) {
                        debug-draw-string-centered("L3: Walk Away  ", 620.00, 1, 0.00, 1.50, 1, 0);
                    } else {
                        debug-draw-string-centered("L3: Stop       ", 620.00, 1, 0.00, 1.50, 1, 0);
                    }
                }
                lambda {
                    if (get-symbol(command-look, self) == away) {
                        debug-draw-string-centered("R3: Face Me    ", 650.00, 1, 0.00, 1.50, 1, 0);
                    } else {
                        debug-draw-string-centered("R3: Turn Around", 650.00, 1, 0.00, 1.50, 1, 0);
                    }
                }
            }
        }
        block event player-attacked {
            track main {
                lambda {
                    go(alert-npc, 1);
                }
            }
        }
        block event player-fired-weapon {
            track main {
                lambda {
                    if (tension-mode-is?(3)) {
                        go(alert-npc, 1);
                    }
                }
            }
        }
        block event npc-died {
            track main {
                lambda {
                    if (event-arg-symbol(0) != get-symbol(npc-held-up, self)) {
                        go(alert-npc, 1);
                    }
                }
            }
        }
        block event breakout-timer {
            track main {
                lambda {
                    go(alert-npc, 1);
                }
            }
        }
        block end {
            track main {
                lambda {
                    set-int32(gun-raycast, -1, self);
                }
            }
        }
    }
    state holdup-sustain {
        block update {
            track main {
                lambda {
                    if (!npc-alive?(get-symbol(npc-held-up, self))) {
                        go(wait-for-aim, 1);
                    }
                }
                lambda {
                    if (npc-should-break-holdup?()) {
                        go(alert-npc, 1);
                    }
                }
                lambda {
                    if (timer-running?(breakout-timer, self) && get-float(breakout-timer, self) < 0.20 && joypad-command-active?(interact-hold, 6)) {
                        start-timer(breakout-timer, 0.20, self, 1);
                    }
                }
                lambda {
                    if (get-state-time(self) > 3.00) {
                        go(alert-npc, 1);
                    }
                }
                lambda {
                    if (can-continue-holdup?(get-symbol(npc-held-up, self), tag-float(max-holdup-dist, self) + 5.00)) {
                        var_3 = go(holdup, 1);
                    }
                }
                lambda {
                    track-last-seen-position();
                }
                lambda {
                    npcs-investigate-holdup();
                }
            }
        }
        block event player-attacked {
            track main {
                lambda {
                    go(alert-npc, 1);
                }
            }
        }
        block event player-fired-weapon {
            track main {
                lambda {
                    if (tension-mode-is?(3)) {
                        go(alert-npc, 1);
                    }
                }
            }
        }
        block event npc-died {
            track main {
                lambda {
                    if (event-arg-symbol(0) != get-symbol(npc-held-up, self)) {
                        go(alert-npc, 1);
                    }
                }
            }
        }
        block event breakout-timer {
            track main {
                lambda {
                    go(alert-npc, 1);
                }
            }
        }
        block end {
            track main {
                lambda {
                    set-int32(gun-raycast, -1, self);
                }
            }
        }
    }
    state alert-npc {
        block start {
            track main {
                lambda {
                    if (npc-alive?(get-symbol(npc-held-up, self))) {
                        if (!characters-in-melee?(player, get-symbol(npc-held-up, self), 1)) {
                            script-caption(get-symbol(npc-held-up, self), "Screw you!", -1.00, 0, 0);
                        }
                        npc-set-script-high-priority(get-symbol(npc-held-up, self));
                        stop-animating(get-symbol(npc-held-up, self), 2, 0.00, 0, 0);
                        clear-gesture(get-symbol(npc-held-up, self), -1, -1.00, 0);
                        npc-clear-look-aim-at(get-symbol(npc-held-up, self));
                        npc-clear-request-movement(get-symbol(npc-held-up, self));
                        npc-stop(get-symbol(npc-held-up, self));
                        npc-set-demeanor(get-symbol(npc-held-up, self), 2, 0);
                        melee-fact-set-bool(get-symbol(npc-held-up, self), being-held-up, 0);
                        wait-one-frame();
                        if (!characters-in-melee?(player, get-symbol(npc-held-up, self), 1)) {
                            npc-make-aware-of-at(get-symbol(npc-held-up, self), player, get-point(last-seen-pos, self), 2, *npc-awareness-options-no-surprise*, 0);
                            npc-unholster-weapon(get-symbol(npc-held-up, self), 2, -1.00, 0);
                        }
                    }
                }
                lambda {
                    go(cooldown, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!npc-alive?(get-symbol(npc-held-up, self))) {
                        go(cooldown, 1);
                    }
                }
            }
        }
    }
    state cooldown {
        block start {
            track main {
                lambda {
                    wait(2.00);
                }
                lambda {
                    go(wait-for-aim, 1);
                }
            }
        }
    }
}
