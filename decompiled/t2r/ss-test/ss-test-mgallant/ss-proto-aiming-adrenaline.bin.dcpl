// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...bin_unpacked/dc1\ss-test\ss-test-mgallant\ss-proto-aiming-adrenaline.bin  #
// #   filesize: 7780 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 19                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        sp-all
    }
    declarations {
        f32 time-scale = 1.00
    }
    state inactive {
        block update {
            track main {
                lambda {
                    if (*(u8 *)(*deprecated-prototypes* + 3)) {
                        go(wait-for-button, 1);
                    }
                }
            }
        }
    }
    state wait-for-button {
        block update {
            track main {
                lambda {
                    if (!*(u8 *)(*deprecated-prototypes* + 3)) {
                        go(inactive, 1);
                    }
                }
                lambda {
                    if (player-is-aiming?() || player-in-state?(GunMove) || player-in-state?(BowMove) || player-in-state?(Prone) && 0 && joypad-command-active?(melee-attack, 6) && character-get-adrenaline(player) >= 50.00) {
                        go(adrenaline-slowdown, 1);
                    }
                }
            }
        }
    }
    state adrenaline-slowdown {
        block start {
            track main {
                lambda {
                    broadcast-event(suspend-adrenaline-prototypes);
                }
                lambda {
                    if (random-integer(1000, 0, 0) == 0) {
                        script-caption(player, "It's high noon...", -1.00, 0, 0);
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!player-is-aiming?()) {
                        if (player-in-state?(GunMove) || player-in-state?(BowMove) || player-in-state?(Prone)) {
                            if (!0 && character-get-adrenaline(player) == 0.00) {
                                go(fade-out, 1);
                            }
                        } else if (character-get-adrenaline(player) == 0.00) {}

                    }
                }
                lambda {
                    set-float(time-scale, lerp-scale(0.00, 0.50, 1.00, 0.30, get-state-time(self)), self);
                }
                lambda {
                    set-clock-multiplier/f(get-float(time-scale, self), 1);
                }
                lambda {
                    character-use-adrenaline(player, 30.00 * get-delta-time());
                }
                lambda {
                    override-render-settings(
                        *zgame-adrenaline-aim-proto*,
                        0.50,
                        1.00,
                        0,
                        32767,
                        0,
                        1,
                        0,
                        1.00
                    );
                }
                lambda {
                    player-force-best-accuracy();
                }
                lambda {
                    foreach (u16 var_4 : all-enemy-npcs) {
                        npc-set-accuracy-multiplier(var_4, lerp-scale(0.00, 0.50, 0.00, 1.00, get-state-time(self)));
                    }
                }
            }
        }
        block end {
            track main {
                lambda {
                    broadcast-event(unsuspend-adrenaline-prototypes);
                }
                lambda {
                    foreach (u16 var_2 : all-enemy-npcs) {
                        npc-set-accuracy-multiplier(var_2, 1.00);
                    }
                }
            }
        }
    }
    state fade-out {
        block update {
            track main {
                lambda {
                    if (player-is-aiming?() || player-in-state?(GunMove) || player-in-state?(BowMove) || player-in-state?(Prone) && 0 && joypad-command-active?(melee-attack, 6) && character-get-adrenaline(player) >= 50.00) {
                        go(adrenaline-slowdown, 1);
                    }
                }
                lambda {
                    +=f(time-scale, 5.00 * get-delta-time(), self);
                }
                lambda {
                    clamp-var(time-scale, 0.30, 1.00, self);
                }
                lambda {
                    set-clock-multiplier/f(get-float(time-scale, self), 1);
                }
                lambda {
                    if (get-float(time-scale, self) == 1.00) {
                        go(wait-for-button, 1);
                    }
                }
            }
        }
    }
}
