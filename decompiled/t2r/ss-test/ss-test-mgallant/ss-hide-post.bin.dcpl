// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 1.52.0                                                            #
// #   listing for file: game_dumps/t2r/dc1/ss-test\ss-test-mgallant\ss-hide-post.bin                 #
// #   filesize: 8682 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 17                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: true                                                                    #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        sp-all
        mg-hide-skill-test
    }
    declarations {
        i32 raycast = -1
        f32 hide-score = 0.00
        f32 dist-to-npc = 0.00
        f32 dist-to-player = 0.00
        bool center-screen = nullptr
        bool on-screen = nullptr
        bool raycast-blocked = nullptr
        bool facing-player = nullptr
        bool blocked-by-player = nullptr
        f32 score = 0.00
    }
    state test {
        block start {
            track main {
                lambda {
                    while (!npc-alive?(tag-symbol(hiding-npc, self))) {
                        wait-one-frame();
                    }
                }
                lambda {
                    while (!is-script-running?(tag-symbol(hiding-npc, self))) {
                        wait-one-frame();
                    }
                }
                lambda {
                    send-event(register-hide-post, tag-symbol(hiding-npc, self), boxed_symbol(get-self-id()));
                }
            }
        }
        block update {
            track main {
                lambda {
                    u64? var_3 = get-spawner-locator(self, 1);
                    set-float(dist-to-player, get-locator-distance(get-object-locator(player), var_3), self);
                    set-float(dist-to-npc, get-locator-distance(get-object-locator(tag-symbol(hiding-npc, self)), var_3), self);
                }
                lambda {
                    set-boolean(on-screen, is-spawner-on-screen?(self), self);
                }
                lambda {
                    set-boolean(center-screen, is-centered-on-screen?(get-spawner-position(self, 1), 250.00), self);
                }
                lambda {
                    set-boolean(blocked-by-player, get-float(dist-to-player, self) < get-float(dist-to-npc, self) && is-behind?(
                        rotate-locator-lookat(get-object-locator(player), get-object-locator(tag-symbol(hiding-npc, self)), 1.00, 0),
                        get-spawner-locator(self, 1),
                        2,
                        180.00
                    ), self);
                }
                lambda {
                    set-boolean(facing-player, is-in-front?(get-spawner-locator(self, 1), get-object-locator(player), 2, 140.00), self);
                }
                lambda {
                    set-boolean(raycast-blocked, get-int32(raycast, self) > 0 && !wait-raycast-hit-nothing?(get-int32(raycast, self)), self);
                }
                lambda {
                    set-float(score, 1.00, self);
                }
                lambda {
                    if (get-boolean(center-screen, self)) {
                        set-float(score, 0.50 * get-float(score, self), self);
                    } else if (get-boolean(on-screen, self)) {
                        set-float(score, 0.80 * get-float(score, self), self);
                    }
                }
                lambda {
                    if (get-boolean(blocked-by-player, self)) {
                        set-float(score, 0.00 * get-float(score, self), self);
                    }
                }
                lambda {
                    if (!get-boolean(facing-player, self)) {
                        set-float(score, 0.00 * get-float(score, self), self);
                    }
                }
                lambda {
                    if (!get-boolean(raycast-blocked, self)) {
                        set-float(score, 0.00 * get-float(score, self), self);
                    }
                }
                lambda {
                    set-float(score, (curve-scale(5.00, 50.00, 1.00, 0.00, get-float(dist-to-player, self), 5) * curve-scale(0.00, 3.00, 0.00, 1.00, get-float(dist-to-player, self), 1)) * get-float(score, self), self);
                }
                lambda {
                    u16 var_4;
                    if (get-float(score, self) > 0.90) {
                        var_4 = 1;
                    } else if (get-float(score, self) > 0.70) {
                        var_4 = 7;
                    } else if (get-float(score, self) > 0.50) {
                        var_4 = 9;
                    } else {
                        var_4 = 2;
                    }
                    debug-draw-string(
                        get-spawner-position(self, 1),
                        dc:format("%.2f", get-float(score, self)),
                        var_4,
                        0.00,
                        1,
                        1.00,
                        0,
                        0
                    );
                }
                lambda {
                    u64? var_1;
                    if (get-boolean(on-screen, self)) {
                        var_1 = camera-locator(0);
                    } else {
                        var_1 = get-joint-locator(player, pelvis);
                    }
                    set-int32(raycast, kick-raycast(var_1, get-spawner-locator(self, 1), player, 1), self);
                }
            }
        }
    }
}
