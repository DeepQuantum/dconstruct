// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...main/bin_unpacked/dc1\ss-test\ss-test-mgallant\ss-proto-door-padlock.bin  #
// #   filesize: 4189 bytes                                                                           #
// #   total entries: 1                                                                               #
// #   functions: 7                                                                                   #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

statescript {
    options {
        player
        sp-all
    }
    declarations {
        i32 raycast = -1
    }
    state wait-for-attack {
        block event_Attack {
            track main {
                lambda {
                    if (event-attack-type?(0)) {
                        go(fall-to-ground, 1);
                    }
                }
            }
        }
        block event_parent-attacked {
            track main {
                lambda {
                    go(fall-to-ground, 1);
                }
            }
        }
    }
    state fall-to-ground {
        block start {
            track main {
                lambda {
                    set-boolean(lock-input, 1, tag-symbol(door, self));
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (get-int32(raycast, self) >= 0) {
                        if (wait-raycast-hit-nothing?(get-int32(raycast, self))) {
                            set-object-locator(self, offset-locator(flatten-locator(get-object-locator(self)), alloc-vector(0.00, get-delta-time() * -3.00, get-delta-time() * 0.50, 1.00, 0)));
                        } else {
                            set-object-locator(self, flatten-locator(wait-raycast(get-int32(raycast, self))));
                            go(unlock, 1);
                        }
                    }
                }
                lambda {
                    set-int32(raycast, kick-raycast(
                        get-object-locator(self),
                        offset-locator(flatten-locator(get-object-locator(self)), alloc-vector(0.00, get-delta-time() * -3.00, get-delta-time() * 0.50, 1.00, 0)),
                        get-self-id(),
                        4097
                    ), self);
                }
            }
        }
    }
    state unlock {
        block start {
            track main {
                lambda {
                    send-event(padlock-destroyed, tag-symbol(door, self));
                }
                lambda {
                    disable-collision(tag-symbol(door, self), 0);
                }
            }
        }
    }
}
