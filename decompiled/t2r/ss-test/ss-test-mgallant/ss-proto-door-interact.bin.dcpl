// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...ain/bin_unpacked/dc1\ss-test\ss-test-mgallant\ss-proto-door-interact.bin  #
// #   filesize: 21365 bytes                                                                          #
// #   total entries: 4                                                                               #
// #   functions: 52                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

u16 draw-door-interact(u64? arg_0) {
    u64? var_0 = doorknob-locator();
    u16 var_3;
    if (is-on-screen?(get-locator-position(var_0), 0.00)) {
        if (arg_0) {
            var_3 = draw-interactable-hud(interact-default-hold, var_0, 0);
        } else {
            var_3 = draw-interactable-hud(interact-default, var_0, 0);
        }
    } else {
        var_3 = 0;
    }
    return var_3;
}

u64? doorknob-locator() {
    u64? var_1;
    if (get-object-look(self, 0) == es-door-bash-open-left) {
        var_1 = offset-locator(get-object-locator(self), alloc-vector(-1.00, 1.00, 0.00, 1.00, 0));
    } else {
        var_1 = offset-locator(get-object-locator(self), alloc-vector(1.00, 1.00, 0.00, 1.00, 0));
    }
    return var_1;
}

u64? pull-door-back?(u64? arg_0, u64? arg_1) {
    return is-in-front?(get-spawner-locator(self, 1), get-object-locator(arg_0), 2, 180.00) || is-npc?(arg_0) || arg_1 && is-in-front?(get-spawner-locator(self, 1), get-predicted-player-bound-frame(1.00), 2, 180.00);
}

statescript {
    options {
        player
        sp-all
    }
    declarations {
        symbol npc-opening = #0000000000000000
        f32 rotate-angle = 0.00
        f32 last-stick-dir = 0.00
        bool commit-dir = nullptr
        f32 y-rotate = 0.00
    }
    state setup {
        block start {
            track main {
                lambda {
                    animate_(self, ajar, #%alloc-array(single-frame, 16, 0, 1));
                }
                lambda {
                    go(closed, 1);
                }
            }
        }
    }
    state closed {
        block start {
            track main {
                lambda {
                    block-nav-poly-under-object(self);
                }
                lambda {
                    enable-action-pack(tag-symbol(door-tap, self), 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    foreach (u16 var_2 : all-enemy-npcs) {
                        if (distance-between-objects-less-than?(self, var_2, 1.50) && npc-using-traversal-action-pack?(var_2)) {
                            set-symbol(npc-opening, var_2, self);
                            go(opening-npc, 1);
                        }
                    }
                }
                lambda {
                    if (player-in-region?(tag-symbol(interact-rgn, self), 2)) {
                        draw-interactable-hud(interact-default-hold, doorknob-locator(), 0);
                        if (joypad-command-active?(interact-hold, 6)) {
                            if (pull-door-back?(player, 1)) {
                                go(opening-slowly-backwards, 1);
                            } else {
                                go(opening-slowly-forwards, 1);
                            }
                        }
                    }
                }
            }
        }
    }
    state opening-forwards {
        block start {
            track main {
                lambda {
                    wait-stub-object-lerp-locator(
                        self,
                        rotate-locator-axis-angle(get-spawner-locator(self, 1), alloc-vector(0.00, 1.00, 0.00, 1.00, 0), -90.00),
                        0.40,
                        1,
                        1.00
                    );
                }
                lambda {
                    go(open, 1);
                }
            }
        }
    }
    state opening-slowly-backwards {
        block start {
            track main {
                lambda {
                    set-boolean(commit-dir, 0, self);
                }
                lambda {
                    set-float(rotate-angle, 0.00, self);
                }
                lambda {
                    set-float(y-rotate, 0.00, self);
                }
                lambda {
                    set-float(last-stick-dir, 0.00, self);
                }
                lambda {
                    set-object-locator(camera-lerp-cube-1, camera-locator(0));
                }
                lambda {
                    camera-enable(
                        tag-symbol(open-bw-camera, self),
                        fixed,
                        1.00,
                        1000,
                        1.00,
                        10.00,
                        1,
                        0,
                        0,
                        0,
                        0,
                        -1.00
                    );
                }
                lambda {
                    u64? var_0 = alloc-gesture-play-params();
                    *(u64 *)(var_0 + 24) = get-locator-position(doorknob-locator());
                    *(u8 *)(var_0 + 16) = 1;
                    gesture_(player, dra-pickup-stand-gesture, var_0);
                }
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 3, 1);
                    *(i32 *)*(*var_0 + (0 * 8)) = 10;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = 0;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = get-spawner-locator(tag-symbol(open-bw-pos, self), 1);
                    *(i32 *)*(*var_0 + (1 * 8)) = 28;
                    *(u64 *)(*(*var_0 + (1 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (1 * 8)) + 8) = 0.50;
                    *(i32 *)*(*var_0 + (2 * 8)) = 31;
                    *(u64 *)(*(*var_0 + (2 * 8)) + 8) = 0;
                    *(u8 *)(*(*var_0 + (2 * 8)) + 8) = 1;
                    animate_(player, ellie-mm-crouch-idle, var_0);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!joypad-command-active?(interact-hold, 6)) {
                        set-boolean(commit-dir, 1, self);
                    }
                }
                lambda {
                    if (!get-boolean(commit-dir, self)) {
                        set-float(last-stick-dir, -2.00 * get-joypad-stick-x(move, 0.20, 1.00), self);
                    }
                }
                lambda {
                    if (!get-boolean(commit-dir, self)) {
                        draw-interactable-hud(interact-left-stick-updown, doorknob-locator(), 0);
                    }
                }
                lambda {
                    f32 var_1;
                    if (get-boolean(commit-dir, self)) {
                        if (get-float(last-stick-dir, self) <= 0.00) {
                            var_1 = -5.00;
                        } else {
                            var_1 = 5.00;
                        }
                    } else {
                        var_1 = get-float(last-stick-dir, self);
                    }
                    if (get-state-time(self) > 0.50) {
                        var_5 = set-float(rotate-angle, get-float(rotate-angle, self) + var_1, self);
                    } else {
                        var_5 = set-float(rotate-angle, lerp-scale(0.00, 0.50, 0.00, -10.00, get-state-time(self)), self);
                    }
                }
                lambda {
                    if (absf(get-joypad-stick-y(move, 0.20, 1.00)) > 0.10) {
                        +=f(y-rotate, (get-joypad-stick-y(move, 0.20, 1.00) * get-delta-time()) * 20.00, self);
                    }
                }
                lambda {
                    clamp-var(y-rotate, -20.00, 20.00, self);
                }
                lambda {
                    if (!get-boolean(commit-dir, self)) {
                        set-object-locator(camera-lerp-cube-1, rotate-locator-angle(lerp-locator(get-spawner-locator(cam-open-close-1, 1), get-spawner-locator(cam-open-close-2, 1), lerp-scale(0.00, -80.00, 0.00, 1.00, get-float(rotate-angle, self))), 0, get-float(y-rotate, self)));
                    }
                }
                lambda {
                    if (!get-boolean(commit-dir, self) && get-float(last-stick-dir, self) > 0.00) {
                        update-ap-ref(player, lerp-locator(get-spawner-locator(pos-lerp-open-bw-1, 1), get-spawner-locator(pos-lerp-open-bw-2, 1), lerp-scale(0.00, -80.00, 0.00, 1.00, get-float(rotate-angle, self))), 0);
                    }
                }
                lambda {
                    clamp-var(rotate-angle, -90.00, 0.00, self);
                }
                lambda {
                    set-object-locator(self, rotate-locator-axis-angle(get-spawner-locator(self, 1), alloc-vector(0.00, 1.00, 0.00, 1.00, 0), get-float(rotate-angle, self)));
                }
                lambda {
                    if (get-boolean(commit-dir, self)) {
                        if (get-float(rotate-angle, self) < -80.00 && get-float(last-stick-dir, self) < 0.10) {
                            go(open, 1);
                        } else if (get-float(rotate-angle, self) > -10.00 && get-float(last-stick-dir, self) > 0.10) {
                            go(closed, 1);
                        }
                    }
                }
            }
        }
        block end {
            track main {
                lambda {
                    if (object-spawned?(player)) {
                        stop-animating(player, 2, 0.30, 0, 0);
                        camera-fade-out(tag-symbol(open-bw-camera, self), 0.30, 0);
                    }
                }
            }
        }
    }
    state opening-npc {
        block start {
            track npc {
                lambda {
                    if (pull-door-back?(get-symbol(npc-opening, self), 1)) {
                        wait-stub-igc-pose(get-symbol(npc-opening, self), pos-lerp-open-bw-1, 0.50, ellie-mm-tense-idle);
                    }
                }
                lambda {
                    signal(npc-done, 0);
                }
            }
            track door {
                lambda {
                    wait-stub-object-lerp-locator(
                        self,
                        rotate-locator-axis-angle(get-spawner-locator(self, 1), alloc-vector(0.00, 1.00, 0.00, 1.00, 0), -90.00),
                        0.40,
                        1,
                        1.00
                    );
                }
                lambda {
                    wait-for(npc-done);
                }
                lambda {
                    go(open, 1);
                }
            }
        }
    }
    state open {
        block start {
            track main {
                lambda {
                    unblock-nav-poly-under-object(self);
                }
                lambda {
                    enable-action-pack(tag-symbol(door-tap, self), 0);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (player-in-region?(tag-symbol(interact-rgn, self), 2)) {
                        draw-door-interact(0);
                        if (joypad-command-active?(interact-release, 6)) {
                            if (pull-door-back?(player, 0)) {
                                go(closing-backwards, 1);
                            } else {
                                go(closing-forwards, 1);
                            }
                        }
                    }
                }
            }
        }
    }
    state closing-backwards {
        block start {
            track player {
                lambda {
                    if (dot-product-locators(get-spawner-locator(self, 1), camera-locator(0), 2) > 0.00) {
                        camera-enable(
                            tag-symbol(open-bw-camera, self),
                            fixed,
                            0.00,
                            1000,
                            1.00,
                            10.00,
                            1,
                            0,
                            0,
                            0,
                            0,
                            -1.00
                        );
                    } else {
                        camera-enable(
                            tag-symbol(open-bw-camera, self),
                            fixed,
                            0.30,
                            1000,
                            1.00,
                            10.00,
                            1,
                            0,
                            0,
                            0,
                            0,
                            -1.00
                        );
                    }
                }
                lambda {
                    u64? var_0 = alloc-gesture-play-params();
                    *(u64 *)(var_0 + 24) = get-locator-position(doorknob-locator());
                    *(u8 *)(var_0 + 16) = 1;
                    gesture_(player, dra-pickup-stand-gesture, var_0);
                }
                lambda {
                    wait-stub-igc-pose(player, tag-symbol(open-bw-pos, self), 0.50, ellie-mm-tense-idle);
                }
                lambda {
                    camera-fade-out(tag-symbol(open-bw-camera, self), 0.30, 0);
                }
                lambda {
                    signal(player-done, 0);
                }
            }
            track door {
                lambda {
                    block-nav-poly-under-object(self);
                }
                lambda {
                    wait(0.10);
                }
                lambda {
                    wait-stub-object-lerp-locator(self, get-spawner-locator(self, 1), 0.40, 1, 1.00);
                }
                lambda {
                    wait-for(player-done);
                }
                lambda {
                    go(closed, 1);
                }
            }
        }
    }
    state closing-forwards {
        block start {
            track main {
                lambda {
                    block-nav-poly-under-object(self);
                }
                lambda {
                    wait-stub-object-lerp-locator(self, get-spawner-locator(self, 1), 0.40, 1, 1.00);
                }
                lambda {
                    go(closed, 1);
                }
            }
        }
    }
}
