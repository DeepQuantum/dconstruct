// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ...bin_unpacked/dc1\ss-test\ss-test-mgallant\ss-proto-door-interact-aim.bin  #
// #   filesize: 30140 bytes                                                                          #
// #   total entries: 8                                                                               #
// #   functions: 88                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

u16 npc-using-door-tap() {
    u16 var_1;
    foreach (u16 var_3 : all-npcs) {
        u16 var_6;
        if (0 == 0 && distance-between-objects-less-than?(self, var_3, 1.50) && npc-using-traversal-action-pack?(var_3)) {
            var_6 = var_3;
        }
        var_1 = var_6;
    }
    return var_1;
}

u64? can-lock-unlock-door?() {
    return *(u8 *)(get-current-player-upgrades() + 30) && !(tag-symbol(key-name, self) == 0) && fact-exists?(player, tag-symbol(key-name, self)) && fact-bool(player, tag-symbol(key-name, self), 1, 0);
}

f32 door-open-angle() {
    f32 var_1;
    if (get-object-look(self, 0) == es-door-bash-open-left) {
        var_1 = 90.00;
    } else {
        var_1 = -90.00;
    }
    return var_1;
}

u64? current-door-angle() {
    return angle-in-between-locators(get-spawner-locator(self, 1), get-object-locator(self), 2);
}

u64? allow-door-interact?() {
    return get-boolean(allow-interact, self) && player-in-region?(tag-symbol(interact-rgn, self), 2) || player-in-state?(Move) || player-in-state?(GunMove) || player-in-state?(BowMove) && 0;
}

u64? joypad-command-door-open?() {
    return joypad-command-active?(interact-release, 6) || player-in-state?(Move) || player-in-state?(GunMove) || player-in-state?(BowMove) && 0 && !get-boolean(lock-input, self);
}

u64? buddy-should-close-door?(u64? arg_0) {
    u64? var_9 = get-object-locator(player);
    u64? var_10 = get-object-locator(arg_0);
    u64? var_11 = get-spawner-locator(self, 1);
    is-in-front?(var_11, var_9, 2, 180.00);
    is-in-front?(var_11, var_10, 2, 180.00);
    is-behind?(var_11, var_9, 2, 180.00);
    return tag-boolean(buddy-can-close, self) && npc-alive?(arg_0) && npc-using-skill?(arg_0, 36) && !object-anim-playing?(arg_0, downed-idle, 0) && !player-in-region?(tag-symbol(interact-rgn, self), 2) && locator-in-region?(get-object-locator(arg_0), tag-symbol(interact-rgn, self), player) && var_9 || var_9 && var_9 && var_9 && is-behind?(var_11, var_10, 2, 180.00);
}

statescript {
    options {
        player
        sp-all
    }
    declarations {
        bool allow-interact = nullptr
        symbol npc-opening = #0000000000000000
        bool locked = nullptr
        bool lock-input = nullptr
    }
    state --all-- {
        block event_destroyed {
            track main {
                lambda {
                    go(destroyed, 1);
                }
            }
        }
        block event_Attack {
            track main {
                lambda {
                    if (!(tag-symbol(glass-door, self) == 0)) {
                        send-event(parent-attacked, tag-symbol(glass-door, self));
                    }
                }
            }
        }
    }
    state setup {
        block start {
            track main {
                lambda {
                    animate_(self, ajar, #%alloc-array(single-frame, 16, 0, 1));
                }
                lambda {
                    if (!(tag-symbol(glass-door, self) == 0)) {
                        set-visible(self, 0);
                    }
                }
                lambda {
                    if (!(tag-symbol(padlock, self) == 0)) {
                        go(padlocked-closed, 1);
                    } else if (tag-boolean(start-locked, self)) {
                        go(locked-closed, 1);
                    } else if (tag-boolean(start-open, self)) {
                        go(open, 1);
                    } else {
                        go(closed, 1);
                    }
                }
            }
        }
    }
    state closed {
        block start {
            track main {
                lambda {
                    block-nav-poly-under-object(self);
                }
                lambda {
                    set-boolean(allow-interact, 0, self);
                }
                lambda {
                    wait-stub-object-lerp-locator(
                        self,
                        get-spawner-locator(self, 1),
                        lerp-scale(0.00, 90.00, 0.00, 1.00, current-door-angle()),
                        1,
                        1.00
                    );
                }
                lambda {
                    set-boolean(allow-interact, 1, self);
                }
                lambda {
                    enable-action-pack(tag-symbol(door-tap, self), 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    set-symbol(npc-opening, npc-using-door-tap(), self);
                }
                lambda {
                    if (!(get-symbol(npc-opening, self) == 0)) {
                        go(open-npc, 1);
                    }
                }
                lambda {
                    if (allow-door-interact?()) {
                        draw-door-interact(0);
                        if (joypad-command-active?(interact-hold, 6) && can-lock-unlock-door?()) {
                            script-caption(player, "LOCKED", 1.00, 0, 0);
                            go(locked-closed, 1);
                        }
                        if (joypad-command-door-open?()) {
                            if (player-is-aiming?() && *(u8 *)(get-current-player-upgrades() + 29)) {
                                go(peek-open, 1);
                            } else if (pull-door-back?(player, 1)) {
                                go(open-backwards, 1);
                            } else {
                                go(open-forewards, 1);
                            }
                        }
                    }
                }
                lambda {
                    if (!get-boolean(allow-interact, self)) {
                        disable-follow-camera-collision();
                    }
                }
            }
        }
    }
    state locked-closed {
        block start {
            track main {
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 1, 1);
                    *(i32 *)*(*var_0 + (0 * 8)) = 28;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (0 * 8)) + 8) = 0.30;
                    animate_(self, idle, var_0);
                }
                lambda {
                    set-boolean(lock-input, 1, self);
                }
                lambda {
                    set-boolean(locked, 1, self);
                }
                lambda {
                    disable-action-pack(tag-symbol(door-tap, self));
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!joypad-command-active?(interact-hold, 6)) {
                        set-boolean(lock-input, 0, self);
                        if (!get-boolean(locked, self)) {
                            go(closed, 1);
                        }
                    }
                }
                lambda {
                    if (allow-door-interact?()) {
                        if (joypad-command-active?(interact-hold, 6)) {
                            if (get-boolean(lock-input, self)) {}
 else if (can-lock-unlock-door?()) {
                                script-caption(player, "Unlocked", 1.00, 0, 0);
                                u64? var_4 = #%alloc-array(single-frame, 16, 1, 1);
                                *(i32 *)*(*var_4 + (0 * 8)) = 28;
                                *(u64 *)(*(*var_4 + (0 * 8)) + 8) = 0;
                                *(f32 *)(*(*var_4 + (0 * 8)) + 8) = 0.30;
                                animate_(self, ajar, var_4);
                                set-boolean(locked, 0, self);
                                set-boolean(lock-input, 1, self);
                            } else if (!(tag-symbol(key-name, self) == 0)) {
                                script-caption(player, "I need a key", 1.00, 0, 0);
                                set-boolean(lock-input, 1, self);
                            }
                        }
                        if (!get-boolean(lock-input, self) && get-boolean(locked, self)) {
                            draw-door-interact(get-boolean(locked, self));
                        }
                    }
                }
            }
        }
    }
    state padlocked-closed {
        block start {
            track main {
                lambda {
                    animate_(self, idle, #%alloc-array(single-frame, 16, 0, 1));
                }
                lambda {
                    disable-action-pack(tag-symbol(door-tap, self));
                }
                lambda {
                    set-boolean(allow-interact, 1, self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (!get-boolean(lock-input, self) && allow-door-interact?() && joypad-command-active?(interact, 6)) {
                        script-caption(player, "Padlocked", 1.00, 0, 0);
                        set-boolean(lock-input, 1, self);
                    }
                }
                lambda {
                    if (!get-boolean(lock-input, self) && allow-door-interact?()) {
                        draw-door-interact(0);
                    }
                }
            }
        }
        block event_padlock-destroyed {
            track main {
                lambda {
                    u64? var_0 = #%alloc-array(single-frame, 16, 1, 1);
                    *(i32 *)*(*var_0 + (0 * 8)) = 28;
                    *(u64 *)(*(*var_0 + (0 * 8)) + 8) = 0;
                    *(f32 *)(*(*var_0 + (0 * 8)) + 8) = 0.30;
                    animate_(self, ajar, var_0);
                }
                lambda {
                    set-boolean(lock-input, 0, self);
                }
                lambda {
                    go(closed, 1);
                }
            }
        }
    }
    state peek-open {
        block start {
            track main {
                lambda {
                    set-boolean(allow-interact, 0, self);
                }
                lambda {
                    wait-stub-object-lerp-locator(
                        self,
                        rotate-locator-axis-angle(get-spawner-locator(self, 1), alloc-vector(0.00, 1.00, 0.00, 1.00, 0), door-open-angle() / 6.00),
                        0.50,
                        1,
                        1.00
                    );
                }
                lambda {
                    set-boolean(allow-interact, 1, self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    set-symbol(npc-opening, npc-using-door-tap(), self);
                }
                lambda {
                    if (!(get-symbol(npc-opening, self) == 0)) {
                        go(open-npc, 1);
                    }
                }
                lambda {
                    if (!player-in-region?(tag-symbol(interact-rgn, self), 2) && !player-is-aiming?()) {
                        go(closed, 1);
                    }
                }
                lambda {
                    if (allow-door-interact?()) {
                        draw-door-interact(0);
                        if (joypad-command-door-open?()) {
                            go(open, 1);
                        }
                    }
                }
            }
        }
    }
    state open-npc {
        block start {
            track npc {
                lambda {
                    if (pull-door-back?(get-symbol(npc-opening, self), 1)) {
                        wait-stub-igc-pose(get-symbol(npc-opening, self), tag-symbol(open-bw-pos, self), 0.50, ellie-mm-tense-idle);
                    }
                }
                lambda {
                    signal(npc-done, 0);
                }
            }
            track door {
                lambda {
                    wait-stub-object-lerp-locator(
                        self,
                        rotate-locator-axis-angle(get-spawner-locator(self, 1), alloc-vector(0.00, 1.00, 0.00, 1.00, 0), door-open-angle()),
                        0.40,
                        1,
                        1.00
                    );
                }
                lambda {
                    wait-for(npc-done);
                }
                lambda {
                    go(open, 1);
                }
            }
        }
    }
    state open-forewards {
        block start {
            track main {
                lambda {
                    u64? var_0 = alloc-gesture-play-params();
                    *(u64 *)(var_0 + 24) = get-locator-position(doorknob-locator());
                    *(u8 *)(var_0 + 16) = 1;
                    gesture_(player, dra-pickup-stand-gesture, var_0);
                }
                lambda {
                    go(open, 1);
                }
            }
        }
    }
    state open-backwards {
        block start {
            track player {
                lambda {
                    camera-enable(
                        tag-symbol(open-bw-camera, self),
                        fixed,
                        0.30,
                        1000,
                        1.00,
                        10.00,
                        1,
                        0,
                        0,
                        0,
                        0,
                        -1.00
                    );
                }
                lambda {
                    u64? var_0 = alloc-gesture-play-params();
                    *(u64 *)(var_0 + 24) = get-locator-position(doorknob-locator());
                    *(u8 *)(var_0 + 16) = 1;
                    gesture_(player, dra-pickup-stand-gesture, var_0);
                }
                lambda {
                    u16 var_2;
                    if (player-is-crouched?()) {
                        var_2 = 18;
                    } else {
                        var_2 = 1;
                    }
                    sid var_4;
                    if (player-is-crouched?()) {
                        var_4 = ellie-mm-crouch-idle;
                    } else {
                        var_4 = ellie-mm-tense-idle;
                    }
                    wait-stub-igc(player, tag-symbol(open-bw-pos, self), 0.50, 0, var_2, var_4);
                }
                lambda {
                    camera-fade-out(tag-symbol(open-bw-camera, self), 0.30, 0);
                }
                lambda {
                    signal(player-done, 0);
                }
            }
            track door {
                lambda {
                    disable-action-pack(tag-symbol(door-tap, self));
                }
                lambda {
                    wait(0.10);
                }
                lambda {
                    wait-stub-object-lerp-locator(
                        self,
                        rotate-locator-axis-angle(get-spawner-locator(self, 1), alloc-vector(0.00, 1.00, 0.00, 1.00, 0), door-open-angle()),
                        0.40,
                        1,
                        1.00
                    );
                }
                lambda {
                    wait-for(player-done);
                }
                lambda {
                    go(open, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    disable-follow-camera-collision();
                }
            }
        }
    }
    state open {
        block start {
            track main {
                lambda {
                    disable-action-pack(tag-symbol(door-tap, self));
                }
                lambda {
                    set-boolean(allow-interact, 0, self);
                }
                lambda {
                    wait-stub-object-lerp-locator(
                        self,
                        rotate-locator-axis-angle(get-spawner-locator(self, 1), alloc-vector(0.00, 1.00, 0.00, 1.00, 0), door-open-angle()),
                        lerp-scale(0.00, 90.00, 1.00, 0.00, current-door-angle()),
                        1,
                        1.00
                    );
                }
                lambda {
                    broadcast-event(door-opened, boxed_symbol(get-self-id()));
                }
                lambda {
                    set-boolean(allow-interact, 1, self);
                }
                lambda {
                    unblock-nav-poly-under-object(self);
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (allow-door-interact?()) {
                        draw-door-interact(0);
                        if (joypad-command-door-open?()) {
                            if (player-is-aiming?() && *(u8 *)(get-current-player-upgrades() + 29)) {
                                go(peek-open, 1);
                            } else if (pull-door-back?(player, 0)) {
                                go(close-backwards, 1);
                            } else {
                                go(close-forewards, 1);
                            }
                        }
                    }
                }
                lambda {
                    if (deterministic-nth-frame?(10) && !allow-door-interact?()) {
                        foreach (u16 var_4 : all-buddy-npcs) {
                            if (buddy-should-close-door?(var_4)) {
                                script-caption(var_4, "Closing door", -1.00, 0, 0);
                                go(closed, 1);
                            }
                        }
                    }
                }
                lambda {
                    if (!get-boolean(allow-interact, self)) {
                        disable-follow-camera-collision();
                    }
                }
            }
        }
    }
    state close-forewards {
        block start {
            track main {
                lambda {
                    u64? var_0 = alloc-gesture-play-params();
                    *(u64 *)(var_0 + 24) = get-locator-position(doorknob-locator());
                    *(u8 *)(var_0 + 16) = 1;
                    gesture_(player, dra-pickup-stand-gesture, var_0);
                }
                lambda {
                    go(closed, 1);
                }
            }
        }
    }
    state close-backwards {
        block start {
            track player {
                lambda {
                    print-float(dot-product-locators(get-spawner-locator(self, 1), camera-locator(0), 2), self);
                }
                lambda {
                    if (dot-product-locators(get-spawner-locator(self, 1), camera-locator(0), 2) > 0.00) {
                        camera-enable(
                            tag-symbol(open-bw-camera, self),
                            fixed,
                            0.00,
                            1000,
                            1.00,
                            10.00,
                            1,
                            0,
                            0,
                            0,
                            0,
                            -1.00
                        );
                    } else {
                        camera-enable(
                            tag-symbol(open-bw-camera, self),
                            fixed,
                            0.30,
                            1000,
                            1.00,
                            10.00,
                            1,
                            0,
                            0,
                            0,
                            0,
                            -1.00
                        );
                    }
                }
                lambda {
                    u64? var_0 = alloc-gesture-play-params();
                    *(u64 *)(var_0 + 24) = get-locator-position(doorknob-locator());
                    *(u8 *)(var_0 + 16) = 1;
                    gesture_(player, dra-pickup-stand-gesture, var_0);
                }
                lambda {
                    u16 var_2;
                    if (player-is-crouched?()) {
                        var_2 = 18;
                    } else {
                        var_2 = 1;
                    }
                    sid var_4;
                    if (player-is-crouched?()) {
                        var_4 = ellie-mm-crouch-idle;
                    } else {
                        var_4 = ellie-mm-tense-idle;
                    }
                    wait-stub-igc(player, tag-symbol(open-bw-pos, self), 0.50, 0, var_2, var_4);
                }
                lambda {
                    camera-fade-out(tag-symbol(open-bw-camera, self), 0.30, 0);
                }
                lambda {
                    signal(player-done, 0);
                }
            }
            track door {
                lambda {
                    block-nav-poly-under-object(self);
                }
                lambda {
                    wait(0.20);
                }
                lambda {
                    wait-stub-object-lerp-locator(self, get-spawner-locator(self, 1), 0.30, 1, 1.00);
                }
                lambda {
                    wait-for(player-done);
                }
                lambda {
                    go(closed, 1);
                }
            }
        }
        block update {
            track main {
                lambda {
                    disable-follow-camera-collision();
                }
            }
        }
    }
    state destroyed {
        block start {
            track main {
                lambda {
                    if (!(tag-symbol(padlock, self) == 0)) {
                        send-event(parent-attacked, tag-symbol(padlock, self));
                    }
                }
                lambda {
                    disable-action-pack(tag-symbol(door-tap, self));
                }
                lambda {
                    unblock-nav-poly-under-object(self);
                }
                lambda {
                    disable-collision(self, 0);
                }
                lambda {
                    set-animation-speed(self, 0.00, 2, 0);
                }
            }
        }
    }
}
