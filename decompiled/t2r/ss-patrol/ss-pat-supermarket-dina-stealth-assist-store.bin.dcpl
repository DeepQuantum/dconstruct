// ####################################################################################################
// #                                                                                                  #
// #   generated by dconstruct ver. 2.1.0                                                             #
// #   listing for file: ..._unpacked/dc1\ss-patrol\ss-pat-supermarket-dina-stealth-assist-store.bin  #
// #   filesize: 12834 bytes                                                                          #
// #   total entries: 3                                                                               #
// #   functions: 24                                                                                  #
// #   has state script: true                                                                         #
// #   game type: t2r                                                                                 #
// #   optimizations enabled: sso var, foreach, match, second var, member access, regex               #
// #                                                                                                  #
// ####################################################################################################

u64? pat-dina-should-cancel-stealth-kill-int(u64? arg_0, u64? arg_1) {
    return !(npc-alive?(arg_0)) && !(arg_1) || !(npc-alive?(npc-runner-stores-4)) || npc-get-demeanor(arg_0) == 4 || npc-get-demeanor(npc-runner-stores-4) == 4 || npc-is-stunned?(arg_0) || npc-is-stunned?(npc-runner-stores-4) || npc-using-skill?(arg_0, 4) || npc-using-skill?(arg_0, 5) && npc-using-skill?(arg_0, 8);
}

u16 pat-update-stealth-target() {
    u16 var_4;
    if (get-boolean(dina-back-of-store?, self) && get-boolean(dina-front-of-store?, self) && npc-alive?(npc-runner-stores-6) && npc-alive?(npc-runner-stores-5)) {
        u64? var_7 = get-facing-angle-xz(camera-locator(0), get-object-locator(npc-runner-stores-6));
        u64? var_10 = get-facing-angle-xz(camera-locator(0), get-object-locator(npc-runner-stores-5));
        if ((var_7 - var_10) >= 0.00) {
            set-symbol(target, npc-runner-stores-5, self);
            set-symbol(target-pos, pos-dina-assist-target-1, self);
        }
        u16 var_11;
        if ((var_7 - var_10) < 0.00) {
            set-symbol(target, npc-runner-stores-6, self);
            var_11 = set-symbol(target-pos, pos-dina-assist-target-7, self);
        } else {
            var_11 = 0;
        }
        var_4 = var_11;
    } else if (npc-alive?(npc-runner-stores-5) || get-boolean(dina-back-of-store?, self) && !(npc-alive?(npc-runner-stores-6))) {
        set-symbol(target, npc-runner-stores-5, self);
        var_4 = set-symbol(target-pos, pos-dina-assist-target-1, self);
    } else if (npc-alive?(npc-runner-stores-6) || get-boolean(dina-front-of-store?, self) && !(npc-alive?(npc-runner-stores-5))) {
        set-symbol(target, npc-runner-stores-6, self);
        var_4 = set-symbol(target-pos, pos-dina-assist-target-7, self);
    } else {
        var_4 = 0;
    }
    return var_4;
}

statescript {
    options {
        player
        npc-runner-stores-4
        npc-runner-stores-5
        npc-runner-stores-6
    }
    declarations {
        symbol target = npc-runner-stores-5
        symbol target-pos = pos-dina-assist-target-1
        bool dina-back-of-store? = nullptr
        bool dina-front-of-store? = nullptr
        f32 time-in-range = -1.00
    }
    state task-check {
        block start {
            track main {
                lambda {
                    if (task-complete?(patrol, pat-supermarket-runner-combat)) {
                        go(complete, 1);
                    }
                }
                lambda {
                    if (task-active?(patrol, pat-supermarket-runner-combat)) {
                        go(wait-setup, 1);
                    }
                }
            }
        }
        block event_task-complete {
            track main {
                lambda {
                    if (task-active?(patrol, pat-supermarket-runner-combat)) {
                        go(wait-setup, 1);
                    }
                }
            }
        }
    }
    state wait-setup {
        block start {
            track main {
                lambda {
                    while (npc-using-skill?(npc-runner-stores-4, 11) && npc-using-skill?(npc-runner-stores-5, 11) && !npc-using-skill?(npc-runner-stores-6, 11)) {
                        wait-one-frame();
                    }
                }
                lambda {
                    go(wait-trigger-stealth-assist, 1);
                }
            }
        }
    }
    state wait-trigger-stealth-assist {
        block update {
            track main {
                lambda {
                    pat-update-stealth-target();
                }
                lambda {
                    if (npc-alive?(npc-runner-stores-5) && npc-alive?(npc-runner-stores-6) || pat-dina-should-cancel-stealth-kill-int(npc-runner-stores-5, 0) && pat-dina-should-cancel-stealth-kill-int(npc-runner-stores-6, 0)) {
                        go(cancel-kill, 1);
                    } else if (npc-alive?(npc-runner-stores-5) && pat-dina-should-cancel-stealth-kill-int(npc-runner-stores-5, 0)) {
                        go(cancel-kill, 1);
                    } else if (npc-alive?(npc-runner-stores-6) && pat-dina-should-cancel-stealth-kill-int(npc-runner-stores-6, 0)) {
                        go(cancel-kill, 1);
                    }
                }
                lambda {
                    if (character-in-meatshield-grab-against-defender?(player, npc-runner-stores-4) && npc-alive?(get-symbol(target, self)) && npc-using-skill?(get-symbol(target, self), 11) && !(npc-get-demeanor(get-symbol(target, self)) == 4)) {
                        go(stealth-assist, 1);
                    }
                }
            }
        }
        block event_npc-died {
            track main {
                lambda {
                    if (!(npc-alive?(npc-runner-stores-5)) || !(npc-alive?(npc-runner-stores-6))) {
                        if (!(npc-alive?(npc-runner-stores-4))) {
                            go(complete, 1);
                        }
                    }
                }
                lambda {
                    if (event-arg-symbol(0) == npc-runner-stores-5) {
                        set-symbol(target, npc-runner-stores-6, self);
                        set-symbol(target-pos, pos-dina-assist-target-7, self);
                    }
                }
            }
        }
    }
    state stealth-assist {
        block start {
            track main {
                lambda {
                    npc-set-script-only-with-perception(dina);
                }
                lambda {
                    npc-holster-weapon(dina, 0, 1, -1.00, 0);
                }
                lambda {
                    while (distance-between-objects-less-than?(dina, get-symbol(target, self), 2.00) || has-line-of-motion?(dina, vector-between-objects(dina, get-symbol(target, self)), 0.00, 1, 1) && get-float(time-in-range, self) > 0.00 && !(time-elapsed(get-float(time-in-range, self)) > 0.20)) {
                        u64? var_9;
                        if (get-symbol(target, self) == npc-runner-stores-5) {
                            var_9 = get-spawner-locator(get-symbol(target-pos, self), 1);
                        } else {
                            var_9 = get-object-locator(get-symbol(target, self));
                        }
                        npc-move-to(dina, var_9, 1, 2, 0, 0, 0, 0);
                        wait(0.10);
                    }
                }
                lambda {
                    wait-buddy-start-stealth-kill-melee-move(dina, get-symbol(target, self));
                }
                lambda {
                    npc-draw-pistol(dina, 2, -1.00, 0);
                }
            }
            track check-reached-range {
                lambda {
                    while (!(get-float(time-in-range, self) > 0.00)) {
                        if (distance-between-objects-less-than?(dina, get-symbol(target, self), 2.00)) {
                            set-float(time-in-range, current-time(1), self);
                        }
                        wait-one-frame();
                    }
                }
            }
        }
        block event_npc-died {
            track main {
                lambda {
                    if (!(npc-alive?(get-symbol(target, self)))) {
                        npc-draw-pistol(dina, 2, -1.00, 0);
                        go(cancel-kill, 1);
                    }
                }
            }
        }
        block update {
            track main {
                lambda {
                    if (pat-dina-should-cancel-stealth-kill-int(get-symbol(target, self), 1)) {
                        npc-draw-pistol(dina, 2, -1.00, 0);
                        go(cancel-kill, 1);
                    }
                }
            }
        }
    }
    state cancel-kill {
        block start {
            track main {
                lambda {
                    npc-set-script-high-priority(dina);
                }
                lambda {
                    npc-stop-script(dina, 0);
                }
                lambda {
                    npc-request-explore(dina, 0, 0, *buddy-post-selector-set*, 12.00, 0, *default-ai-explore-follow-settings*, 0);
                }
                lambda {
                    go(complete, 1);
                }
            }
        }
    }
}
